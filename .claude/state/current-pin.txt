MODE: loop-driven **가동 중** — 영호 GO(2026-07-25) "마일스톤 끝까지 완주" + GO(2026-07-26) "모든 백로그 전부 끝까지 완주 / SubAgent 활용 허가". attended·무인배치 X. 게이트는 work-judge (b)(c)에서만.
WORK-ID: A_gate-hygiene — 「A 전체」 게이트 위생 스프린트 / 브랜치 chore/harness-renewal-opus5
PHASE: 백로그 **14건 중 9건 해소**(1·2·3·5·6·8·9·11·12). ⭐ 남은 5건 중 **4·7·10·13은 Codex 세션 몫**(CORE-12), **14번은 다음 창**(읽기 명령이 봉인에 걸리는 저우선순위 마찰) — Claude 트랙 소진. 등급: 복잡 / 깃발: harness, trust-boundary
📌 **상세는 pin이 아니라 문서가 소유한다** — 백로그 12건 전문·해소 경위·HR2 회고 = `01_Phases/21_HR2-opus5-renewal/HR2-DONE.md`. 결정의 근거 = `.claude/CHANGELOG.md` 최상단 2건. 여기 복제하면 드리프트만 생긴다.

현재 작업: A 스프린트 **Claude 트랙 완주**. 창 묶음(5·8·9)까지 끝났다.
다음 액션: ① push(영호 직접 — `! git push origin chore/harness-renewal-opus5`). ② 남은 백로그 **4·7·10·13은 영호의 Codex 세션**에서 — 인수인계 브리프 = `01_Phases/21_HR2-opus5-renewal/A-sprint-codex-handoff.md`(「지시문」 절을 통째로 붙여넣으면 된다). ③ A 스프린트 종결 보고서·PR 여부는 영호 판단.
📌 **Codex 세션이 물어올 미해결 1건** — `.claude/CHANGELOG.md`에 Codex가 자기 변경을 남길 수 있는가. CORE-12 조문의 격리 범위는 `hooks/**`·`state/**` 뿐이라 CHANGELOG는 **조문상 격리 대상이 아니다**. 그런데 위치는 Claude 어댑터 층이고(ADR-034), 선언된 역할은 *"헌법·ADR·하네스·공유계약 변경 이력"* = 저장소 전체 이력이다 — **어댑터 소유권과 전 저장소 이력이 한 파일에서 충돌**한다. 영호 판단 필요.

✅ **push 완료**(2026-07-26, 영호 직접 실행 — `6983ca9..e7a8d4e`, 5커밋). origin과 동기화 상태. PR은 아직 없다.

⚠️ **CORE-06 v2 발효 — AI는 비가역 명령을 실행할 수 없다.** `push`·`gh pr create/merge`·`gh release`·`npm publish`·`npm run package`는 `dangerous-cmd-guard` 축②가 GO 여부와 무관하게 exit 2로 자른다(승인으로 풀리는 게이트가 아니라 **실행 주체가 사람으로 고정**된 것). AI는 명령을 조립해 제시하고, 영호가 `! <명령>` 으로 실행한다. 차단 메시지는 사고가 아니라 설계대로 작동한 것이므로 **우회 시도 금지**.

주의할 약속:
- 하네스 = 영호 단독 통제(ADR-038·CORE-11) · `.claude/settings.json`과 `gate-open.flag`는 **창 상태물이라 커밋 금지**
- CORE-12 격리 — `.codex/**`는 읽기도 금지
- ✅ **유지보수 창 종결**(2026-07-26, 영호 `CLOSE-GATE.bat`). 봉인 복구 **실측 확인** — Edit deny 15줄 복귀 · `$comment` 봉인판 · `gate-open.flag` 삭제. ⭐ 재봉인이 SEALED를 `settings.json`으로 **복사**하므로 창 안에서 고친 `$comment`(제거 조건 + CORE-06 v2)가 이때 정본에 실렸고, 그래서 `settings.json`이 **커밋 대상이 됐다**(창이 열린 동안의 "커밋 금지"는 개방판에 대한 규칙이다).
- ⚠️ 에이전트 description 압축(de61d05)의 실발화는 여전히 **미관측**. 규범은 생겼다(`subagent-routing.md` §4-5) — **다음 통상 작업에서 자동 발화를 1회 보면 여기에 기록**하고, 통상 작업 3회가 지나도록 못 보면 `de61d05`를 되돌린다.
- 📌 **Second Brain 축적 제안은 문턱을 높인다** — 영호 피드백(2026-07-26): *"너무 과도한 축적은 Noise일거 같다."* 마일스톤·창 종결급이나 성향이 실제로 갈린 지점에서만 제안한다.

이번 세션(2026-07-26)에 한 일:
ⓐ ⭐ **CORE-06 v2 — 비가역 사람 게이트를 권한 1층 → 훅+권한 2층으로**. 진단의 핵심은 "모드 의존"이 아니라 **층의 비대칭**이었다(CORE-11 봉인은 deny+훅 2층이라 같은 모드에서 멀쩡했다). 설계는 **"묻기"가 아니라 "닫기"** — 훅은 payload로 승인 여부를 알 수 없고 승인 flag를 두면 에이전트가 그 flag를 쓰므로, 에이전트에겐 항상 닫고 주체를 사람에게 넘겼다.
ⓑ ⭐⭐ **선재 봉인 구멍 2종 봉합**(reviewer 발견) — `(cd .claude/hooks && echo x > …)`가 **괄호 하나로 봉인을 통과**했고, `bash -s <<'SH' … SH`는 축①·축②·봉인을 **동시에** 눈멀게 했다. 네 판정기가 토큰화를 공유해 CORE-06·07·11·ADR-038이 함께 뚫려 있었다.
ⓒ ⭐ **e2e userData 격리(백로그 2) → 백로그 1이 함께 풀렸다**. `--user-data-dir` 부재로 영호 실 prefs의 워크스페이스가 복원돼, 앱이 *탐색기가 이미 열린 상태로* 부팅했다 — "폴더 선택" 버튼 클릭이 실패한 게 아니라 **버튼이 DOM에 없었다**. 11파일 격리 후 `core-loop` **4/4**. 덤으로 `shell.e2e.ts` F8이 영호 실 대화 이력에 얹힌 **false green**이었음도 드러났다(격리 없는 e2e는 **양방향**으로 거짓말한다).
ⓓ **테스트 위생 3건**(백로그 3·11·12, SubAgent 위임) — 유령 마일스톤 경로 27파일 28건(접두 `NN_` 누락) · `m7-explorer-lazy` TC-3 상시 skip 부활(5/5) · e2e 실행 명령 오표기 11파일. ⭐ TC-5에서 **skip보다 나쁜 것**이 나왔다: `'src/'` 가 `02_Source/renderer/src/store` 의 중첩 경로에 우연히 걸려 **pass로 위장**한 채 의도와 다른 것을 보고 있었다. 그리고 12번은 **백로그 3의 검출 패턴(`01_Phases/`)이 좁아서 살아남은 유령**이다 — 위생 작업도 패턴이 좁으면 유령을 남긴다.

ⓔ **창 묶음 5·8·9 동시 해소**(창 1회 개방) — 백로그 9는 제거 조건을 `settings.SEALED.json` `$comment`에 명문화(⚠️ *사전 설계가 뒤집힌 지점* — 개방판엔 옛 4줄이 아예 없어 봉인판 단독 수정이 옳았다. 같은 자리에서 **CORE-06 v1 잔재 문장**도 정정). 백로그 8은 `subagent-routing.md` **§4-5** 신설(되돌림 조건이 규범의 절반). 백로그 5는 45건 전수 판정 → **정정 23 / 보존 22**(*"3곳"* 추정이 크게 빗나갔다). ⭐ 여기서도 전제가 좁았다 — `reports/HR1-P05-…html`은 점→언더스코어를 고쳐도 MISS였고 실물은 `reports/milestones/` 하위였으며, 넘버링 접두까지 빠진 건도 섞여 있었다. **백로그 13 신규 등재** = `ADR-033:28`은 `.codex/**` 설정값 인용이라 CORE-12로 **판정 재료에 접근조차 불가**(격리가 만든 사각지대).

루프 상태: 기계 판정분 전부 green — 훅 **113/113** · conformance **13/13** · harness-conformance vitest 12/12 · vitest **5330 passed | 10 skipped** · e2e `core-loop` 4/4 · e2e `m7-explorer-lazy` 5/5 · typecheck 0 · lint 0(⚠️ eslint는 `.ts,.tsx`만 봐서 훅 `.mjs`는 대상 밖).
마지막 갱신: 2026-07-26

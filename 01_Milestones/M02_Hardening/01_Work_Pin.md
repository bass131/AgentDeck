# work-pin — 작업 좌표 (M02)

세션 재개의 단일 인수인계 표면입니다.
좌표와 마감 요약은 현재 상태만 덮어쓰기로 유지하고, 이력은 git이 보유합니다.
결정과 검증의 정본은 같은 폴더의 `_MilestonePreview.md`와 Phase 파일 다섯 개입니다.
gate-config의 pin 포인터가 이 파일로 전환되면(Phase 1 Step 3), 세션 기동 시 이 파일의 전문이 자동 주입됩니다.

## 좌표

- 마일스톤: **M02 Hardening (하네스 강도 수리)** — `01_Milestones/M02_Hardening/_MilestonePreview.md` (Phase 파일 5개)
- 상태: **Phase 3 Steps 1~10 집행 완료** (2026-08-10) — 게이트 판독 계약 네 건(tdd-guard 전체 실행 판별·stop-gate 마감 요약 3줄·plan-gate 절 헤더·pass-watcher 기준선 부재)이 Red→Green으로 수리됐고, 상태 파일 동시 쓰기가 두 모드 seam으로 결정론화돼 손실 재현(post-read)과 무손실 증명(pre-lock)이 갈라졌습니다. 하드 게이트 다섯 종 green에 `npm test` 실패 0입니다.
- 다음 액션: **Phase 4 워커 사이클** — 시작 Step은 Phase 3 인수 대조(검증 기록 줄·`git log`의 `M02-P3` 토큰·`git status` clean·hook-log의 stop-gate 확인 후 Phase 3에 확인 줄 append)이고, 본체는 표피 피드백 훅 신설·문서 정합·AD-02 설계 문답입니다. [USER] 마일스톤 완주 기동 승인이 있어 Phase별 기동은 승인 없이 진행하되, AD-02 문답(P4)·처분 승인·삭제 직접 실행(P5)·마감 판정·push는 사용자 게이트로 남습니다.
- 규율: Phase 경계는 전부 새 워커 세션(top-level, `claude --agent worker` 기동 — 역할 신호 agent_type=worker 실측 성립)입니다. `--resume`은 전 구간 금지이고, 재개는 이 pin과 계획 문서로만 합니다. 인수 대조는 사람 감사 수준([USER] 결정 1)입니다.
- 모델 라우팅: 정본은 `00_Documents/02_Rules/02_모델-라우팅-정책.md`입니다.

## 마감 요약 (최신 1건 — 3줄: 바뀐 것 / 내린 결정 / 봐야 할 것)

- 스탬프: 2026-08-10T20:58:30+09:00
- 바뀐 것: 게이트 판독이 문면대로 강해졌습니다 — tdd-guard는 전체 vitest 실행만 Green 증거로 인정하고(단건·필터·잘린 출력은 무기록) 증거의 ANSI를 벗기며, stop-gate는 마감 요약 라벨 3종의 실존과 비어 있지 않음을 검사하고, plan-gate는 제목 단독 줄만 「검증 기록」 절 헤더로 인정하며, pass-watcher는 세션 첫 Write에서 무장전 대신 장전으로 기웁니다. 상태 파일 쓰기는 33·40·50 공통 헬퍼의 배타 잠금 아래로 들어갔고, 두 모드 seam으로 만든 진짜 동시 실행에서 손실 재현과 무손실 증명이 함께 실측됐습니다.
- 내린 결정: pin 스탬프 비교는 세션 스코프를 「증인 귀속」으로 구현했습니다 — pass-watcher가 pin 갱신을 세션에 귀속시키고 stop-gate는 자기 장전 이후의 갱신이 남의 것뿐이면 반려하며, 귀속 불능은 종전 판정을 그대로 씁니다. 상태 저장은 안 A(배타 잠금 + stale 회수)를 택했고 안 B(버전 CAS)는 스키마 변경과 원자 primitive 이중화 때문에 기각했습니다. 동시 실행 러너는 기존 race-scoping의 순차 회귀를 지키려고 따로 신설했습니다.
- 봐야 할 것: Step 7에 안 A 잠금 획득까지 함께 넣은 순서 이탈과 상태 파일 오염 자가 수리를 포함한 자기보고 4건이 검증 기록 20:56:00 줄에 있습니다. Backlog 5~7번은 계획 밖이라 손대지 않은 관찰 3건이고, 그중 7번은 코디네이터의 pin 갱신이 이 Phase 실행 중 실제로 일어난 기록입니다.

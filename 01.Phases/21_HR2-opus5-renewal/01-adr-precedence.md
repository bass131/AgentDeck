---
owner: 영호
milestone: HR2
phase: 01
title: ADR 선행 — 010·028·033·038 개정
status: done
grade: 복잡
risk: harness
loop_track: human-gate
estimated: 3~5h
domain: cross
summary: coordinator/worker 조직론·루트 구분자 점(.) 결정·모델명 유지 결정·OpenGate 방어범위 네 ADR이 이번 변경에 정면으로 걸린다 — CORE-08상 선행.
---

# Phase 01: ADR 선행 — 010·028·033·038 개정

> **상태**: ✅ done (2026-07-25)
> **마일스톤**: HR2
> **등급**: 복잡 (risk: harness)
> **담당**: 메인 직접 (하네스 = 영호 단독 통제 대행, CORE-11)

---

## 🎯 목표

이번 마일스톤이 뒤집는 **기존 결정 3건**을 ADR로 먼저 정리한다. 헌법 CORE-08("디렉토리 경계·구조 변경 = ADR 선행")과 문서 지도의 *"바꾸려면 ADR부터"* 원칙상, 이 Phase 없이 P02~P03을 진행하면 **결정 기록 없이 결정을 뒤집는** 상태가 된다.

---

## ⏪ 사전 조건

- [ ] 영호가 `OPEN-GATE.bat` 실행 (`00.Documents/adr/**`·`ADR.md`는 봉인 대상 — ADR-037)
- [ ] 브랜치 `chore/harness-renewal-opus5` (생성 완료 2026-07-25)

---

## 📝 작업 내용

- [x] **ADR-010 처리 결정** → **개정 절 추가**(신설 X) · **rename 안 함**.
  - *개정 vs 신설*: **개정 1 절**을 파일 하단에 추가. 근거 = ADR-033이 같은 상황(원 결정이 크게 뒤집혔는데 파일 유지 + 개정 절)에서 쓴 **선례**.
  - *rename*: **하지 않는다.** 비용 실측 결과 ADR-010 인용 7곳 중 **파일 링크는 `ADR.md:18` 하나뿐**이라 비용은 낮았으나, ① ADR-033 선례 ② 파일명은 *이 결정이 처음 무엇이었나*의 기록이기도 함 ③ 이번 마일스톤이 이미 대규모 개명 중이라 추가 rename은 노이즈 — 셋을 근거로 이득 < 비용 판정.
- [x] ADR-010 본문에 **실측 근거 #2·#3** 박제 — 표로 정리(v2.1.220 중첩 OFF 관측 + `.claude/CHANGELOG.md:56` 결정의 창). **"결정이 틀린 게 아니라 전제가 만료"** 라는 독해를 명시.
- [x] **⭐ 신규 근거 발견 — 별칭 규칙은 이미 한 번 무너졌다.** `.claude/CHANGELOG.md:80`(2026-07-03)이 *"Worker 5는 `sonnet` 별칭 → `claude-sonnet-5` 명시 고정"* 이라 기록했으나 **2026-07-25 디스크 실측은 전부 별칭으로 되돌아가 있었다**(4건 + qa는 `opus`로 상이 = 5건 드리프트, 되돌아간 시점 기록 없음). → *"full ID는 문서 규범만으로 유지되지 않는다"* 를 ADR-010 개정 5항에 박고 **P02 완료 조건에 회귀 테스트 요구**로 승격.
- [x] ~~**ADR-033 개정** — `:7`의 결정을 뒤집는다~~ → ⚠️ **초안 서술이 부정확했다. 정정.**
  - `:7`이 속한 원 3항은 **개정 1(2026-07-12)이 이미 철회**했다 — 뒤집을 살아 있는 결정이 아니었다.
  - 다만 철회 문구가 *"Sol/Terra/Luna 모델 비용 계층(**워커 함대 전제**)"* 로 괄호 한정돼 있어, Codex와 무관한 *"Claude의 Opus/Sonnet 모델명 유지"* 문장이 함께 죽었는지 **문면상 결정되지 않는다.** 실제로 이 Phase 초안이 그 모호함 때문에 오독했다.
  - → 조치 성격을 *뒤집기*에서 **범위 모호함 해소 + 소유권 이관**(개정 2)으로 변경. Claude 모델 규범의 소유자 = **ADR-010 개정 1**.
- [x] **ADR-038 개정** — 개정 1 추가. **방어 층을 분해해 축소 범위를 정확히 특정**했다:

  | 벡터 | 무엇이 막는가 | `supervisor-guard.sh:70-72`의 기여 |
  |---|---|---|
  | flag·canonical **쓰기** | `shell-policy.mjs:223`(sealed) + `:244`(CANDIDATE_RE) + settings deny | **중복** |
  | `.bat` **실행** | `:70-72` **단독** | ⭐ **유지 필수** |
  | 상태 **읽기 명령** | `:70-72` | ⚠️ **미달성 방어** |

  ⚠️ **핵심 논거**: 읽기 차단은 `Read`/`Glob`이 열려 있어 성립하지 않고, **차단 메시지 자체가 그 대체 경로를 안내한다**(*"에이전트는 Read/Glob 도구로 상태 확인만"*). 방지턱이 아니라 **표지판**이다. 반면 부작용은 실재 — `*harness_opengate*` 부분 문자열 매칭이라 `git log <경로>` 같은 명백한 읽기도 차단된다.
  ⚠️ **축소가 잔여 구멍을 넓히지 않음도 함께 명시** — bat 코드 읽기는 축소 전에도 `Read`로 가능했다. 사라지는 건 *이미 달성되지 않던 방어*뿐.
  ⚠️ **초안의 다른 오독 하나도 정정** — 작업 중 *"ADR(전면 차단)과 CLAUDE.md(상태 읽기 허용)가 자기모순"* 이라고 판단했으나 **틀렸다.** 구현은 **Bash만** 막고 Read/Glob은 열어두므로 모순이 아니다(훅 주석 `:23`도 *"Bash 접근"* 으로 정확히 한정). 실제 문제는 모순이 아니라 **부분 문자열 매칭의 과차단**이었다.
- [x] ⚠️ **ADR-028 개정 (개명의 정당성 근거 — 빠지면 ADR 없이 ADR을 뒤집는다)** — 개정 1 추가 + **매핑표 5행 박제**(`98.Management` 포함 — 원 ADR 이후 신설이라 원 표에 없었다).
  - ⭐ **"시각 구분"보다 강한 근거를 새로 세웠다**: `.`은 **정규식 메타문자**라 훅이 `00\.documents`로 이스케이프해야 하고, 그 이스케이프가 fail-open의 구조적 원인이었다(`shell-policy.mjs:220-223,244`). `_`는 메타문자가 아니라 함정 자체가 사라진다. 즉 이득은 취향 통일이 아니라 **판정 정규식의 단순화**다.
  - ⚠️ **과장 금지 항목도 명시** — 원 ADR `:18`이 *"큰 단점"* 으로 적은 **생태계 마찰은 해소되지 않는다.** `02_Source/`도 `src/`가 아니다. 비표준 A → 비표준 B이지 표준 복귀가 아니다.

  대상 원문(`00.Documents/adr/ADR-028-root-restructure.md:3,13`):
  > *"구분자 `.`(점) 선택: ADR-027 하위폴더는 `_`(언더바)였으나 최상위는 `.`으로 시각 구분 강화(**영호 선택**). 경로 세그먼트 중간의 점은 확장자가 아니므로 모듈 해석 무탈."*

  P07~P08이 이 결정의 정면 반전인데, CORE-08("구조 변경 = ADR 선행")상 개정 없이 진행하면 이 Phase가 스스로 적은 *"순서가 곧 정당성"*이 여기서 깨진다. **개명 매핑표를 이 ADR에 박제**한다.
- [x] ADR-028의 트레이드오프 절(`:20`)을 **P07 설계 근거로 인용** — 개정 1에 「hook 리터럴 함정 — **2회차**」 절로 박제. **부트스트랩 자물쇠**(`supervisor-guard.sh:30`의 `GATE_FLAG` 하드코딩)도 함께 명시해 P07→P08 순서 강제의 근거를 ADR 층에 남겼다:
  > *"hook 리터럴 함정 — `*src/*` glob·`$PROJ/tests` lookup은 rename에 안 안전 → 동반 갱신. `tdd-guard` 테스트 lookup·`reviewer-auto-trigger` 경계 glob 2건은 **에이전트 자동매핑이 놓쳐 직접 정독으로 포착**."*

  이번 P07의 *"부분 수정이 가장 나쁘다"*와 정확히 같은 사고의 **2회차**다. 그 사실을 적어 두면 3회차를 막는다.
- [x] **ADR 신설 여부 판단** → **신설하지 않는다.** `chief-tech-operator`와 모델 티어 4층을 **ADR-010 개정 1에 함께** 담았다. 근거: 모델 티어는 *"누가 무엇을 하는가"* 의 부속 결정이고, 분리하면 두 문서가 서로를 참조하며 드리프트한다 — 이 마일스톤이 치우고 있는 3세대 규범 겹침이 정확히 그 비용의 실례다.
- [x] `00.Documents/ADR.md` 인덱스 정합 — 4행 갱신.
  - ⚠️ **드리프트 1건 발견·정정**: `033` 행이 `✅ 활성 (개정 **예정** — HR1 P05 전담 보조 전환)`으로 남아 있었으나 그 개정은 **2026-07-12에 이미 완료**됐다. 인덱스만 stale이었다.

---

## ✅ 완료 조건

- [x] ADR-010·**028**·033·038 각각 개정 또는 superseded 표기 완료, `ADR.md` 인덱스와 **상태·제목 일치** (인덱스 4행 갱신 + 033 stale 1건 정정)
- [x] ADR-028에 **개명 매핑표 5행**(`00.Documents`→`00_Documents` 등)이 박제됨 — P08이 이 표를 근거로 실행
- [x] 각 ADR 본문에 trade-off 절 존재 (헌법 "결정엔 항상 trade-off") — 010(4항)·028(4항)·033(1항)·038(대안 a/b/c)
- [x] 실측 근거가 file:line과 함께 박제 — `CHANGELOG.md:56,80` · `shell-policy.mjs:220-223,244` · `supervisor-guard.sh:30,70-72` · `.codex/harness-contract.test.mjs:131-136`
- [x] 회귀 게이트 실행 — ⚠️ **초안 서술 정정**: ~~`harness-conformance.test.ts`가 ADR 구조를 검사하므로~~ → **검사하지 않는다**(318줄 전수 확인, `ADR`/`adr` 매칭 0건). 따라서 ADR 편집이 red를 만들 경로는 없다. 그럼에도 게이트를 돌리는 이유는 **기준선 확보**(이후 Phase가 코드·훅을 건드릴 때 red의 출처를 가리기 위함)다.
  - **📌 HR2 기준선 (2026-07-25, P01 시점)**: `typecheck` 0 · `test` **394 파일 / 5,325 passed / 10 skipped** · `lint` 0.
  - 이후 Phase에서 이 수치가 달라지면 **그 Phase가 원인**이다.
- [x] CHANGELOG `[H]` 엔트리 1줄 — 4건 개정 + 「기억의 3층」 신설 + 부수 정정 2건 박제

---

## 📚 학습 포인트

- **ADR(Architecture Decision Record)의 supersede 관례** — 결정을 지우지 않고 "이 결정은 X에 의해 대체됨"으로 남긴다. 왜 그때 그렇게 정했는지가 지워지면, 나중에 같은 함정을 다시 밟는다.
- **결정의 유효기간** — ADR은 그 시점의 환경을 전제한다. 2026-07-11 "coordinator Agent 유지"는 *당시엔 옳았고* 런타임이 바뀌어 무효가 됐다. 결정이 틀린 게 아니라 **전제가 만료**된 것 — 이 구분을 ADR에 적어야 한다.

---

## ⚠️ 함정

- **ADR을 형식으로만 쓰기** — "바꿨다"만 적고 근거를 안 적으면 다음 점검이 또 조사한다. 실측 워크플로가 108만 토큰을 썼다는 사실 자체가 근거 박제의 가치다.
- **ADR-038 개정을 P05와 묶어 뒤로 미루기** — 그러면 P05가 "계약을 어기는 코드 변경"이 된다. 순서가 곧 정당성이다.
- **파일 rename 유혹** — `ADR-010-multiagent-coordinator-worker.md`의 이름이 실질과 어긋나 보이지만, ADR 파일명 변경은 인덱스·인용 링크 전수를 건드린다. 이름을 바꿀지는 **비용을 확인하고** 정한다.

---

## 담당 SubAgent

**메인 직접** — 하네스·ADR은 영호 단독 통제이며 메인이 대행한다(CORE-11). 에이전트 위임 X.

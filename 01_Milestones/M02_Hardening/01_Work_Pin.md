# work-pin — 작업 좌표 (M02)

세션 재개의 단일 인수인계 표면입니다.
좌표와 마감 요약은 현재 상태만 덮어쓰기로 유지하고, 이력은 git이 보유합니다.
결정과 검증의 정본은 같은 폴더의 `_MilestonePreview.md`와 Phase 파일 다섯 개입니다.
gate-config의 pin 포인터가 이 파일로 전환되면(Phase 1 Step 3), 세션 기동 시 이 파일의 전문이 자동 주입됩니다.

## 좌표

- 마일스톤: **M02 Hardening (하네스 강도 수리)** — `01_Milestones/M02_Hardening/_MilestonePreview.md` (Phase 파일 5개)
- 상태: **Phase 1 Steps 1~12 집행 완료** (2026-08-10) — 역할 신호 worker 실측, 헌법 2·8조 문면 정합, tdd-guard 테스트 인정 Red→Green, 동등성 기준표 59행의 표 구동 배선까지 끝났고 하드 게이트 다섯 종이 green입니다.
- 다음 액션: **Phase 2 워커 사이클** — 시작 Step은 Phase 1 인수 대조(검증 기록 줄·`git log`의 `M02-P1` 토큰·`git status` clean·hook-log 확인 후 Phase 1에 확인 줄 append)이고, 본체는 가드 우회면 수리로 기대-실패 플래그 18건을 0으로 만드는 일입니다. [USER] 마일스톤 완주 기동 승인이 있어 Phase별 기동은 승인 없이 진행하되, AD-02 문답(P4)·처분 승인·삭제 직접 실행(P5)·마감 판정·push는 사용자 게이트로 남습니다.
- 규율: Phase 경계는 전부 새 워커 세션(top-level, `claude --agent worker` 기동 — 역할 신호 agent_type=worker 실측 성립)입니다. `--resume`은 전 구간 금지이고, 재개는 이 pin과 계획 문서로만 합니다. 인수 대조는 사람 감사 수준([USER] 결정 1)입니다.
- 모델 라우팅: 정본은 `00_Documents/02_Rules/02_모델-라우팅-정책.md`입니다.

## 마감 요약 (최신 1건 — 3줄: 바뀐 것 / 내린 결정 / 봐야 할 것)

- 스탬프: 2026-08-10T19:37:15+09:00
- 바뀐 것: 두 헌법의 2·8조가 승인 문안으로 정합되고, tdd-guard가 `.claude/hooks/*.test.mjs`를 테스트로 인정하게 수리됐으며, 동등성 기준표 59행이 표 구동 테스트로 배선됐습니다.
- 내린 결정: 기준표의 기대 판정은 「Moodie 동등 이상 + 오탐 금지」로 뒀습니다 — 원본이 못 잡던 표면(`git.exe`·stash·branch 동치)과 원본의 인용 본문 오탐을 함께 플래그로 등재했습니다.
- 봐야 할 것: 기대-실패 플래그 18건이 Phase 2의 수리 목록이고, Step 7 수리가 「원자적 편집 1회」 제한을 못 지켜 2회로 나뉜 자기보고 줄입니다(검증 기록 19:21:16).

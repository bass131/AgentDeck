# REPLICA_GAP — AgentCodeGUI 1:1 완전복제 갭 추적 + 루프 드라이버

> **이 파일은 `/loop`의 durable 드라이버다.** 매 iteration이 이 파일을 읽고(현재 갭/다음 웨이브 확인) → 작업 → 갱신한다. 컨텍스트가 압축돼도 여기서 이어간다.
> 권위 인벤토리: 원본=소스 실측(아래 §원본 기능), 우리=소스 실측(아래 §현황). 타깃 시각 스펙=`docs/UI_FIDELITY.md`. 기능 추적=`docs/FEATURE_MAP.md`.

## 🔁 루프 절차 (사용자 정의)
1. **실측** — AgentCodeGUI와 우리 프로젝트를 각각 측정(버튼·동작·세부 설정창 디테일). 1회차에 전수 인벤토리 작성(완료), 이후엔 작업 영역만 재측.
2. **갭 Phase 계획** — 부족분을 구현할 Phase 분해(`phases/NN_*/`), plan-auditor 검증.
3. **구현** — 도메인 Worker(renderer 중심, 디자인 우선) + TDD.
4. **테스트** — typecheck·단위·e2e·lint 게이트 green.
5. **1:1 검증** — 원본과 시각/구조 대조(스크린샷 육안). 완벽하면 해당 웨이브 ✅. 전 웨이브 ✅ = **루프 탈출**, 아니면 다음 웨이브로 반복.

**전략(사용자 지침)**: **디자인부터 전부 만들어 놓고(시각 셸), 이후 기능만 연결**. 따라서 웨이브 F7~F14는 *시각/구조 우선*(백엔드 실데이터는 placeholder, 실동작은 M3 Git·M4 멀티·M5 배포·LSP 기능 트랙에서 연결). 루프 탈출 조건 = 시각 + 기능 모두 1:1.

**자율 실행 지침(2026-06-22 사용자)**: 남은 디자인 웨이브 **F9~F14를 끝까지 자율로** 수행. 각 웨이브 = phase→plan-auditor→Worker TDD→reviewer→시각검증→커밋→이 드라이버/status/task 갱신→다음 예약. **중간 웨이브마다 사용자 보고 생략**(브리프 로그만). **F14(마지막 디자인 웨이브) 완료 후에만 종합 보고**. 막히면(plan-auditor 🔴 반복·게이트 적색 해소 불가·범위 모호) 그때만 사용자에게 질의.

## 상태 범례
✅ 완료(1:1 검증) · 🚧 진행 · 🟡 시각만/placeholder(동작 미연결) · ⬜ 없음

---

## 📋 웨이브 백로그 (디자인-우선 순서 — 루프가 위에서부터 소진)

> 각 웨이브 = 한 milestone(`phases/NN_*`). 한 iteration이 한 웨이브(또는 그 일부)를 처리. **F1~F6 = 완료(충실도 토대).**

### F7 — 설정 모달 5탭 완성 ✅ (커밋 — iteration #2)
원본 Settings 좌 nav **5탭**(Claude Code·MCP·Skill·Code·Theme) 시각 1:1 복제. 정적 샘플, 새 IPC 0.
- [x] nav 2탭 → 5탭(Claude Code/MCP/Skill/Code/테마 아이콘+라벨).
- [x] **Claude Code 탭**: 「현재 엔진」카드 + 버전 vpick 드롭다운(설치/현재/최신 vtag 행) — 정적, 실설치=M5. 🟡 install-card(진행)=M5 후속.
- [x] **MCP 탭**: scope 탭(전체/전역/로컬+카운트) + 서버 행(이름·scope배지·transport칩·토글) + note — 정적.
- [x] **Skill 탭**: scope 탭 + 스킬 행(이름·scope·설명·토글) + note — 정적.
- [x] **Code 탭(LSP)**: 서버 행(FileBadge ts/py/cs/cpp·언어·ver-chip[내장/설치됨/요구사항]·확장자·설치/삭제) — 정적. 🟡 install-card(진행)=M2-LSP 후속.
- [x] **Theme 탭**: F6 셀렉터 이동(라이트/다크, aria-pressed).
- [x] 공통 ScopeTabs + ToggleSwitch(role=switch) + vpick 드롭다운. knob 색=테마-불변 --knob 토큰.
> 검증: settings-tabs 25 단위 + 8 shell e2e + 탭별 스샷 육안 1:1. reviewer/plan-auditor 통과. **잔여 🟡 = install-card(엔진/LSP 진행 카드) → 실설치 연결(M5/M2-LSP) 때 함께.**

### F8 — 사이드바 세션 + 멀티 토글 ✅ (커밋 — iteration #3)
정적 샘플 세션 + 로컬 state CRUD(시각). 세션 CRUD/모드전환/새대화 실동작=M4.
- [x] **단일/멀티 에이전트 토글**(`.sb-mode` 단일 IconSquare/멀티 IconGrid, 로컬 state, 라벨 시각 전환) — 모드 실동작=M4.
- [x] **세션 목록 행**: 상태 점(idle/done/run/err) + 제목 + 프롬프트 글리프(pr-mark) + 상태 부텍스트 + more 메뉴. 정적 샘플 5 + 빈/검색없음.
- [x] **세션 컨텍스트 메뉴**: 이름 변경/프롬프트 설정/삭제(danger) — 좌표 클램프, 바깥클릭/Esc/resize/blur 닫기.
- [x] **이름 변경 다이얼로그**(로컬 제목 변경) + **삭제 확인 다이얼로그**(로컬 행 제거, 되돌릴 수 없습니다).
- [x] 새 대화 버튼 활성(시각, 생성=M4) + 검색 필터(로컬, 제목 부분일치).
- [x] 프로필 풋 동적화(샘플 아바타 색+이니셜+닉네임, sb-foot=설정 트리거). 실 Profile 연동=F12.
> 검증: sidebar-sessions 35 단위 + shell.e2e F8 + 스샷 육안 1:1. Sidebar props 무변경(내부 로컬 state)·Shell.tsx 무변경. plan-auditor(🔴2→반영)·reviewer CRITICAL0.

### F9 — 컴포저 리치 트레이 ✅ (커밋 — iteration #4)
정적 샘플 + 로컬 state. 실행/해석/저장/큐드레인=M4.
- [x] **슬래시 커맨드 메뉴**(`/`): 명령어(ask/init/clear/compact/review/security-review + 설명) + 스킬 섹션. ↑↓/Enter/Tab/Esc. 실행=M4.
- [x] **@mention 팔레트**: @token 샘플 파일/폴더 브라우즈·필터·dir 드릴다운(상위 복귀). @path 삽입. 해석=M4.
- [x] **이미지 첨부 트레이**: attach→샘플 썸네일(data URL) + 드롭 오버레이 + 썸네일+X. 저장=M4.
- [x] **예약 큐 스트립**(optional queued prop, "예약된 메시지 N" + 취소) — 큐 드레인=M4.
- [x] placeholder 3-상태(busy/started[hasStarted]/신규).
> 검증: composer-trays 31 단위 + visual-viewer F9 e2e(슬래시/멘션/첨부 스샷) + 육안 1:1. 큐/드롭/busy=단위 전담. plan-auditor(🟡4 반영)·reviewer CRITICAL0. 495 단위+20 e2e.

### F10 — RecentFiles 탭바 + 에이전트 패널 todo/서브에이전트 ✅ (커밋 — iteration #5)
RecentFiles=실 opened-files(renderer state); 패널 populated=optional prop+샘플(라이브 빈상태, M4 데이터).
- [x] **RecentFiles 탭바**(코드 패널 위): FileBadge+basename+X. 좌클릭 열기·중클릭/X 닫기·드래그 재정렬(FLIP)·우클릭 메뉴(닫기/다른탭/오른쪽/모두). cap 20. 🟡 변경마커 N/M=M4(tag 데이터 부재).
- [x] **할 일 섹션 강화**: 진행바(done/total%) + 체크박스(완료 IconCheck/running spin) + todo 행(.done/.running/.planned). optional prop, 데이터=M4.
- [x] **서브에이전트 카드**: 역할 아이콘+이름+역할부제+상태(running/done/queued)+chev → **SubAgentModal**(활동/도구 섹션). optional prop, 데이터=M4.
- [x] **변경된 파일 FileRow**: FileBadge+path+stat(add/del/tag optional)+chev. 라이브=경로만(diff add/del/tag=M4 샘플 시연).
> 검증: recentfiles 13 + agentpanel-detail 19 단위 + RecentFiles 탭바 e2e + 스샷 1:1. 패널 populated=단위 전담(라이브 빈상태). 새 IPC 0·Shell 최소변경·AgentPanel 무인자. plan-auditor(🟡2 반영)·reviewer CRITICAL0. 527 단위+21 e2e.

### F11 — 모달 군 1 (Git·폴더전환·프롬프트·Ask) ✅ (커밋 — iteration #6)
정적 샘플 + 자기완결 트리거(F8/F9 계약 무파손). git 백엔드=M3·ask 엔진=M4.
- [x] **GitModal**: 헤더(레포·⎇브랜치 ahead/behind·pull/push) + 좌nav(변경/히스토리/브랜치/원격/태그) + 히스토리 리스트(일자그룹·커밋행) + 커밋 상세(파일 리스트·해시 복사) + 변경뷰(커밋 컴포저·Claude). 고정크기+최대화 토글. 탐색기 git 버튼 트리거. git 실동작=M3.
- [x] **FolderSwitchDialog**: "작업 폴더를 변경할까요?" 취소/변경(danger). 단위 전용(라이브 트리거=M4).
- [x] **PromptModal**: IconSpark+"프롬프트 설정" textarea(4000자 카운터)+비우기/취소/저장. 사이드바 ctx-menu 트리거(Sidebar 내부 로컬).
- [x] **AskModal**: `/ask` 분리 대화(orb 헤더·휘발성 pill·최소화 알약·컴포저, 빈상태 기본). Composer onSlashAsk(하위호환). 엔진=M4.
> 검증: gitmodal 32 + dialogs-f11 51 단위 + visual-viewer F11 e2e(Git/Prompt/Ask 스샷) + GitModal 육안 1:1. 새 IPC 0·Sidebar props 무변경·composer 하위호환. plan-auditor(🔴3→4 Phase 분리·자기완결 트리거 반영)·reviewer CRITICAL0. 610 단위+22 e2e. 🟡 exp-act CSS 소유권(비차단).

### F12 — 모달 군 2 (ImageViewer·온보딩·게이트·로그인) ✅ (커밋 — iteration #7)
정적 샘플. ImageViewer=라이브 트리거(첨부); 5개=라이프사이클(Shell open state default off, 라이브 트리거=M5, 단위 시각 검증).
- [x] **ImageViewer 라이트박스**: 오버레이(파일명·카운터·기본앱열기[no-op]·닫기) + 다중(좌우 chevron·썸네일 필름스트립) + 클릭 줌. 컴포저 첨부 트리거(라이브 e2e).
- [x] **WhatsNew 스플래시**: 6슬라이드 온보딩 데크(wn-scrim·칩 네비·CTA). open prop, 자동표시 off.
- [x] **UpdateNotes**: 메탈 그라디언트 hero+키워드 마퀴+번호 리스트.
- [x] **EngineGate** + **AppUpdateGate**: install-card 진행 카드(스피너/체크/경고·로그·상태바, phase별). 실동작=M5.
- [x] **Profile/로그인 온보딩**: 2분할(브랜드 패널 / 닉네임+아바타 색 12종 그리드+입장하기). 자체 TitleBar 미렌더. 진입 게이트=M5.
> 검증: imageviewer 21 + onboarding 24 + gates-profile 60 단위 + ImageViewer e2e(엘리먼트 스샷 다크 라이트박스 1:1) + 라이프사이클 5개 런치 미표시 단정. 새 IPC 0·자동표시 off·Profile TitleBar 중첩 회피. plan-auditor(🟡4 반영)·reviewer CRITICAL0. 715 단위+23 e2e. 🟡 ImageViewer onOpenImage 단위 약함(e2e가 트리거 커버).

### F13 — 멀티에이전트 워크스페이스 ✅ (커밋 — iteration #8)
정적 샘플 패널 + store mode 트리거(자기완결). 동시 실행=M4.
- [x] **MultiWorkspace 그리드**: 2~6 패널(헤더 숫자 탭 COLS{2:2,3:3,4:2,5:3,6:3}, 그리드 열 변동) + 패널(슬롯번호·상태·제목·pill·폴더·프롬프트·컨텍스트 링 conic·빈 스레드·"크게 보기"·RunPickers·PanelComposer) + 일괄 폴더(FolderSwitch)·확장 모달. 사이드바 단일/멀티 토글(store mode)→Shell 메인 영역 교체(사이드바 유지). 동시 실행·패널별 엔진=M4.
> 검증: multiagent-f13 42 단위 + shell.e2e F13(멀티 토글→그리드·count 탭·확장 모달) + 2×2 그리드 스샷 1:1. store mode(Sidebar props 무변경)·새 IPC 0(window.api.multi 미사용)·store 누수 차단. plan-auditor(🟡3 반영)·reviewer CRITICAL0. 757 단위+24 e2e.

### F14 — 폴리시/디테일 ✅ (커밋 — iteration #9, 마지막 디자인 웨이브)
정적 샘플/로컬. 권한/질문 응답·선택 인용·단축키 실동작=M4.
- [x] **ZoomBadge**(Ctrl+휠 줌 ±0.1, "N%" 일시 배지, localStorage) — 채팅 연결.
- [x] **PermissionModal**(허용/항상/거부, 숫자키 1·2·3·Esc) + **QuestionModal**(번호 옵션·스텝·잠깐 내려두기 q-mini-pill) — 스레드 중앙 모달, default off. 연결=M4.
- [x] **메시지 타임스탬프**(메타 시간) + 선택 툴바(복사/더 자세히) + thinking/notice 아이템.
- [x] **전역 단축키 골격**: Ctrl+N/O/F·`` ` ``(사이드바 토글)·Shift+Tab·↑↓·Esc(모달 우선). 미연결=no-op(M4).
- [x] **창 스냅 존**(좌/우/모서리/maximize — main geometry 순수함수 computeSnapZone/snapBounds + 릴리스 스냅). 🟡 고스트 프리뷰=의도적 생략(자식창 신규=trust-boundary 확대 보류).
> 검증: f14-modals 20 + chat-polish 13 + shortcuts 7 단위 + snap golden 27(main) + ZoomBadge 라이브 e2e. 새 IPC 0(창 스냅 순수함수+기존 controls)·모달 default off·Esc 모달 우선. plan-auditor 승인·reviewer CRITICAL0(신뢰경계 통과). 824 단위+25 e2e.

### F15 — 레이아웃 정정 (탐색기 디테일 + 파일 뷰어 플로팅 모달) ✅ (커밋 — iteration #10)
사용자 실측 발견: 우리 셸은 파일 클릭 시 **이중 탭 자동전환**(탐색기→diff, 대화→코드)으로 탐색기·채팅이 둘 다 사라짐 → 원본과 다름. **원본 직접 조작 실측**(Playwright로 레퍼런스 앱 구동): 원본=탐색기·채팅 항상 유지 + 파일=플로팅 모달(`.fv-overlay`/`.rzm`, 기본 최대화). 사용자 결정="**원본기반 + 기본 창모드**"(코드+채팅 동시, 최대화 토글=원본).
- [x] **이중 탭 자동전환 제거**: Shell 좌측(탐색기/diff)·중앙(대화/코드) pane-tab 전부 제거 + 자동전환 useEffect 2개 삭제. 탐색기·채팅 **항상 표시**.
- [x] **FileModal 플로팅 모달**: fragment 레벨 `position:fixed` 오버레이(다른 모달 패턴). **센터 정렬 + 뒤 다크 블러 스크림**(`oklch(0.1 0.004 60/0.55)`+`blur(3px)`), **기본 최대화**(큰 센터 카드, 원본 ref-03 1:1) / 복원 토글=1140px 센터 카드. 헤더 `.diff-head`(FileBadge22 + mono 경로[dir dimmed] + **`읽기` 파랑 알약** + 최대화/복원 + 닫기, 더블클릭 최대화 — 원본 styles.css L1337~1349 이식, FileModal 스코프). 본문 라우팅(DiffViewerPane/Code/Markdown/Image 재사용). `resizableModal` 훅(localStorage). Esc/스크림 클릭=닫기(전역 preventDefault 0). store `closeOpenedFile` 추가. *[이력: 처음 우측 도킹 창모드로 구현→사용자 피드백(변경파일 목록 클릭 불편)으로 원본대로 센터+블러 변경]*
- [x] **탐색기 디테일 1:1**: `.fe-head`(탐색기 라벨+git+접기) + `.fe-folders`(메인 폴더 버튼+Ctrl O+레퍼런스+`폴더 추가` 점선) + 검색 Ctrl F 힌트 + `.fe-blank` 카드 + **viewing 스위처 모델**(레퍼런스 폴더=`.fe-frow` 클릭 전환, 기존 `.fe-ref-section` 스택 제거). 트리 `.fe-tree`/`.fe-file` 보존.
> 검증: f15-fileexplorer 15 + f15-filemodal 17 단위 + e2e 신모델 재작성(탭 제거·모달 표시·viewing 스위처·스크림 차단→Esc 후 탭조작). **핸즈온 실측**(사용자 요청): 원본 Playwright 구동 + 우리 앱 프로브 반복 — ①모달 stacking 버그(absolute→fixed·pane내부→fragment) ②센터+블러 검증(overlay align center·scrim·blur·modal x40/w1840) ③헤더 정합(`.diff-head` base 누락→FileModal 스코프 이식, `읽기` 알약 추가, 버튼 우측 정렬). 새 IPC 0(모달=순수 renderer). plan-auditor(🔴2 e2e 핸드오프 반영)·reviewer CRITICAL0. **856 단위+25 e2e**. 🟡 scrim/shadow raw색=기존 관례, diff 정확성=M3.

---
## 🏁 디자인 트랙 F1~F14 ✅ + F15 레이아웃 정정 ✅ — AgentCodeGUI 시각/구조 1:1 복제 토대 완성
다음 = **기능 연결 트랙**(M3 Git ✅ 완료·M4 멀티/대화고도화·M2-LSP·M5 배포) — 시각 셸 위에 실데이터/실동작 연결. 완전 복제(루프 탈출)엔 기능 트랙까지 필요.

---

## 🔌 기능 연결 트랙 (디자인 완료 후 — 별도 milestone)
> 시각 셸(F7~F14) 위에 실데이터/실동작을 연결. 루프 탈출엔 이것도 필요.
- **M3 Git ✅ (완료, iteration #12)**: GitModal 실데이터 연결(git.* 핸들러 status/log/commit/push/pull, AI 커밋 위임), fs.diff HEAD 스냅샷 버그수정. `src/main/git.ts` execFile 직접(라이브러리 0). 실 origin push=인간 게이트.
- **M4 멀티·대화고도화** (Phase 20~): **M4-1 ✅**(model/effort/mode 매핑 + 토큰 게이지) → **엔진 SDK 전환 ✅(ADR-016, Phase 21)**(claude -p CLI→@anthropic-ai/claude-agent-sdk `query()`, 원본 engine.ts 미러; 실 contextWindow 게이지 채택; 커밋 1c47d58/d52b139) → **M4-2 ✅(Phase 22)**(슬래시 실행·@mention 해석·이미지 첨부 저장·큐 드레인; 커밋 560645d/52e7356/74ea489/18def9c; 이미지 비전 인지 라이브 검증 ✅) → **M4-3 ✅(Phase 23)**(멀티 6패널 동시실행·세션 CRUD; 커밋 627f229/f74ff70/5ae1033/57b0efd/add3d59; 2패널 동시 독립 실행 라이브 검증 ✅) → **M4-4 ✅(Phase 24)**(thinking·todo[24a]·subagent B4[24b]·권한 양방향[24c push-queue 리팩터]·질문[24d]; 커밋 f6be012/1e722c4/23d7fb4/a4aed8c; 단위 1583; 권한·질문 백엔드 직접 라이브 스모크 PASS). 잔여 항목: **B8 usage 분석(히스토리/비용)**, B9 입력 히스토리(↑↓), 멀티 세션 영속(maGet/maSave 등가), 전역 구독 runId 게이팅(다중방어), thinking 실시간 partial(`--include-partial-messages`).
- **M2-LSP**: 호버/정의이동/시맨틱 토큰 + LSP 서버 설치(Code 탭 실동작).
- **M5 배포**: NSIS 패키징, electron-updater, 엔진 버전 관리(Claude Code 탭 실동작), MCP/Skill 실토글, ui-prefs IPC 영속, WhatsNew/UpdateNotes 트리거, Profile 진입.

---

## 📜 Iteration 로그
- **#20 (2026-06-24 — M4-4 ✅ Phase 24: 권한/질문 응답 + thinking/subagent/todo 이벤트)** — Task #10. 대화 고도화 마지막 조각. AgentEvent 6종→12종 확장 + 에이전트↔사용자 **양방향**(권한·질문). **착수 전 차단 게이트 스파이크**(`artifacts/permission-canusetool-spike.mjs`, 3회 반복): ① SDK가 `canUseTool` Promise를 **await로 일시정지**(1.5s 지연이 tool_result까지 전파) → push-queue 토대 검증 ② **canUseTool 발화 전제 = 인라인 `settings.permissions.defaultMode` 핀 + `settingSources`**(없으면 사용자 전역설정이 선승인) ③ 안전 bash(echo)는 자동승인 → **부수효과 도구(Write/Edit)만** 발화. **thinking 스모크 G1-OK**(`includePartialMessages:false`에서도 완성 thinking 블록 수신 → partial 불요). **plan-auditor PASS**(차단0, 가드 4건: G1 thinking 수신·G2 questions shared 단일정의·G3 abort waiter 정리·G4 requestId 유일성). **서브웨이브**: **24a(f6be012)** thinking·todo — claude-stream thinking 블록→thinking(+within-msg thinking_clear)·TodoWrite→todos(tool_call 억제), store thinkingText/todos. **24b(1e722c4)** subagent B4 — Task/Agent→subagent(running)·메시지레벨 parent_tool_use_id→tool_call.parentToolId·tool_result로 done(렌더러 책임), AgentPanel/SubAgentModal 실배선. **24c(23d7fb4, 최고위험)** 권한 양방향 — **ClaudeAgentRun을 pull 제너레이터→push-queue(채널)로 리팩터**(데드락 해소: canUseTool이 permission_request push 후 respond까지 await해도 events 드레인 안 막힘), `AgentRun.respond(requestId,RunResponse)` 신설(backend-contract, Echo/Codex no-op), canUseTool 권한게이트(mode 분기·allow_always 세션규칙)·settings 핀·**abort 시 미해결 waiter deny 정리(G3 좀비방지)**, RunManager.respond+PERMISSION_RESPOND 핸들러(입력검증), pendingPermission(envelope runId 캡처)+PermissionModal. **24d(a4aed8c)** 질문 — `_waiters`를 RunResponse로 통합(권한·질문 공용), handleAskQuestion(question_request emit→formatAnswers로 deny+message, 원본 미러)·parseQuestions·AskUserQuestion tool_call 억제, QUESTION_RESPOND 핸들러, pendingQuestion+QuestionModal. **패턴**: 차단게이트 스파이크→Phase 정의→plan-auditor PASS→{단방향 24a/24b 병렬, 양방향 24c→24d 순차} domain Worker TDD→각 reviewer **🔴 0**(24c는 최고위험 정밀점검)→**백엔드 직접 라이브 스모크**(vite-node로 실 ClaudeCodeBackend 구동: 권한 Write allow→실행/deny→차단, 질문 AskUserQuestion→respond([['Red']])→"You chose Red")→conventional commit→문서 갱신. **단위 1583 green(+239)**. **교훈: 양방향(canUseTool await)이라는 토대 가정을 phase 투자 *전* 실 SDK 스파이크로 de-risk(M4-3 runId 레이스 교훈 계승) — settings 핀 필요·부수효과만 발화라는 비자명 사실을 선발견해 설계 반영. 단위가 못 닫는 "우리 백엔드+실 SDK" 이음새는 vite-node 백엔드 직접 스모크로 정밀 타격. [[agentcodegui-reference]]·[[replica-loop]].** **막힘 정책 갱신([[loop-stuck-policy]]): 막힘 시 원본 참고+Opus 서브에이전트 5회 의논 후 중단.** **다음: B8**(사용량 분석: 히스토리/비용).
- **#19 (2026-06-23 — M4-3 ✅ Phase 23: 멀티 동시실행 + 세션 CRUD)** — Task #9. 두 기능 동시 도입(원본 미러). **plan-auditor가 🔴 발견**: `ipc/index.ts`의 `runIdBox` 늦은 바인딩이 동시 다중 run에서 이벤트를 잘못된 runId(또는 `''`)로 태깅하는 레이스(단일 모드는 reducer가 runId 무시라 무해했음) → 멀티 라우팅 메커니즘 붕괴. **REVISE 반영해 차단 게이트 23a 선행**. **23a 토대(627f229)**: `agent-runs.ts` `onEvent` 시그니처 `(event)`→`(event, runId)` 복원(runId는 소비 *전* 동기 발급 — 첫 이벤트 포함 정확 태깅) + IPC box 제거. **EchoBackend 2-동시-run 단위**로 runId 격리 증명(got1=id1만·got2=id2만·`''` 0). **23b 세션 CRUD(f74ff70)**: sqlite store 마이그레이션 v2(`custom_title`) + `delete(id)`/`rename(id,title)` + **save 제목 보존**(`CASE WHEN custom_title=1 THEN title ELSE excluded.title`)으로 rename을 자동제목이 안 덮음. 신규 IPC `CONVERSATION_DELETE/RENAME`(입력검증·prepared statement). store 액션 list/select(id)/rename/delete/new. **23c 사이드바(5ae1033)**: 샘플→실 conversations + select/rename/delete/new store 액션 배선, 활성=conversationId. **23d 패널 훅(57b0efd)**: `usePanelSession()` 신규(원본 useAgentSession 미러) — 패널 1개 독립 live run, 순수 `panelApply(state,payload)`가 **payload.runId !== currentRunId면 state 불변**(타 패널 무시), onAgentEvent 구독+필터, send/abort. 전역 appStore 독립(import 0). **23e 멀티 배선(add3d59)**: MultiWorkspace 샘플→6 고정 훅(s0~s5)+PanelView 실데이터(thread/status/ctx게이지/composer onSend→session.send/abort)+cwd=workspaceRoot(null 시 send 비활성). **전역 격리**: multi 모드 시 Shell이 `<Conversation>` 언마운트→전역 구독 cleanup, MultiWorkspace는 전역 sendMessage/subscribeAgentEvents 미호출(패널 훅만). **패턴**: plan-auditor(🔴 선발견)→23a 차단게이트→{23b·23c·23d 병렬}→23e→각 reviewer **🔴 0**. **단위 1344 green(+109)**. **라이브 검증 ✅(criterion ⑧)**: 2개 query() 동시 병렬(`artifacts/multi-concurrent-smoke.mjs`, gitignore) → 패널A=`MULTI_A`만·패널B=`MULTI_B`만(교차 오염 0, 둘 다 is_error=false). 동시 실 실행 독립성 실증. **잔여 🟡(비차단·후속)**: 전역 subscribeAgentEvents runId 무필터(현재 Shell 언마운트 단일 방어선 — 회귀 가드 테스트 + 다중방어 게이팅 후속); 멀티 세션 영속·패널 폴더전환 실동작=범위 외. **교훈: "백엔드가 이미 동시 지원" 같은 토대 가정은 plan-auditor가 IPC 어댑터 레이어까지 추적해 레이스를 잡아냄 — 멀티 착수 *전* 차단 게이트로 선검증. [[agentcodegui-reference]]·[[replica-loop]].** **다음: M4-4**(권한/질문 응답).
- **#18 (2026-06-23 — M4-2 ✅ Phase 22: 대화 고도화 4기능)** — Task #8. SDK 전환(Phase 21)으로 헤드리스 제약이 풀린 4기능을 실 데이터/동작에 배선. **원본은 4기능 모두 prompt를 STRING으로 유지**(이미지·@mention=텍스트 노트 첨부→에이전트 Read; 슬래시=`/command` raw 전송→SDK 네이티브 해석) → **AgentRunInput.prompt(string)·백엔드·shared 계약 불변**, 노트는 renderer 합성. **22a 슬래시(560645d)**: `/clear`·`/ask` 클라이언트 인터셉트, 나머지 raw 전송(in-list 커맨드 SDK 실행=스파이크 PASS). **22b @mention(52e7356)**: 신규 IPC `LIST_FILES`(인자 없음·현재 워크스페이스만 열거=신뢰경계) → main `listProjectFiles`(BFS·SKIP_DIRS·MAX_FILES, 원본 files.ts 이식) → renderer `mentions`(extractMentions/mentionEntries 원본 1:1) + 팔레트 실데이터 + 노트 합성(표시 버블=원문, 엔진 history 마지막만 promptForEngine 교체). **22c 이미지(74ea489)**: 신규 IPC `SAVE_IMAGE_DATA`(앱 attachments dir 한정·파일명 main uuid·ext 화이트리스트=신뢰경계) + preload `pathForFile`(webUtils, 디스크 경로 직득) → store `attachImagesFromFiles`(pathForFile→saveImageData 폴백) → 컴포저 트레이·드롭/붙여넣기/picker·사용자 버블 썸네일·이미지 단독 전송. 표시=**data URL**(CSP img-src data: 허용 — 커스텀 프로토콜 미도입, MVP 분기). **22d 큐(18def9c)**: store `queue`+enqueue/dequeue(FIFO)/removeQueued(reducer 신규) + Conversation `dispatchSend` 추출(라이브/드레인 공유) + 드레인 effect(was 가드, busy→idle 전이 1건, 원본 App.tsx:660 미러) + abort가 큐 폐기. **패턴**: 각 서브웨이브 shared(main 직접 작성, 신뢰경계 정밀)→domain Worker TDD(main-process/renderer 병렬)→reviewer **🔴 0**(신뢰경계 CRITICAL 통과)→typecheck 양쪽+단위 green→커밋. **단위 1235 green(+89)**. **라이브 검증 ✅(criterion ⑦)**: 슬래시 네이티브 실행=Phase 22 스파이크 기검증(/context turns=0). **이미지 비전 인지=실 SDK 스모크**(`artifacts/image-vision-smoke.mjs`, gitignore): 생성한 solid-red PNG 경로를 buildEnginePrompt 노트 포맷으로 query()에 전달 → 에이전트가 **Read 도구 호출**(tools=["Read"]) + **"Red" 인지**(turns=2, is_error=false). 경로 첨부→Read→비전 체인이 "조용한 무동작" 아님을 실증. **교훈: 단위가 못 닫는 단 하나의 결합부(SDK Read의 이미지 디코딩 동등성)를 실 SDK 스모크로 정밀 타격. [[agentcodegui-reference]]·[[replica-loop]].** **다음: M4-3**(멀티 6패널 동시실행·세션 CRUD).
- **#17 (2026-06-23 — 엔진 SDK 전환 ✅ Phase 21, ADR-016 구현)** — Task #7. ClaudeCodeBackend를 `claude -p` CLI spawn → `@anthropic-ai/claude-agent-sdk@0.3.186` `query()`로 재작성(원본 `src/main/claude/engine.ts` 미러). **착수 전 스모크 검증**(`artifacts/sdk-smoke.mjs`, gitignore): query()가 **구독 인증 구동**(ANTHROPIC_API_KEY 불요)·session_id 캡처·`modelUsage.contextWindow=200000`(haiku, 우리 상수 일치)·모델 alias `'haiku'` 해석. **약관 검토**: 구독+SDK 유효(위반 아님, CLI와 동일 한도). **ESM/CJS 스파이크 해소**: SDK=ESM-only이나 **Electron 42.4.1=Node 24.16.0**가 `require(esm)` 지원 + SDK 진입점 top-level await 0 → `externalizeDepsPlugin` 유지(번들 변경 불요). **패턴**: Explore(원본 engine.ts 전수 매핑)→Phase 정의(`phases/21_engine-sdk`)→plan-auditor **APPROVE-WITH-NOTES**(최대 리스크 ESM을 스파이크로 선해소)→agent-backend Worker TDD→reviewer **🟢 PASS(CRITICAL 0)**. **구현(21a~c)**: ① shared `AgentEventDone += contextWindow?`(실 컨텍스트 창, additive·하위호환). ② agent-backend `buildQueryOptions`(CLI 플래그→SDK 옵션, KNOWN_MODELS/MODEL_EFFORT_SUPPORT 재활용, effortToOptions/modeToPermission 미러[auto→acceptEdits]) + `mapClaudeStreamLine` 확장(result is_error/subtype, windowFromModelUsage→done.contextWindow, stream_event 무시) + ClaudeCodeBackend 재작성(**lazy dynamic import 주입형** — mock query 테스트가 실 SDK 비접촉, canUseTool 자동허용 `TODO(M4-4)`, abort=abortController+interrupt() best-effort). spawn/taskkill/detectClaudeCli/CLI 플래그 리터럴 제거. ③ renderer 게이지 `done.contextWindow` 우선·`MODEL_CONTEXT_WINDOW` fallback(**회귀 0** 명시 테스트). **커밋 1c47d58(21a/b)·d52b139(21c). typecheck 양쪽 green · 1096 tests green(+42) · lint green · 완료조건 ①~⑧ 충족.** **잔여 nit(비차단)**: (1) 주입 seam `as any`→`import type`로 컴파일타임 타입 복원(후속). (2) canUseTool 테스트 mode↔tool-name 문구(cosmetic). **ADR-016 상태=구현 완료(ADR 사용자 통제 — ADR.md 노트 갱신 권고).** **라이브 검증 ✅(2026-06-23, 실 Electron + 실 SDK)**: `tests/e2e/live-sdk.e2e.ts`(opt-in `LIVE_SDK=1`, AGENTDECK_E2E 미설정 → 실 ClaudeCodeBackend) 실행 → 프롬프트 "Reply with token LIVE_SDK_OK, no tools" → **Claude(Opus 4.8)가 정확히 `LIVE_SDK_OK` 응답**(echo였다면 프롬프트 전체 에코). 전 경로 닫힘: IPC(agent.run)→ClaudeCodeBackend.start()→SDK query()→실 모델→mapClaudeStreamLine→AgentEvent→webContents.send→renderer. 게이지=3K/1M 토큰(Opus 4.8 실 usage+실 컨텍스트 창). 스크린샷 `artifacts/live-sdk-run.png`. **교훈: 핵심 가정(SDK 구독 인증 구동)을 phase 투자 *전* 스모크로 de-risk + 완료 후 실 electron 라이브로 닫기(N9b 교훈). [[agentcodegui-reference]]·[[replica-loop]].** **다음: M4-2**(슬래시 실행·@mention·이미지·큐 — SDK가 헤드리스 제약 해소).
- **#16 (2026-06-23 — 진실 정합 패스 + 엔진 SDK 채택 결정)** — 사용자가 M4-2 헤드리스 제약(슬래시/이미지 실행불가)을 보고 "원본이 SDK인데 왜 우리가 CLI?"를 정확히 지적 → **확인**: 원본 `package.json`에 `@anthropic-ai/claude-agent-sdk`(SDK 기반), 우리는 SDK 의존성 0·`claude -p` CLI 단일. ADR-004("SDK 우선·CLI 폴백")는 *의도*였고 M1이 폴백만 구현. **결정: Agent SDK 채택(ADR-016)** — ClaudeCodeBackend를 원본 `src/main/claude/engine.ts` 미러로 재작성. 사용자 지시로 **`/workflow` 멀티에이전트 감사**(원본 전수 매핑 8 + 드리프트 5축 + 통합) 실행 → **34건 드리프트** 발굴(통합 에이전트가 43분 런어웨이[python payload 생성 루프]라 중단→캐시 재개 24ms 회수, 메인 통합). **일괄 정정 커밋 2e4e448**: 엔진 SDK-vs-CLI(ADR/PRD/ARCHITECTURE/CLAUDE/.claude/주석) "현재 CLI→SDK 전환(ADR-016)"으로 통일 · 마일스톤 상태 과대(FEATURE_MAP "전부✅=완전복제 달성"↔표 모순, B8 게이지 ⬜→✅) · 스택 출처(Zustand/sqlite/Vitest 등=원본 아닌 AgentDeck 확장) · 유령 명령/스택(npm run package·electron-builder 미설치=M5) · 하네스(simple-git→git.ts execFile) · 메모리(M2 동결) · LSP(omnisharp→Roslyn). typecheck green. **교훈: 문서를 부분 갱신하면 드리프트 누적 — 마일스톤 경계마다 진실 정합 패스 권장.** **다음: 엔진 SDK 전환 구현**(의존성 추가 + ClaudeCodeBackend 재작성, agent-backend Worker, 원본 engine.ts 미러) → 그 위에서 M4-2~4.
- **#15 (2026-06-23 — 라이브 검증: 풀 green + N9b 진짜 수정)** — 사용자 "먼저 라이브 검증" 결정 후 dev 앱 종료 → `rebuild:node`. **① store.test 11 green → 전체 1054 단위 green**(이전 적색은 실행중 앱 ABI 잠금 확정). **② M4-1 CLI 라이브 확인**: 설치된 `claude 2.1.186` `--help`가 `--model`(alias)·`--effort low/medium/high/xhigh/max`·`--permission-mode acceptEdits/auto/bypassPermissions/default/dontAsk/plan` 정확히 지원 → run-args 매핑이 실존 플래그·실허용값 타깃 확인(토큰 미사용). **③ N9b = 실제 버그였음(커밋 440aff3, #13의 "stale 캡처" 결론 오판 정정)**: 실 electron 진단(computed style + CSSOM 매칭 덤프)으로 `.login-body` 런타임 CSSOM 매칭 0 발견 → 원인=Profile.css 헤더 주석 `login-body/.lg-*/.pf-*`의 `*/`가 CSS 주석 조기종료 → 무효 셀렉터에 `.login-body{flex:1;display:flex}` 블록 흡수돼 규칙 드롭 → display:block 폴백 → 브랜드 패널 풀높이 미충족. **수정=주석 `*/` 제거 + e2e `login-body display==='flex'` 회귀 가드.** 재캡처로 풀높이=원본 ref-00 일치 확인. **교훈: 순수 CSS 버그도 격리 chromium 재현이 아니라 실 electron 빌드로 검증해야(번들러는 텍스트 보존하나 브라우저 파서가 오파싱). [[agentcodegui-reference]]에 기록.** 다음=사용자 체크인(M4-2 등).
- **#14 (2026-06-23 — M4-1 ✅ 기초+단일 에이전트 실 실행)** — Phase 20(`20_m4-1-foundation`). 사용자 "M4-1만 먼저, 후 체크인" 결정. **원본 매핑(Explore 9기능)→Phase 정의→plan-auditor 승인(blocker0,🟡5 반영)→서브웨이브 Worker TDD→reviewer(CRITICAL0)** 패턴. 커밋 **30996fa**(phase)·**fbe6272**(20a~c 백엔드)·**a341ab4**(20d renderer). **claude-code-guide 권위 확인**: `--model`(alias opus/sonnet/haiku/fable)·`--effort low|medium|high|xhigh|max`(모델의존: haiku 미지원·sonnet xhigh 미지원)·`--permission-mode`. **구현**: ① shared `AgentRunRequest +=model/effort/mode` + `MODEL_CONTEXT_WINDOW`(opus/sonnet/fable=1M·haiku=200K). ② agent-backend `run-args.ts`(순수, electron0) `buildRunArgs` allowlist 매핑(haiku effort 생략·sonnet xhigh→high 클램프·minimal 생략·미지 id 무시) + ClaudeCodeBackend base args 뒤 append. ③ main 핸들러 string 가드 패스스루. ④ renderer Composer picker→sendMessage→agentRun + ContextStrip을 `gaugeCalc`(done.usage÷window) 실데이터화. **신뢰경계 CRITICAL**: buildRunArgs=allowlist → 플래그 주입(`--dangerously-skip-permissions` 등) 차단, 결과 항상 플래그+값 쌍. 새 IPC/의존성 0. **TDD: run-args 26 + renderer 14 골든 green. typecheck 양쪽 green, 1043 단위 통과**(store.test 11 실패=실행중 앱 ABI 잠금, M4-1 무관). reviewer 통과(차단0). **🟡 신규 백로그**: (1) ClaudeCodeBackend `spawn shell:win32` + untrusted `-p prompt`(MVP부터 사전존재, M4-1 미도입) → 후속 셸 미경유 하드닝(`shell:false`+절대경로 or stdin). (2) pickerOptions haiku `ctx:1000` 표시 vs 게이지 window 200K 불일치(Phase 명시 범위 외) → picker ctx 정정. **다음: 사용자 체크인 → M4-2(슬래시 실행·@mention·이미지·큐).**
- **#13 (2026-06-23 — 🟡 백로그 정리 ✅, M4 전 출발선)** — task #5 5건 처리. **#2 onAskClaude nonce키**(커밋 584e5fd): `setTimeout(0)` 리셋 핵 → `InjectedInput{text,nonce}` 단조증가 키(같은 프롬프트 재클릭도 트리거, Conversation/Shell). **#3 push/pull 자격증명 마스킹**(커밋 fccc100): `maskCredentials`(scheme://userinfo@host→***) git.ts write 에러경로 적용, TDD git-mask.test 5 + 통합 1. **#4 nothing-to-commit 단언 강화**(fccc100): `ok===false`. **#5 JSDoc 핸들러 14→20**(커밋 5329947). **#1 N9b(Profile 브랜드 풀높이): 코드 변경 불필요 — 검증 결과 현재 CSS가 이미 풀높이 정상 렌더.** Chromium 순수 CSS 충실 재현(실제 `.pf-overlay`/`.login-body`/`.lg-brand` 규칙 + 실 폼 콘텐츠 + 동일 1888×1071)에서 brand 패널 풀높이·헤드라인 중앙=원본 ref-00 일치. 경쟁 글로벌 CSS 0·transform/filter 조상 0(fixed 격리 없음). ⇒ 05:15 `profile-onboarding.png`는 Wave2 중간 상태 **stale 캡처**. typecheck/lint green, 1003 단위 통과. ⚠️ **env**: 실행 중 dev 앱이 `better_sqlite3.node`(electron ABI) 잠금 → store.test 11건 적색(코드 무관) + 실 electron e2e 재캡처 불가. 해소=앱 닫고 `npm run rebuild:node`(미강제). ARCHITECTURE.md:40 🟡는 aac23ef에서 이미 정정됨. **다음: M4(멀티 동시실행·슬래시 실행·@mention·세션 CRUD·토큰게이지·권한/질문 — task #6). 원본 매핑(Explore)→Phase(`20_m4-*`)→plan-auditor→서브웨이브 Worker TDD→reviewer 패턴.**
- **#12 (2026-06-23, 야간 자율 루프 — Wave 2 + M3 Git ✅)** — **Wave 2(2차 시각검증) 커밋 2a6563d** + **M3 Git 완료(4커밋 3f8be90·8cb5b61·4f71573·b3c843a)**. **Wave 2**: Shell navigator.webdriver 게이트 test-open 훅 + visual-viewer 캡처로 default-off 모달 실스크린샷 → UpdateNotes/WhatsNew 원본 일치 확인, **N9 발견·수정**(Profile 오버레이 투명→앱 비침 → `.pf-overlay` 불투명 배경+z-index 200). 잔여 N9b(브랜드 패널 풀높이, M5게이트라 저우선). **M3 Git**(원본 AgentCodeGUI git.ts 미러, execFile 직접·의존성0): 3a read(shared git.* 9채널+preload + main/git.ts gitRoot/Status/Log/CommitDetail/FileAt/WorkingFile + 핸들러 6 + GIT_ROOT isAbsolute) · 3b write(gitCommit/Push[upstream -u 재시도]/Pull[--ff-only] + 핸들러 3, 로컬 bare remote 테스트, **실 origin push=인간 게이트**) · 3c(GitModal 샘플→실 IPC, gitSampleData 타입 단일화, AI커밋 위임 onAskClaude, Shell git.root 와이어링) + fs.diff HEAD 스냅샷 버그수정(ipc/index.ts FS_DIFF '빈기준→모두add' → gitHeadContent). **시각검증: GitModal이 실 git 워크스페이스의 history 2커밋·changes 1파일·커밋 컴포저·AI커밋 버튼을 원본 3컬럼 그대로 렌더(gitmodal-history/changes.png).** plan-auditor 승인(차단0)·reviewer 신뢰경계 CRITICAL 0(전 단계, git.ts electron import 0·자동 push 0). **1008 단위 + e2e 14 green.** D1/D2/D3/D4 ✅(FEATURE_MAP). 신뢰경계 절대 준수. **🟡 이월**: ARCHITECTURE.md:40(`src/main/git/` simple-git→실제 `src/main/git.ts` execFile, **사용자 정정 필요**=문서 단독통제), onAskClaude nonce키·push stderr 자격증명 마스킹·nothing-to-commit 단언·JSDoc 핸들러개수(저우선). **다음(사용자 결정 2026-06-23): ① 잔여 🟡 백로그 정리(N9b·onAskClaude nonce·push stderr 마스킹·nothing-to-commit 단언·JSDoc 개수 — task #5) → ② M4(멀티 동시실행·슬래시 실행·@mention·세션 CRUD·토큰게이지·권한/질문 — task #6).** 이후 M2-LSP·M5.
- **#11 (2026-06-23, 야간 자율 루프)** — **시각 audit 1차 패스 완료 + Wave 1 fix(Phase 18) ✅ 커밋 e3225d7**. 방법=원본 소스(C:/Dev/AgentCodeGUI) ↔ 우리 소스 직접 diff. **15 컴포넌트 충실 확인**, nit은 **단일/멀티 피커 분리로 멀티 옵션 드리프트**(N2~N6) + 인사말 동적데이터 미연결(N1) + todos scroll(N7)에만. **fix**: 공유 `pickerOptions.ts` 신설(단일/멀티 공유, 드리프트 차단) + Fable5 추가(우리 정식 ID) + Bypass 모드 공유 모드셋(빨강+warn) + 인사말 닉네임 + todos scroll. 897 테스트+37 신규 green, reviewer CRITICAL0, 시각검증(멀티 그리드 Opus4.8/Effort 매우높음/⚠Bypass/1M·빈채팅 "…개발자님?") 원본 1:1. 의도적 갭 N8=WhatsNew 풀스크린 비디오 생략. **다음(야간 루프): Wave2(2차 시각검증 — 모달 실캡처 대조) → Wave3+ M3 Git(AgentCodeGUI 레퍼런스). push는 인간 게이트.** 상세=task #1~4.
- **#10 (2026-06-23)** — **F15 ✅ (레이아웃 정정)**. 사용자 실측("탐색기 디테일 다름 + 채팅/코드 자동전환 의문") → **원본 직접 조작 실측**(Playwright `_electron`으로 레퍼런스 앱 구동, 프로필 게이트 통과·dialog 스텁·파일 클릭 → `.fv-overlay`/`.rzm` 플로팅 모달 확인). 결정=원본기반+기본 창모드. 이중 탭 자동전환 제거→탐색기·채팅 항상 표시, FileModal(fragment fixed 오버레이, 창모드 우측 도킹/최대화 토글), 탐색기 viewing 스위처+헤더/폴더리스트/kbd 힌트. 새 IPC 0. 2 Phase(`17_fidelity-f15-layout`), plan-auditor(🔴2 e2e 핸드오프 명시 반영)·reviewer CRITICAL0. **핸즈온 시각검증으로 모달 stacking 버그(absolute→fixed·pane 내부→fragment) 발견·수정.** 856 단위+25 e2e. **다음: 기능 연결 트랙(M3 Git/M4/M2-LSP/M5) 또는 추가 폴리싱.**
- **#1 (2026-06-22)** — 실측 전수 완료(원본·우리 양쪽 소스 인벤토리). 본 드라이버 작성. 갭을 웨이브 F7~F14(디자인) + 기능트랙(M3/M4/M5/LSP)으로 구조화. **다음: F7(설정 5탭) 분해·구현.**
- **#2 (2026-06-22)** — **F7 ✅**. 설정 5탭(Claude Code 버전 vpick / MCP·Skill scope+토글 / Code LSP / 테마) 시각 1:1, 정적 샘플, 새 IPC 0. 3 Phase(`09_fidelity-f7`), plan-auditor 승인·reviewer CRITICAL0. settings-tabs 25 단위 + 8 shell e2e + 탭별 스샷 육안검증. 잔여 🟡=install-card(M5/LSP). ⚠️ env: store.test 11건 better-sqlite3 ABI 잠금(실행 중 앱) — node ABI 복구 필요. **다음: F8(사이드바 세션+멀티 토글).**
- **#3 (2026-06-22)** — env 정상화(앱 닫음→`rebuild:node`, store.test 11/11 green). **F8 ✅**. 사이드바 단일/멀티 토글·세션 목록(5 샘플)·컨텍스트 메뉴·rename/삭제 다이얼로그·프로필 풋 설정 트리거. 정적 샘플+로컬 CRUD(시각), 세션 실동작=M4. Sidebar props 무변경(내부 로컬 state)·Shell.tsx 무변경. 3 Phase(`10_fidelity-f8`), plan-auditor(🔴2 반영)·reviewer CRITICAL0. 464 단위 + 19 e2e + 스샷 육안 1:1. **다음: F9(컴포저 트레이: 슬래시/@멘션/첨부/큐).**
- **#4 (2026-06-22, 자율)** — **F9 ✅**. 컴포저 슬래시 메뉴(6 커맨드+스킬)·@멘션 팔레트(샘플 트리, dir 드릴/상위복귀)·이미지 첨부 트레이(샘플 썸네일)·드롭 힌트·예약 큐 스트립(optional prop)·placeholder 3-상태(hasStarted). 실행/해석/저장/드레인=M4. 새 IPC 0. 3 Phase(`11_fidelity-f9`), plan-auditor(🟡4 반영)·reviewer CRITICAL0. 495 단위 + 20 e2e + 슬래시 스샷 육안 1:1. **다음: F10(RecentFiles 탭바 + 패널 todo/서브에이전트).**
- **#5 (2026-06-22, 자율)** — **F10 ✅**. RecentFiles 탭바(코드 패널 위, 실 opened-files=store recentFiles, FLIP 재정렬·ctx-menu) + AgentPanel 강화(Todos progress·SubAgent 카드·SubAgentModal·FileRow). 패널 populated=optional prop+샘플(라이브 빈상태, M4). 새 IPC 0·Shell 최소변경·AgentPanel 무인자. 3 Phase(`12_fidelity-f10`), plan-auditor(🟡2 반영)·reviewer CRITICAL0. 527 단위 + 21 e2e + 탭바 스샷 1:1. e2e 상태오염(leftTab) 수정. **다음: F11(모달군1: Git/폴더전환/프롬프트/Ask).**
- **#9 (2026-06-23, 자율)** — **F14 ✅ = 디자인 트랙 F1~F14 완성**. 폴리시: ZoomBadge(Ctrl+휠)·PermissionModal/QuestionModal(default off)·메시지 타임스탬프·thinking/notice·SelectionToolbar·전역 단축키 골격·창 스냅(main geometry 순수함수). 새 IPC 0(창 스냅 순수함수+기존 controls). 4 Phase(`16_fidelity-f14`), plan-auditor 승인·reviewer 신뢰경계 CRITICAL0. 824 단위+25 e2e. 🟡 고스트 프리뷰 생략. **디자인 트랙 종료 → 사용자 종합 보고. 다음=기능 연결 트랙(M3/M4/M5/LSP).**
- **#8 (2026-06-23, 자율)** — **F13 ✅**. 멀티에이전트 워크스페이스 그리드(2~6 패널 헤더 count 탭·열 변동·패널[슬롯·상태·컨텍스트 링·RunPickers·PanelComposer]·확장 모달·일괄 폴더). store workspaceMode 트리거(Sidebar mode 로컬→store, props 무변경)·Shell multi 분기(메인 영역 교체·single 상태 보존). 새 IPC 0(window.api.multi 미사용). 3 Phase(`15_fidelity-f13`), plan-auditor(🟡3)·reviewer CRITICAL0. 757 단위+24 e2e. afterEach store reset 동기화. **다음: F14(폴리시 — 마지막 디자인 웨이브) → F14 후 종합 보고.**
- **#7 (2026-06-22, 자율)** — **F12 ✅**. 모달군2: ImageViewer 라이트박스(첨부 트리거·줌·필름스트립) + WhatsNew(6슬라이드) + UpdateNotes(마퀴·번호) + EngineGate/AppUpdateGate(install-card) + Profile(2분할 로그인). ImageViewer=라이브 e2e; 5개=라이프사이클 단위 시각(default off·트리거 M5). iv-overlay absolute→fixed 수정. 4 Phase(`14_fidelity-f12`), plan-auditor(🟡4)·reviewer CRITICAL0. 715 단위+23 e2e. **다음: F13(멀티에이전트 워크스페이스 그리드).**
- **#6 (2026-06-22, 자율)** — **F11 ✅**. 모달군1: GitModal(헤더·5nav·일자별 커밋 히스토리·커밋 상세·변경뷰 컴포저) + PromptModal(4000 카운터) + AskModal(orb·휘발성·최소화 알약) + FolderSwitchDialog. 자기완결 트리거(GitModal=탐색기 git버튼, PromptModal=Sidebar 내부 로컬, AskModal=Composer onSlashAsk 하위호환, FolderSwitch=단위). 정적 샘플, git=M3·ask=M4. 4 Phase(`13_fidelity-f11`), plan-auditor(🔴3→자기완결+4분리 반영)·reviewer CRITICAL0. 610 단위+22 e2e + GitModal 스샷 1:1. e2e 워크스페이스 보장 위해 visual-viewer로 이동. **다음: F12(모달군2: ImageViewer/WhatsNew/UpdateNotes/게이트/Profile 로그인).**

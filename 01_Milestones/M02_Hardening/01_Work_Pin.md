# work-pin — 작업 좌표 (M02)

세션 재개의 단일 인수인계 표면입니다.
좌표와 마감 요약은 현재 상태만 덮어쓰기로 유지하고, 이력은 git이 보유합니다.
결정과 검증의 정본은 같은 폴더의 `_MilestonePreview.md`와 Phase 파일 다섯 개입니다.
gate-config의 pin 포인터가 이 파일로 전환되면(Phase 1 Step 3), 세션 기동 시 이 파일의 전문이 자동 주입됩니다.

## 좌표

- 마일스톤: **M02 Hardening (하네스 강도 수리)** — `01_Milestones/M02_Hardening/_MilestonePreview.md` (Phase 파일 5개)
- 상태: **M02 종결** (2026-08-10) — 마감 절차 ①~⑩이 전부 끝났습니다. Phase 1~5 집행 완료(검증 기록 합계 PASS 62·FAIL 0·USER-INPUT 5), 검수 두 축, 마감 보고서 등재, [USER] 마감 판정, 원격 push까지 마쳤습니다.
- 다음 액션: **M03 노션 사례 보강** — 새 마일스톤이라 계획 문서부터 세웁니다. 착수 전에 볼 것은 `99_Backlog.md` 10건인데, 그중 10번(래퍼 뒤 플래그·할당 토큰이 명령 위치 연쇄를 끊는 가드 회귀 2표면)이 가장 무겁습니다 — 후속 하네스 마일스톤의 최우선 후보입니다.
- 규율: Phase 경계는 전부 새 워커 세션(top-level, `claude --agent worker` 기동 — 역할 신호 agent_type=worker 실측 성립)입니다. `--resume`은 전 구간 금지이고, 재개는 이 pin과 계획 문서로만 합니다. 인수 대조는 사람 감사 수준([USER] 결정 1)입니다.
- 모델 라우팅: 정본은 `00_Documents/02_Rules/02_모델-라우팅-정책.md`입니다.

## 마감 요약 (최신 1건 — 3줄: 바뀐 것 / 내린 결정 / 봐야 할 것)

- 스탬프: 2026-08-10T22:47:03+09:00
- 바뀐 것: M02가 종결됐습니다 — 코디네이터가 검수 두 축의 공통 지목을 두 가드 직접 대조로 실측 확인하고, [USER] 마감 판정 줄과 Backlog 10번을 등재한 뒤 하드 게이트 다섯 종을 다시 통과시켜 종결 커밋을 만들고 원격에 push했습니다.
- 내린 결정: 가드 회귀 2표면(`bash -c` 래퍼·`env VAR=값` 접두)은 수리하지 않고 이월하며, 완료선 둘째 조항은 「동등성 기준표가 열거한 표면 범위 안에서 Moodie 동등 이상」으로 읽어 충족으로 판정했습니다 — 방어층은 안건 2와 같은 논리로 Claude Code 내장 권한 분류기입니다.
- 봐야 할 것: Backlog 10번이 다음 하네스 작업의 최우선 후보입니다 — 위험 명령을 `bash -c "…"`로 감싸면 현재 가드를 통과하고, 같은 뿌리로 push 게이트도 훅만으로는 닫히지 않습니다.

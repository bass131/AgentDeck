# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
out/
build/Release/
release/
*.tsbuildinfo

# Electron / packaging
dist-electron/
app.asar
*.exe
*.nsis
latest.yml
*.blockmap

# Secrets / env (CRITICAL: 키 하드코딩 금지)
.env
.env.*
!.env.example
secrets/
*.pem
*.key

# Local DB / persistence
*.sqlite
*.sqlite3
*.db
userData/

# Logs
*.log
!99_Others/98_V1_Archive/claude-state/*.log
npm-debug.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# Playwright e2e 산출물
test-results/
playwright-report/
.playwright/

# 시각 검증 스크린샷 산출물 (재생성 가능 — 커밋 X). 루트 한정 —
# 00_Documents/Artifacts(마일스톤 검증 증거, 재생성 불가)는 커밋 대상 (영호 2026-07-14)
/artifacts/

# CodeGraph 인덱스 (로컬 캐시 — init 2.9s로 재생성 가능, 머신별 로컬)
.codegraph/

# Harness 실행 상태 (로컬)
.claude/state/*
.claude/scheduled_tasks.lock
.codex/state/

# 게이트 상태물 (M01 Bootstrap) — 훅이 발화하면서 만드는 세션 로컬 상태다.
# 저장소에 박제되면 다른 머신이 남의 세션 상태를 물려받은 채로 시작한다.
# 좌표 포인터인 gate-config.json은 커밋 대상이므로 여기 넣지 않는다.
98_Management/01_GateState/*.state.json
98_Management/01_GateState/hook-log.jsonl

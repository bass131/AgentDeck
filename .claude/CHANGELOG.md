# CHANGELOG — 헌법·ADR·하네스·공유계약 변경 이력

> **역할**: 헌법(`CLAUDE.md`)/ADR/하네스(`.claude/**`)/공유계약(`src/shared/**`)을 바꿀 때마다
> 한 줄 박제. **compact·세션 경계에서 "옛 결정 기반으로 작업하는 사고"를 예방**(솔로+AI 맥락 —
> ClaudeDev의 "팀원 매일 열람"을 세션 기억으로 적응). 코드 기능 변경은 git log가 진실원 — 여기엔
> **결정/계약/하네스** 변경만.
>
> **갱신 규칙**:
> - 변경 commit 직후 한 줄 추가(최신이 위).
> - 형식: `YYYY-MM-DD — 한 줄 요약 (영향 범위 / 위험도)`
> - 위험도: `[L]` 저위험(추가만) / `[M]` 중간(행동·계약 변경) / `[H]` 고위험(결정 뒤집기·헌법 수정)
> - `[H]`는 별도 사용자 확인 권장.

## 위험도 짧게
- **[L]** — 새 커맨드·새 서브에이전트·새 ADR 박제(기존 결정 불변)·새 공유 *추가* 필드(옵셔널).
- **[M]** — 커맨드 동작 변경·기존 ADR 보강·공유 계약 *행동* 변경·하네스 게이트 변경.
- **[H]** — 헌법 절대 원칙 수정·기존 ADR 뒤집기·권한/영역 경계 변경.

---

## 이력 (최신이 위)

| 날짜 | 변경 | 위험도 |
|------|------|--------|
| 2026-07-26 | **SubAgent 턴 상한(`maxTurns`) 전면 폐기 + 폭주 감지를 「진전 축」으로 확장 (영호 결정)** — 발단은 실측이다. `chief-tech-operator`에 HR2 전반 검토를 위임했더니 최종 보고가 아니라 **문장 중간**이 돌아왔고(누적 192,843 토큰 / 도구 호출 37회), 원인은 frontmatter `maxTurns: 25`였다. 여기서 둘이 드러났다 — ① **값에 근거가 없었다**(25·40은 실측이 아니라 산술이었고, 대체안으로 제안한 150·250도 같은 성질이라 영호가 그 점을 짚었다) ② **막는 방향이 틀렸다**(`maxTurns`가 세는 것은 *에이전트 턴* = 도구 사용 왕복이지 *진전 여부*가 아니라서, 폭주는 못 막고 성실한 장기 작업만 절단한다). ⇒ 10역할 frontmatter에서 전량 제거하고 **SDK 기본값(무제한)** 으로 환원(잔량 0 · frontmatter 7키 균일). 빈자리는 둘로 메웠다. ▪ **[hook] `circuit-breaker` 판정 2축화** — 기존 *총량 축*(같은 변이 도구 N회/5분, 등급별 임계 5·10·15·20)에 **대상 축**(같은 `주체·도구·대상` 조합 10회/5분, 등급 무관)을 더했다. 근거 실측: 같은 날 정상 작업이 5분에 Edit 15회를 찍었지만(에이전트 정의 10개 일괄 수정) 대상별로 쪼개면 파일당 1~2회였고, 같은 파일 반복의 최대는 6회였다 — 즉 폭주의 신호는 "얼마나 많이 했나"가 아니라 **"진전이 있나"** 이고, 임계 10은 실측 최대의 약 1.6배(경보 피로 회피). ⭐ **읽기 도구 면제를 대상 축에서만 해제** — 면제 목록(`Bash|Read|Grep|Glob|Task`)이 읽기 전용 역할 4종(`chief-tech-operator`·`reviewer`·`plan-auditor`·`coordinator`)의 도구 목록과 **정확히 일치**해 그들에겐 폭주 감지가 **0**이었다. 같은 파일을 10번 읽는 것은 정당한 반복이 아니므로 대상 축에서는 감시한다(총량 축 면제는 유지 — 읽기 총량은 정상적으로 많다). 두 축이 동시에 걸려도 알림은 하나(stdout JSON 파서 보호, 구체적인 대상 축 우선). ▪ **[정책] `subagent-routing.md` §5.7 「절단 감지와 재개」 신설** — `maxTurns`가 없어도 런타임 사정으로 결과는 잘릴 수 있고, **절단은 오류가 아니라서 읽기 전엔 성공과 구분되지 않는다**. 규범: 서브 결과가 §6 브리프 *출력* 항목의 형식이 아니면(문장 중간 종료·결론 없는 관찰 나열·"이제 …하겠습니다" 종결) **절단으로 간주해 채택하지 않고** `SendMessage`로 재개한다(새 `Agent` 호출은 처음부터라 토큰 이중 지불). 같은 지점에서 또 끊기면 브리프가 큰 것 → 쪼개 재위임 + work-pin 기재. ⚠️ 이 검사는 **훅이 대신하지 않는다**(문장이 유일한 방어선). ▪ **[test] TDD 5종 신설**(`_lib/circuit-breaker.test.mjs`) — 같은 대상 반복 발화 / ⭐ 흩어진 12파일 무발화(정상 작업 회귀 고정) / 읽기 도구 대상 축 감시 / 주체별 카운터 분리(병렬 서브 오탐 방지) / 기존 총량 판정 무회귀. 기존 `hook-advisory.test.mjs`의 등급 추출 회귀 테스트는 **픽스처를 정정**했다 — 같은 파일 11회를 반복하던 입력이 신설 대상 축에 걸려 red가 됐는데, 그 테스트가 검증하는 것은 *등급 추출*이지 "같은 파일 11회가 정상이다"가 아니므로 대상을 파일마다 흩었다(판정기 완화 아님 — 2026-07-17 「조용한 사망」 회귀 가드는 그대로). 훅 회귀 **101/101 green**. | 🟡 중 |
| 2026-07-26 | **완료 보고 정책 개정 — HTML 시각화가 의무에서 「영호 요청 시」로 (영호 결정)** — 동기는 **실측된 비용 역전**이다. `Report-YYH-Style` 조판 계약이 아트워크 선택·적대 검증 4렌즈·조판 게이트 3종 수렴(어절 절단·가로 넘침·의미줄 꺾임 전부 0건)을 요구해서, 마일스톤 종결마다 **보고 내용보다 조판이 비쌌다** — HR2 종결이 실제로 그 지점에서 멈췄고 영호가 *"매 마일스톤마다 HTML 쓰는 과정이 너무 무거우니 원할 때만"* 으로 결정했다. ⭐ **무거운 쪽은 조판이지 보고가 아니므로 MD 계약은 한 줄도 안 낮췄다** — 필수 H2 4종(`TL;DR`·`5단계 보고`·`AC 검증 결과`·`학습 일지 후보 키워드`)·5단계 라벨·AC 실행 증적은 그대로 기계 강제다. 내린 것은 `report_html` **하나**(필수 → 선택). 구현 = `done-report-policy.mjs` 필수 필드 목록에서 제외 + HTML 실재·라벨 검사를 **필드를 적었을 때만** 발동(TDD — red 확인 후 구현). ⚠️ **선택 = "적고 안 지켜도 된다"가 아니다**: 적은 순간 그 HTML은 실재해야 하고 5 라벨을 담아야 하며, 회귀 테스트가 이를 고정한다(신설 2건, 훅 **94 → 96 pass**). 문서 정합 6곳: `reporting-format.md`(§1 표·§3 제목·강제 출처 주석·갱신 이력) · `grade-and-risk.md`(4등급 표·처리 패턴·산출물 표) · `pin-and-done.md`(frontmatter 예시·strict 항목 1·3·라이프사이클 트리) · `done-md-template.md`(주석 처리된 선택 필드) · `INDEX.md` 카탈로그. ⚠️ **비대칭 잔존**: 같은 계약을 `.codex/hooks/agentdeck-hook.mjs`가 **독립 구현**하는데 CORE-12로 Claude는 읽지도 고치지도 못한다 — Codex 대칭은 영호가 Codex 세션에서 처리해야 한다(P10 선례). 반영 전까지 **엔진별 게이트 판정이 갈린다**. | [M] |
| 2026-07-26 | **HR2 유지보수 창 — Opus 5 리뉴얼 + 폴더 언더스코어 개명 (✅ 완료, 12 Phase)** — 창 단위 누적 엔트리(2026-07-24 선례). ▪ **[종결 2026-07-26]** 12 Phase 전부 done + 종결 작업 완료. 회고·Phase별 결과·AC 증적·백로그 7건 = **[`01_Phases/21_HR2-opus5-renewal/HR2-DONE.md`](../01_Phases/21_HR2-opus5-renewal/HR2-DONE.md)**. 마감 게이트 재실행(description 압축 + 보고 정책 변경 반영 후) **6/6 green** — typecheck 0 · lint 0 · vitest **395파일 5,330 passed** · build 성공 · 훅 **96/96** · conformance 12/12. 📌 **기준선 대비 회귀 0**(P01 394파일/5,325 → +1파일·+5는 P02 신설분뿐) — 1,342파일 이동 + 2,472줄 치환의 최종 성적. 남은 것은 사람 게이트 둘(창 CLOSE · PR)뿐이다. ▪ **[P12 이관분, 2026-07-26 창 재개방] 에이전트 description 압축 — 상주 listing 762 → 677 tok** — 에이전트 10종 중 5종 수정(2,069→1,731자, **−338**): `secretary` 319→201 · `agent-backend` 268→153 · `coordinator` 219→157 · `main-process` 200→164 · `shared-ipc` 224→217. 나머지 5종은 트리거 조건·승인 규칙이 본체라 **무변경**. 삭제분은 **전건 본문 중복**(ADR·Phase 번호·SDK 패키지명·부연 괄호) — 다섯 파일 본문 대조로 확인, 이동처 필요 0건. ⭐ **P11 이관이 전제한 "새 세션이면 발화 프로브가 가능하다"가 절반만 맞았다** — listing은 **세션 시작 스냅샷**이라 *이전 창 편집분*의 검증엔 성립하지만(디스크 = 이 세션 listing 일치로 실증) *이번 편집분*엔 같은 벽이 다시 선다. 편집 세션과 검증 세션은 **구조적으로 겹칠 수 없고**, 세션을 더 태워도 무한 후퇴다. ⇒ **검증을 설계로 대체**: 발화를 좌우하는 요소(`Use PROACTIVELY`·경로 글롭·트리거 조건절)를 불가침으로 두고 `git show HEAD:` 대조로 보존 확인(**5/5**). 게이트 훅 **94/94** · `harness-conformance` **12/12** · `agent-model-canon` **5/5**. 📌 잔여 절감 여지 ≈ 0(스킬 512는 `work-run` 자동발화라 불가침, 커맨드 6종 평균 78자). HR2 다이어트 누계 **4,460 → 3,439 tok(−1,021)**. ▪ **[P11] 발화 프로브 7종 전부 green + 마감 게이트 6/6 — 창을 닫고 실제 운영 조건에서 실측** — ① **프로브 ① 봉인 차단**: 새 경로 `00_Documents/harness/**`에 shell 쓰기 시도 → `supervisor-guard` 차단. ⭐ **P07의 존재 이유가 라이브로 증명됐다** — 옛 정규식(`00\.documents`)이었다면 여기서 **통과**했을 것이고 그게 P07이 임시 사본으로 실측했던 fail-open이다. `[._]` 문자 클래스 전환이 실제로 작동한다. canary는 **존재하지 않는 경로**를 써서, 통과했더라도 정본이 손상되지 않게 했다(잔존 0건). ② ⭐ **프로브 ② — 훅 출력만 봤으면 오독할 뻔했다.** `02_Source/main/*.ts` Write 시도에 화면엔 **tdd-guard 메시지 하나만** 떴는데, `guard-blocks.log`는 `22:05:04.435 supervisor-guard block 앱 코드 편집` / `22:05:04.489 tdd-guard block`을 기록했다 — **supervisor-guard가 54ms 먼저 차단**했고 두 훅이 병렬로 **둘 다 발화**했다. ⇒ **로그가 훅 출력보다 강한 증거다.** 훅이 여럿 걸리는 경로에서 화면 메시지는 *"가장 먼저 막은 훅"* 이라는 보장이 없다. ③ ⭐ **프로브 ③ 분류 오류를 실측이 교정** — Phase 문서가 ③(OpenGate 감사 로그)을 *"창을 닫아야 실측 가능"* 칸에 넣었는데 **정반대**였다. ①②는 *차단*을 보므로 봉인이 살아 있어야 하고, ③은 *통과 기록*을 보므로 봉인이 풀려 있어야 한다. **"창 상태에 의존한다"는 공통점이 "같은 시점에 검증한다"를 뜻하지 않는다.** 닫기 직전에 확보한 결과 = `open-gate` **489건**(`flag age` 동반 기록). P03이 🔴로 발견하고 P05가 봉합한 `guard-log.mjs:29` 이분법 결함(당시 grep **0건**)의 **첫 실증**이고, ADR-038:9가 위협 모델 완화의 *대가*로 내세운 *"전량 open-gate로 남아 사후 감사 가능"* 계약이 선언 이후 **처음 지불됐다**. ④ **P07 이관 검증 green** — CLOSE가 SEALED판을 복원하자 `settings.json` deny **20줄**에 새 경로 4줄(`00_Documents/harness/**` 외 3)이 나타났다. ⭐ 이 검증이 CLOSE 이후에만 가능했던 이유가 그대로 확인 — P07은 그 파일을 **일부러 손대지 않았다**(창이 열린 동안 그건 OPEN 사본이고, **permission 계층은 gate flag로 우회되지 않으므로** 거기에 deny를 쓰면 그 즉시 자기 세션이 잠긴다). 이번 창 내내 *"개방판이라 커밋 금지"* 였던 파일이 CLOSE 후 **정당한 산출물로 성격이 바뀌어** 커밋 대상이 됐다. ⑤ **덤: tdd-guard 안내 경로가 새 이름을 가리킨다** — 차단 메시지가 `99_Others/tests/**`를 안내했다. P07이 대상 판정과 테스트 탐색을 **짝으로** 고치며 남긴 완료 조건(*"차단 메시지도 같은 변수를 쓰므로 개명 후 없는 경로를 안내하는 일이 구조적으로 불가능해졌다"*)의 확인이다 — 한쪽만 고쳤다면 훅은 새 경로에서 막으면서 안내는 **존재하지 않는 `99.Others/tests/`** 를 가리켰다. ⑥ ⚠️⭐ **예상 못 한 프로브 — 회귀 게이트 실행이 막혔다.** `npm run typecheck` → *"회귀 게이트 실행은 secretary에 위임하세요"*. 창이 열린 동안엔 전부 통과했으므로(실측 ⑰) 이번 창에서 처음 마주친 발화다. ⇒ **`execution-owner` 판정표의 "게이트 실행 = 위임"은 문서 규범이 아니라 기계 강제**다(P06 강제 출처 3층에서 이 항목 등급 상향). **동시에 구조적 막힘을 드러냈다** — 헌법(*"done 판사 = CI 회귀 게이트"*) + 훅(메인 금지) + 세션 지시(*"Agent 호출 금지"*)가 동시에 참이면 **메인이 게이트를 돌릴 경로가 없다**. 영호 승인으로 secretary 위임 해소. ⭐ 위임이 작동하는 근거는 훅 소스에 있다 — `[ -n "$AGENT_TYPE" ] && exit 0`(서브는 ②절 면제). 즉 *"위임하세요"* 는 **실제로 작동하는 안내**이지 막다른 길 표지판이 아니다(ADR-038 개정 1이 걷어낸 유형과 대비). ⑦ **마감 게이트 6/6 green**(봉인 복귀 = 실제 운영 조건): typecheck 0 · lint 0 · vitest **395파일 / 5,330 passed** · build 0 · **훅 94/94** · e2e `engine-update` **2 passed**(실 npm registry 라이브). 📌 **기준선 대비 회귀 0** — P01 시점 394파일/5,325에서 +1파일·+5는 P02 신설분(`agent-model-canon.test.ts`)뿐이다. **1,342 파일이 이동하고 2,472줄이 치환된 창의 최종 성적.** ⑧ **잔여 = description listing 압축, 마일스톤은 다음 세션까지 열어둔다**(영호 결정). ⚠️ **구조적 모순 발견** — 편집은 `.claude/agents/**`가 봉인 대상이라 **창 필요**, 검증(발화 프로브)은 정의 워처 사망 때문에 **새 세션 필요**. 한 세션이 둘 다 만족하려면 **새 세션에서 OPEN-GATE를 함께 실행**해야 한다. P12가 "P11 이관"이라고만 적었을 때는 못 본 모순이다. ▪ **[P10] Codex 대칭 갱신 — 영호가 직접 기동한 Codex 세션이 수행(CORE-12 준수 경로)** — Claude는 `.codex/**`를 **한 줄도 쓰지 않고** 브리프 작성·결과 검증·커밋·문서 마감만 맡았다. ① **치환 63건 예측 일치**(실제 바뀐 문자열 토큰은 **65개** — 한 줄에 옛 경로가 2개인 곳이 둘). Claude 교차 검증 4종 전부 통과: 점 표기 잔존 **0건** · 이스케이프 정규식 잔존 **0건** · 의미 손상 패턴(`(새이름).{0,14}(→\|옛\|이전\|리네임\|개명)`) **0건** · fail-open 매처 본체 육안(`riskFlagsFor` 384-389 · `isImplementationPath` 395-397 · `htmlTarget` 631 · 테스트 루트 449 · TDD deny 588) **전부 새 경로**. ② ⭐ **Codex가 브리프에 없던 것을 잡았다 — `hooks.json`의 SHA-256 캐시버스터 8곳.** 훅 본문을 고치면 digest가 바뀌어 훅이 **신뢰되지 않은 상태**가 되는데 브리프는 이를 빠뜨렸다. 놓쳤다면 *경로는 다 고쳤는데 훅 자체가 안 도는* 가장 나쁜 반쪽 수정이 됐을 자리다(계약 테스트 `:141`이 red로 잡았겠지만 **테스트가 없었다면 조용히 죽는다**). ⇒ 브리프에 **왜 하는지**(fail-open이 침묵한다)를 적은 것이 범위 밖 추론을 가능하게 했다 — *"시킨 것만 하는 실행기"* 로 대하면 이런 발견은 안 나온다. **위임의 품질은 브리프의 정확도에 비례하지 않는다 — 넘어서기도 한다.** ③ ⭐ **새 계약이 옛 계약보다 강해졌다** — 폐기된 `:132`(*"coordinator만 `tools: Agent` 보유"*, ADR-010 개정 1로 무효)를 대신한 단언은 역할 5개를 **손으로 나열**하던 것을 `fs.readdirSync` **동적 열거 + `roles.length === 10`**으로 바꿨다. 역할이 하나 늘면 옛 테스트는 조용히 통과했겠지만 새 것은 **red**다 — **계약이 문서가 아니라 파일 시스템에 붙었다.** `:186`은 `풀 8` 금지 가드를 유지하고 `SubAgent 풀 분해 적정성 (10개 적정한가)` **적극 단언을 추가**(금지만 있으면 문구가 통째로 사라져도 통과한다). `:103`은 개명·재귀 계약과 무관해 **수정 안 함**(Claude 판단에 Codex 동의). ④ ⚠️ **미해소 1건 = `REVALIDATION_REQUIRED`(codex-cli 0.145.0 ≠ baseline 0.144.1) — 버그가 아니라 계약이 일한 것.** 원인은 개명이 아니라 **CLI 업그레이드라는 환경 드리프트**이고, ADR-033:30이 설계한 *"CLI 버전 불일치 시 **결과가 같아도** exit 3"* 의 **2회차 발화**다(1회차 = 2026-07-13 도입 당일 0.144.0→0.144.1). ⭐ baseline을 0.145.0으로 덮어쓰면 즉시 green이 되지만 그건 **재실측 없이 계약을 통과시키는 것** — ADR-033이 정확히 막으려던 행위다. **게이트를 green으로 만드는 가장 빠른 길이 게이트를 무력화하는 길과 같을 때가 있다.** 해소에 필요한 격리 canary 재실측은 Codex 세션 재기동을 요구하므로(attended 절차) **범위 밖 안건으로 백로그 등재** — 후속 2단계(`00_Documents/harness/codex-baseline.json` 갱신 + ADR-033 재실측 이력)는 Claude 영역이고 **봉인 해제 불필요**다. ⑤ **1차 진단 세션의 재료가 그대로 적중** — 계약 red의 1차 원인은 예상했던 coordinator가 아니라 `harness-doctor.mjs:18`의 옛 baseline 경로였고(**import 단계 사망**이라 개별 test에 도달조차 못 했다), 고치자 나머지 단언이 보였다. ⑥ **검증**: `.codex` 테스트 **30/31**(위 ④ 제외 전부 green) · typecheck **exit 0** · vitest **395파일 passed / 6 skipped / 0 failed**. ⑦ **잔여 = `/hooks` 재검토·재신뢰**(훅 digest 변경 — 신뢰된 새 세션 필요, **영호 몫**·P11 절차). Codex가 CORE-11 충돌(*"`.claude/**` 수정 금지"*)로 못 쓴 CHANGELOG는 **Claude가 대행**. ⑧ **학습**: CORE-12는 *"못 하게 하는 규칙"* 이 아니라 ***"누가 하는지 정하는 규칙"*** 이었다 — 제약은 작업을 막지 않고 **브리프를 정확히 쓰는 일**로 형태를 바꿨을 뿐이며, 그 브리프가 부정확했을 때(42건) 위임받은 쪽이 바로잡았다(63건). **격리는 비용이지만 교차 검증의 기회이기도 하다.** ▪ **[P12] 상주 컨텍스트 다이어트 — 4,460 → 3,524 tok (−936, 선불 포함 −1,070)** — 판정선은 "길다/짧다"가 아니라 **도출 가능성**이다. ① **최대 절감 = 멀티에이전트 표(823→~320 tok)이고 정보 손실 0** — `_routing.md:10-13,17-20,78-87`(R/W/**금지** 3열로 더 상세)·`:28-30`(등급·모델 티어)·에이전트 description이 같은 내용을 이미 갖고 있었다. **세 번째 사본이었고 셋 중 가장 얕았다.** ⇒ 중복 제거와 삭제는 다르다 — **길이가 아니라 사본 수가 판정 기준**이다. ② ⚠️ **표에만 있던 규칙 1건을 건져 별도 줄로 승격** — *"chief-tech-operator는 영호 승인 후에만 호출"*(Fable 5 단가). **행 단위로 "중복이냐"를 물으면 놓친다 — 셀 단위로 물어야 했다.** 승격 없이 표를 지웠으면 비용 사고 벡터가 열렸다. ③ ⚠️ **"완료 마일스톤 폴더 보존"(영호 2026-07-03 빈 폴더 원칙 폐기)은 CLAUDE.md가 유일 소유자였다** — 저장소 전체 grep 0건. `01_Phases/INDEX.md`에 **이동처를 새로 만들어** 옮겼다. 이게 이 Phase가 경고한 *"이동처 없는 삭제는 삭제"* 의 실사례이고, grep 없이 잘랐다면 영호의 결정이 조용히 소실됐다. ④ **명령어 절 폐지**(144→0) — `package.json` scripts와 1:1 파생. 절 안에 섞여 있던 *명령이 아닌 것* 3건(릴리스 ask 게이트 = 개발 프로세스 절이 이미 소유하던 중복 · `/work-plan` 산출 흐름 = SKILL.md 소유 · 위 ③)은 각각 이동처를 확인하고 처리. ⑤ **기술 스택 절(351→~215)** — 버전·패키지 나열은 `package.json` 파생이라 전량 삭제하고, *왜 그 선택인가*(원본 일치 vs AgentDeck 확장 **분류** · `claude -p` 폴백 없는 제거 · writeFileAtomic 미이식 · sqlite 제거의 대가)만 남겼다. **긴 쪽이 잘리고 짧은 쪽이 남는 전형.** ⑥ **문서 지도(229→~150)** — 파일 *존재*는 Glob 파생이라 자르고 **읽는 순서**(1~5)와 *무엇의 정본인가*만 남겼다. FEATURE_MAP·REPL_TRANSITION의 진행 상태 복제는 드리프트 소스라 삭제(이 창이 그 드리프트를 여러 건 치웠다). ⑦ ⭐ **자기 보호 문장 신설** — P06의 발견(*"기계는 하지 말 것[봉인·파괴·비가역]은 막지만 해야 할 것[reviewer·plan-auditor 호출·게이트 실행·등급 기재]은 강제하지 않는다"*)을 헌법 하네스 게이트 절에 경고로 박았다. **순 절감을 조금 깎아서 다음 다이어트가 문서 규범 전용 문장을 지우는 것을 막는 거래**다. ⑧ **`MEMORY.md` 인덱스 717→548** — 후크 문장만 압축, 본문 파일 무변경(인덱스는 "무엇이 있는지"만 알면 되고 상세는 필요할 때 읽힌다). ⑨ **description listing은 미수행 — P11(새 세션) 이관.** 완료 조건이 요구하는 *"줄인 뒤 발화 프로브 1회, 안 뜨면 되돌린다"* 가 **이 세션에선 거짓 결과**를 낸다(에이전트 정의 워처 사망 — 이 창에서 기계 확정). 절감 잠재량은 상위 3개를 30% 깎아도 ~60 tok인데 발화가 죽으면 **절감이 아니라 기능 상실**이라 위험 대비 이득이 없다. 실측 현황 = 프로젝트 description 3,046자 / 762 tok. ⑩ **검증**: CRITICAL 8종 문구 무변경(`git diff | grep -E "^[+-].*CRITICAL"` → **출력 0줄**) · 훅 **94/94** · ⭐ **CORE conformance 13/13 PASS**(CLAUDE.md는 CORE 어댑터라 조항 매핑이 깨지면 여기서 red — 다이어트가 계약을 안 건드렸다는 **기계 증거**) · 문서 규범 전용 항목 삭제 **0건**(스폰 상한은 `subagent-routing.md` §5.6 이동처 실재 확인). 원장 = `01_Phases/21_HR2-opus5-renewal/12-context-diet-log.md`. ▪ **[P09] 대량 경로 치환 — 434파일 2,229건 + 정본 문서 + 사각지대 봉합 (P08 red 전면 해소)** — ① **치환 규모**: `99_Others/**` 434파일 2,229건(순수 치환) · `.claude/**` 31파일 192건 · 정본 문서 11파일 80건(`CLAUDE.md` 26 · `README.md` 13 · `AGENTS.md` 5) · `02_Source/**` 31파일 48건(주석 속 골든 테스트·설계 문서 포인터) · `00_Documents/harness/**` 7건. 최종 `git diff --numstat` = **ins 2,472 / del 2,475**, 차 −3은 전부 의도된 것(`tdd-guard.sh` +6 · `CORE.md` +2 · `.claude/settings.json` −11 = **OpenGate 개방판, 커밋 제외**). ② ⭐ **조항 버전 방침 신설 — "경로 표기 갱신은 버전 상향 대상이 아니다"**(`CORE.md:8`). 개명으로 CORE-04 본문의 경로 표기가 바뀌었지만 조항의 *의미*("IPC 계약은 `shared` 한 곳에서 정의한다")는 그대로다. `CORE.md:6` 개정 규칙의 조건이 *"조항 **의미** 변경"* 이므로 해당 없음 → **CORE-04는 v1 유지**. 판단 기준을 한 줄로 박았다 — *"이 줄을 읽고 행동이 달라지는가"*. 다만 `core-manifest.json`은 conformance가 경로 **실재**를 검사하므로 갱신 자체는 필수(버전과 무관). ③ **e2e 유령 경로는 4곳이 아니라 5파일 11건**이었다 — Phase 정의의 열거가 또 전수가 아니었다(*"계획 열거 = 전수 아님"* 3회차). 파일을 실제로 쓰는 `SHOT_DIR` 상수 5곳 + 짝 주석을 실재 폴더(`07_BF3-backlog-sweep`·`04_LR2-loop-replmode`·`06_LR3-loop-ux`)로 정정. 파일을 쓰지 않는 주석 속 문서 참조 ~30곳은 유령 경로를 만들지 않으므로 **범위 밖 + 백로그 등재**. ④ ⚠️ **`tdd-guard` 테스트 루트 탐색이 순서 의존적이었다** — P07이 넣은 *"실재하는 첫 폴더 하나만 고르는"* 방식은 두 폴더가 공존할 때(부분 롤백·샌드박스 픽스처) 한쪽 테스트를 못 찾아 **과차단**한다. 샌드박스 테스트가 **우연히 순서 덕에** 통과하고 있었다 → `GATE_FLAG`와 같은 **OR 루프**로 전환(신·구 둘 다 탐색, 안내 메시지는 실재하는 첫 루트). ⑤ ⚠️ **산문 대조 표기 파손 2건** — 옛→새 대조 문장에서 양쪽이 다 치환돼 의미가 죽었다(work-pin `:4` · `doc-thresholds.md:125`). 원문을 `git show HEAD:` 로 확인해 복원하고 `— 원 표기는 개명 전 …` 주석을 부기. 의심 패턴 grep(`(새이름).{0,14}(→|->|옛|이전|리네임|개명)`) 전수 검사로 나머지 없음 확정. ⇒ **일괄 치환의 진짜 위험은 코드가 아니라 "옛 이름을 이야기하는 산문"이다.** ⑥ **README 이미지는 8개가 아니라 4개**(Phase 정의 오측) — 전부 새 경로로 디스크 실재. GitHub 렌더링 육안은 push 후에만 가능하므로 **영호 몫**(CORE-06). ⑦ **저장소 밖 3곳 갱신** — 글로벌 스킬 `Report-YYH-Style/SKILL.md` + 메모리 2건(`reports-location-convention`·`verify-fixes-empirically`) + MEMORY 인덱스. git이 추적하지 않지만 **매 세션 주입**되므로 저장소만 고치면 다음 세션이 옛 경로로 유도한다. ⑧ **의도적 존치** — `supervisor-guard.sh:42`(GATE_FLAG OR 옛 분기) · `tdd-guard.sh:45`(`TESTS_RELS`) · `settings.SEALED.json` deny 8줄 병행 · 훅 테스트의 *"옛 이름도 여전히 봉인"* 회귀 단언 · 대조 서술 주석. 새 이름으로 바꾸면 **병행 수용 검증 자체가 사라진다**. ⑨ **위임 경로가 닫혀 있어 메인 직접 수행** — 세션 지시(*"Do not call the AgentTool unless the user requested it"*)로 `qa`·`secretary` 위임 불가. 대상이 기계적 rename이라 규율 축(*"테스트 설계를 코드 작성자가 겸하지 않는다"*)의 보호 가치가 낮다고 판단. **대가 = 컨텍스트 격리 상실**(434파일 치환 로그가 메인 컨텍스트에 적재). ⑩ **reviewer 스킵** — `review-tiering.md:38`("주석/오타/rename만") 근거, 의미 변경 0건 + 기계 판정 전 범위 커버. 무언의 생략과 구분하기 위해 Phase 문서 §7에 명시. ⑪ 게이트: 훅 **94/94** · typecheck 0 · lint 0 · test **395파일 5,330 passed** · build 성공 · e2e `core-loop` **4 passed** ⇒ P08 red 전면 해소. ▪ **[P08] `git mv` 5폴더 + 빌드 체인 33지점 (1,342 files changed)** — `00.Documents`·`01.Phases`·`02.Source`·`98.Management`·`99.Others` → `_` 프리픽스(ADR-028 개정 1). 빌드 체인 8파일 동반: `tsconfig.node.json` 8 · `tsconfig.web.json` 8 · `electron.vite.config.ts` 6(`__dirname` 지점) · `.gitignore` 4(**Phase 열거에 없던 파일** — `gate-open.flag` 무시 규칙을 담고 있어 놓쳤다면 **개방 상태 flag가 커밋에 섞일 수 있었다**) · `package.json` 3 · `vitest.config.ts` 2 · `playwright.config.ts` 1 · `.eslintrc.cjs` 1. ⚠️ **완료 조건 오측 정정** — Phase 정의는 *"test만 red 허용"* 이라 했으나 테스트가 `tsconfig` include에 있어 **typecheck도 red**였다(lint·build만 green). ⚠️ **`git mv`의 이력 보존은 파일 단위 `--follow` 한정** — 디렉토리 로그·blame은 개명 지점에서 끊긴다(실측: 디렉토리 로그 1커밋). 보완으로 `01_Phases/INDEX.md`에 **개명 항법 노트** 신설(매핑표 5행 + 디렉토리 이력은 `git log -- '02.Source/**'` + blame은 `git blame <개명커밋>^ --`). ▪ **[P07] 훅·봉인 경로 선행 — 부트스트랩 자물쇠 해소 + ⭐ "개명이 봉인을 조용히 푼다"를 실측으로 확정** — ① **구분자 `[._]` 하나로 신·구 병행 수용**(정규식 4지점 + 셸 glob 8지점 + `permissions` deny 4줄). 대안 비교: (a) 새 이름 일원화는 전환 구간에 반드시 구멍이 생기고 부분 롤백·이력 체크아웃에서 봉인이 조용히 풀린다 (b) 두 패턴 `|` 나열은 정확하지만 지점마다 패턴이 두 배가 되어 **다음 사람이 한쪽만 고칠 여지**를 남긴다(이 Phase가 막으려는 바로 그 실패 양식) (c) `[._]`는 한 지점에 한 패턴을 유지하면서 둘 다 받는다 → **(c) 채택**. 단점(제3의 이름은 여전히 불수용 · 이유를 모르는 사람이 `\.`로 되돌릴 수 있음)은 지점별 주석 + 회귀 테스트로 방어. ② ⭐ **양방향 검증 — 정적 grep으로는 안 보이는 방향을 실측했다.** 매칭 실패는 곧 `'unrelated'` = **통과**라 옛 정규식으로도 테스트는 계속 green이다(false green). 그래서 옛 정규식으로 되돌린 **임시 사본**에 같은 경로를 쏴 대조: `00_Documents/harness/CORE.md`·`00_Documents/adr/**`·`00_Documents/ADR.md`·`98_Management/Harness_OpenGate/gate-open.flag` **4경로 전부 `false`(봉인 밖)** 였고 `tee 00_Documents/harness/CORE.md`도 `null`(통과)였다. ⇒ P08을 P07 없이 먼저 했다면 **의미 정본 층과 OpenGate가 통째로 열린 채** 개명이 끝났을 것이다. 특히 flag 줄은 **자기 개방 벡터** — 에이전트가 flag를 직접 만들어 창을 연다. ③ ⭐ **`GATE_FLAG`만은 일원화 금지 — 신·구 두 경로 OR 검사.** 개명은 봉인 대상 파일을 고치는 작업이라 창이 열려 있어야 하는데, 여기를 새 경로로만 바꾸면 개명 **전**에 flag를 못 찾아 즉시 봉인 복귀 → 남은 봉인 파일을 그 자리에서 못 고친다(**P07이 막으려는 사고를 P07이 스스로 일으킨다**). 다른 경로는 틀려도 "차단이 늦게 걸릴 뿐" 회복 가능하지만 GATE_FLAG는 **회복 경로 자체를 끊는다** — 이 비대칭이 근거. 단 OR가 "둘 중 하나라도 있으면 무조건 개방"이 되지 않도록 각 경로가 TTL·하한 0·자릿수 상한을 그대로 통과해야 함을 회귀 테스트로 고정. ④ **`tdd-guard`는 대상 판정(`:27,:30`)과 테스트 탐색(`:39-40`)을 짝으로** — 한쪽만 고치면 전 파일 과차단(fail-closed) 또는 여전히 fail-open, **부분 수정이 가장 나쁘다**(ADR-028 `:20`이 남긴 경고의 2회차). 탐색 루트를 `TESTS_REL` 변수로 뽑아 실재 폴더를 고르게 했고, 차단 메시지도 같은 변수를 쓰므로 **개명 후 없는 경로를 안내하는 일이 구조적으로 불가능**해졌다. ⑤ ⚠️ **검증 도중 밟은 함정 — 되돌림 스크립트에서 `\.`가 `.`로 뭉개졌다.** 셸 이스케이프가 한 겹 먹혀 `00\.documents`가 아니라 `00.documents`로 들어갔고, 정규식에서 `.`는 임의의 한 글자라 `00_Documents`도 매칭돼 **되돌린 상태인데 테스트가 전부 통과**했다. 하마터면 *"프로브가 아무것도 안 잡는다"* 로 오독할 뻔했다 — 실제로는 **주입이 실패한 것**이었다. ⇒ 역방향 주입 검증은 **주입이 실제로 됐는지를 먼저 눈으로 확인**한다(ADR-028이 개명 근거로 든 *"`.`은 정규식 메타문자"* 가 이번엔 fail-open이 아니라 **거짓 통과** 방향으로 다시 물었다). ⑥ **`settings.SEALED.json` deny 16 → 20줄**(새 이름 4줄 추가). ⚠️ **`.claude/settings.json`은 손대지 않았다** — OpenGate 파생물이라 창을 열고 닫을 때마다 통째로 덮어써지고, 창이 열린 지금 거기에 경로 deny를 써 넣으면 **permission 계층은 gate flag로 우회되지 않으므로**(flag는 supervisor-guard 훅만 통과시킨다) 그 즉시 자기 세션이 잠긴다. 반영 확인은 P11의 `CLOSE-GATE.bat` 실행 후. ⑦ **부수 실측 3건** — (a) OpenGate 배치파일은 경로가 전부 `%HERE%` 상대(`%HERE%..\..\.claude\settings.json` 포함)라 폴더명 하드코딩 **0건**, 개명에 **이미 안전** (b) `openGateExecReason`도 `harness_opengate` 마커 기반이라 상위 폴더명과 무관 — RED 9건 중 이것만 처음부터 green (c) `settings.OPEN.json` deny는 **5줄**(Phase 정의의 "6줄"은 오측, 경로 deny 부재라는 사실 자체는 정확). ⑧ **훅 주석·설명 문구는 P08로 이월** — 개명 *전*에는 옛 이름이 맞기 때문이다. 동작 코드의 `[._]`는 개명 후에도 **그대로 둔다**(부분 롤백 방어). ⑨ TDD RED(96e190e, 9건 중 8 red) → GREEN. 게이트: typecheck 0 / test **395파일·5,330 passed** / lint 0 / **훅 94/94**(P06 83 → 신규 11). 창 생존 확인 = `guard-blocks.log`에 `open-gate` 기록 지속(age 237m 시점). ▪ **[P06] 검증 구조 3분류 재편 + 강제 출처 라벨링 + CORE-13 v2 — ⭐ "삭제"로 출발했다가 실측이 전제를 무너뜨린 Phase** — ① **① 자기검증 독려 = 공집합(0건).** 공식 권고(*"Opus 5는 시키지 않아도 자기 검증한다 — 'use a subagent to verify' 같은 지시와 legacy harness scaffolding을 제거하라"*)가 지우라는 대상을 전수 grep(`다시 확인|재확인하|재검토 후|double-check|스스로 검증|최종 검증 단계|verify|self-check` 등)으로 훑었더니 걸린 셋이 **전부 ①이 아니었다** — `CLAUDE.md`의 *"Phase에서 이미 해결된 건 재확인하지 않는다"* 는 오히려 **같은 편**이고, `execution-owner.md`의 *"검증은 게이트·셀프체크가 소유"* 는 축 분리 **구조 서술**, *"커밋 전 `git status` 셀프체크"* 는 일반 독려가 아니라 `git add .` 금지(CORE-07)의 **짝인 구체 절차**다. ⇒ **삭제할 것이 없었다.** 영호가 초안의 "삭제"를 "라벨링"으로 튼 결정이 실측으로 지지됨. ② ⭐ **이 Phase의 핵심 발견 = 강제의 비대칭.** 전수 지도를 그리자 **차단(`exit 2`)하는 훅은 4종**(`supervisor-guard`·`dangerous-cmd-guard`·`tdd-guard`·`phase-gate-validator`)뿐이고 **5종은 전부 알림**(`pin-injector`·`risk-detector`·`circuit-breaker`·`reviewer-auto-trigger`·`convention-size-guard` — `exit 2` 0건), 나머지는 `permissions` deny 16·ask 6줄이다. 기계는 *하지 말 것*(봉인·파괴·비가역)을 잘 막지만 ***해야 할 것*(reviewer 호출·plan-auditor 호출·회귀 게이트 실행·등급 상향 기재)은 거의 강제하지 않는다.** ⇒ 그 문장들이 곧 **유일한 방어선**이다. ③ **강제 출처 3층 라벨 도입** — `[기계: X]`(차단) / `[알림: X]`(환기만) / `[문서 규범]`(둘 다 없음). `.claude/policies/*.md` **12/12 전수**에 공통 범례 + 규칙 라벨 ≥1건 부착(미부착 0 검증). ⚠️ 범례에 *"`[문서 규범]`은 지워도 되는 문구가 아니라 **그것이 유일한 방어선**이라는 뜻"* 을 못박음 — 라벨이 삭제 허가증으로 오독되면 이 Phase가 정확히 반대 효과를 낸다. `INDEX.md`에 **강제 출처 열 신설**(정책을 "정리"하려는 사람이 먼저 읽어야 할 열). ④ **`execution-owner.md` 오분류 정정** — `:23`이 `plan-auditor`·`reviewer`를 기계 게이트로 열거하고 있었다. 실측상 훅의 `plan-auditor` 참조는 **0건**이고 `reviewer-auto-trigger.sh:3-4`는 스스로 *"차단/자동호출 아님"* 을 선언한 advisory다. **이 표를 지도 삼아 "기계가 받쳐주니 문구는 군더더기"라고 판단하면 정확히 방어선을 지우게 된다.** `:73` 판정표도 카테고리별 강제 출처 열로 8행 분해. ⑤ **③ 규율 축 근거 재서술 1건**(`subagent-routing.md` §5.5) — 옛 근거 *"상향 모델도 **실수 0을 보장하지 않으므로**"* 는 공식 권고에 직격당하고 모델이 좋아질수록 약해진다. 새 근거 = *"검증자가 필요한 이유는 더 똑똑해서가 아니라 **작성자는 자기 전제를 의심하지 못하고 자기가 읽지 않은 파일을 알지 못하기** 때문"* → **모델 성능과 독립**. ⭐ 실증 2건을 같은 창에서 확보: (a) P05 reviewer가 잡은 🔴 중 하나는 **작성자가 그 창에서 직접 만든 회귀**였고 작성자 시야에선 *"오탐 해소 ✅"* 로만 보였다 (b) plan-auditor가 잡은 결함 2건은 메인이 **읽지 않은 파일**에 있었다 — 자기 검증으로 도달 불가능한 영역. ⑥ **spawn cap 등재**(`subagent-routing.md` §5.6) — 신설이 아니라 **런타임 3중 한도의 사실 등재**(세션 200 / 동시 20 / 중첩 0) + 등급표 "Worker 3~4"를 **규범 상한으로 승격**. ⚠️ **ultracode 세션은 동시 한도 면제**라 그 경우 규범 상한이 **유일 브레이크**다. ⑦ **CORE-13 v1 → v2 3점 동기** — ⑤ 응대 톤(친근한 Pair Programmer·Pair Architect Engineer, 반말체)을 어댑터에서 코어로 승격(`CORE.md` 본문·요약표 + `core-manifest.json` + `AGENTS.md` §3). ⭐ **넣으려다 뺀 3건** — 범위 규율·정정 절제·반복 제거는 **Claude Code 시스템 프롬프트에 거의 같은 문장으로 이미 있다**(착수 시 대조). 적었다면 상주 예산만 먹고 *"Then: Repeat yourself"* 안티패턴이 됐다. ⚠️ 반복 제거를 **"짧게 써라"로 옮겨 적지 말 것**(CORE-13 ④ 완성된 한국어 문장과 정면 충돌 — 걷어낼 대상은 서론·중복 요약이지 문장의 완결성이 아니다)을 코어에 경고로 박음. ⑧ 부수 드리프트 정정 2건 — `INDEX.md` 제목 "정책 목록 (11개)"→"(정책 11개 + 본 INDEX = `*.md` 12파일)"(모호했다), execution-owner 요약의 "모델 티어 3층"→**4층**(P02에서 바뀐 뒤 미갱신). 게이트 4종: typecheck 0 / test **395파일·5,330 passed**(conformance green = CORE-13 v2 3점 동기 기계 검증) / lint 0 / 훅 **83/83**. ⚠️ 완료 조건 (3)의 잔존 grep 1건(`subagent-routing.md:148`)은 *옛 근거를 인용한 경위 설명*이며 주장이 아니다 — **다음 점검이 같은 논거로 되돌리지 않도록 의도적으로 남김**. ▪ **[P05] 훅 보안 봉합 — 봉인 우회 5건 + reviewer 2차 봉합 5건 + 감사 계약 복구 + 게이트 연결** — ① ⭐ **토큰화 fail-open 폐쇄(2지점)**. `shell-policy.mjs`는 따옴표 불균형에 토큰 0을 반환했고, 그러면 sealed 후보가 통째로 사라져 봉인이 열렸다 — `tee .claude/settings.json # it's fine`이 통과했고 **bash는 `#` 이후를 버리고 명령을 정상 실행**하므로 실제 우회였다. 조치 = **셸 주석(`#`) 선처리**(POSIX상 단어 시작 위치의 `#`만 주석 — `a#b`·따옴표 안 `#`은 아님) + **판정 불가 시 best-effort 재토큰화**(fail-open → fail-closed). ⚠️ **계획 열거 밖에서 동일 결함 2번째 지점 발견** — `shell-tokens.js`도 같은 fail-open이었고 `supervisor-guard.sh` **②절(실행 경계)**이 `[ ${#TOKENS[@]} -eq 0 ] && exit 0`이라 `git add . # it's fine`에 통째로 열려 있었다. P03에 이어 **두 번째로 "계획 열거가 전수가 아니다"** 를 확인. ② **`parse-payload.js` 단일 실패점 폐쇄** — 전 훅 공유 파서가 죽으면 `eval ''`로 `TOOL_NAME`조차 미설정돼 봉인 검사를 통째로 건너뛰었다(**9종 전면 fail-open**). ⚠️ **파서는 JSON 파싱 실패 시 exit 0 + 빈 출력**이라 종료코드로 감지되지 않는다 — 키가 없어도 항상 5줄을 내므로 **빈 출력 = 실패**로 판정. `HOOK_PAYLOAD_PARSED` 신호 + `require_parsed_payload` 신설, **차단 성격 훅 2종에만** 적용(advisory까지 exit 2면 파서 사고가 사용자 입력·알림 경로를 죽인다). ③ **OpenGate TTL 하한 0** — 미래 epoch가 flag에 들어가면 age가 음수라 `-lt TTL`이 언제나 참 = **무기한 개방**. 미래·만료·판독불가를 각각 다른 알림으로 구분. + **`gate-open.flag`를 `.gitignore`에 등재**(실측: 등재 전 `git check-ignore` 무매치 + `git status`에 `??` 노출 — 개방 상태가 커밋되면 그 커밋을 받은 다른 머신이 창이 열린 채 시작한다). ④ **git 서브커맨드 pathspec 판정 신설** — `mv|rm|restore|checkout|apply|stash|clean`은 워킹트리를 바꾸므로 봉인 판정에 통과시킨다(P08이 `git mv`를 대량 승인시켜 **승인 피로가 곧 우회 키 입력**이 되는 구조). `add`/`commit`은 파일 내용을 안 바꾸므로 제외(실행 경계 ②절 소관), `diff`/`log`/`show`는 통과 유지. ⑤ **sed를 이름 기반 → 동작 기반 판정으로** — `-i`·`-i.bak`·`--in-place[=SUF]`·결합 단축(`-ni`) + **스크립트 본문의 `w`/`W`**(주소부 `w` · `s///w` 플래그)만 쓰기. 읽기 전용 `sed -n '1,50p' <sealed>` 통과(오탐 해소). ⚠️ **세그먼트 좁히기는 하지 않았다** — 기록된 오탐 표본은 sed와 sealed 경로가 같은 세그먼트라 좁히기로는 해소되지 않고 변수 우회(`F=<sealed>; sed -i … $F`) 방어만 잃는다. ⑥ 🔴 **`open-gate` 감사 라벨 — 선언만 됐고 작동한 적이 없다**. ADR-038 `:9`가 위협모델 완화의 *대가*로 내세운 *"개방 중 통과 이력은 전량 `guard-blocks.log`에 `open-gate`로 남아 사후 감사 가능"* 이 **한 번도 지불되지 않았다**(실측 `grep -c open-gate` → **0건**, 실제로는 notify 2,526건에 혼입). 원인 = `guard-log.mjs`의 `action==='block'?'block':'notify'` **이분법**이 훅이 정확히 넘긴 라벨을 삼킨 것 — **훅은 처음부터 옳았고 로거가 삼켰다**. 조치 = allowlist(`LOG_ACTIONS`) 전환 + 전 훅 라벨 전수 확인(사용 중 3종이 전부 커버). ⚠️ **소급 불가·소급 안 함**을 ADR-038 「보완」 절 + README에 명시(로테이션으로 옛 이력 복원 불가, 남은 notify에서 개방분만 분리할 근거 없음 — **감사 가능 구간은 수정 시점 이후**). ⑦ **OpenGate 방어 범위 축소 이행**(ADR-038 개정 1) — 부분문자열 매칭 → **실행 벡터 한정**(`openGateExecReason` 신설: 실행 위치 토큰이 OpenGate 스크립트이거나 실행기 인자로 지목된 경우만). `ls`·`echo`·`cat` 언급·읽기는 통과 — 읽기는 `Read`/`Glob`이 열려 있어 Bash만 막는 건 달성되지 않는 방어였고 차단 메시지가 대체 경로를 안내하는 **표지판**이었다. ⑧ **게이트 연결** — `npm run test:hooks` 신설(훅 테스트가 어떤 npm 스크립트에도 안 물려 있어 실행법이 수동 `node --test`뿐이었다). ⚠️ `node --test <dir>`는 **디렉토리 인자를 받지 않아**(실측) cwd 이동 방식 채택. **훅 테스트 49 → 73**(2차 봉합 후 **83**), `hook-advisory.test.mjs` 신설로 **테스트 0건 훅 = 0개**. ⚠️ Phase 정의의 *"`guard-log.mjs`도 테스트 0건"* 은 **오측**(131줄 테스트가 이미 존재·baseline 포함). ⑨ ⭐ **테스트를 쓰다 밟은 함정을 그대로 박제** — circuit-breaker는 **변이 도구(Edit|Write)만** 감시하는데 Bash로 쏴서 "발화 0"이 **거짓 통과**였다. 음성 단언만으로는 검출 패턴의 죽음을 못 잡는다는 이 파일의 존재 이유를 스스로 재현한 셈이라 **양성 대조**(임계를 실제로 넘기면 발화)를 함께 등재. ⑩ TDD RED(36d9a17·332e632) → GREEN(43c9aeb·f2afa08) 준수, 단 마지막 묶음(d85f947)은 **커밋 분리 미준수**(순서는 지킴). ⑪ ⭐ **reviewer Tier 2-A → 결함 0이 아니었다: 🔴 4건 / 🟡 6건. 전량 2차 봉합.** 넷 다 *"판정기가 쓰기 명령이나 sealed 경로를 못 찾아 통과시킨다"* 는 **같은 계열**이고, **하나는 이 Phase가 직접 연 회귀**다. (a) 🔴 **개행이 세그먼트 구분자가 아니었다** — 둘째 줄 명령이 첫 세그먼트에 흡수돼 실행 이름이 앞줄로 읽혔다. `echo hi`↵`sed -i … <sealed>` / `echo hi`↵`rm -rf …` 로 **봉인(CORE-01/11)과 파괴 금지(CORE-07)가 동시에** 열렸고, Bash 도구는 여러 줄 명령을 일상적으로 받으므로 **우연히도 밟을 수 있는** 경로였다. 조치 = `\n`을 명령 구분자로 승격 + **짝으로 heredoc 본문 제거**(안 하면 `cat > note.md <<EOF … rm -rf … EOF` 같은 정상 문서 작성이 통째로 오탐 — 개행이 세그먼트를 안 나누던 성질이 역설적으로 오탐을 막고 있었다. **한쪽만 고치면 반대편이 깨지는 관계**). 미종료 heredoc은 제거하지 않는다(삼키면 뒤 명령이 사라져 fail-open). (b) 🔴 **sed 조건부 판정의 신규 미탐** — `1!w`·`$!w`·`/x/!w`·`\%x%w`·`1e tee …` 전부 통과였다(GNU sed 4.9로 다섯 형태 모두 실제 쓰기 확인). ⭐ **오탐을 고치면 미탐이 생긴다** — "무조건 차단"에서 "조건부"로 가는 변경은 정밀도의 *경계마다* 새 구멍을 낸다. 조건식 하나가 아니라 **주소 문법 전체의 열거**(행번호·`$`·범위·`/re/`·임의 구분자 `\c…c`·수식자·부정)가 필요했다. `e`는 패턴스페이스를 셸 명령으로 실행하므로 파일 쓰기보다 넓은 통로. (c) 🔴 **리다이렉트 대상이 언제나 프로젝트 루트 기준** — `cd .claude/hooks && echo x > supervisor-guard.sh` 로 훅을 덮어쓸 수 있었다(대상 토큰에 마커가 없어 후보 추출조차 안 됨). 조치 = 세그먼트 체인 따라 `cd`/`pushd` 추적. (d) 🔴 **따옴표 밖 백슬래시 미처리 + 후보 추출 좌측 greedy** — `tee \`↵`<sealed>`(줄 이음, 모델이 긴 명령을 정렬할 때 **자연히 나오는 형태**)와 `sed 'w.claude/settings.json'`(w 뒤 공백은 선택)이 통과. 조치 = POSIX 이스케이프 처리 + `/` 경계 기준 2차 추출. ⚠️ **대가가 즉시 드러났다** — 토큰 단계에서 Windows 백슬래시 경로가 깨져 `cmd /c 98.Management\Harness_OpenGate\OPEN-GATE.bat` 차단 **기존 테스트가 red**. 후보 소스에 원본 명령 추가 + `openGateExecReason`은 정규화 사본도 판정해 복구(**기존 테스트가 회귀를 잡은 사례**). 🟡 5건 반영(`JSON.parse("5")`가 파싱 성공으로 오인되던 파서 구멍 — `null`만 우연히 TypeError로 걸렸을 뿐 **설계된 방어가 아니었다** / git 쓰기 서브커맨드 6종 추가 / `git stash show`·`list` 오탐 해소 / `sed -f`는 내용이 시야 밖이라 쓰기로 간주 / `git apply` 한계 주석), 1건 백로그(`git switch`·`stash pop`은 경로 토큰 없이 워킹트리를 갈아엎는 **구조적 한계** = ADR 사안). ⑫ 🔴 **미검증 항목을 이어서 실측했더니 결함이 하나 더 나왔다 — ReDoS 확정.** reviewer가 *"전혀 측정하지 못했다"* 며 정직하게 남긴 #3을 측정: `sed 's/a\\…\\b/c' <sealed>` 백슬래시 n=30 → 29ms, **n=40 → 3,652ms**(≈125배 지수). 원인 = `(?:\\.|[^])*?` 두 분기가 **백슬래시에서 겹쳐** 한 글자 소비 방법이 둘이 되는 모호성. 겹침 제거 후 **0.1ms**. ⭐ **느린 정규식은 성능 문제가 아니라 보안 결함이다** — 판정기가 멈추면 훅이 타임아웃되고 훅 타임아웃은 차단이 아니라 **조용한 통과**다. 회귀 테스트로 고정(임계 500ms). 함께 봉합: TTL flag **자릿수 상한**(초장문 숫자는 bash 산술 오버플로우로 wrap — 지금은 음수라 하한 0에 걸려 *우연히* 안전했으나 양수 신선 구간으로 떨어지는 값도 원리상 존재. 안전을 검사 하나에 의존하지 않는다) · **실행 접두사 6종**(`exec`·`nohup`·`command`·`time`·`xargs`·`stdbuf`) 건너뛰기(종전 `sudo`·`env`만이라 `exec tee <sealed>`가 통과). ⚠️ 여전히 **미검증으로 남긴 것** — `|| assignments=''`(node 부재) 분기는 재현에 node 제거가 필요해 도달 못 함. ⑬ **OpenGate TTL 4h → 7h**(영호 지시, **ADR-038 개정 2**) — 창이 도중 만료되면 봉인 복귀로 끝나지 않고 **루프 전체가 사람을 기다리며 멈춘다**(에이전트는 설계상 재오픈 불가). 12 Phase 창에 4h는 짧다. 대가 = 닫기 망각 시 노출 3시간 증가(위협 모델 범위 내 감수). 동기화 6지점. ⚠️ **이 과정에서 테스트가 상수를 복제하던 걸 발견** — `nowSec() - 14401`이 확장 순간 "신선" 구간으로 넘어가 **거짓 통과**가 됐다. 이제 `supervisor-guard.sh`에서 `GATE_TTL_SEC`을 파싱해 쓴다(*상수를 문서와 테스트가 각자 베껴 적으면 값을 바꾸는 날 조용히 어긋난다*). 게이트 4종: typecheck 0 / test **395파일·5,330 passed**(P04 대비 변동 0) / lint 0 / **훅 83-83**(baseline 49 → 1차 73 → 2차 83). ⚠️ **미결 1건** — **훅 글루 레벨 발화 프로브**: 창이 열려 있으면 `supervisor-guard`가 `exit 0`으로 전체 통과시키는 것이 설계 의도라 창 안에서는 실측 불가 → **P11로 이관**(그 사이 회귀는 샌드박스 글루 테스트가 방어). ⭐ **이 Phase의 가장 큰 교훈** — 🔴-2는 작성자 관점에서 완료 조건의 *"sed 읽기 오탐 해소 ✅"* 로만 보였다. **작성자는 자기가 만든 구멍을 "해소 완료"로 읽는다.** reviewer가 필요한 이유는 능력 차이가 아니라 **위치 차이**다(P06 §③ 근거 재서술과 같은 논지 — 심판을 세운 판단이 데이터로 증명됐다). ▪ **[P04] effort 도입 — ⭐ 창 안에서 판정이 한 번 뒤집힌 Phase** — ① **1차 판정 = 드롭.** 프로브 ⓪(`secretary`, effort 키 없음 → `CLAUDE_EFFORT=xhigh`)이 **세션 상속을 확정**했고, 프로브 ①(`qa`에 `effort: low` 주입 → 여전히 `xhigh`)이 *"frontmatter는 세션을 못 이긴다"* 로 읽혔다. ② **영호 지시로 공식 문서 실측 → 판정 반전.** `sub-agents`가 `effort`를 **정식 frontmatter 필드**로 문서화하고, `model-config`가 *"그 서브가 활성인 동안 세션 레벨을 오버라이드한다"* 고 명시한다. 우선순위 = **env `CLAUDE_CODE_EFFORT_LEVEL` > frontmatter > 설정 `effortLevel` > 모델 기본(`high`)**. ⚠️ **내 메모리가 틀렸다** — 「`CLAUDE_CODE_EFFORT_LEVEL`은 존재하지 않는 변수」로 박아뒀으나 **실재하고 심지어 frontmatter까지 무력화하는 최상위 손잡이**다. *설정돼 있지 않다*를 보고 *존재하지 않는다*를 적은 것. ③ ⭐ **반전의 기계 근거 = 정의 워처 사망.** §D-4가 예고한 결정적 판별(`model` frontmatter 변경 × 서브 트랜스크립트 대조)을 실행 — 디스크는 `claude-sonnet-5`인데 per-invocation 없이 스폰한 서브는 **`claude-opus-5`** 로 떴다. 즉 **런타임이 디스크의 새 정의를 읽지 않았고, `effort: low`는 도달한 적이 없다.** ⇒ **프로브 ①은 effort에 대해 아무것도 말하지 않는다**(*증거 없음*을 *부재의 증거*로 읽은 오류). 마커 3회 ABSENT도 모델 자기보고 오류가 아니라 진짜 부재였음이 확정. ⚠️ 공식 문서상 워처는 수 초 내 반영하고 재시작이 불필요하며 예외 2건(세션 시작 시 없던 `agents` 디렉토리 / `--disable-slash-commands`) 둘 다 해당 없음 — **원인 미상**(후보: Windows 파일 감시 한계 / 장수·다중 compact 세션 / v2.1.220 버그). ④ **배분 적용 10/10** — `effort: xhigh` **3**(`reviewer`·`plan-auditor`·`chief-tech-operator`) / `effort: high` **7**(Worker 4종·`qa`·`secretary`·`coordinator`). ⚠️ **Phase 완료조건 「나머지 9역할 effort 키 0건」은 전제 붕괴로 교체** — 그 조건은 *상속값이 기본 `high`* 를 전제했는데 ⓪이 **`xhigh`** 로 확정했으므로, 키 0건이면 서브 전원 xhigh가 되어 영호 결정("나머지는 기본")과 정반대가 된다. 기본으로 만들려면 **명시 키가 필요**하다. 메인 세션은 `effortLevel: xhigh` **유지**(D-1의 "메인을 내려야 한다"는 결론은 잘못된 전제에서 나온 것). ⑤ **`effortLevel`은 user scope = 전 프로젝트 전역**(`~/.claude/settings.json`) — 완료조건 명시 요구 이행. ⑥ ⭐ **새 판독구: 트랜스크립트 `.effort`** — 서브 `agent-<id>.jsonl`의 모든 `assistant` 줄 최상위에 실효 effort가 기계 기록되고, 짝 `.meta.json`에 `{agentType, model, spawnDepth}`. **과거 스폰 전수 소급 조회**가 가능하고 `spawnDepth: 1`은 **런타임 중첩 OFF의 기계 증거**로도 쓰인다(판독구 3종 = env·훅 payload·트랜스크립트). ⑦ **품질 A/B**(영호 승인, `Workflow` `opts.effort` — 이 경로는 실측상 **작동 확인**) — 심은 결함 9개 정답지를 **결과 도착 전에** 원장에 박고 채점: high 9/9 · xhigh 9/9 **동수**, 토큰 34,954 vs 35,561, 85.0s vs 110.7s. xhigh만 잡은 것 = **심지 않은 진짜 자기모순 1건** + 질적 깊이. ⇒ *"판정 렌즈만 xhigh"* 배분을 데이터가 지지. ⑧ **별칭 비대칭 확정** — `sonnet` → `claude-sonnet-5`(최신)인데 `opus` → `claude-opus-4-8`(최신 아님). **대칭이 아니므로 full ID 규범 유지**. ⑨ ⚠️ **사정거리는 effort가 아니다 — 이 창에서 고친 `.claude/agents/**` 전건이 현 세션 미반영**이다(P02 full ID 전환·P03 `disallowedTools`/`maxTurns`/`color`·**coordinator Agent 반납**). 특히 *권한을 줄이는 변경*은 미반영 시 옛 권한이 살아 있다. ⇒ **"하네스 에이전트 정의를 건드린 창의 검증은 반드시 새 세션에서"** 를 이번 창 한정이 아니라 **상시 절차 규범**으로 원장 §E-7에 박고 **P11 완료조건에 편입**(같은 세션 검증은 거짓 음성). ⑩ 낡은 기록 **2차 정정** — `reviews/2026-07-17:92`·`reports/next/NEXT-핸드오프:64`(P04 도중 1차 정정한 내용마저 §E가 갱신 대상으로 만듦 — 「제거 또는 no-op 주석」 조치 **철회**) + 글로벌 메모리 `claude-code-effort-precedence` **전면 재작성** + **신설** `agent-definition-edits-need-new-session` + `MEMORY.md` 2줄. 원장 = `01.Phases/21_HR2-opus5-renewal/04-effort-probe-log.md`(§A 사전 확정 사실 / §B 판정 기준 *실행 전* 고정 / §C 프로브 / §D 1차 판정[무효 표기 후 **소급 삭제 없이 존치**] / §E 문서 실측 + 최종 판정). ⭐ **판정 기준을 프로브 전에 고정한 것이 실제로 값을 했다** — 과잉 추론 1건(§D-3)을 스스로 걸러냈고, 반전 시에도 "무엇을 언제 알았는가"가 추적된다. ▪ **[P03] 역할 재편 — SubAgent 풀 9역할 → 10역할** — ① **`chief-tech-operator` 신설**(`claude-fable-5`·purple·maxTurns 25·R only) — 설계 분기 자문(선택지 비교·ADR 초안 *문자열* 반환, 파일 Write는 CORE-11로 차단) + 막힌 문제 진단(에스컬레이션 **4차** 최종단). ⚠️ **자동 발화 금지** — `description`에 PROACTIVELY 미사용, "메인이 제안 → 영호 승인" **혼합 방식**(Fable 5 단가). `coordinator.md`의 **분해 패턴 카탈로그 3종**(IPC 기능·백엔드 어댑터·3-pane UI)은 저장소 유일 서술이라 통째 이관. ② **`coordinator` 축소** — `Agent` 반납 + `disallowedTools` 명시, *분해·위임·통합* → **경계 코드 정합 검증**(IPC 채널↔shared↔preload·AgentEvent↔타입·테스트↔코드 4대조) 단일 역할. 권한 회수가 아니라 **이미 죽어 있던 항목의 정리**(중첩 OFF로 2단 위임 실행 불가). ⭐ **"메인만 위임자"의 담보가 바뀌었다** — 옛 담보 *"coordinator만 Agent 보유"* → 새 담보 **런타임 중첩 OFF**(도구 부재) + 전 역할 `disallowedTools: Agent` 이중 잠금. *중첩이 다시 켜지는 버전이 오면 재검토*를 4곳에 경고로 박음. ③ **`secretary` 재정의** — 존재 근거를 ~~"싼 모델로 비용 절감"~~ → **컨텍스트 격리**("입출력이 큰 작업을 메인 밖에서 돌리고 결론만 회수"). 메인이 Opus 5가 되며 단가 비대칭이 소멸했고 2026-07-24 실측은 반대(대필세)를 측정했다. **커밋·게이트 소유는 유지**(빼면 훅 차단 메시지 3개·판정표 NO행·CORE-09 conformance가 고아). ④ **frontmatter 5키 전수** — 10/10 전부 `model`·`tools`·`disallowedTools`·`maxTurns`·`color`. 키 유효성은 **CLI 바이너리 문자열 실측**으로 확인(파싱 반환 객체에 `tools`/`disallowedTools`/`skills`/`mcpServers`/`hooks`, 검증 에러 메시지에 `maxTurns`·`permissionMode`·`effort`, color 8색 `_FOR_SUBAGENTS_ONLY`) — ⚠️ **이 실측이 P04를 흔든다**: 메모리 「Claude Code effort 실측」의 *"frontmatter effort는 no-op"* 박제가 **현 버전에선 부정확**(`.md` 파싱 맥락에 effort 검증 에러 메시지 실재). ⑤ **숫자·잔재 스윕 9파일** — 9→10(`subagent-routing`·`INDEX`·`loop-driver`·`harness-review`·`work-plan/SKILL`) + 3세대 규범 겹침 해소. ⭐ **계획 열거가 전수가 아니었다** — 패턴 전수 스캔(`coordinator + 분해|위임|escalate`)으로 **추가 4파일 12지점** 발견(`doc-thresholds`·`main-process`·`qa`·`shared-ipc`·`agent-backend`·`renderer`·`plan-auditor`). ⑥ **`work-plan/SKILL.md:47` 정면 모순 정정** — *"Phase 파일은 메인 직접 Write를 supervisor-guard가 차단하므로 secretary 위임"*(A안 2026-07-04)이라 적혀 있었으나 **훅에 그 차단이 없다**(`supervisor-guard.sh:16` 주석이 "01.Phases 차단 제거"로 명시). ⑦ **Codex 짝 열 삭제**(고치지 않고 삭제) — ADR-033 개정 1이 워커 함대·Sol/Terra/Luna를 철회해 Codex엔 `main-process` 같은 Worker가 **애초에 없다**. 1:1 짝이 없는데 짝 열을 두면 계속 거짓을 적게 된다. ⑧ `supervisor-guard.sh:85` 절 제목 `② Supervisor 전임` → `② 실행 경계(잡무 기준 v1)` — 같은 파일 `:12` 본문과 **자기모순**이던 마지막 폐기 규범 잔재(주석 1줄, 동작 무변경). ⑨ 메모리 `coordinator-longrun-visibility` — Agent 유지 결정 철회 표기 + 가시성 습관 2가지(progress.md 로그·30분 liveness 프로브)는 *모든 장기 위임*으로 일반화 존치. 🔴 **범위 밖 발견 → P05 등재**: `guard-log.mjs:29`의 `action==='block'?'block':'notify'` 가 훅이 넘긴 **`open-gate` 라벨을 삼킨다** — `grep open-gate guard-blocks.log` **0건**(notify 2,526건에 혼입)이라 **ADR-038 `:9`가 위협모델 완화의 대가로 내세운 "전량 open-gate로 남아 사후 감사 가능"이 성립하지 않고**, P11 발화 프로브 #3이 현 상태로 반드시 실패한다(고치지 않음 — P05의 TDD RED→GREEN 순서 보존). reviewer는 **스킵 + 사유 명시**(실행 코드 변경 0, `review-tiering.md:38` 해당 — 대신 기계 심판 2종 부착). 게이트: typecheck 0 / test **395파일·5,330 passed**(P02 대비 변동 0) / lint 0 / 훅 테스트 **49/49**. ▪ **[P02] 모델 정본 + 티어 4층** — 별칭 → **full ID 전환 9개**(`.claude/agents/*.md`, 잔존 0) · `execution-owner.md` §3 **3층 → 4층 교체**(옛 표 3행 중 "기계 잡무=하위 티어(secretary 기본)"은 `secretary.md`가 `opus`였으므로 **처음부터 거짓**, "Fable 상속 방지"는 **전제 소멸** — 철회 2건을 취소선으로 박아 재기재 차단) · ⭐ **회귀 테스트 신설** `99.Others/tests/agents/agent-model-canon.test.ts`(5 테스트: 파일 발견 0건 red[경로 드리프트 fail-open 차단]·별칭 잔존 0·full ID 형식+날짜접미사 금지·역할별 티어 일치·**티어 표 미등록 에이전트 0**[신설 시 정책 갱신 강제]) — **양방향 검증 완료**(별칭 주입 시 3개 red + 파일명 지목 → 복구 green) · Claude↔Codex **짝 표 15지점** 갱신(표 칸 = full ID / 산문 = 세대 이름, `subagent-routing.md:179` 변경 이력은 의도적 미수정) · `secretary.md` 커밋 트레일러는 **값이 아니라 출처를 규정**(`Claude Fable 5`가 stale로 남았던 재발을 원천 차단 — "메인이 지정한 값을 그대로, 없으면 묻는다") · **글로벌 자산 4건**(메모리 3 + `Report-YYH-Style/SKILL.md`) + `MEMORY.md` 인덱스 3줄 — ⚠️ `verify-lens-agents-use-opus`는 **틀린 값을 지시하고 있었다**(`model: "opus"` = 4.8 스폰) · `CLAUDE.md` 모델 목록에 Opus 5 등재 + **`claude-haiku-4-5-20251001` → `claude-haiku-4-5`**(스킬 정본 *"never append date suffixes"*) + 날짜 접미사 금지 규칙 1줄. 게이트: typecheck 0 / test **395파일·5,330 passed**(기준선 대비 +1파일·+5 = 신설분, 다른 회귀 0) / lint 0. ▪ **[P01] ADR 선행 4건 개정(010·028·033·038) + 헌법 「기억의 3층」 절 신설** — Opus 5 리뉴얼·폴더 개명이 뒤집는 기존 결정을 CORE-08("구조 변경 = ADR 선행")에 맞춰 먼저 정리. ① **ADR-010 개정 1**: coordinator의 *분해·위임·통합* 축 철회 + `chief-tech-operator`(Fable 5) 신설 + **모델 티어 4층** + **별칭 금지(full ID 필수)**. 철회 근거 = *결정이 틀린 게 아니라 전제가 만료* — v2.1.220 + SPAWN_DEPTH 미설정 = 서브 중첩 기본 OFF로 `main→coordinator→Worker` 2단 위임이 **이미 실행 불가**(서브 런타임에서 `Agent` 도구 부재 직접 관측)이고, `CHANGELOG:56`의 2026-07-11 "유지 결정"은 중첩이 켜져 있던 v2.1.172~216 창 안의 결정이었다. ⭐ **별칭 규칙은 이미 한 번 무너진 전력** — `CHANGELOG:80`(2026-07-03)이 *"Worker 5를 `claude-sonnet-5` 명시 고정"* 이라 기록했으나 2026-07-25 디스크 실측은 **전부 `sonnet` 별칭으로 회귀**(4건 + qa는 `opus`로 상이 = 5건 드리프트, 회귀 시점 기록 없음) → *full ID는 문서 규범만으로 유지되지 않는다*를 P02 회귀 테스트 요구로 승격. ⚠️ Agent 반납으로 `.codex/harness-contract.test.mjs:131-136` red 예정 — CORE-12상 Claude 수행 불가(P10). ② **ADR-028 개정 1**: 구분자 `.`→`_` 전환 + **개명 매핑표 5행 박제**(P08 실행 근거). 새 근거 = `.`은 **정규식 메타문자**라 훅이 `00\.documents`로 이스케이프해야 했고 그것이 fail-open의 구조적 원인(`shell-policy.mjs:220-223,244`) — 이득은 취향 통일이 아니라 **판정 정규식 단순화**. 과장 금지 명시: 생태계 마찰(`src/` 관례 이탈)은 **해소되지 않음**(비표준 A→B). hook 리터럴 함정 **2회차**임과 부트스트랩 자물쇠(`supervisor-guard.sh:30` GATE_FLAG 하드코딩 → P07→P08 순서 강제)도 박제. ③ **ADR-033 개정 2**: 원 3항의 Claude 모델 조항 **소유권을 ADR-010으로 이관**. 성격은 뒤집기가 아니라 **범위 모호함 해소** — 개정 1(2026-07-12)의 철회 문구가 *"(워커 함대 전제)"* 로 한정돼 Claude 모델명 문장의 생사가 문면상 미결이었고 실제로 본 Phase 초안이 오독했다. ④ **ADR-038 개정 1**: 방어 범위를 *`harness_opengate` 참조 Bash 전면 차단* → **실행·쓰기 벡터 차단**으로 축소(P05 선행 계약). 층 분해 실측 = 쓰기는 `shell-policy.mjs:223,244`+settings deny로 **중복 차단**, `.bat` 실행은 `supervisor-guard.sh:70-72` **단독(유지 필수)**, 읽기 차단은 `Read`/`Glob`이 열려 있어 **미달성 방어**이며 차단 메시지가 대체 경로를 직접 안내(방지턱이 아니라 표지판). 축소가 잔여 구멍을 넓히지 않음도 명시. ⑤ **헌법 「기억의 3층」 절 신설**(+166 tok) — 저장소/프로젝트 메모리/Second Brain 경계 + 축적 제안 트리거 4종. 연동 상세는 글로벌 `~/.claude/CLAUDE.md`가 정본(중복 기재 금지). ⑥ 부수 정정: `ADR.md` 인덱스 4행 갱신 + **033 행 stale 1건**(개정 *예정*으로 남아 있었으나 2026-07-12 완료) · P01 완료 조건의 *"harness-conformance.test.ts가 ADR 구조를 검사"* 는 **오서술**(318줄 전수 확인, ADR 매칭 0건). | [H] |
| 2026-07-24 | **유지보수 창 — 잡무 기준 v1(execution-owner) + ADR-038 OpenGate + 안건 3·5·7 일괄 봉합** — ① 대필세(relay tax) 실측(supervisor-guard가 *작성*이 아니라 *타이핑*만 막아 메인이 scratchpad에 대필→secretary 전사, 토큰 이중 지출)으로 구 Supervisor 전임을 **실행 주체 판정표 v1**로 재정의: `.claude/policies/execution-owner.md` 신설(판단 살아있는 산출물[Phase 문서·pin·CHANGELOG 문구·DONE 회고·조판·커밋 메시지 문구·국소 probe] = 메인 직접 / 기계 잡무[git add·commit *실행*·전 스위트 회귀 게이트·대량 정리·새 재료 실측 심부름] = 위임 / 02.Source·tests = Worker/qa 전임 유지[규율 축] / 잔여 차단 = 과속방지턱[의도 노출+원장 기록] 재정의, 모델 티어 3층) + 헌법·grade-and-risk·subagent-routing·secretary.md 동기화 + supervisor-guard ②절 재작성(01.Phases 차단 제거 — auto mode classifier가 가드 제거 편집을 차단해 영호 직접 삭제, 게이트·커밋 차단은 유지). ② **ADR-038 OpenGate**: `98.Management/Harness_OpenGate/` 신설 — OPEN/CLOSE bat(영호 단독)이 canonical settings 스왑(`settings.SEALED.json`/`settings.OPEN.json`) + `gate-open.flag`(epoch초, TTL 4h 자동 재봉인)로 창 원클릭 개폐. 에이전트는 3중 차단(settings deny + shell-policy sealed + supervisor-guard Bash 케이스), bat 모방 우회는 부분 보장으로 위협모델에 정직 선언. 영구 settings 변경은 canonical 동기화 의무. ③ 안건 3(DONE 훅 HTML 주석 라벨 = 정식 계약 승격) · 5(PRD "MVP 제외 사항" 앵커 절 신설) · 7(multiStore "미러"→"대응 확장 구현") + LP1 인계 Doc 불일치 7건 봉합. 재봉인 검증: 프로브 3종 차단·flag 부재·SEALED 복원(CLOSE bat 첫 실사용 실증)·hook-exit 17/17·shell-policy 12/12 green. 커밋 29145c1·491a4e8·a0a2b8d·d85d01c + 최종 봉합 커밋. 잔여 안건(훅 셀프테스트·Cap[오탐 표본 2건]·무인 기동)은 다음 창. | [H] |
| 2026-07-24 | **LP1 마일스톤 5/5 완주 마감 마커 — 엔진 패리티 루프 파일럿(문서 전용 트랙)** — 판정 8/8 영호 확정(2026-07-18)·지표 원장 개시·HTML 보고서 푸터 라벨—칩 nowrap 봉합(조판 게이트 3종 0건, V7)·05 flip. 전 과정 02.Source 무접촉 — 코어/계약/하네스 행동 변경 없음(pin·CHANGELOG 예외 2파일 + Phase/reports 문서만). 잔여 = push·PR 사람 게이트(CORE-06) → 유지보수 창. | [L] |
| 2026-07-17 | **shared 계약 additive — 라이브 모델 전환 채널 + model-effort 상수 모듈 (LM1)** — AGENT_SET_MODEL 채널(SetModelRequest/Response, 버전 bump 아님) + shared/model-effort.ts 신설(MODEL_EFFORT_SUPPORT 승격, IPC 아님·순수 상수) (LM1 P01·P06). | [M] |
| 2026-07-17 | **유지보수 창 — 하네스 점검(🔴1·🟡17) 일괄 봉합 + ADR-037 봉인 확장** — 깃발 SSOT 동기화 6지점(reviewer 무조건={backend-contract·shared-contract}, 정본 grade-and-risk — review-tiering·reviewer.md·work-run SKILL·_routing×2) · 문서 드리프트 정정(콜론 /work-plan 3곳·훅 개수 8→9 2곳·ADR-011 인용→CORE-06/07·INDEX shared-contract·main-process 라벨 4곳·plan-auditor 입력계약 인라인·reviewer flags 5키 정합·루트 config 소유 명시) · effort frontmatter 9파일 제거(CLI no-op 실측 2026-07-03) · /harness 커맨드 삭제(레거시 — /work-plan 짝 정본, .codex 계약 테스트 영호 동기 수정) · 훅 봉합(circuit-breaker 등급 값매치 grep[임계 오동작 종결]·pin-injector 계약 주석·shell-policy bash/sh -c 중첩+perl 등재, shell-policy.test 11/11) · 기술 봉인 확장(00.Documents/harness/**·adr/**·ADR.md sealed+deny — ADR-037 신설) · pin 헤더 중복 제거+다이어트. 유지보수 창(영호 오픈·재봉인·/hooks 재신뢰). | [H] |
| 2026-07-17 | **TG1 P08 마감 후 편입 — 스플릿 뷰 균등 셀·정적 하이라이트·지그재그 (8 Phase 화해)** — 2026-07-16 7 Phase 마감 뒤 영호 육안 피드백 3건(① 활성 셀 확대[flex-grow 2:1] 폐기→전 셀 균등 ② 활성 표시 정적 하이라이트[크기 불변] ③ 좌측 선채움→지그재그[짝=좌·홀=우])을 P08로 편입(GAP1 P16 마감 후 편입 선례). computeColumns 지그재그 + rowWeights/ACTIVE_WEIGHT/ROWS_PER_COLUMN 삭제(균등은 CSS flex:1 1 0 단독) + flexGrow 인라인 주입 제거→.sag-cell--active 클래스 + noteActivity running 한정 보존 + 정적 하이라이트(--accent-line ring·--accent-soft 헤더 틴트, 신규 HEX 0). 정본 테스트 교체(policy 37·container 18 RED→GREEN), TG1SHOTS p08 2컷, 게이트 typecheck 0·Vitest 5247 passed/0 failed·lint 0·reviewer 🔴 0, 옛 계약 옵트인 2종 재베이스라인(qa 0875317). 커밋 = 구현 A fb6f954 + 추적 8 Phase 화해 B. 추적 문서 8 Phase 화해(TG1-DONE addendum·완주 보고서 P08 부록·육안 16컷 가이드·FEATURE_MAP·_milestone-plan — "7/7 done" 명제 전수 정정, GAP1 P16 plan-auditor 🔴 교훈 선반영). GAP1 p14 골든 10장 명시 경로 복원(역사 기록). 본 항목은 마감 후 편입 마커 — **코어/계약/하네스 행동 변경 없음**(renderer 국소 + 정본 테스트 + pin·CHANGELOG 예외 2파일 + Phase/reports 문서, shared/preload/main 무접촉). | [L] |
| 2026-07-16 | **TG1 사고 GUI 데스크톱 스타일 7/7 구현 완주 — 턴 블록 통합·한 줄 상태 라인·공식 Claude 아바타 (마감 마커)** — GAP1 마감 육안 피드백 2건에서 이월된 마일스톤을 7 Phase로 완주(브랜치 feature/tg1-thinking-gui, 전부 로컬 커밋·미push). ① 턴 블록 통합(groupIntoTurnBlocks — 사고 상태·전문·답변을 한 턴 블록=아바타 1개로, P16 인접 연출 CSS는 구조적 통합으로 대체·제거) ② 한 줄 상태 라인(StatusLine — ✻ 심볼·유희적 동사 순환[WORKING_PHRASES]·경과 초[P02 순수함수+로컬 인터벌]·실시간 토큰[estimatedTokens 꼬리 조회] 통합, 답변 시작 시 thinkingStartedAt null 리셋으로 소멸) ③ 공식 Claude Spark 아바타(.ava-spark 실렌더, Claude 엔진 한정 교체·Codex 유지) — 단일챗·멀티패널(PanelView 턴 블록화)·서브에이전트(정적 ✻·우아한 부재) 표면 3종 전파. 커밋: P01 b61798a·P02 48eecaa·P03 9a0567c·P04 4315426·P05 8d082e2·P06 00dc14f·P07 9a08494 + 헌팅 fix c291b2c(StatusLine 이중 말줄임 봉합). 게이트: typecheck 0·Vitest 5246 passed/0 failed·lint 0·라이브 배터리 GREEN·옵트인 shot 4/4 무재베이스라인·census 셀렉터 파손 0(96 시각·라이브 계약 보존)·reviewer 전 Phase 🔴 0. **P05 명시 보류**: 서브에이전트 사고 토큰·훅 알림 = SDK 귀속 채널 부재(SDKThinkingTokensMessage·SDKHook*Message에 parent_tool_use_id 없음)로 보류, 재개 조건 = SDK parent_tool_use_id 부여, 서브는 우아한 부재 처리(코드 0·조용한 드롭 아님). **상표 게이트 M5 인계**(Claude 로고 #7645254 — 대화 아바타 한정·앱 아이덴티티 금지·M5 전 Trademark Guidelines 확인·Newsroom press kit 정본). 마감 산출물 = TG1-DONE.md(5단계 라벨·report_html) + HTML 완주 보고서 + 육안 14컷 가이드 + 추적 2문서 완주 플립(FEATURE_MAP·_milestone-plan). 잔여 사람 게이트 3건(영호 육안 14컷·push·PR[CORE-06]) 후 M5 배포. 본 항목은 마감 마커 — **코어/계약/하네스 행동 변경 없음**(pin·CHANGELOG 예외 2파일 + Phase/reports 문서만, shared/preload/main 무접촉). | [L] |
| 2026-07-16 | **권한 규칙 구식 형식 정리 (CLI 경고 소거 — 봉인 범위·거동 불변)** — Claude Code 권한 체계 통합(파일 읽기 도구 전부 = `Read(경로)` 규칙 단일 판정 / 파일 편집 도구 전부 = `Edit(경로)` 규칙 단일 판정)으로 더 이상 인식되지 않던 죽은 규칙을 `settings.json`에서 제거: allow의 `Glob(**)`(기존 `Read(**)`가 커버) + deny의 `Write(...)` 7건(하네스 봉인은 동일 경로 `Edit(...)` deny가 Write/MultiEdit까지 커버 → 봉인 범위 변화 0). CLI 실행 시마다 뜨던 "not matched by file permission checks" 경고 소거 목적. 영호 명시 요청 GO(2026-07-16, 하네스 사용자 단독 통제 게이트 통과 — C:\Dev 세션에서 대행). JSON 유효성 검증 통과, 훅·코드 무변경. | [L] |
| 2026-07-15 | **GAP1 P16 마감 후 편입 완료 + 추적 3문서 16 Phase 화해(plan-auditor 🔴 해소)** — 초기 15 Phase 마감 플립(8d540ce) 뒤 영호 마감 육안 피드백 2건(사고 인디케이터↔답변 분리감·훅 차단 턴 assistant 빨간 배지 + 표면 3종 적용)을 P16으로 편입. 순수 파생 isThinkingContinuous·deriveHookTurnBadges(차단 술어 = permission-denied[hook]/informational[warning·preventContinuation] → 턴 경계 최근접 후속 assistant, 부재 시 선행) + HookBadge 공용 조각(NoticeItem tone='error' --red/--red-soft 재사용, 신규 HEX 0) + 표면 3종(단일챗 인라인·멀티패널/서브 MessageBubble prop 자동 전파·서브 연속성). 커밋 3개(15b0794 RED 21 FAIL → c03fada 구현 13파일 GREEN → 87f176d 채증 8컷), 게이트 typecheck 0·Vitest 5174 passed/0 failed(378 files)·lint 0, reviewer 위반 0(🟡 2 비차단), **shared/preload/main diff 0**(계약 무접촉 — HookRun.runId는 renderer 내부 additive). 서브에이전트 훅 배지·토큰 카운트는 SubAgent 계약(agent-events.ts:287-300) 데이터 부재로 명시 보류(shared additive 확장 후보 백로그). 마감 후 편입이라 추적 3문서(_milestone-plan·GAP1-DONE·FEATURE_MAP)의 "15/15 done·사람게이트만 잔여" 명제를 16 Phase 체제로 정정 + GAP1-DONE addendum + HTML 보고서 P16 부록 증분(5단계 라벨 5종 보존·검증기 EXIT=0·report_html 경로 불변). 잔여 사람 게이트 3건(영호 육안 49컷[P13 2+P14 10+P15 29+P16 8, 다음 세션 이월]·push·PR) 후 차기 마일스톤(사고 GUI Desktop 스타일+공식 로고 아바타 — 영호 2026-07-15 확정) → M5 배포. 본 항목은 마감 마커(코어/계약/하네스 행동 변경 없음 — pin·CHANGELOG 예외 2파일 + Phase/reports 문서만). | [L] |
| 2026-07-15 | **GAP1 마일스톤 15/15 구현 완주 — P15 라이브 버그 헌팅 루프 수렴 종결 + 마감 플립** — P15가 R3(0)+R4(0) 연속 2라운드 신규 결함 0으로 4라운드 수렴(서킷브레이커 5 이내, 원장 = 15-rounds-log.md, 커밋 9개 1e94f2d~d2464c0). 최종 게이트: typecheck 0(node+web)·Vitest 5153 passed/0 failed(375 files)·lint 0·라이브 배터리 9 spec 43 tests GREEN. 배포 게이트 "AgentDeck으로 AgentDeck 개발 가능" = dogfood 통주 + 실환경 4라운드 실증. P15 시드 봉합 6·명시 보류 4(백로그 귀속)·승계 2. 마감 플립(P15·_milestone-plan done) + GAP1-DONE.md 초안(HTML 짝 = GAP1-코어패리티-15페이즈-완주-보고서.html 착지 58ba8f0·경로 정합 — 단 HTML 내 5단계 라벨 부재로 phase-gate strict 미통과 잔존, 라벨 반영 = 문서 담당 몫). **잔여 사람 게이트 3건**: 영호 육안 일괄(P13 2컷+P14 10컷+P15 20컷)·push 1회(영호 승인)·PR GO(+원격 머지완료 브랜치 8건 삭제 여쭘) → M5 배포. 본 항목은 마감 마커(코어/계약/하네스 행동 변경 없음 — pin·CHANGELOG 예외 2파일만). | [L] |
| 2026-07-14 | ADR-036 박제(bg tail 하이브리드) + GAP1 12→15 Phase 확장(영호: P13 세션 중 모드 전환·P14 SubAgent 스플릿 뷰·P15 라이브 버그 헌팅 루프 편입, 배포 게이트 강화 = "AgentDeck으로 AgentDeck 개발 가능한 성능·안정성·UX"). | [L] |
| 2026-07-14 | GAP1 P09 — bg_task 계약 additive 확장(output tail 조각) + AGENT_TASK_STOP 채널·preload agentTaskStop 신설(신뢰 경계 신규 문 1, untrusted 검증·reviewer 통과). 옵셔널 필드/신규 채널만 — 기존 행동 불변. | [L] |
| 2026-07-14 | 멀티머신 git-first 셋업 — .gitignore 재구획: `/artifacts/` 루트 한정(00.Documents/Artifacts 마일스톤 증거 승격, 재생성 불가 판정), `.claude/state/*` 전환 + pin·tdd-enforce 커밋 예외(두 머신 공통 진실). 왕복 프로토콜(마디 커밋 우선·wip: 예외·pull --ff-only·이력 재작성 금지) pin 등재. 근거 = ignore 전수 감사(디스크 실존 11건 판정). 영호 결정 2026-07-14. | [M] |
| 2026-07-13 | **supervisor-guard 봉인 판정 앵커화(C-core)** — indexOf 부분일치 폐기, classifyHarnessPath 단일 분류기(repo/홈 앵커·`..` 해소). `..` 재진입 우회(실증)·홈 `~/.claude/plans` 오탐(plan 모드 차단)·memory 예외 repo 구멍 봉합. 다른 저장소 .claude는 보호 범위 밖으로 의미 변경. 홈 config fail-closed. 유지보수 창(영호 오픈·직접 설치), Codex 상담 C안, 테스트 25/25. 한계(심볼릭 realpath·8.3) = C-full 백로그. | [H] |
| 2026-07-13 | **GAP1 P03 — AgentEvent 정규화 taxonomy ADR-035 박제**(probe-first, 신규 이벤트 9종 + `permission_request.planReview` 확장, additive-only). SDK 원시 시스템 메시지 10 카테고리 → 공통 AgentEvent 정규화 계약을 `02.Source/shared`에 선행 정의(방출·소비는 P04~P09). 훅 3메시지→단일 `hook_lifecycle`(phase 판별) 통합·`session_state`(실행상태) vs `session`(resume 토큰) 역할 대비 reviewer 🟡 2건 ADR 반영. Codex stub 영향 0·typecheck 0·4760 pass·lint 0. | [L] |
| 2026-07-13 | **BL1 마일스톤 완료 — HR1 이후 잔여 백로그 청소(7 Phase) + goal 표시 수명 일원화** — 그룹 A 하네스 2(P06 훅 견고성 [H]·P07 CORE-03 재정합 [M] — 별도 기록) + 그룹 B 앱 5(offKeys prune·유예 타이머 단순화·goal 배너 stale-watchdog·복원 데드락 진단→정직 클릭 회복). 마감일 후속: goal 표시 수명 일원화(`AppState.goalRun` 단일 상태 — 점등 커맨드 입력·소등 백엔드 ended/error/abort, `autonomyActive` 가시성 게이트 제거, reviewer 🔴 1건 봉합) + 시각검증 하네스 8컷. 게이트 3종 green(typecheck 0·Vitest 4715 pass/8 skip·lint 0)·영호 육안 8컷 판정 OK. BL1-DONE.md + 종합 HTML + INDEX 플립. **영호 결정(2026-07-13)**: 배포 순서 = GAP1 코어 패리티 마일스톤 → M5 배포, 배포 게이트 = "AgentDeck 안에서 AgentDeck 개발 가능". PR = 영호 게이트 대기. 코어/계약/하네스 행동 변경은 P06/P07에 별도 기록 — 본 항목은 마감 마커. | [L] |
| 2026-07-13 | **BL1 P06 훅 견고성** — shell-policy 크래시 시 dangerous-cmd-guard·supervisor-guard fail-open → fail-closed(exit 2) 전환 + emit_system_message \|\| true + .sh exit code 회귀 테스트 13종(_lib/hook-exit.test.mjs, 크래시 주입 샌드박스). 유지보수 창(영호 오픈) 작업, 재봉인+/hooks 재신뢰 필요. | [H] |
| 2026-07-13 | **BL1 P07 CORE-03 재정합** — core-manifest claude.impl에 settings.json 추가·stale note(기계 차단 없음) 교정·부분 보장(Read 커버/Bash 미커버) 선언. canary 프로브 실측 근거. 행동 변경 없음. | [M] |
| 2026-07-13 | **Sol 봉합-확인 재리뷰 3건** — PowerShell 콜론 바인딩 시크릿 우회 차단(발견1) + doctor canary os.tmpdir 이전·소유 샌드박스 self-clean으로 assistant 프로필 EPERM 해소(발견2) + conformance verifyTypes 고정 allowlist화로 자기재정의 차단(발견3). 시크릿 가드 구문 손질 종결(denylist 무한 회피 — "부분 보장" 선언에 위임). 훅 digest 재갱신(재신뢰 필요). Codex 31/31·doctor exit 0·conformance 13/13. | [H] |
| 2026-07-13 | **Codex Sol PR리뷰 봉합** — 시크릿 가드 표현식 내 리터럴 경로 우회 차단(P1, CORE-03 유일 실효 방어선 강화) + doctor canary 파일 삭제 방지·고유 토큰화(P2) + doctor import-safe화로 회귀 테스트 연결. 훅 digest 갱신(재신뢰 필요). `codex review --base master`(gpt-5.6-sol)가 머지 전 전체 diff 재리뷰로 자기 어댑터 결함 2건 발견 → 유지보수 창 직접 수정·재봉인. Codex 32/32·doctor exit 0·conformance 13/13. | [H] |
| 2026-07-13 | conformance 게이트 재작업 — Codex Sol 첫 실전 리뷰 봉합(🔴 중복·비정규 헤더 false-green / 🔴 표준 흐름 미연결 → Vitest 스펙 11케이스로 npm run test 연결 / 🟡 경로 탈출 차단·회귀 테스트). reviewer 라벨 실적용(gpt-5.6-sol·xhigh·restricted) 세션 로그로 기계 확인 — ADR-033 live acceptance 종결. | [M] |
| 2026-07-13 | **HR1 마일스톤 완료 — 하네스 3층 구조(ADR-034) 수립** — 코어 정본 CORE-01~13+`core-manifest.json`(P02) · `CLAUDE.md` 어댑터 재정렬(P03) · 훅 관측성 systemMessage+`guard-blocks.log`(P04) · Codex 전담 보조 원자 전환(P05) · conformance 게이트 기계화(`conformance-check.mjs` PASS 13/13, 음성 검증 8/8 FAIL)+통합 검증(P06). 게이트 7종 green(typecheck 0·Vitest 4632 pass/8 skip·lint 0·Claude 훅 23·Codex 30·doctor --live exit 0·conformance 13/13) · reviewer CRITICAL 0(minor 3 = 유지보수 창 백로그) · agent-runs.ts 라인참조 부식 정리(H3 ③). 6 Phase(P01~P06) 전부 done, HR1-DONE.md+종합 HTML 박제. PR = 영호 게이트 대기. | [H] |
| 2026-07-13 | **HR1 P05 — Codex 전담 보조 원자 전환** — AGENTS.md 전면 재작성(코어 어댑터화·위임 조직론 삭제·권한 진입 계약) + root 최소권한(agentdeck-assistant)·rescue 제한 쓰기 프로필 + 훅 시크릿 직접 참조 차단 신설(실측: sandbox 읽기 deny 미강제 → 보상 통제, 부분 보장 선언) + custom agent 9→2·브리지 8→2·doctor 3축+baseline 외부화(codex-baseline.json — CLI 0.144.1 재실측 반영) + stash 브리지 5종 OID 검증 drop(아카이브 존치) + ADR-033 개정 1. Sol 3턴 검토 차단 2건 봉합, 단일 green 커밋(851499c)+후속 3커밋, 라이브 스모크 통과(승인 후에도 봉인 훅 차단 = 심층 방어 증명). 창 재봉인+프로브 완료. | [H] |
| 2026-07-12 | **HR1 P04 — 훅 관측성 리뉴얼(systemMessage + guard-blocks.log)** — 알림형 4종(risk-detector·reviewer-auto-trigger·convention-size-guard·circuit-breaker) echo>&2 → stdout JSON systemMessage 전환(공식: exit-0 stderr는 debug 전용) + 차단형 4종(supervisor-guard·dangerous-cmd-guard·tdd-guard·phase-gate-validator) exit 2 유지+원장 기록, legacy advisory도 가시화. `.claude/state/guard-blocks.log` 원장 신설(allowlist·redaction·라인 append·락 회전 512KB→.1) + `_lib/guard-log.mjs`·`system-message.mjs`·테스트 23/23·bash -n 10/10. exit-0 permissionDecision:"deny" 경로 실측 유효(채택 = 영호 결정 대기). Codex(Sol) 리뷰 3건 반영. 창 재봉인+프로브 완료. | [H] |
| 2026-07-12 | **HR1 P03 — CLAUDE.md를 3층 코어의 Claude 어댑터로 재정렬** — 머리말 어댑터 선언(ADR-034) + CRITICAL 요지+CORE-NN 참조 축약(MAPPING §A 이중 서술 해소, 상세 정본=코어) + CORE-07 파괴 명령 명시 신규 + 문서 지도 CORE.md 추가·ADR 인덱스 정정 + REPL_TRANSITION stale 정정(라이브 e2e 완료·LR4 종결) + 모델 목록 Sonnet 5 갱신 + policies/INDEX.md 코어 참조 1줄. 영호 창 개방→diff 전체 리뷰 승인→git restore 재봉인→봉인 프로브 확인. 섹션 헤더 9종 보존(훅·정책 참조 파손 0). | [H] |
| 2026-07-12 | **HR1 P02 — 엔진 중립 하네스 코어 정본 채택(영호 설계 승인)** — `00.Documents/harness/CORE.md`(CORE-01~13 조항·조항별 버전) + `core-manifest.json`(조항×어댑터 구현·검증 지점 기계 선언 — manual 허용·선언 부재 불허) + `MAPPING.md`(이중 서술 13건 P03/P05 액션 지도) + ADR-034(3층 구조) 신설. 검토 포인트 4건 원안 확정, CORE-03 Claude 기계 차단 공백은 백로그 등재. P03(CLAUDE.md 어댑터화)·P05(AGENTS.md 원자 전환)가 소비. | [H] |
| 2026-07-12 | **HR1 P01 — ADR.md 세분화(1결정=1파일)** — 460줄·33건 단일 파일을 `00.Documents/adr/ADR-NNN-slug.md` 개별 파일로 분리 + `ADR.md`는 41줄 인덱스로 전환(경로 보존 — 기존 참조 무파손). 결정론 분할 스크립트로 **preamble+33블록 바이트 재조립 === 원본** 검증(의미 변경 0 기계 증명). 앵커 참조 0·훅 소비 주석 인용뿐·doc-thresholds "ADR 외부화" 정책 예시의 이행. superseded 4건(006·011·014 전체, 004 부분)·개정 1건(025) 상태 표기. | [M] |
| 2026-07-12 | **HR1 계획 v2 재편 — Codex(Sol) adversarial review No-ship 전건 반영** — 구 P05+P06 합병(Codex 전환을 단일 green 커밋 원자 처리 — RED 중간 커밋 설계 폐기) + stash 불변 OID 앵커(99704c1b) + 최소권한 기본 프로필 신설 계획 + 코어 clause ID·machine-readable manifest + conformance 게이트(P06) + guard-blocks.log 보안·동시성 + 차단 가시화(exit-0 permissionDecision 경로). 7→6 Phase. plan-auditor 재검증 GO(🔴0·🟡4 즉시 반영, stash OID 실측 일치). 계획 문서만 — 하네스 본체 무변경. | [L] |
| 2026-07-12 | **HR1 착수 — 하네스 전면 리뉴얼 마일스톤 정의 (계획 수립, 코드 무변경)** — 3층 구조(엔진 중립 코어 `00.Documents/harness/` / Claude 어댑터 / Codex 경량 어댑터) 재설계 7 Phase. 영호 확정: Codex = 전담 보조(리뷰·진단·rescue, 풀 드라이버 철회) · 훅 관측성(systemMessage + guard-blocks.log, H3 ④ 채택) · ADR.md 세분화(P01). H3 안건 4건 흡수, stash 브리지 5종은 편입→폐기 권고 전환(본문 복제 포크 판명, P05 영호 게이트). plan-auditor 조건부 GO → 🔴1(ADR-033 개정 착지) 즉시 봉합. | [L] |
| 2026-07-12 | **ADR-024 개정 — replMode 스코프 이관 반영 (영호 위임·메인 직접)** — LR4 P07 실장(전역 단일 → 세션별)에 맞춰 ADR-024 본문 정합: 제목 꼬리에 "스코프 이관 2026-07-12" 추가 + 상태줄을 라이브 e2e 사인오프 **완료**로 정정 + "갱신(2026-07-12) 스코프 이관" 블록 신설(전역 단일 필드 → 대화별 `ConversationRecord.replMode?`·패널별 `PanelThreadSnapshot.replMode?` 세션 독립 토글, additive optional·구 레코드 무필드=undefined 폴백). 결정 문서(ADR)라 영호 명시 위임 하 메인 직접 편집, 코드 무변경(P07 실장은 별도 커밋). | [M] |
| 2026-07-12 | **LR4 P07 — replMode 전역→세션별 이관 + 공유계약 additive 확장** — `replMode`를 전역 단일 필드(스코프 과대)에서 대화별(단일: `ConversationRecord.replMode?`)·패널별(멀티: `PanelThreadSnapshot.replMode?`)로 내려 세션 독립 토글. shared 계약(`ipc/conversation.ts`·`ipc/multi.ts`)은 additive optional 확장(IPC 버전 bump 없음·구 레코드 무필드=undefined graceful 폴백)이라 하위호환 유지. 기존 전역 prefs는 `lib/replModeDefault.ts` 기본값으로 이관·마이그. JSON 영속 스키마 마이그(버킷 c)는 **영호 승인 + 커밋 GO(2026-07-12)**. reviewer(shared-contract) CRITICAL 0 · 🟡#1 대칭화 2줄 renderer 반영. 게이트: typecheck 0 · test 4632 pass/8 skip/323 files · lint 0. ADR-024 본문 반영은 영호 직접(잔여). | [M] |
| 2026-07-11 | **supervisor-guard 봉인에 사용자 홈 메모리 예외** — `shell-policy.mjs` `isClaudeHarnessPath`가 `~/.claude/projects/<프로젝트>/memory/`(세션 간 학습 기록)를 하네스로 과잉 매칭해 메인 memory Write가 차단되던 것을 제외 규칙 1줄로 봉합(+회귀 2건: 메모리 통과·path traversal 여전히 차단). 영호 창 개방(settings.json deny·훅 임시 제거) 하에 메인 직접 수정 대행 → git restore 재봉인 → 라이브 프로브(가드 재무장 차단 + 봉인 중 메모리 쓰기 통과) 확인. | [M] |
| 2026-07-11 | **coordinator Agent 도구 유지 결정(영호)** — 장기 실행 가시성 불편 제기 → 권한 회수 대신 운영 보완(위임 브리프에 `progress.md` 스텝 로그 지시 + 30분 무소식 시 liveness 프로브)으로 결론. 하네스 무변경. | [L] |
| 2026-07-11 | **01.Phases 마일스톤 폴더 15종 시간순 NN 접두 리네임** — `00_RF1-cleanup`~`14_H1-codex-harness-hardening`(git mv 이력 보존) + `INDEX.md` 연대기 목차 운영 — 영호 매핑 승인. 과거 문서 내 옛 경로 표기는 소급 미수정(M5 권고, INDEX가 항법). | [L] |
| 2026-07-11 | **LR4 P03 — idle-close 유예 후 판정 + 자율 턴 상한** — done 직후 즉시 close → 3초 유예(`IDLE_CLOSE_GRACE_MS=3000`, goal stop-hook continuation 흡수) + 연속 자율 턴 상한 100(`MAX_CONSECUTIVE_AUTONOMOUS_TURNS`, 영호 확정 — 사용자 입력 시 리셋·초과 시 종료+통지·resume 무손실 재개) + `autonomy_status` 생존신호 additive 이벤트(shared +48/−0, 기존 필드 변경 0 — P05 소비 예정). goal 자멸 재현 신설 RED→GREEN 6/6, LR3 8/8 회귀 유지, reviewer CRITICAL 0. | [M] |
| 2026-07-11 | **LR4 P02 — 688ms idle-close teardown 창 소거** — AgentBackend 계약에 optional `onSessionClosing?` 추가(additive — Codex/Echo no-op, ADR-003 정합)·펌프 idle-close commit 지점 1회 동기 발화·persistentRuns identity-checked 원자 제거(done/activeRuns 미개입). P01 `it.fails` 2건 실green 전환, reviewer CRITICAL 0. | [L] |
| 2026-07-11 | **Codex source-command 스킬 브리지 5종 처리 방침 확정** — 하네스 skills 계약(8종 정확 일치)과 충돌해 커밋 보류·stash 보관, 영호 결정으로 H3 동기화 묶음 편입(H3 착수 시 계약 등록 확장과 동시 처리). `.agents/skills/**` 방침·코드 무변경. | [L] |
| 2026-07-11 | **Harness 중요 결함 H1·H2·H4 봉합** — Codex 구현 Worker 5개를 역할별 permission profile로 분리하고 공통 read-only 기반에서 자기 도메인만 write로 열었다. Secretary는 운영 문서·로컬 Git·build/E2E 출력(`out`·`artifacts`·`test-results`)만 write하며 제품 코드는 read-only다. `harness-doctor --live`를 profile 초기화 7개 + 저장소·`:tmpdir` 밖 격리 workspace root allow/deny 경계 16개 canary로 강화했다. Claude coordinator에는 위임용 `Agent` 도구만 추가하고 Worker 재귀 위임 금지는 유지했다. Claude shell policy는 실제 node `-e/-p`·Deno·PowerShell 실행 segment의 내장 파일 API와 PowerShell cmdlet/별칭 Harness 쓰기를 차단하되 설명 텍스트·읽기 명령·Claude state·CHANGELOG 예외는 보존했다. 초기 TDD RED 6건과 Reviewer 후속 RED → Harness 38/38, doctor live, Bash Hook 구문 10/10, 최종 Reviewer PASS. | [H] |
| 2026-07-10 | **Codex Harness H1 실행 계약 보강** — root는 Full Access, 점검은 read-only, Worker는 workspace, secretary는 operations permission profile로 분리하고 SubAgent 프로필에 `.env*`/`secrets/**` deny-read를 적용. project execpolicy로 push/PR/merge/release/package/publish=prompt·curl/wget/Invoke-WebRequest=forbidden을 실측. 역할별 비용 계층은 Sol high(조정·리뷰·계획감사) / Terra medium·high(구현·QA) / Luna low(secretary)로 분리. UserPromptSubmit 입력 명확성 3분기, 사용자 승인 maintenance 환경, Node/.NET Harness 우회 탐지, Hook script SHA-256 cachebuster, static/live를 구분하는 doctor를 추가. Windows sandbox 초기화 실패와 stale digest exit1을 분리하고, `.codex/state` write 승격 충돌을 제거했으며 stale Hook은 조용히 no-op하도록 했다. 자식 프로세스 시작 실패도 예외 대신 진단 결과로 반환하도록 27개 계약 테스트와 doctor live canary로 고정. ADR-033. | [H] |
| 2026-07-10 | **Codex Windows Hook 실행기 복구** — 네 이벤트(`UserPromptSubmit`·`SubagentStart`·`PreToolUse`·`PostToolUse`)가 공유하던 CMD 전용 `commandWindows`(`@for /f`)를 PowerShell 네이티브 Git-root 해석으로 교체하고 `$LASTEXITCODE`를 명시 전달. 기존 실행기는 Codex App Server의 PowerShell에서 매번 `ParserError`·exit 1을 내 work-pin·파괴 명령·TDD·완료 게이트를 무력화했고, 단순 PowerShell 교체만으로는 정책 차단 exit 2가 exit 1로 축약되는 2차 결함도 있었음. 실제 `pwsh.exe`로 네 이벤트를 저장소 하위 디렉터리에서 실행하고 차단 코드 2를 보존하는 회귀 테스트를 RED→GREEN으로 추가(15 PASS). Hook 정의 hash 변경으로 `/hooks` 재신뢰와 새 세션 검증 필요. | [H] |
| 2026-07-10 | **Codex Hook 신뢰 전 정합 봉합** — 새 `-DONE.md`에 `gate_version: 1`·`report_html`을 도입하고 Claude/Codex 독립 validator가 frontmatter·placeholder/status·필수 H2·AC 실행 결과·MD/HTML 5단계 라벨을 strict `exit 2`로 검사. 기존 추적 문서는 마이그레이션 전까지 advisory 유예. Codex TDD guard는 기존 테스트 Update+구현 Update same-patch 우회를 차단하고, 두 엔진 risk-detector는 실제 `CodexBackend.ts`를 trust-boundary로 표시. 공식 `PreToolUse` payload에 root/subagent 구분자가 없는 한계를 명문화해 Codex Supervisor 전임을 Hook이 기계 강제한다고 가장하지 않고 `AGENTS.md`·custom agent 권한으로 유지. Hook 정의 hash와 script 본문 재검토도 구분. | [H] |
| 2026-07-10 | **Claude/Codex Hook runtime 완전 격리 + 공통 결함 봉합** — Claude는 `.claude/settings.json`→`.claude/hooks/**`→`.claude/state/**`, Codex는 `.codex/hooks.json`→`.codex/hooks/**`→`.codex/state/**`만 사용하도록 경계를 명문화하고 Codex의 Claude pin fallback을 제거. 양쪽 구현은 서로 import/source하지 않는 독립 테스트·독립 parser를 유지하면서 path traversal, 파괴 명령 변형, backend/shared-contract 검출 누락을 각각 봉합. Codex는 새 테스트+구현 same-patch TDD 우회도 차단. Windows의 `core.autocrlf=true`로 Claude shell Hook이 CRLF 체크아웃돼 Bash에서 무력화되던 기존 결함은 `.gitattributes` LF 강제로 해소. 공용 정책 의미(`CLAUDE.md`·policies·agents)는 공유하되 Hook 실행·runtime state는 공유하지 않음. | [H] |
| 2026-07-05 | **CP1 공유계약 확장 — 서브에이전트 표시명·사이드카 영속·패널 cwd 스캔 (PR#18 머지, master 955849d)** — additive optional 3파일: ① `02.Source/shared/agent-events.ts` `SubAgentInfo.displayName?: string`(모델이 지은 이름 — `name`=subagent_type 계약 불변, 표시 전용 additive) ② `02.Source/shared/ipc/conversation.ts` `PersistedSubAgent extends SubAgentInfo`(단일챗 서브에이전트 사이드카 영속) + `SUBAGENT_PERSIST_LIMITS` + `ConversationRecord.subagents?`(버전 필드 없이 graceful — 구 레코드 무필드 = undefined 정상) ③ `02.Source/shared/ipc/settings.ts` `SkillListRequest`/`CommandListRequest.root?: string`(패널 cwd 스캔 — untrusted renderer 입력 → main 재검증). 전부 additive optional·IPC 버전 bump 없음·reviewer 🟢(trust-boundary·shared-contract CRITICAL 0). | [M] |
| 2026-07-05 | **LR4 착수 — REPL/goal 세션 안정성 마일스톤 정의 (계획 수립, 커밋 898f531 — 코드 미착수)** — idle-close 창 봉합·goal 자멸·무한대기 + 세션별 `replMode` 토글을 다룰 안정성 마일스톤 정의. ADR-024 최대위험 구역 예정 — P07이 `replMode` cross-session→per-session 반전 예고(ADR 본문 반영은 영호 직접). 본 엔트리는 방향·착수 박제만(코드 무변경). | [L] |
| 2026-07-04 | **Sonnet 5 전환 — `@anthropic-ai/claude-agent-sdk` 0.3.186→0.3.201 bump(선언 범위 내, 영호 GO A안)** — 별칭 'sonnet'의 모델 해석을 Sonnet 5로 올리기 위한 엔진 의존성 갱신. 라이브 실측: 0.3.186에서 별칭 'sonnet'=claude-sonnet-4-6 → 0.3.201 bump 후 claude-sonnet-5 해석 확인(2회, opt-in 라이브 프로브 보존). 권위 확인(claude-code-guide, 2026-07-04): Sonnet 5 = 1M ctx 불변·effort xhigh/max 지원(4.6은 xhigh 미지원)에 따라 `run-args`의 sonnet xhigh 클램프 해제(false→true, 주석 실측 근거 갱신). shared(`ipc/agent.ts`)는 주석 1줄만(타입·값 무변경). 전체 회귀 green(typecheck 0 / test 4385 / lint 0) — SDK bump로 인한 타입·계약 파손 0. 코드·테스트는 feat 커밋 a960bbf, 본 엔트리는 별도 docs 커밋. | [M] |
| 2026-07-04 | **supervisor-guard A안 — 메인 세션 `01.Phases/**` 편집 차단 추가(Phase 문서도 secretary 전임, 영호 직접 수정·커밋 대행)** — `supervisor-guard.sh` ② Supervisor 전임 case에 `*/01.Phases/*` 추가: 메인 세션의 `01.Phases/**` Edit/Write를 차단(Phase 문서 생성·갱신·status 플립 전부 secretary 위임 강제, 서브에이전트는 면제). **발견 경위**: FB2 분해 중 메인이 `/work-plan` Skill 지시대로 Phase 파일을 직접 Write → 훅 미차단을 영호가 실측 발견 → 헌법(분담표 `01.Phases/**`=secretary 영역) vs Skill 본문(메인 직접 생성) **모순 확인** → 영호 A안(헌법 우선, 메인 차단) 결정. 동반: `work-plan/SKILL.md` §4를 "Phase 파일 생성·pin 시드 = secretary 위임"으로 정합화(헌법-Skill 모순 해소). 실측: 메인 Write 차단(exit 2)·서브에이전트 면제 **양방향 프로브** 확인(2026-07-04). 본 변경은 영호가 에디터로 직접 수정, 메인 세션은 커밋만 대행. | [M] |
| 2026-07-04 | **하네스 봉인 이중 잠금 완성 — settings.json permissions deny에 하네스 Edit/Write 14종 추가 (영호 직접 수정, 커밋 대행)** — `.claude/settings.json`의 permissions.deny에 하네스 편집 차단 14종(7쌍 Edit/Write) 등재: `hooks`·`agents`·`policies`·`skills`·`commands`·`settings.json`·`CLAUDE.md`. `state`(current-pin.txt)·CHANGELOG는 secretary 예외 2파일이라 제외. supervisor-guard hook 봉인(런타임 payload 검사)과 **별개 권한 레이어**(permission 시스템 정적 차단) — 이중 잠금으로 하네스 자체 변경을 영호 단독 통제로 강제. 본 변경은 영호가 에디터로 직접 수정, 메인 세션은 커밋만 대행. **동반**: `supervisor-guard.sh` `is_harness_path` case 패턴을 절대경로 전용(`*/.claude/...`)→상대경로 포함(`*.claude/...` + bare `CLAUDE.md`)으로 패치 — hook 봉인이 상대경로 payload도 차단(영호 직접 수정, 본 커밋 동봉). | [H] |
| 2026-07-04 | **UC1-ultracode-redesign 완료 — ADR-032 v1+v2 구현·라이브 실증·통합 reviewer 🟢** — UltraCode 상호작용 재설계 10 Phase(P01~P10) 마감. v1: 단발성(one-shot) 폐기 → 지속 토글 + Workflow 상시 노출(disallowedTools 제거) + canUseTool 턴별 동적 게이트로 held-open 후속 턴 orchestration 라이브 반영(SDK 세션중 도구변경 부재 실측 대응). v2: 키워드 턴 승격 폐지(전송 orchestration = 토글 단일 진실원, 감지함수는 하이라이트·힌트로 존속) + 토글 기본 ON + OFF+Workflow 시도 시 `orchestration_denied` additive 이벤트 → 대화창 시스템 라인 가시화. 완료조건 ①~⑥ 전부 실증: 라이브 e2e Test3 mid-session OFF→ON 토글 후 perm-card 등장(구 세션고정 구조면 불가 — ADR-032 핵심 증거)·Test4 키워드 비승격 + deny 시스템 라인 PASS·ultracode-demo 프로필 격리 PASS. 기계 게이트: typecheck 0 / vitest 4091 / lint 0 / build 0. IPC 이벤트 유니온 additive만(기존 필드 변경 0). 잔여: push·PR 영호 게이트, FB1 별도 브랜치. 상세 = 01.Phases/UC1-ultracode-redesign/UC1-DONE.md. | [M] |
| 2026-07-04 | **supervisor-guard hook 신설(Supervisor 전임 기계 강제 + 하네스 봉인) + hook 파서 python→node 전환 (영호 세션 지시, 메인 세션 대행)** — ① `supervisor-guard.sh` 신규: 메인 세션 = Supervisor 전임(방향·위임·게이트 판단만)을 기계 강제하고 `.claude/**` 하네스 편집을 봉인(영호 단독 통제 대행). ② **python MS Store 스텁으로 전 payload hook이 무력화돼 있던 것 실측 발견** — `python` 호출이 Microsoft Store 리다이렉트 스텁으로 잡혀 payload 파싱 훅들이 조용히 no-op 상태였음. hook 파서를 node로 전환(`_lib/parse-payload.js`·`_lib/shell-tokens.js` 신설, `hook-common.sh`·`dangerous-cmd-guard.sh` 배선). settings.json 훅 등록 갱신. | [H] |
| 2026-07-04 | **secretary(비서) 에이전트 신설 + 메인 세션 Supervisor 전임 (영호 지시)** — 메인 = 방향·위임 지시문·게이트 판단·사람 소통만, 잡무 전면 위임: ① 회귀 게이트 실행·요약 ② git add(명시 파일)·commit ③ work-pin·본 CHANGELOG 갱신(`.claude` 예외 2파일 — 그 외 `.claude/**`는 여전히 금지) ④ Phase 상태 플립·DONE·보고서 초안 ⑤ 실측 심부름. 코드·테스트 수정 절대 X(도메인 Worker 몫)·push/PR/merge/reset 금지. 등급 "단순=메인 직접" 원칙 폐기 → secretary/Worker 위임(하네스·헌법·ADR만 메인 직접 = 영호 단독 통제 대행). 동기화: CLAUDE.md 분담표·`_routing.md`(매핑·등급·권한경계)·`subagent-routing.md`(§3·함정). 모델 opus(영호 직접 조정). | [H] |
| 2026-07-03 | **ADR-032 개정 v2 — 키워드 턴 승격 폐지·토글 단일 진실원 (영호 확정, P01~P04 구현 후 UX 재검토)** — §2 키워드 OR 결합("보이지 않는 승격" — 전송 값 ≠ 화면 토글 상태 → 사용자 혼란)을 폐지: ① 권한 진실원 = 토글 단일(전송 orchestration = 토글 그대로, 키워드는 비승격 — 감지 함수는 하이라이트·힌트 용도 존속) ② 토글 기본 ON(사용 시점 perm-card G1/G2가 명시적 체크, OFF = 명시적 차단 의사 → G4 deny 키워드로도 우회 불가) ③ OFF+키워드 = 뮤트 하이라이트 + 명시적 사용 유도 힌트 ④ G4 deny 가시화(`orchestration_denied` additive 이벤트 → 대화창 시스템 라인). §4 백엔드 축(P02·P03 상시 노출+라이브 게이트)은 불변 활용. 구현 = UC1 P07~P10 신설(plan-auditor 🔴2 봉합 — P07/P09 깨질 기존 테스트 열거 보강) + P06 사전조건 갱신. IPC는 이벤트 유니온 additive만(기존 필드 변경 0). | [M] |
| 2026-07-03 | **ADR-032 박제(초안) — UltraCode 상호작용 재설계 (영호 구두 확정, 본문 검토 대기)** — ① 단발성(one-shot) 폐기 → 지속 토글 ② 키워드 트리거("ultracode" 단어경계·"/workflows" 언급 시 그 턴 orchestration, 토글 OR 결합) ③ 컴포저 키워드 보라 그라데이션(미러 오버레이, UI.md 네온 예외 확장 예정) ④ 백엔드 A안 — Workflow disallowedTools 제거(상시 노출) + canUseTool 턴별 동적 게이트(held-open 후속 턴 라이브 반영). 근거: SDK 0.3.186 세션 중 도구 변경 공식 수단 없음 실측 + 라이브 일괄의 mid-session 무력 실증. 구현 = UC1 마일스톤 6 Phase(plan-auditor 🔴2 인라인 봉합 — ultracode-demo 라이브 게이트 누수·ON→OFF 재봉인 커버). | [L] |
| 2026-07-03 | **하네스 8종 frontmatter 영호 직접 재편성** — ① **qa 모델 Opus 승격**(`claude-sonnet-5` → `opus`): 라이브 e2e 일괄에서 qa의 다층 원인 진단(부팅 관문→프로필 격리→턴 정착→거짓양성 검출) 품질이 판정 성격임을 확인 — 판정 3종과 동급 정렬. ② Worker 4종 모델 명시 ID → **별칭 회귀**(`claude-sonnet-5`→`sonnet`, 4677866 일부 되돌림 — 신모델 자동 추종 재선택). ③ **전 8종 `effort: xhigh` 재도입**(e2da70e 되돌림 — 현재 실측 no-op이나 향후 지원 대비 선언 유지 선택). 같은 날 실측 결론(frontmatter effort 무효)은 불변 — 재도입분은 현시점 장식임을 양지. | [L] |
| 2026-07-03 | **하네스 8종 effort frontmatter 제거 (영호 지시)** — 2차 A/B 실측으로 서브에이전트 `effort:` 키가 세션 레벨 유무 무관 무효(no-op)로 확정됨에 따라 8개 에이전트 정의에서 `effort: xhigh` 줄 삭제(장식 상태 해소 — 정의가 실효와 일치하도록). `model:` frontmatter(Sonnet 5/Opus 고정)는 실측 정상이라 유지. 서브 effort 상향이 필요하면 메인 세션 effort로 제어. 향후 Claude Code가 frontmatter effort를 지원하게 되면 재도입 검토. | [L] |
| 2026-07-03 | **ADR-031 구현 완료 — 멀티세션 영속 shared 계약 행동 변경 (RMW1-single-writer 5 Phase)** — blob 통짜 `MULTI_SESSION_SAVE`('multi.save') 채널·타입·preload 노출·main 핸들러 **제거**(renderer RMW 재발 = 컴파일 에러), 쓰기는 명령 5종 `multi.cmdUpsert/Create/Delete/Rename/Select`(응답 `{ok, state}` 병합 후 권위 상태)만. main이 read→merge→write를 await 0 동기 원자 블록으로 실행 — 단일 기록자. 게이트 확정(영호): 채널명 단일 dot camelCase / **미지 id upsert = no-op+ok:false**(stale upsert 부활 차단 — P02 reviewer 🟡 봉합). P01 경합 재현 3계열 `.fails` 제거 → GREEN(lost-update 소멸 기계 증거). `MULTI_SESSION_LOAD`는 읽기 복원용 유지·디스크 포맷 version 2 불변(마이그 0). 완료조건 ④ 라이브 e2e는 "라이브 e2e 일괄" 이월. 상세 `01.Phases/RMW1-single-writer/RMW1-DONE.md`. | [M] |
| 2026-07-03 | **effort frontmatter 실측 반전 — 서브에이전트 `effort:` 키는 세션 레벨 유무 무관 무효(no-op)** — `effortLevel` 키 삭제 + `/effort` 미실행 새 세션에서 A/B 재검증(같은 복면산, qa frontmatter low vs xhigh → 23,476 vs 23,483 토큰·55.0s vs 54.7s 완전 동급). 직전 실측의 "세션이 frontmatter를 덮음" 해석을 "frontmatter 자체 휴면"으로 강화 — 하네스 8종 `effort: xhigh` 명시는 현재 장식(제거/유지는 영호 결정 대기). model frontmatter는 정상(spawn별 반영) 재확인. | [L] |
| 2026-07-03 | **하네스 서브에이전트 effort 명시 + Worker 모델 Sonnet 5 고정 (영호 지시)** — `.claude/agents/` 8개 정의 frontmatter에 `effort: xhigh` 추가(키·값 = claude-code-guide 공식 문서 검증). Worker 5(agent-backend·main-process·qa·renderer·shared-ipc)는 `model: sonnet` 별칭 → **`claude-sonnet-5` 명시 고정**(영호 "sonnet은 Sonnet 5" — 별칭 해석 모호성 제거 + Sonnet 5는 xhigh 지원). 판정 3(coordinator·plan-auditor·reviewer)은 `opus`(4.8, xhigh 지원) 유지. 근거: 판정·구현 품질 우선(토큰 비용 감수). 트레이드오프: 명시 ID = 신모델 자동 추종 포기(교체는 수동). | [L] |
| 2026-07-03 | **ADR-031 박제 — 멀티세션 영속 동시성: renderer 분산 RMW → main 명령 기반 이관 (영호 채택)** — BF3 P05 reviewer 🟡 "RMW lost-update" 별도 건 착수. `multi-agent.json` 단일 blob에 renderer 다중 주체(autosave×언마운트 flush×CRUD 5액션×다중 패널)가 각자 LOAD→수정→SAVE 분산 RMW → 간극에 낀 write가 last-write-wins로 통째 소실. 해법 = **병합 책임 main 이관**: 의도 명령 IPC(upsert/create/delete/rename/select)·main 동기(run-to-completion) 원자 처리·blob 통짜 SAVE 채널 제거(단일 기록자 = main)·renderer Zustand는 명령 응답 미러. 기각: CAS/세대 토큰(감지형 — 재시도 복잡성 잔존)·main 쓰기 직렬화만(무효)·세션별 파일 분리(마이그 유발). 디스크 포맷 version 2 불변. 구현 = 별도 마일스톤(`/work-plan`, TDD 선행 — 본 엔트리는 방향 박제만). | [M] |
| 2026-07-03 | **헌법 개정 — `01.Phases/` 빈 폴더 원칙 폐기 (영호 결정)** — 완료된 마일스톤 폴더(-DONE.md·ScreenShot·계획·검수 기록 포함)를 **기록·참고용으로 보존**. 기존 "평소 빈 폴더 — 큰 마일스톤에만 사용" 문구 삭제. 동기: Phase 진행 방식(분해·게이트·reviewer 사이클)의 실례가 다음 마일스톤의 참고 자산 — BF3부터 적용(`01.Phases/BF3-backlog-sweep/` 보존). 곁: 헌법 Phase 작업 라인의 stale `/work:plan` 표기 → `/work-plan` 정정(2026-06-30 Skill 승격 시 누락 드리프트). | [M] |
| 2026-07-03 | **ADR-030 박제 — 권한 요청 UI 인라인 카드 전환 (Track-1 충실도 의도적 이탈, 영호 확정)** — 원본 AgentCodeGUI의 중앙 권한 모달 미러를 폐기하고 **컴포저 바로 위 인라인 카드**(`07_notice/PermissionCard`)로 전환. 근거 3: ① 권한 대기 중 ■(중단) 봉쇄 ② 대화 맥락 차단 ③ 멀티패널 응답 수단 부재. ADR-013/014 충실도 원칙의 **명문화된 waiver**(ADR-029(a) 선례) — plan-auditor 결함-1(무근거 이탈) 지적 → ADR 게이트 경유로 해소. 데이터 계약(shared·preload·IPC) 0줄, 프레젠테이션만 교체. 구현 = BF3 P06(PR #12, master 1f13e50). e2e 셀렉터 계약 `.perm-modal`→`.perm-card`(helpers/permSelectors.ts 단일화). | [M] |
| 2026-07-03 | **UI.md gold 팔레트·네온 예외 + ADR-024 재재고 반영 (LR3 마무리, 영호 승인·대행)** — ① **UI.md §1 기능색**: `--gold`를 REPL 상태 표시등에 재사용(Fable 5 도트와 공유 — 새 HEX 발명 아님) + 짝 토큰 `--gold-soft`/`--gold-line`·형광 pulse 알파 `--gold-glow-1`/`--gold-glow-2`(라이트 0.45/0.85·다크 0.42/0.80) 팔레트 표 신설 + 4R~5R 조정(gold 채움·네온 림·bloom halo·`.toggle-chip` 아이콘·UltraCode 대칭 "네온 pill"). **§5 안티슬롭**: "네온 글로우 금지"에 **REPL·UltraCode 토글 pill 한정 명시적 예외**(영호 승인, 시안 `ScreenShot/버튼_개선안.png`) 등재. ② **ADR-024 재재고(2026-07-03)** 블록 in-place append(기존 결정 이력 보존): 2026-07-01 flip의 "resume 기본·held-open 옵트인" 양자택일 → **AUTO(세션 수명=활동의 파생)** 로 대체·`replMode` 기본 ON 복귀. 정면 반박 3단 실측(P01-(a) 재시작 코드워드 회상 56.7s / P01-(d) 기동 지연 ±30ms / P01-(b) 잔존 크론 150s 3회). 코드(claudeAgentRun idle-close·agent-runs 0줄)는 LR3에서 이미 구현·게이트 green — 본 항목은 docs 반영. 근거=`01.Phases/LR3-loop-ux/_ui-md-gold-draft.md`·`_adr-024-rerethink-draft.md`. | [H] |
| 2026-07-01 | **ADR-024 기본값 재고 — 지속세션(REPL) held-open 기본 → resume 기본(BF1 P05)** — 영호 실사용 불편(30분~24h 자리비움 후 맥락끊김)의 원인=**PC종료/절전에 held-open 프로세스 증발**(idle 아님 — `bf1_idle_probe.mjs`가 7분 idle 견딤 실측). 기본 세션=**resume**(ADR-023 디스크 영속, PC종료 생존), held-open=빌트인 자율루프(/loop·/goal) **옵트인**으로 격하. **(4b)의 귀결**(복원책임을 이미 resume에 넘김). 코드 삭제 0 — `replMode` 활성 default만 전환(구현=별 마일스톤, 정확한 resume 버그 1순위). loop=빌트인 /goal·/loop+GUI(P04 §B 결정2). 반영=ADR-024 "재고(2026-07-01)" 블록+제목표식 · REPL_TRANSITION.md 상태줄+§11. 근거=`01.Phases/BF1-interrupt-loop/` P04 결정문·P05 초안. | [H] |
| 2026-07-01 | **RF1-followup — 거대파일 추가분해·DRY·타입정합 + Codex 방식 미확정 정합** — RF1-cleanup(15/15) 후속 잔여정리, **거동 불변**: `ClaudeCodeBackend` 1596→336·`eventNormalizer` 770→434(7모듈 분해 — `permissionCoordinator`[권한경계 canUseTool, push콜백 1개만 의존]·`sdkOptions`·`queryFn`·`claudeAgentRun`·트래커3)·`00_ipc/index` 191→106(`context.ts` 인프라/공유상태 분리)·`_sanitizeDescription` DRY(2중복→1)·`QueuedMessageView` 타입분리(도메인 vs 뷰 — 단일화 아님)·lint부채 0. **하네스**: `agent-backend.md` + ARCHITECTURE·PRD의 Codex `CLI/OpenAI` 단정 → **미확정**(Track2/M6 재설계 시 SDK/CLI 결정 — 구두결정 박제누락 정정). reviewer 전수 GO·test 3841 green. (브랜치 `refactor/rf1-followup`) | [M] |
| 2026-06-30 | **work 시리즈 Skill화 — `/work-plan`·`/work-run`(Phase 분해·실행 짝)** — ① `/work:plan` 슬래시(`commands/work/plan.md`) → **`/work-plan` Skill**(`skills/work-plan/SKILL.md`) 승격. ② **`/work-run` Skill 신설**(`skills/work-run/`) — 미착수 Phase(`status:pending`) loop-driven 실행 프리셋: 의존성 정렬 → Worker 위임 → reviewer 무조건(깃발) → 회귀 게이트(typecheck/test/build) → Phase별 commit, 버킷 (c)/(b) 정지. `allowed-tools`=git add/commit·npm 게이트만(**push/PR/배포 미포함** = 영호 게이트 도구레벨 보존). **자동발화 허용**(`disable-model-invocation` 미설정 — 미완 Phase "이어서 진행" 자동 인지 = 놓침 방지). 네이밍 `/work-xxx` 하이픈(콜론 네임스페이스는 새 Skill 불가 — Windows 폴더명 제약, claude-code-guide 확인). ③ 정합: CLAUDE.md 하네스게이트(슬래시/Skill 분리)·`loop-driver` §7(work-run = Phase 실행 프리셋, refactor-sweep 옆)·`session/start.md` 5.루프기동(work-run 연결). **동기**: 운영모드 봉합의 *실행 짝* — Phase가 정의돼도 자율진행으로 안 이어지고 매번 놓치던 빈틈을 Skill 자동인지로 메움. 영호 GO. | [M] |
| 2026-06-30 | **운영 모드(loop-driven) 헌법 박제 — 자율 루프 정착 봉합** — 진단: 하네스 이식 시 정책(`loop-driver`·`work-judge`)은 port됐으나 헌법(CLAUDE.md)에 연결하는 "운영 모드" 섹션이 MANIFEST **P2 재작성 체크리스트(절대원칙·Stack·Pillars·문서지도)에서 누락** → INDEX 자인("헌법 참조 위치는 P2 시점 확정")이 미완 → 에이전트가 정책을 행동에 못 옮기고 **기본 신중모드 폴백**(매 스텝 되묻기 = throughput 병목). 봉합 3개: ① CLAUDE.md **`## 운영 모드(loop-driven)`** 섹션 신설(사람=방향+게이트·엔진 구동, work-judge 3버킷 (c)판단·비가역/(b)ui-visual서만 정지, done=CI 게이트, attended·무인배치X) ② `session/start.md` **"5. 루프 기동"** 단계(GO 후 loop-driven 진입·연속 진행 후 묶음 보고) ③ work-pin `pin-template`+`current-pin` **`MODE: loop-driven`** 줄(매 턴 주입). `loop-driver` line3 "헌법 운영모드 링크" 전제가 이제 성립. 영호 GO. | [H] |
| 2026-06-30 | **루트 디렉토리 재구성 — 번호접두 최상위 카테고리 (ADR-028)** — 영호 직접 폴더 이동: `docs/`→`00.Documents/`·`phases/`→`01.Phases/`·`src/`→`02.Source/`·`tests`·`scripts`·`out`→`99.Others/`. AI 배선 4커밋: ① 빌드(test import **1192곳** 깊이보정 `../../src`→`../../../02.Source`·config 8개·이동 472 rename) ② **hook 기능 글롭**(risk-detector·tdd-guard[lookup `$PROJ/99.Others/tests`]·convention-size·reviewer-auto-trigger — 에이전트 자동매핑 누락분 2건 직접 정독 포착, **수동 승인 하**) ③ 하네스 docs(CLAUDE.md 문서지도·R/W표·agents·정책 8) ④ ADR-028+ARCHITECTURE 구조도+living docs. **불변**: `@shared`/`@renderer` alias 타깃만 갱신(별칭명·소스 import 무변경)·신뢰경계·IPC 단일정의·`out/` 루트 유지. 검증 typecheck+test **3619**+build green. 역사기록(기존 ADR·본 CHANGELOG 옛 항목·HARNESS_PORT_MANIFEST) 미변경 — ADR-028이 매핑. 브랜치 `feature/rf1-trackC`(미push). | [H] |
| 2026-06-27 | **RF1 트랙B 구조 — src/main 번호접두 + hooks 이동 + dangling 정리** — ① `src/main` 7모듈 번호접두(ipc→00_ipc·agents→01_agents·fs→02_fs·lsp→03_lsp·persistence→04_persistence·settings→05_settings·window→06_window, ADR-027; 진입점·직속 .ts flat). 하네스 정합: risk-detector 글롭 `*src/main/*ipc/*`(renumber 생존)·agent R/W 글롭·정책·ARCHITECTURE 트리. ② hooks `scripts/hooks/`→`.claude/hooks/`(.claude 통합 — ADR-026 위치 갱신; 배선+62곳). ③ P08 docs 번호접두 취소(저가치·고churn) → dangling 인용 16 주석 정리. 진입점/alias/신뢰경계/글롭 의미 불변. 역사적(ADR·MANIFEST) 미변경. | [M] |
| 2026-06-27 | **ADR-027 박제 — 디렉토리 번호접두 컨벤션 `NN_name` (RF1 P04)** — 일부 하위폴더에 언더바 번호접두 도입(구분자=`_`·촘촘 `00,01,02`·논리/데이터흐름 순). 범위=`components/`+`src/main` 내부 모듈+`docs/`, **최상위 `src/{main,preload,renderer,shared}` 제외**(electron-vite 진입점·alias 고정). 불변: agent 글롭 `/**`·alias·신뢰경계. ⚠️ hook 리터럴(`risk-detector` `*src/main/ipc/*`)은 글롭과 달리 rename에 안 안전 → P07 동반 갱신. 영호 GO. 구현=트랙 B(P05~08). 브랜치 `chore/rf1-restructure`. | [L] |
| 2026-06-27 | **RF1-cleanup 정리 마일스톤 시작 + REPL docs-drift 정정 (Phase 15)** — `/work:plan`으로 RF1-cleanup(위생·번호접두 구조·거대파일 14+1 Phase, 독립 RF 트랙) 생성 + plan-auditor 봉합. **Phase 15: ADR-024(지속세션 REPL) status 드리프트 정정** — 헤더가 "제안·미승인"인데 *본문*(line 208 "✅승인(사용자 GO)")·*코드*(`appStore.ts:576 replMode:true` 기본활성)·*커밋*(`46ae0a4` "사용자 결정: REPL 기본 모드"·`d62fc88` watchdog 드롭)이 전부 "채택·구현" → stale 헤더/status를 ✅채택·구현으로 정합(`ADR-024`·`CLAUDE.md` 문서지도·`REPL_TRANSITION.md` 헤더 3곳). **결정 자체 불변 — stale 헤더만 보정**(거버넌스 갭 아님; 4 서브에이전트 실측+커밋으로 승인 확인). 트랙 A: P01(lock gitignore 추적해제)·P02(프로브 14개 실측→전부 STALE→삭제, gitignored 무영향). 브랜치 `chore/rf1-hygiene`(미push — 인간 게이트). | [M] |
| 2026-06-27 | **하네스 정식 이식 (ClaudeDev→AgentDeck, ADR-026) — ADR-025 policies 스킵 개정** — `docs/HARNESS_PORT_MANIFEST.md`(외부 무편향 재결정) 단일 진실원, `HARNESS_GAP.md`(자기편향 진단) supersede→삭제. ① **`.claude/policies/` 10개 신설**(ADR-025 H5 "스킵" 개정 — 헌법 슬림 350임계 + INDEX 카탈로그, `_routing`=빠른 매핑/policies=상세 정책 분담): reporting-format·pin-and-done·doc-thresholds·grade-and-risk·subagent-routing·review-tiering·pr-and-merge-gate·loop-driver·work-judge·review-throughput. ② 훅 8종(`pin-injector`·`convention-size-guard` 신규 + settings 배선). ③ 위험깃발 +`backend-contract`·`shared-contract`·`ui-visual`(risk-detector가 옛 shared-discipline 흡수). ④ Phase 시스템 `/work:plan`+템플릿 3(done-md·pin·phase) — `scripts/execute.py` 폐기. ⑤ 커맨드 session/{start,end,review}·harness-review·`_escalation` 신규, `/harness` work:plan 정합. ⑥ 솔로 정합(§5.5: 팀언어 제거·admin-bypass 휴면·unity-bridge N/A). ⑦ 곁(C-동결 해제, 영호 지시): phases 37개 이력 삭제·`UI_GUIDE`+`UI_FIDELITY`→`docs/UI.md`(실측 Clay HEX)·docs 드리프트(sqlite→JSON)·baseline(CustomGUI_Agent→AgentDeck) 수정. 스킵(D): knowledge·`/engine:goal`·`/cross-review`·setup. 회귀 green(typecheck+test 3619). 브랜치 `chore/harness-port`(미push — 인간 게이트). | [H] |
| 2026-06-26 | **자율 루프 반복 #1 — 하네스/docs 드리프트 정정 + lint 게이트 복구 + README 정리** — ① 에이전트 정의(main-process·agent-backend·_routing·reviewer·shared-ipc)·FEATURE_MAP의 구식 표현 정정: sqlite→JSON 영속(ADR-006)·`claude -p` CLI→SDK query()(ADR-016)·LSP 예정→완료(ADR-017)·깨진 `REPLICA_GAP` 링크→archive·settings.json $comment(6훅 명시·"deny 차단"→규범 게이트)·CLAUDE.md 문서지도 CHANGELOG 포인터·backend-contract ADR-003 참조강화. ② lint: `eslint-plugin-react-hooks` devDep 복구(코드가 이미 disable 주석으로 가정하던 룰 — 신규 스택 결정 아닌 *복구*) → 36 errors→0(거동불변·3619 green, Opus 2 독립검수 통과). ③ README: AgentCodeGUI 언급·링크·"완전 복제" 클론 프레이밍 제거(단독 서술, 사용자 디렉티브). 보류: settings allow 확대(refactor-sweep용 `git checkout -b`/`restore`)=권한 확대라 사용자 게이트. | [L] |
| 2026-06-26 | **ADR-025 하네스 보강 박제 + H4/H6 적용** — UltraCode 심층감사(4 병렬+합성) 후: ADR-025 신설(보강 결정·port/skip 근거) + `phase-gate-validator` 훅(완료보고 5단계, advisory, H4) + work-judge 3버킷(기계/육안/비가역 → `_routing.md`, H6). **H5 별도 정책파일 스킵**(_routing.md가 이미 단일정의 — 중복 방지). settings.json 훅 6종. deny 작업 후 복원. | [L] |
| 2026-06-26 | **대규모 정리 STAGE0 + 거대파일 감사** — `out/`·`*.tsbuildinfo`·`artifacts/*.html\|png` 삭제(git 미추적, ~10M+). 합성이 정리 오판 정정: `composerSampleData`·`f14SampleData`·`run-args.ts`=keep(실측 참조). 거대파일은 refactor-sweep 진단 보류. 드라이버=`docs/HARNESS_GAP.md`. | [L] |
| 2026-06-26 | **하네스 보강 H1+H2 적용 (ClaudeDev 참고)** — CHANGELOG 신설 + risk-detector(PreToolUse 4깃발 검출)/reviewer-auto-trigger(PostToolUse 경계파일) 훅 등록(H1) + `/refactor-sweep` 커맨드 — TS 적응, 신뢰경계/ADR-003 영구제외·회귀 baseline·push금지·v0 dry-run(H2). `.claude/**` deny는 작업 후 **복원**. 드라이버=`docs/HARNESS_GAP.md`. H3(5단계 DONE)은 보류. | [L] |
| 2026-06-26 | **ADR-024 (4) 분리·확정** — (4a) app-close `closeAll`(끄면 세션 사망, 좀비0) 구현 + **(4b) watchdog auto-revive 드롭**(사용자: "끄면 죽어야". 복원=다음 프롬프트 resume). `before-quit→disposeAllRuns→RunManager.closeAll`. | [M] |
| 2026-06-26 | **컨텍스트 게이지 영속 + 마지막 대화 자동복원 + 텍스트 선택** — 단일챗 `ConversationRecord`에 `lastContextWindow`/`lastUsage` 추가(공유 계약 *추가*). `conversation.lastActiveId` pref로 재시작 시 자동복원. `setPref` 방어 가드. body `user-select:none` 위 채팅 본문 재활성화. | [L] |
| 2026-06-26 | **ADR-024 지속세션(REPL) self-re-arm + watchdog 승인·구현** — query()-per-message → held-open streaming-input 세션. `persistent`/`sessionKey` 공유 계약 추가. 내장 `/loop` 크론 자율 발동. 단발 경로 옵트인 회귀0. (3)interrupt IPC·(5)렌더러 UI 포함. | [M] |
| 2026-06-26 | **ADR-023 Phase 1·1.5 맥락 resume** — system/init `session_id` 캡처 → 중립 `session` 이벤트 → 영속(`ConversationRecord.sessionId`) → 다음 턴 `resume` 옵션. 앱 재시작 후 맥락 복원. `resume` 리터럴은 어댑터 내부(ADR-003). | [M] |
| 2026-06-26 | **ADR-021 오케스트레이션 결과복귀 + 진행표면화 + 인라인 서브에이전트** — Workflow fire-and-watch 결과가 메인 복귀(run 생명주기 done 병합). `task_*` → 중립 `orchestration_progress`. 채팅 인라인 서브에이전트 카드. | [M] |
| 2026-06-24 | **ADR-017 LSP 채택·구현(M2-LSP)** — typescript-language-server + pyright 번들, hover/definition/semanticTokens. 자식프로세스=main 단독(신뢰경계). | [M] |
| 2026-06-24 | **ADR-016 엔진 SDK 전환 완료(Phase 21)** — `claude -p` CLI spawn/taskkill 전면 제거 → `@anthropic-ai/claude-agent-sdk` `query()` 단일. 폴백 없음(SDK 하드 의존). 엔진 고유 리터럴은 `ClaudeCodeBackend`/`claude-stream` 내부(ADR-003). | [H] |
| 2026-06-24 | **ADR-006 supersede — sqlite 제거, JSON 파일 영속** — better-sqlite3 → `src/main/persistence`+`multiStore`(원본 maStore 미러). 네이티브 ABI 마찰 0. | [M] |
| 2026-06-22 | **하네스 v1 — 8 서브에이전트 + 3 훅(ADR-010)** — coordinator/reviewer/plan-auditor + 5 worker(main-process/agent-backend/renderer/shared-ipc/qa). 훅: dangerous-cmd-guard·tdd-guard·circuit-breaker. 등급(단순/보통/복잡/대규모) + 깃발. | [M] |

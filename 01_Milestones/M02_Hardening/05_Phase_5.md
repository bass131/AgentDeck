# M02 Phase 5 — 이주 잔재·무효 테스트 정리

## Phase 5 — 이주 잔재·무효 테스트 정리 · 태그: `cleanup` · 의존: Phase 4

**목표**: 하네스·루프 이주의 잔재 문서와 유효하지 않은 테스트 코드가 사용자 승인 목록대로만 정리되고(삭제는 사용자 직접 실행·워커는 이동만), 참조 무결과 테스트 기준선이 재실측으로 확인된다.

Steps

1. **Phase 4 마감·배선 인수** (인수 대조 절) — ① Phase 4 검증 기록에서 결과·세션 id·기준 HEAD를 읽는다. ② `git log`로 HEAD가 그 기준에서 전진했고 새 커밋 메시지에 `M02-P4` 토큰이 있는지 대조한다. ③ `git status`가 clean인지 확인한다. ④ hook-log에서 Phase 4 세션 id의 stop-gate 줄이 `allow`인지 확인한다. ⑤ **배선 확인** — 같은 세션 id의 **표피 훅 Stop 줄**을 찾는다. `verdict`가 `발화`든 `통과`든 배선 영수증으로 인정하고(`판정불가`면 사유를 함께 적는다), 줄이 없으면 배선 FAIL로 본다. 이때 그 줄에 `agent_id`가 실렸는지와 줄이 몇 건인지도 함께 기록한다 — Phase 4는 자기 Stop을 관측할 수 없어 이 실측은 여기서만 유효하다 (Preview 「실행 중 확정 항목」). ⑥ 하드 게이트 다섯 종 재실행 green. ⑦ 코디네이터가 같은 실물을 대조한다. 확인이 모이면 Phase 4 검증 기록에 배선 PASS·사후 게이트 PASS와 커밋·clean·정상 Stop 확인 줄을 append하고, 자기 세션 id와 기준 HEAD를 읽어 둔다. Stop을 새로 프로브하지 않는다 — 비차단 Stop 재프로브는 이 세션을 끝내 추기가 불가능해진다.
2. 적격 기준을 처분 형태별로 분리해 목록 작성 전에 고정한다 — 기준 밖 항목은 처분 목록에 올릴 수 없다.
   - 삭제 적격: (가) 검증 대상 기능이 저장소에 없음(v1 전용 포함), (나) 옵트인 사유(LIVE=1류 환경변수) 없이 상시 건너뛰는 죽은 스킵. **집행은 사용자 직접 실행뿐이다** (안건 6) — 워커는 어떤 삭제도 집행하지 않고 결과를 실측 확인만 한다.
   - 재생성 산출물: (다) 빌드·테스트가 재생성하는 로컬 산출물 — 「삭제 유지」 대 「재생성 허용(.gitignore 등재 확인으로 대체)」 두 안을 병기해 승인 문답에서 확정한다.
   - 이동 적격: (라) 살아 있는 재료지만 현 편제 밖 — 삭제 금지, 목적지 승인 필수, 집행은 워커가 `git mv`로 한다 (안건 3 해석 적용 — 계획 명시 + 계획 승인을 받은 내용 무변경 이동만 예외).
3. 이름 확정 처분 목록을 파일별 처분안·사유로 작성한다.
   - 이주 잔재: `01_Documents/` 13건은 이동 적격(라) — M03 재료라 삭제가 아니라 이주이며, 목적지는 후보 2안(`00_Documents/04_Design/` — 현 README가 UI·UX 문서 전용이라 소유권 확장 승인 필요 / `00_Documents/00_Architecture/`)과 제3안 여지를 병기해 사용자 선택 안건으로 올린다. 훅 주석 2건(10_start-brief.cjs·20_plan-gate.cjs)의 경로 갱신을 동반한다. `99_Others/`의 loop-design-handoff.md·deepagents-guide.html 처분안을 적는다.
   - 무효 테스트: skip 마커 46개 파일의 유효 옵트인·죽은 스킵 분류, 상시 실패 e2e 2건(multi-agent-ops·orig-probe)의 판정안(회귀 판정 시 처리는 안건 5의 확정 방향 — 기준선에 알려진 실패로 명기 + Backlog 이월), driver 문자열 히트 2건(main/read.test.ts·main/roots-security.test.ts)의 정밀 판독 결과.
   - 재생성 산출물: 로컬 산출물 3종의 최종 상태 두 안 병기. **실물 위치는 저장소 루트의 `out`·`test-results`·`artifacts`다** — `99_Others/` 아래 대응 경로는 없다.
   - 차분 고정: 파일별 삭제·유지 집합과 unit·e2e 수집의 예상 전후 차분(수집 파일 수·테스트 수)을 수치로 고정한다.
   - 검사 방식 고정: Step 6이 쓸 판정 방식을 항목별로 못 박는다. 이동 대상 문서 경로는 문자열 grep으로, 산출물 3종은 **루트 경로의 존재 여부 판정**으로 본다 — 짧은 단어를 그대로 grep하면 산문·코드의 일반 단어에 걸려 판정이 무의미해진다. 문자열로 찾아야 하는 항목이 있으면 경계 인식 패턴(`(^|[/\\])out([/\\]|$)` 형태)만 쓴다.
4. USER-INPUT — 처분 목록을 사용자 승인 게이트에 올린다. 명시 안건은 네 가지다 — ① 목록 자체, ② `01_Documents/` 13건의 목적지 소유권, ③ 산출물 3종의 최종 상태, ④ e2e 2건의 판정. 포괄 승인은 무효이고 목록 승인만 유효하다. 승인 요청과 함께 삭제 처분분의 **명령 수준 실행 목록**을 전달한다 — 삭제는 사용자가 직접 실행하기 때문이다.
5. 승인된 목록만, 승인된 경로로 집행한다.
   - 이동: 워커가 `git mv`로 집행하되 안건 3에서 확정된 헌법 8조 해석(건별 예외안)을 따른다. 훅 주석 경로 갱신을 동반하고, 이동된 13건 문서 내부의 옛 경로 자기 참조도 함께 갱신한다.
   - 삭제: 워커는 집행하지 않는다. 사용자가 직접 실행할 때까지 기다렸다가, 실행 후 대상 경로의 부재와 목록 밖 파일의 무변경을 실측 확인하고 결과를 검증 기록 줄로 남긴다. 대기 중에는 다른 Step으로 넘어가지 않는다.
   - 목록 밖 파일은 건드리지 않는다. `.claude/hooks/fixtures/`·`99_Others/scripts/run-e2e.cjs`·`99_Others/assets/`·`98_V1_Archive/`는 처분 대상이 아니다.
6. 참조 무결과 기준선을 재실측한다.
   - 참조 검사의 표면은 **활성 소비자 표면 하나뿐이다** — 「git tracked 전 파일 − append-only 역사 allowlist(`01_Milestones/M01_Bootstrap/`·`98_Management/00_ADR/`·`00_Documents/03_Reports/`) − 현 마일스톤 폴더(`01_Milestones/M02_Hardening/`)」. 이 표면에서 Step 3이 고정한 경로 문자열의 grep이 0건이어야 하며, 이동된 문서 자신의 내부 링크·평문 경로도 이 표면에 포함된다.
   - 현 마일스톤 폴더는 **전체가 처분 명세·검증 기록의 역사 표면**이다 — Steps의 실행 지시, DoD, 처분 목록, 검증 기록 어디든 옛 경로 이름이 남는 것이 정상이고, 0건도 하위 조건도 요구하지 않는다. 판정 로그에는 이 폴더를 제외했다는 사실과 제외 경로만 남긴다.
   - 산출물 3종은 grep이 아니라 루트 경로 `out`·`test-results`·`artifacts`의 존재 여부로 판정한다.
   - 링크 검사기(Phase 4 수리분)는 루트 README 표면의 보조 판정으로 실행한다 — 활성 소비자 표면 판정의 정본은 grep 전수다.
   - 수집 차분 — 집행 후 unit·e2e 수집 실측이 Step 3에서 고정한 예상 차분과 정확 일치해야 한다. collection-baseline의 판정을 하한(≥ 402)에서 집행 후 정확값 등호 판정으로 전환한다.
   - 산출물 3종의 최종 상태 — 승인이 「삭제 유지」면 모든 재실측이 끝난 뒤 최종 정리를 이 Step의 마지막 행위로 두되 집행은 사용자 직접 실행이고 워커는 부재 판정만 한다. 「재생성 허용」이면 .gitignore 등재를 확인한다.
   - start-brief·plan-gate 발화 프로브 정상, `npm test` 실패 0 재실측, e2e 차분 재실측과 기준선 갱신(`.claude/commands/gate.md` 세칙에 등재).
7. 종료 순서를 지킨다 — ① 하드 게이트, ② 검증 기록 추기(관측된 결과·세션 id·기준 HEAD까지만), ③ pin 마감, ④ 마감 커밋(`M02-P5` 토큰), ⑤ `git status` clean 확인, ⑥ Stop. ④⑤⑥ 확인은 **Phase 5 정상 Stop 뒤에 새로 여는 마감 세션**이 인수 대조로 수행해 이 Phase 검증 기록에 append한다 (Preview 마감 절차).

DoD

- 처분 목록 실물(적격 기준·차분 수치·검사 방식·삭제 실행 목록 포함)과 명시 안건 네 가지의 승인 [USER] 줄이 있다.
- 워커가 집행한 삭제가 0건이고, 승인된 삭제 전건이 사용자 직접 실행으로 이뤄졌음이 실측 확인 줄로 기록돼 있다.
- 활성 소비자 표면에서 이동·삭제 경로 grep이 0건이고, 제외한 두 표면의 경로 목록이 판정 로그에 명시돼 있다. 현 마일스톤 폴더는 판정 대상이 아니다.
- 산출물 3종의 판정이 루트 경로 존재 여부로 이뤄졌고, 짧은 단어의 무경계 grep은 쓰이지 않았다.
- 루트 README 링크 검사가 green이다.
- start-brief·plan-gate 발화 프로브가 hook-log 줄로 있고, Phase 4 검증 기록에 배선 PASS(표피 훅 Stop 줄과 그 `verdict`·`agent_id` 실측·줄 건수)·사후 게이트 PASS와 커밋(HEAD 전진 + `M02-P4` 토큰)·clean·정상 Stop 확인 줄이 append돼 있다.
- unit·e2e 수집 실측이 목록의 예상 차분과 정확 일치하고, collection-baseline이 정확값 등호 판정으로 갱신돼 있으며, `npm test`가 실패 0이고, e2e 기준선 갱신이 gate.md에 등재돼 있다.
- 산출물 3종의 최종 상태가 승인안대로다 (부재 판정 또는 .gitignore 등재 확인).
- 이 Phase의 세션 id와 기준 HEAD가 검증 기록 줄에 등재돼 있다.
- 하드 게이트 다섯 종이 전부 green이다.
- 마감 커밋·clean·정상 Stop은 이 Phase 안에서 판정하지 않는다 — 새 마감 세션이 append한 확인 줄로 판정한다.

검증 기록

- PASS 2026-08-10T21:27:28+09:00 — 기동 전 plan-gate 직접 구동 영수증(Phase 5 워커용, 코디네이터 세션 eaa74de0-49ab-4e39-b734-12e4f73f93ea): 합성 스폰 입력 단독 실행, exit 0·stdout 무출력·hook-log 마지막 plan-gate 줄 verdict "allow" — 두 신호 일치, 기동 자격 확인. 코디네이터 선행 실물 대조 — Phase 4 마감 커밋 ab0cc1c(`M02-P4` 대문자 토큰)·git status clean·stop-gate allow(세션 56fabb25, 스탬프 21:24:30 ≥ 장전 21:23:27)·같은 세션 표피 훅 Stop 줄 실존(21:25:47, verdict "판정불가"·rule "assistant-text-부재" — 마지막 assistant 메시지에 text 블록이 없는 종료 형태라 fail-open 계약대로 기록, 줄 실존으로 배선 영수증 성립)·`npm run test:hooks` 코디네이터 직접 재구동 러너 14종 「전부 통과」·유지 대장 AD-02 「진행」 상태와 결정 대장 [USER] 안 B 줄 실존 확인.
- PASS 2026-08-10T21:33:30+09:00 — Step 1 인수 집행(이 Phase 워커 세션 8d223c90-af75-4ee2-9d84-c8db4423190a, 기준 HEAD ab0cc1c00e16c532fbf0d4c930a086ffe34568e4): 절차 ①~⑥을 집행해 04_Phase_4.md 검증 기록에 확인 줄 2건을 append했다. 대조한 실물은 다섯이다 — Phase 4 검증 기록의 결과·세션 id·기준 HEAD, `git log`의 HEAD 전진과 `M02-P4` 대문자 토큰, `git status`(유일한 변경이 마감 커밋 이후의 코디네이터 PASS 줄뿐), hook-log의 stop-gate allow 줄과 같은 세션·같은 스탬프의 표피 훅 Stop 줄 1건, 하드 게이트 다섯 종 재실행 exit 0. 표피 훅 배선은 verdict "판정불가"·rule "assistant-text-부재"·`agent_id` null·줄 1건으로 영수증이 성립해 PASS다.
- PASS 2026-08-10T21:38:30+09:00 — Step 2·3 적격 기준 고정과 이름 확정 처분 목록 작성: 실물은 같은 폴더의 `05_처분-목록.md`(141줄)이고 적격 기준 (가)~(라)·파일별 처분안·차분 수치·검사 방식·삭제 실행 목록을 담는다. 실측 결과는 넷이다 — ① `01_Documents/` 13건 전수와 줄 수를 확인하고 목적지 3안을 병기했으며 동반 갱신 대상이 훅 주석 2건(10_start-brief.cjs 6행·20_plan-gate.cjs 4행)과 문서 자기 참조 3건(01_SSOT-위치 13·66행·03_독자-소유권-지도 20행)임을 grep으로 고정했다. ② skip 마커 파일은 정밀 패턴 `(describe|test|it)\.(skip|skipIf|todo)\(`로 세어 정확히 46건이고 **죽은 스킵 0건**이다 — 46건 전부 옵트인 게이트이며 `m7-explorer-lazy.e2e.ts`의 무인자 `test.skip()` 2건도 `NODE_MODULES_EXISTS` 런타임 가드 안이다. ③ 삭제 적격은 `orig-probe.e2e.ts` 1건뿐이다 — 검증 대상이 저장소 밖 하드코딩 절대 경로의 v1 원본 앱이고 M01 이동 전 기준선부터 실패였다. `multi-agent-ops.e2e.ts`는 `.orch-toggle`·`.orch-on`이 `Composer.css`에 실존해 삭제 부적격이며 둘 다 회귀가 아니다. ④ driver 히트 2건은 `C:/Windows/System32/drivers/etc/hosts` 부분 일치라 처분 대상이 아니다. 차분은 unit 417 불변·e2e 50파일 175테스트에서 49파일 174테스트·실행 2 실패에서 1 실패로 고정했고, collection-baseline은 하한 402에서 등호 417로 전환한다. 산출물 3종은 루트 존재 판정 결과 `out` 5파일·`test-results` 3파일·`artifacts` 74파일이며 셋 다 이미 .gitignore 등재·untracked다.
- USER-INPUT 2026-08-10T21:48:00+09:00 — Step 4 처분 목록 사용자 승인(코디네이터 문답 경유): 명시 안건 네 가지가 전부 확정됐다. ① 목록 승인하되 `01_Documents/12_M01-Bootstrap-Phase-Steps.md`는 이동에서 **삭제로 재분류**한다 — 이관처 `01_Milestones/M01_Bootstrap/`가 정본으로 실존하는 이관 전 중복본이라는 사유이고, 이동은 13건에서 12건이 된다. ② 목적지는 **안 라-3** — `00_Documents/05_Design_Notes/` 신설이며 자리표시 README를 동반한다. ③ 산출물 3종은 **안 다-2 재생성 허용** — .gitignore 등재 확인으로 대체하고 `rm -rf` 블록은 실행하지 않는다. ④ e2e 2건 판정 승인 — `orig-probe.e2e.ts`는 삭제 적격, `multi-agent-ops.e2e.ts`는 존치에 gate.md 기준선 「알려진 실패 1건」 명기와 Backlog 이월이다. 삭제 2건(`orig-probe.e2e.ts`·`12_M01-Bootstrap-Phase-Steps.md`)의 명령 수준 실행 목록은 승인 요청과 함께 전달했고 집행은 사용자 직접 `git rm`이다 — 워커 집행은 0건이다.
- PASS 2026-08-10T21:51:00+09:00 — Step 5 승인 집행: 이동은 워커가 `git mv` 12회로 집행했고 git이 12건 전부를 rename(`R`)으로 인식했다 — 출발지 `01_Documents/`가 비었고 목적지 `00_Documents/05_Design_Notes/`에 12건이 실존한다. 신설 자리표시 `00_Documents/05_Design_Notes/README.md` 1건을 함께 두었다. 동반 갱신 5건도 집행했다 — 문서 자기 참조 3건(01_SSOT-위치 13·66행·03_독자-소유권-지도 20행)을 새 경로로 고쳤고, 훅 주석 2건(10_start-brief.cjs 6행·20_plan-gate.cjs 4행)의 설계 원 기록 경로를 갱신했다. 삭제는 **워커 집행 0건**이고 사용자가 직접 `git rm` 두 줄을 실행했다 — 실측 확인 결과 `02_Project/01_TestCode/e2e/orig-probe.e2e.ts`와 `01_Documents/12_M01-Bootstrap-Phase-Steps.md`가 디스크에서 부재이고 인덱스에 `D`로 스테이징돼 있다. 목록 밖 무변경도 실측했다 — `git status` 전량이 승인 집합(rename 12·삭제 2·신설 README·자기 참조 3·훅 주석 2)과 이 마일스톤 폴더 기록물뿐이고, 제외 표면인 `.claude/hooks/fixtures/`·`99_Others/scripts/run-e2e.cjs`·`99_Others/assets/`·`99_Others/98_V1_Archive/`에는 변경이 0건이다. 산출물 3종은 안 다-2 확정대로 손대지 않았다.
- PASS 2026-08-10T22:02:00+09:00 — Step 6 참조 무결 재실측: 활성 소비자 표면은 tracked 955건에서 제외 20건을 뺀 **935건**이고, 이 표면에서 고정 문자열 세 종의 grep이 전부 **0건**이다 — `01_Documents` 0건, `orig-probe` 0건, `12_M01-Bootstrap-Phase-Steps` 0건이며 미추적 신규물인 `00_Documents/05_Design_Notes/README.md`도 0건이다. 제외 경로는 역사 allowlist 3경로(`01_Milestones/M01_Bootstrap/` 9건·`98_Management/00_ADR/` 1건·`00_Documents/03_Reports/` 1건)와 현 마일스톤 폴더 `01_Milestones/M02_Hardening/` 9건이고, 이 폴더는 처분 명세·검증 기록의 역사 표면이라 판정 대상이 아니다. 표면 구성에서 `git ls-files`의 기본 경로 인용(비ASCII 파일명을 `"..."`로 감싸는 표기)이 접두 매칭을 조용히 빗나가게 해 제외가 17건으로 잡히는 일이 있었고, `-c core.quotepath=false`로 재구성해 20건으로 바로잡은 뒤 판정했다. 산출물 3종은 grep이 아니라 루트 경로 존재 판정으로 봤다 — `out`·`test-results`·`artifacts` 셋 다 존재이고 tracked 0건이며 .gitignore 8·48·54행에 각각 등재돼 있어 안 다-2 「재생성 허용」이 추가 편집 없이 성립한다. 짧은 단어의 무경계 grep은 쓰지 않았다. 링크 검사기 보조 판정은 「내부 링크 10건 전부 실존」 exit 0이다.
- PASS 2026-08-10T22:02:30+09:00 — Step 6 기준선 재실측과 갱신: 수집 차분이 Step 3 고정치와 **정확 일치**한다 — unit 수집 417파일 불변, e2e 수집 50파일 175테스트에서 **49파일 174테스트**다. `collection-baseline.test.ts`의 판정을 하한 `toBeGreaterThanOrEqual(402)`에서 정확값 등호 `toBe(417)`으로 전환했고 단독 실행 4테스트 전부 통과다. `npm test`는 Test Files 411 passed·6 skipped(417), Tests 5465 passed·12 skipped, **실패 0**으로 기준선과 차분 0이다. `npm run test:e2e` 전량 재실행은 **1 실패·62 통과·111 스킵**으로 고정치와 일치하고, 남은 실패는 `multi-agent-ops.e2e.ts:145`의 UltraCode 토글 한 건뿐이다. e2e 기준선은 `.claude/commands/gate.md`에 새 절로 등재했다 — 기준선 실패 1건·수집 49파일 174테스트·스킵 111건이 옵트인이라는 판독·회귀 판정식(실패 2건 이상이거나 다른 파일 실패면 회귀)을 적었다. 수리 이월은 `99_Backlog.md` 8번이고, 이동 문서 색인의 Moodie 좌표 표기 관찰은 9번에 계획 밖 변경 금지 규율대로 보고만 남겼다. `10_start-brief.cjs`·`20_plan-gate.cjs` 발화 프로브는 둘 다 exit 0에 hook-log verdict "allow"이고, plan-gate는 판정 4종 통과에 FAIL 집계가 Phase 1~5 전부 0이다.
- PASS 2026-08-10T22:04:00+09:00 — Step 7 ① 하드 게이트 다섯 종과 `npm test` 차분(이 Phase 워커 세션 8d223c90-af75-4ee2-9d84-c8db4423190a, 기준 HEAD ab0cc1c00e16c532fbf0d4c930a086ffe34568e4): typecheck:node·typecheck:web·lint·test:hooks·build 전부 exit 0이고, test:hooks는 러너 열네 종이 전부 「전부 통과」에 기대-실패 플래그 잔여 0건이다. `npm test`는 Test Files 411 passed·6 skipped, Tests 5465 passed·12 skipped, 실패 0으로 기준선(실패 0)과 차분 0이다. 이 줄까지가 이 세션이 관측한 사실이며, 마감 커밋·`git status` clean·정상 Stop은 다음 인수자가 append한다.
- PASS 2026-08-10T22:07:50+09:00 — 마감 세션 기동 전 plan-gate 직접 구동 영수증(코디네이터 세션 eaa74de0-49ab-4e39-b734-12e4f73f93ea): 합성 스폰 입력 단독 실행, exit 0·stdout 무출력·hook-log 마지막 plan-gate 줄 verdict "allow" — 두 신호 일치, 기동 자격 확인. 코디네이터 선행 실물 대조 — Phase 5 마감 커밋 bad0ac5(`M02-P5` 대문자 토큰)·git status clean·stop-gate allow(세션 8d223c90, 스탬프 22:05:00)·같은 세션 표피 훅 Stop 줄 실존(verdict "판정불가" — P4와 같은 fail-open 종료 형태)·이동 12건+자리표시 README 실물(`00_Documents/05_Design_Notes/`)과 `01_Documents` tracked 0건·gate.md e2e 기준선 신설 절·`npm run test:hooks` 재구동 러너 14종 「전부 통과」 확인.
- PASS 2026-08-10T22:40:00+09:00 — 마감 세션 인수 대조로 Phase 5의 마감 커밋·clean·정상 Stop을 판정한다(이 마감 세션 13ff02a5-59ed-420d-94b2-b72b2716564a, 기준 HEAD bad0ac5144e492be274e82baf5e5eefd6f1327d4, main 역할 plain claude 기동). 대조한 실물은 넷이다 — ① `git log`의 HEAD가 기준 ab0cc1c에서 bad0ac5로 전진했고 커밋 메시지 접두가 `feat(M02-P5)` 대문자 토큰이다. ② Phase 5 워커 종료 시점의 `git status`는 clean이었다 — 이 마감 세션 기동 시점의 변경은 코디네이터가 마감 커밋 이후에 추기한 검증 기록 줄과 pin 갱신 둘뿐이고, 워커 산출물의 미커밋 잔여는 0건이다. ③ hook-log의 stop-gate 줄이 세션 8d223c90에 대해 verdict "allow"·rule "마감-요약-실측"이고 스탬프 22:05:00이 장전 22:03:53 이상이라 종료 허용 조건이 성립했다. ④ 같은 세션·같은 시각(22:05:44)의 표피 훅 Stop 줄이 verdict "판정불가"·rule "assistant-text-부재"로 실존해 배선 영수증이 성립한다 — Phase 4와 같은 fail-open 종료 형태다. 세 신호가 일치하므로 Phase 5의 정상 종료를 확인한다.
- PASS 2026-08-10T22:40:30+09:00 — 마감 절차 ①~③ 집행 결과(이 마감 세션 13ff02a5-59ed-420d-94b2-b72b2716564a). ③ 하드 게이트 다섯 종 재실행은 typecheck:node·typecheck:web·lint·test:hooks·build 전부 exit 0이다. ② 마감 보고서는 워커 위임으로 `00_Documents/03_Reports/M02-하네스-강도-수리-마감.html`에 등재됐고 4절·접기 6개 구성이다 — 헌법 3조대로 `01_Milestones` 밖 편집은 워커가 집행했다. ① 검수는 두 축을 돌렸고 발견 10건을 수집했다 — 리뷰어 1차 4건(높음 1·중간 1·낮음 2), Codex 교차 6건(높음 3·중간 3)이며 어느 발견도 수리에 착수하지 않았다(계획 밖 변경 금지). **완료선 판정이 두 축에서 갈렸다** — 리뷰어는 조항 1·3 충족에 조항 2 조건부 충족이고, Codex는 조항 1 충족에 조항 2·3 미충족이다. 두 축이 독립으로 확증한 표면은 셸 `-c`·`env` 접두 실행 우회(`bash -c "rm -rf dist"`가 현행 allow·Moodie deny)이며 이는 [USER] 기각된 공통 한계 3종에 속하지 않는 미등재 통과면이다. 이 세션은 발견의 처리 방향(수리·기각·이월)을 판정하지 않는다 — 마감 판정은 절차 ⑨의 사용자 게이트다.
- USER-INPUT 2026-08-10T22:44:54+09:00 — 마감 절차 ⑨ 사용자 마감 판정(코디네이터 세션 eaa74de0-49ab-4e39-b734-12e4f73f93ea, 기준 HEAD 77cb458): 판정은 「이월 후 종결」이다. 코디네이터가 판정 전에 두 축의 공통 지목을 자기보고에 의존하지 않고 직접 실측했다 — 같은 입력을 두 가드에 넣은 대조에서 `bash -c "rm -rf dist"`와 `env FOO=1 rm -rf dist`는 Moodie deny·AgentDeck allow였고 `rm -rf dist` 단독은 양쪽 deny였으므로, 갈리는 지점이 래퍼 뒤 플래그·할당 토큰의 명령 위치 연쇄 단절임이 확정됐다. 함께 `sh -c "git push origin main"`이 양쪽 모두 allow임도 관측했다(회귀 아님·헌법 10조 관점의 한계). 사용자 판정에 따라 수리는 착수하지 않고 Backlog 10번으로 이월했으며, 완료선 둘째 조항은 「기준표 열거 범위 안에서 동등 이상」으로 읽어 충족으로 판정했다. 근거 줄은 결정 대장 [USER] 2026-08-10(마감 절차 ⑨)이다. 나머지 발견 8건은 Backlog 1~9번에 등재된 상태로 남는다.

# M02 Phase 5 — 이름 확정 처분 목록 (승인 대기)

- 작성: 2026-08-10 · 워커 세션 8d223c90-af75-4ee2-9d84-c8db4423190a · 기준 HEAD ab0cc1c
- 이 목록은 `05_Phase_5.md` Step 2·3의 산출물이고, Step 4의 사용자 승인 대상이었습니다.
- 포괄 승인은 무효입니다 — 아래 항목별 처분안의 승인만 유효합니다.

## 0. 승인 결과 ([USER] 2026-08-10T21:48:00+09:00 · Step 4 확정)

| 안건 | 판정 |
|---|---|
| ① 목록 자체 | **승인** — 단 13번 `12_M01-Bootstrap-Phase-Steps.md`를 이동에서 **삭제로 재분류**한다. 사유는 이관처 `01_Milestones/M01_Bootstrap/`가 정본으로 실존하는 이관 전 중복본이라는 것이다. 이동 13건 → **12건**, 삭제 1건 → **2건**. |
| ② 목적지 | **안 라-3** — `00_Documents/05_Design_Notes/` 신설, 자리표시 README 동반. |
| ③ 산출물 3종 | **안 다-2 재생성 허용** — .gitignore 등재 확인으로 대체하고 `rm -rf` 블록은 실행하지 않는다. |
| ④ e2e 2건 | **승인** — `orig-probe.e2e.ts` 삭제 적격, `multi-agent-ops.e2e.ts` 존치에 gate.md 「알려진 실패 1건」 명기 + Backlog 이월. |

재분류로 바뀐 수치는 이동 건수와 삭제 건수 둘뿐입니다 — 13번은 문서라 unit·e2e 수집 차분에는 영향이 없습니다.
아래 2·4·8·11절의 원 문면은 append-only 규율대로 지우지 않고, 각 절 머리의 「승인 반영」 줄이 확정본을 지시합니다.

## 1. 적격 기준 (Step 2 — 목록 작성 전 고정)

| 기준 | 정의 | 집행 주체 |
|---|---|---|
| (가) 삭제 | 검증 대상 기능이 저장소에 없다 (v1 전용 포함) | **사용자 직접 실행**, 워커는 실측 확인만 |
| (나) 삭제 | 옵트인 사유(`LIVE=1`류 환경변수) 없이 상시 건너뛰는 죽은 스킵 | **사용자 직접 실행**, 워커는 실측 확인만 |
| (다) 재생성 산출물 | 빌드·테스트가 재생성하는 로컬 산출물 — 두 안 병기 후 확정 | 안에 따라 다름 |
| (라) 이동 | 살아 있는 재료지만 현 편제 밖 — 삭제 금지, 목적지 승인 필수 | 워커 `git mv` |

기준 밖 항목은 이 목록에 올릴 수 없습니다. 아래 「기준 밖 판정」 절이 제외분을 명시합니다.

## 2. 이동 처분 (라) — `01_Documents/` 13건

**승인 반영** — 확정은 **12건 이동**입니다. 13번은 안건 ① 판정으로 삭제(4절)로 옮겨졌고, 목적지는 안 라-3 `00_Documents/05_Design_Notes/`입니다.

13건 전부 하네스 v2 설계 쟁점의 논의본이고 M03 재료라 삭제가 아니라 이주입니다.

| # | 파일 | 줄 | 성격 |
|---|---|---|---|
| 1 | `00_쟁점-목록.md` | 32 | 폴더 안내·쟁점 색인 |
| 2 | `01_SSOT-위치.md` | 83 | 쟁점 01 — 확정본 원본 위치 |
| 3 | `02_매뉴얼-스타일-참고.md` | 66 | 쟁점 02 재료 — 문체 분석 |
| 4 | `03_독자-소유권-지도.md` | 80 | 쟁점 02 — 표면별 소유권 |
| 5 | `04_노션-규칙-점검.md` | 51 | 자기 규칙 재검사 기록 |
| 6 | `05_가독성-실측.md` | 112 | 쟁점 12 — 조판 규칙 |
| 7 | `06_실패-카운터.md` | 111 | 쟁점 03 — 3회 실패 카운터 설계 |
| 8 | `07_주도권-게이트.md` | 101 | 쟁점 04 — 주도권 게이트 신호 |
| 9 | `08_Clear-게이트.md` | 100 | 쟁점 13 — Phase 경계 리셋 |
| 10 | `09_계획-작성-소속.md` | 93 | 쟁점 05 — 계획 작성 소속 |
| 11 | `10_분업-라우팅-규격.md` | 108 | 쟁점 06 — 라우팅 규격표 |
| 12 | `11_부트스트랩.md` | 49 | 쟁점 08 — 부트스트랩 범위 |
| 13 | `12_M01-Bootstrap-Phase-Steps.md` | 193 | M01 계획의 이관 전 원본 |

**목적지 3안 (안건 ②)** — 하나를 골라 주세요.

- **안 라-1 `00_Documents/00_Architecture/`** (추천) — 현 README 문면이 「시스템 구조와 아키텍처 문서」 자리이고 실물이 0건입니다. 13건은 하네스 구조 설계의 논의본이라 갈래가 맞고, 소유권 확장 승인이 필요 없습니다.
- **안 라-2 `00_Documents/04_Design/`** — 현 README가 「UI·UX 디자인 문서」 전용이라 소유권 확장 승인이 필요하고, 그 README 문면 갱신이 이동에 동반됩니다.
- **안 라-3 `00_Documents/05_Design_Notes/` 신설** — 논의본이라는 성격을 별 갈래로 세웁니다. 새 폴더와 자리표시 README 신설이 동반되고, 편제 갈래가 하나 늘어납니다.

**동반 갱신 (승인 시 워커가 같은 커밋에서 집행)**

- `.claude/hooks/10_start-brief.cjs` 6행 주석 — `AgentDeck 01_Documents/08_Clear-게이트.`
- `.claude/hooks/20_plan-gate.cjs` 4행 주석 — `AgentDeck 01_Documents/06_실패-카운터 · 07_주도권-게이트 · 10_분업-라우팅-규격.`
- 이동 문서 자신의 자기 참조 3건 — `01_SSOT-위치.md` 13·66행, `03_독자-소유권-지도.md` 20행.

**관찰 (처분안 아님)** — 13번 `12_M01-Bootstrap-Phase-Steps.md`는 자기 문면에 「Phase 1에서 저장소로 이관하며, 이관 뒤에는 저장소 사본이 진실의 원천」이라 적혀 있고 그 이관처(`01_Milestones/M01_Bootstrap/`)가 이미 실존합니다. 원본이라 삭제 적격 (가)(나)에는 걸리지 않아 이동안으로 올립니다 — 이관 전 원본을 남길 필요가 없다고 판단하시면 승인 답변에 적어 주세요.

## 3. 이동·존치 판정 — `99_Others/` 2건

| 파일 | 크기 | 처분안 |
|---|---|---|
| `99_Others/loop-design-handoff.md` | 212줄 | **존치 (추천)** — 삭제 적격 (가)(나) 어디에도 걸리지 않는 살아 있는 학습·설계 handoff입니다. 이동안을 원하시면 13건과 같은 목적지로 함께 옮깁니다. |
| `99_Others/deepagents-guide.html` | 111,586바이트 | **존치 (추천)** — 위 문서가 대조 재료로 지목한 외부 강의 노트입니다. 같은 이유로 삭제 적격이 아닙니다. |

두 파일은 서로를 참조하고(3곳), 활성 소비자 표면의 다른 참조는 없습니다.

## 4. 삭제 처분 (가)(나) — 1건

**승인 반영** — 확정은 **2건 삭제**입니다. 안건 ① 판정으로 `01_Documents/12_M01-Bootstrap-Phase-Steps.md`가 (가)에 추가됐습니다 — 이관처 `01_Milestones/M01_Bootstrap/`가 정본으로 실존하는 이관 전 중복본이라는 사유이며, 근거 문면은 2절 「관찰」 문단입니다.

**(가) 검증 대상 기능이 저장소에 없음 — 1건**

- `02_Project/01_TestCode/e2e/orig-probe.e2e.ts` (테스트 1건) — 검증 대상이 이 저장소가 아니라 하드코딩된 절대 경로 `C:/Dev/AgentCodeGUI/out/main/index.js`의 **v1 원본 앱**입니다. 저장소 안에 그 런타임이 없고 경로가 머신 고정이라 다른 머신에서는 성립하지 않으며, M01 이동 **전** 기준선(2026-08-09T19:20:15)부터 계속 실패 상태입니다.

**(나) 죽은 스킵 — 0건**

skip 마커 46파일을 전수 판독한 결과 **죽은 스킵은 없습니다**. 46건 전부 환경변수 옵트인 게이트입니다.

| 옵트인 변수 | 파일 수 |
|---|---|
| `LIVE_SDK=1` 단독 (`describe.skipIf(!LIVE)`·`test.skip(!LIVE, …)`) | 26 |
| `LIVE_SDK=1` + 2차 변수 (`BF2INT2`·`BF3SHOTS`·`P01B`·`P04L`·`P06STOP`) | 5 |
| 2차 변수 단독 (`GAP1DOGFOOD`·`GAP1DOGFOOD2`·`GAP1P13`·`GAP1HUNT1~4`·`P14SHOTS`·`P16SHOTS`·`GAP1SHOTS`·`TG1SHOTS`·`BL1P03SHOTS`·`LM1E2E`·`LR2_03_SCREENS`) | 14 |
| 런타임 환경 가드 (`m7-explorer-lazy.e2e.ts` — `NODE_MODULES_EXISTS` 거짓일 때만 무인자 `test.skip()`) | 1 |

## 5. 재생성 산출물 (다) — 3종 (안건 ③)

| 루트 경로 | 파일 수 | 크기 | git 상태 | .gitignore |
|---|---|---|---|---|
| `out` | 5 | 3.3M | untracked | `out/` 등재 |
| `test-results` | 3 | 25K | untracked | `test-results/` 등재 |
| `artifacts` | 74 | 9.2M | untracked | `/artifacts/` 등재 |

- **안 다-1 「삭제 유지」** — 모든 재실측이 끝난 뒤 사용자가 직접 삭제하고 워커가 부재를 판정합니다.
- **안 다-2 「재생성 허용」 (추천)** — .gitignore 등재 확인으로 대체합니다. 세 경로 모두 이미 등재·untracked라 추가 편집이 없습니다. 추천 이유는 `out`이 하드 게이트 `npm run build`의 산출물이라, 종료 순서(하드 게이트 → 기록 → 커밋)에서 부재 판정이 성립할 수 있는 시점이 없다는 것입니다.

## 6. e2e 상시 실패 2건 판정 (안건 ④)

| 파일 | 회귀 여부 | 처분안 |
|---|---|---|
| `orig-probe.e2e.ts` | **회귀 아님** — 이동 전 기준선부터 실패 | 삭제 적격 (가) · 위 4절 |
| `multi-agent-ops.e2e.ts` | **회귀 아님** — 같은 기준선부터 실패 | **삭제 부적격 · 존치** — 검증 대상(`.orch-toggle`·`.orch-on`)이 `Composer.css`에 실존합니다. 안건 5 확정 방향대로 gate.md 기준선에 「알려진 실패 1건」으로 명기하고 수리는 Backlog 이월합니다. |

## 7. driver 문자열 히트 2건 정밀 판독 — 처분 없음

- `02_Project/01_TestCode/main/read.test.ts` 45행 · `02_Project/01_TestCode/main/roots-security.test.ts` 90행
- 둘 다 문자열 `C:/Windows/System32/drivers/etc/hosts`의 부분 일치입니다. 루트 밖 절대 경로 접근을 거부하는지 보는 보안 테스트의 **표적 경로**이지 드라이버 관련 죽은 코드가 아닙니다. 처분 대상이 아닙니다.

## 8. 차분 수치 고정 (집행 후 실측이 정확 일치해야 통과)

| 지표 | 집행 전 (실측) | 집행 후 (예상) |
|---|---|---|
| unit 수집 파일 (`.test.ts`·`.test.tsx`) | 417 | 417 (변화 없음) |
| `npm test` Test Files | 411 passed · 6 skipped | 411 passed · 6 skipped |
| `npm test` Tests | 5465 passed · 12 skipped · 실패 0 | 5465 passed · 12 skipped · 실패 0 |
| e2e 수집 파일 | 50 | 49 |
| e2e 수집 테스트 | 175 | 174 |
| e2e 실행 결과 | 2 실패 · 62 통과 · 111 스킵 | 1 실패 · 62 통과 · 111 스킵 |
| `collection-baseline` 판정 | 하한 `≥ 402` | 등호 `=== 417` |

## 9. 검사 방식 고정 (Step 6이 쓸 판정)

- **표면**: git tracked 전 파일 − 역사 allowlist 3경로(`01_Milestones/M01_Bootstrap/`·`98_Management/00_ADR/`·`00_Documents/03_Reports/`) − 현 마일스톤 폴더(`01_Milestones/M02_Hardening/`).
- 이동 대상 문서 경로 — 문자열 grep `01_Documents`. 폴더명이 고유해 경계 문제가 없습니다.
- 삭제 대상 — 문자열 grep `orig-probe`. 같은 이유로 경계 문제가 없습니다.
- 산출물 3종 — **루트 경로 `out`·`test-results`·`artifacts`의 존재 여부 판정**입니다. 짧은 단어의 무경계 grep은 쓰지 않습니다. 문자열로 봐야 할 항목이 생기면 경계 인식 패턴(`(^|[/\\])out([/\\]|$)`)만 씁니다.
- 링크 검사기(`98_Management/03_Tools/check-readme-links.cjs`)는 루트 README 표면의 보조 판정입니다 — 판정의 정본은 grep 전수입니다.

## 10. 기준 밖 판정 (목록 등재 불가)

- `.claude/hooks/fixtures/` · `99_Others/scripts/run-e2e.cjs` · `99_Others/assets/` · `99_Others/98_V1_Archive/` — Preview 「제약」과 Step 5 문면이 처분 대상에서 제외합니다.
- `multi-agent-ops.e2e.ts` — 검증 대상 기능이 저장소에 실존해 (가)에 걸리지 않습니다.
- skip 마커 46파일 — 전부 옵트인이라 (나)에 걸리지 않습니다.

## 11. 삭제 처분의 명령 수준 실행 목록 (사용자 직접 실행)

승인된 항목만 실행해 주세요. 워커는 실행하지 않고 결과만 실측 확인합니다.

```bash
# (가) 1건 — 저장소 밖 v1 원본을 겨누는 e2e 스펙
git rm 02_Project/01_TestCode/e2e/orig-probe.e2e.ts
```

```bash
# 안 다-1 「삭제 유지」를 고르신 경우에만 — 모든 재실측이 끝난 뒤 마지막 행위
rm -rf out test-results artifacts
```

**승인 반영** — 확정 실행 목록은 아래 두 줄이고, 두 번째 `rm -rf` 블록은 안건 ③이 안 다-2로 확정돼 **실행하지 않습니다**.

```bash
git rm 02_Project/01_TestCode/e2e/orig-probe.e2e.ts
git rm 01_Documents/12_M01-Bootstrap-Phase-Steps.md
```

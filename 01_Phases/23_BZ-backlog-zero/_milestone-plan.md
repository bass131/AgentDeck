# BZ — 백로그 제로 (마일스톤 계획)

> **등급: 대규모** · **깃발: harness** · **Phase 7개** · **유지보수 창 2회** · **PR 1개**
> 세션 플랜 원본은 휘발한다 — **이 문서가 저장소 정본**이다.

## 🎯 왜 하는가

영호 결정 (2026-07-27): *"이참에 백로그들 전부 마일스톤화해서 한번에 처리하자. 계속 백로그 생겨서 또 고치고 지겹다. 슬슬 개발로 넘어가고 싶은데."*

- HR1→HR2→NC 내내 백로그가 계속 생긴 것은 **하네스 정비 작업이 하네스 결함을 드러내는 자기 순환**이었다(개발-성향 19). 이 마일스톤으로 대장을 0으로 만들고 **개발 마일스톤으로 복귀**한다.
- 범위 = `00_Documents/BACKLOG.md` 미해소 전량(14·15·16·21·23·24·25) + HR2 몫 deny 일몰(조건 충족) + 이월 잔여(ev-test 목록화). **BZ 종결 = NC 몫 일몰 타이머(NC 머지+1마일스톤) 조건 충족** — 실행은 다음 마일스톤 창.

## ⚖️ 확정 분기 (영호, 2026-07-27 — 재확인하지 않는다)

1. **21 게이트 = vitest globalSetup 스냅샷·대조** (사후 검출임을 명시). CI 제한 권한은 **백로그 신규 등재**로 추후.
2. **`getUserDataPath()` 폴백 = throw 전환** — 소비처 전수 grep 선행, 폴백 의존처 발견 시 그 갈래 멈추고 보고.
3. **23·24 스윕 = 보수 치환** — 확장자·슬래시 있는 명시 경로만, 예시·개념 호칭 불변, 애매하면 목록으로 남겨 영호 검토.
4. **밤 자율 = 전부 승인** (1회 예외 — 아래 계약).
5. 14 수리 설계 = **역할 기반 귀속** (아래 P01 — 진단·재현 3중 확증 완료 2026-07-27).

## 🌙 밤 자율 계약 (헌법 「무인 배치 금지」의 1회 예외 — 영호 명시 승인 2026-07-27)

- 범위 = **P02 · P03 · P04-A구간 · P05만** (🔴2 봉합 — P04-B구간의 trust-boundary 경로는 work-judge (c) 버킷이라 내일 주간·영호 재석). 커밋까지, push·PR·merge 없음.
- 창 개방 없음 · `.claude/**`·ADR 정본·하네스 편집 없음 (ADR은 **스크래치패드 초안만**).
- 예상 밖 설계 분기 → **그 갈래만 멈추고** 아침 보고, 다른 갈래 계속.
- red를 판정 기준 완화로 풀지 않는다(성향 17). 게이트 출력은 트랜스크립트에 남긴다.
- 아침 보고 = 5단계 요약 + 갈래별 커밋 해시.

## 🗺️ Phase 지도

| Phase | 제목 | 등급 | 문 | 시점 |
|---|---|---|---|---|
| 01 | 창 1 — 봉인 오탐 수리(14) + 템플릿 정합(25) + HR2 deny 일몰 + **검증 렌즈 Fable 5 정식 승격**(영호 2026-07-27) | 복잡 | **창 1** | 오늘 (영호 재석) |
| 02 | 테스트 격리 전역 게이트 신설 (21①) | 복잡 | 없음 | 밤 |
| 03 | getUserDataPath 폴백 throw 전환 (21②) | 보통 | 없음 | 밤 |
| 04 | 옛 경로 주석 보수 스윕 (23·24) — 실측 5도메인/61파일 | **대규모** | B구간 = 주간·reviewer | **A = 밤 / B = 내일** |
| 05 | ADR-040·041 초안 + 16 역참조 실측 | 보통 | 없음(초안) | 밤 |
| 06 | 창 2 — ADR 반영 + conformance 축(15) + CHANGELOG 이동(16) | 대규모 | **창 2** + ADR 승인 | 내일 |
| 07 | 종결 — 대장 0 확인 + BZ-DONE + push/PR | 보통 | push 게이트 | 내일 |

**의존성**: P01 독립. **P02 → P03 → P04 순차** — P02의 게이트가 먼저 서야 P03·P04 작업 중 홈 오염도 감지되고(보호막 우선), **P02·P03은 `99_Others/tests/main/engineVersions.test.ts`를 순차로 수정하므로 역전 시 좌표(`:107`·`:370-379`·`:449,464,499`)가 어긋난다**(🟡a), P03·P04는 `02_Source/main/engineVersions.ts` 파일이 겹친다. **P05는 P02~04와 병렬 가능**(문서 vs 코드 — 실제로는 밤에 순차 실행해도 무방). P06은 P05 승인 후. P07 마지막.

**브랜치**: `chore/bl21-test-isolation-gate` 하나로 전 Phase 진행(생성 완료, pin 커밋 `62a33a6` 탑재) → PR 1개.

## 🧪 회귀 게이트 정본 (Phase AC는 여기를 참조한다 — 복제 금지)

| 게이트 | 명령 | 기준선 (NC 종결 `31184da`) |
|---|---|---|
| G1 훅 셀프테스트 | `npm run test:hooks` | **120/120** — P01이 픽스처를 늘린다: **증가는 기록, 감소는 red** |
| G2 CORE conformance | `node 00_Documents/00_Harness/conformance-check.mjs` | 13/13 — ⚠️ P06이 이 파일을 수정한다(ADR-040) |
| G3 harness-conformance | `npx vitest run 99_Others/tests/harness-conformance.test.ts` | 12/12 — **P06이 늘린다**(ADR-040 red 테스트): 증가는 기록 (🟡b) |
| G4 단위 전체 | `npx vitest run` | **5,336 passed \| 10 skipped** — P02·P03이 늘린다: 증가는 기록 |
| G5 타입·린트 | `npm run typecheck && npm run lint` | 0 / 0 (⚠️ eslint는 `.ts,.tsx`만 — 훅 `.mjs`는 골든 테스트가 유일 판정) |
| G6 e2e 핵심 | `npx playwright test 99_Others/tests/e2e/core-loop` | 4/4 |

> 📐 **기준선 변동 규칙** (🟡c): 증가는 기록하되 **`Tests N passed | M skipped` 원문 줄을 해당 Phase 문서 하단에 박제**(다음 Phase의 절대 앵커) · 감소는 red · **주석 전용 Phase(P04)는 등호** — 증가도 red다(주석 치환이 코드를 건드렸다는 신호).

### 마일스톤 고유 검증

| # | 검증 | 왜 |
|---|---|---|
| V1 | 창 폐쇄 후 canary 봉인 생존 프로브 (판정은 `guard-blocks.log`) | G1~G6은 봉인이 죽어도 green |
| V2 | 재봉인: `gate-open.flag` 부재 + `settings.json ≡ settings.SEALED.json` | TTL 만료는 훅 층만 복귀 (HR2 실측) |
| V3 | **14 라이브 재검증** — 오탐 3종 프로브 **통과** + 양성(쓰기 세그먼트에 봉인 경로) **차단** | 단위 테스트 green ≠ 라이브 발화. 창 안에서는 훅 전체 exit 0이라 **창 폐쇄 후에만 성립** |
| V4 | 백로그 대장 전수 대조 — 각 항목에 해소 커밋 해시 명기, 미해소 잔량 = CI 게이트(백로그 신규분)만 | "전부"는 목록 대조 없이 주장할 수 없다 |

## 🚦 사람 게이트

| # | 지점 | 주체 |
|---|---|---|
| 1 | 창 1 개방·폐쇄 (`OPEN-GATE.bat`/`CLOSE-GATE.bat`) | 영호 |
| 2 | 밤 자율 진입 GO (P01 검증 보고 후) | 영호 |
| 3 | ADR-040·041 승인 (아침) | 영호 |
| 4 | 창 2 개방·폐쇄 | 영호 |
| 5 | ev-test 잔해 54개 — 이름 확정 목록 승인 + 삭제 실행 | 영호 |
| 6 | `~/.agentdeck-dev` activeVersion 앱 확인 (이월 ②) | 영호 |
| 7 | push / `gh pr create` / merge | 영호 `!` 직접 (CORE-06 v2) |

## ⚠️ 마일스톤 전역 함정

| 위험 | 근거 | 대응 |
|---|---|---|
| 봉인 판정기 수술이 기존 방어를 깎는다 | 14의 전역 AND는 변수 우회 방어의 **의도된 대가**였다 | P01: 기존 양성 픽스처 **전량 green 유지**가 절대 조건. 오탐 해소로 양성이 하나라도 죽으면 되돌린다 |
| 창 안에서는 라이브 발화 검증 불가 | `supervisor-guard.sh` 창 개방 중 전체 exit 0 | V3를 창 폐쇄 후로 고정 |
| `settings.SEALED.json`이 정본, `settings.json`은 파생물 | NC P05 사고(성향 21) | deny 일몰은 **SEALED만** 수정. settings.json 창 안 커밋 금지 |
| HR2 몫 4줄과 NC 몫 2줄의 일몰 타이머가 다르다 | pin 이월 ① | 이번엔 **HR2 몫(`00.Documents`·`98.Management`)만**. NC 몫은 BZ 다음 |
| P04 일괄 치환이 예시 문자열을 오폭 | NC P07 교훈 "규칙 정교화보다 범위 축소" | 보수 기준 + 애매 목록 이월. **치환 전 대상 목록을 커밋 메시지가 아니라 Phase 문서에 박는다** |
| 게이트 신설이 기존 테스트와 간섭 | globalSetup은 모든 vitest 실행에 얹힌다 | P02: 스냅샷 대상을 `~/.agentdeck-dev`로 한정(홈 루트 전체는 비결정론·오탐원) |
| 쓰기·조회 혼합 오탐(14)이 **수리 전 Phase 실행 자체**를 문다 | 3중 확증 2026-07-27 | P01이 첫 Phase인 이유. 수리 전까지는 쓰기·조회 분리 규율 유지 |

## 🔍 Codex 교차 플랜 체크 반영 (2026-07-27 밤 — 영호 지시 "플랜에 정합해서 수정")

job `task-ms3b59pf-4429ze` (Final output 14:26Z). 🔴 7 · 🟡 2 · ✅ 1 — **전부 계획 문서 수정으로 봉합, 설계 뒤집기급 분기 없음**:

| 축 | 지적 | 반영 위치 |
|---|---|---|
| 1 🔴 | P07 Codex 인계문이 실행 가능 명세가 아님 (실소유 좌표·기동 플래그·검증 명령·캐시버스터 부재) | P07 신규 등재 ⑤ 전면 확장 |
| 1 🟡 | `AGENTS.md`·`.codex/README.md`에 신 CHANGELOG 위치 부재 | P07 ⑤ 인계문 요소로 |
| 2 🔴 | P06 §C `:415` 예외 존치는 방향 반대 — 이동 후 포인터는 CORE-11 봉인 대상 | P06 §C 반전 (예외 제거 + 픽스처 반전) |
| 2 ✅ | P01 shell-policy 수리는 Codex 무결합 (계약 테스트 23/23 green) | 기록만 |
| 3 🔴 | ADR-040에 conformedVersion 수명주기 계약 부재 (승인 주체·receipt 전제·중간 상태·부트스트랩) | P05 A 확장 |
| 3 🔴 | "갱신 누락 = red"는 CORE 의미 버전 상승만 잡음 — 경로·구현 드리프트 미탐 | P05 A 한계 명시 + P07 ③ 후속 축 |
| 3 🟡 | red 검사가 `< v` 한 경우뿐 — 누락·비정수 통과 위험 + manifestVersion 판단 | P05 A·P06 B 정확 동등성 |
| 4 🔴 | P02 목표 과대 — 스냅샷 대조는 순 변화만 검출 (생성→삭제 미탐) | P02 목표 축소 + 한계 병기 |
| 4 🔴 | 줄 좌표 참조 6건이 포인터화로 사망 (`불변` 판정과 충돌) | P05 B 좌표 치환 열 신설 |
| 4 🔴 | BACKLOG 번호 allocator stale (다음 번호 = 25인데 25 실재) | P07 완료 조건 추가 |

## 📦 종결 산출물

- `BZ-DONE.md` + 5단계 보고 (필수 H2 4종 — ⚠️ P01이 25를 고치면 템플릿 표기가 곧 훅 표기)
- `.claude/CHANGELOG.md` [H] 행 (P06 창 안에서)
- `00_Documents/BACKLOG.md` 전수 갱신 (V4)
- work-pin 갱신 · HTML 보고서 없음 (영호 요청 시에만 — 현행 규율)

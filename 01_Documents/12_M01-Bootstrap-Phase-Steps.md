# M01 — Moodie_Agent_Workflow 부트스트랩 · Phase-Steps

- 마일스톤: M01 (쟁점 08 실물화) · 작성: 2026-08-01 · 상태: 검증 대기
- 이 문서는 v2 계획 템플릿이 요구하는 형식(결정 대장·검증 기록·도메인 태그·의존성)을 갖춘 첫 실사용 인스턴스다.
- 검증 통과 후 Phase 1에서 저장소의 `01_Milestones/M01_Bootstrap/00_Phase_Steps.md`로 이관하며, 이관 뒤에는 저장소 사본이 진실의 원천이다.

## 목표

카드 3·4·5·6·13이 설계한 게이트·템플릿·레이아웃을 `C:\Dev\Moodie_Agent_Workflow` 저장소에 골격 스코프로 실물화한다.
부트스트랩 자체를 v2 루프로 도그푸딩한다 — 이 문서와 결정 대장, FAIL 줄을 수기로 운용하고, 게이트 역할은 사람이 맡는다.

## 스코프

포함: 표준 레이아웃 인스턴스, 헌법 2종 골격, Phase-Steps 계획 템플릿, 게이트 3종(스폰·리셋·250줄), 재개 프로토콜, 세션 마감 요약 재부착, F3 정리, 03장 「현재」 열 갱신.

- **AgentDeck 저장소는 무변경이다 — 실제 구조를 예시 재료로 참조만 한다** (개편은 파일럿 09의 몫).
- **실측 항목(K 감독 폭 상한·드라이버 자동 리셋·Orca 세션 통신·교차 검수 체인)은 이 마일스톤에 없다 — 파일럿(쟁점 09)에 동반한다.**

노션 규범 문서의 변경은 인벤토리에 명시된 두 가지(F3, 03장 현재 열)만 수행하고, 반영 후 fetch 대조로 확인한다.

## 목표물 — 표준 레이아웃 (노션 01장 정본)

```
Moodie_Agent_Workflow/
├─ CLAUDE.md              # 헌법(CLAUDE)
├─ AGENTS.md              # 헌법(CODEX)
├─ 00_Documents/
│   ├─ 00_Architecture/  ├─ 01_PRD/  ├─ 02_Rules/  ├─ 03_Reports/  └─ 04_Design/
├─ 01_Milestones/
│   └─ M01_Bootstrap/00_Phase_Steps.md
├─ 02_Project/
│   ├─ 00_Source/  └─ 01_TestCode/
├─ .claude/  (agents/ skills/ hooks/)
├─ .codex/   (agents/ hooks/)
├─ .agents/  (Codex SKILL.md)
├─ 98_Management/  (_HarnessGate/  00_ADR/)
└─ 99_Others/
```

## 도메인 태그 어휘 (초안)

이 저장소의 Phase에 붙이는 도메인 태그는 다음 다섯으로 한다 (선정 근거는 결정 대장 [AI] 줄 참조).

| 태그 | 커버 범위 |
| --- | --- |
| `layout` | 폴더 구조, 헌법 2종(CLAUDE.md·AGENTS.md), 저장소 골격 |
| `template` | Phase-Steps 계획 템플릿과 그 필수 절 규격 |
| `hooks` | 게이트 훅, SessionStart·세션 마감 등 하네스 훅 실물 |
| `harness-agents` | SubAgent 정의(.claude/agents 등) — M01에서는 미사용, 파일럿(09)부터 사용 |
| `docs` | 규범 문서(노션 00~04장) 동기와 보고서 |

## 라우팅 판독 (분업 2 적용)

이 계획의 구조 신호는 태그 4종(layout·template·hooks·docs)에 일부 독립 의존이라, 표대로면 크로스 도메인 병렬 + 교차 검수 2차에 해당한다.
그러나 [USER] 도그푸딩 결정에 따라 실행은 메인 세션이 수기 직렬로 진행하고, 교차 검수 체인은 실측 항목이므로 검수는 사람 게이트 1차만 둔다.

## Phase 1 — 저장소·레이아웃·헌법 골격 · 태그: `layout` · 의존: 없음

**목표**: 저장소를 만들고 표준 레이아웃과 헌법 골격을 세운 뒤, 이 계획 문서를 저장소 정본으로 이관한다.

Steps

1. `C:\Dev\Moodie_Agent_Workflow`를 생성하고 로컬 `git init`을 실행한다. 원격(remote)은 등록하지 않는다.
2. 목표물 절의 트리대로 전체 폴더를 생성한다. 빈 폴더는 `.gitkeep`으로 보존한다.
3. CLAUDE.md(헌법 CLAUDE)·AGENTS.md(헌법 CODEX) 골격을 작성한다 — 정체 선언, 핵심 규칙 참조, 금지 사항의 최소 절만 담는다.
4. 이 문서를 `01_Milestones/M01_Bootstrap/00_Phase_Steps.md`로 이관한다. 이후 검증 기록과 결정 대장 추기는 저장소 사본에만 한다.
5. 첫 커밋을 만든다.

DoD

- [ ] 목표물 트리의 모든 폴더가 실제로 존재한다 (트리와 1:1 대조).
- [ ] CLAUDE.md·AGENTS.md가 루트에 존재하고 최소 절 구성을 갖춘다.
- [ ] `01_Milestones/M01_Bootstrap/00_Phase_Steps.md`가 존재하고, 이관 시점 기준으로 이 문서와 내용이 일치한다 (이후 추기는 저장소 사본에만 한다).
- [ ] `git log`에 커밋이 있고 `git remote -v` 출력이 빈다.

검증 기록

- (기록 없음)

## Phase 2 — 계획 템플릿 + F3 정리 · 태그: `template`, `docs` · 의존: 1

**목표**: 다음 마일스톤부터 쓸 Phase-Steps 계획 템플릿을 만들고, "1~8 Steps" 파생 값의 소유자를 지정한다.

Steps

1. `00_Documents/02_Rules/`에 계획 템플릿을 작성한다 — 필수 절은 개요, Phase 구획(목표·Steps·DoD·도메인 태그·의존성), 결정 대장(출처 태그 [USER]/[AI]/[AI·비가역]/[USER·위임]), 검증 기록(FAIL·PASS·USER-INPUT 스킴)의 네 가지다.
2. 템플릿의 Phase 상한(1~8)은 숫자 사본을 새로 만들지 않고 소유자 참조로 표기한다.
3. F3 정리 — 노션의 "1~8 Steps" 파생 값 4곳을 소유자 1곳 + 언급으로 정리한다. 소유자 위치 확정은 이 Phase에서 내리고 결정 대장에 추기한다. 반영 후 fetch 대조로 확인한다.
4. 커밋.

DoD

- [ ] 템플릿 파일이 필수 절 네 가지를 모두 포함한다.
- [ ] 템플릿에 "1~8"의 새 숫자 사본이 없다 — 소유자 참조만 있다.
- [ ] F3: 파생 값 4곳이 소유자 1곳 + 언급으로 정리됐고, fetch 대조로 확인됐다.
- [ ] 커밋이 존재한다.

검증 기록

- (기록 없음)

## Phase 3 — 게이트 3종 · 태그: `hooks` · 의존: 2

**목표**: 스폰 게이트, 리셋 게이트, 250줄 상한 훅을 실물로 만들고 각각의 발화를 실측한다.

Steps

1. 구현 SubAgent 스폰 게이트 — 스폰 전 판정 네 가지를 집행한다: ① 계획 문서 검증 통과 ② 마지막 USER-INPUT 줄 이후 FAIL < 3 ③ 결정 대장 존재 + 발동 판정([AI·비가역] ≥ 1 즉시, [AI] 수 > [USER] 수면 문답 분기, 대장 부재 = 반려) ④ 라우팅 표 적용.
2. 리셋 게이트 훅(대화형) — 검증 통과 후 리셋 전까지 구현 도구(Edit·Write 급)를 차단한다.
3. 문서 250줄 상한 훅(PostToolUse) — 산출 문서가 250줄을 넘으면 발화한다. ⚠️ 목표 H로 실측된 구멍이므로 프로브를 생략하지 않는다.
4. 세 훅 모두 판정 근거를 훅 로그 파일로 남긴다 — 병렬 발화 시 화면 출력은 유실되므로 판정은 로그로만 한다.
5. 커밋.

DoD

- [ ] 스폰 게이트: 통과 1케이스 + 차단 3케이스(검증 미통과 / FAIL ≥ 3 / 대장 부재)가 각각 **실제 발화 프로브**로 확인되고 로그에 남았다.
- [ ] 스폰 게이트: [AI] 과반 계획 입력에서 문답 분기 발화가 **실제 발화 프로브** 1케이스로 확인됐다.
- [ ] 스폰 게이트: 라우팅 판독 근거가 훅 로그에 1케이스 남았다.
- [ ] 리셋 게이트: 검증 통과 직후의 구현 도구 차단과 리셋 후 해제가 **실제 발화 프로브**로 확인됐다.
- [ ] 250줄 훅: 251줄 문서 산출 시도에서 발화가 **실제 발화 프로브**로 확인됐다.
- [ ] 세 훅 모두 판정 근거가 훅 로그 파일에 있다.
- [ ] 커밋이 존재한다.

검증 기록

- (기록 없음)

## Phase 4 — 재개 프로토콜 + 세션 마감 요약 · 태그: `hooks` · 의존: 1

**목표**: Clear 이후의 재개를 구조로 보장하고, 세션 마감 요약 3줄 재부착을 실물화한다.

Steps

1. work-pin 표면(작업 좌표 파일)을 도입한다 — 생존 문서 4종(Phase-Steps·work-pin·결정 대장·검증 기록)의 마지막 조각. 거처는 이 Phase에서 확정하고 결정 대장에 추기한다.
2. SessionStart 재개 프로토콜 — 세션 기동 시 생존 문서 4종의 로드를 확인한다. 재개는 문서로만 하며 `--resume` 금지를 헌법에 명문화한다.
3. 세션 마감 요약 3줄 재부착을 구현한다 — Stop 훅을 우선 후보로 하되, 실물 형태는 이 Phase의 실측으로 확정하고 결정 대장에 추기한다.
4. 커밋.

DoD

- [ ] 새 세션 기동에서 SessionStart 훅 발화가 **실제 발화 프로브**로 확인되고, 생존 문서 4종의 로드가 관측된다.
- [ ] `--resume` 금지가 헌법에 명문으로 존재한다.
- [ ] 세션 마감에서 요약 3줄 재부착이 1회 이상 실측으로 관측됐다.
- [ ] 커밋이 존재한다.

검증 기록

- (기록 없음)

## Phase 5 — 발화 프로브 회귀 + 규범 동기 · 태그: `hooks`, `docs` · 의존: 3, 4

**목표**: 훅 전수를 최종 상태에서 재검증하고, 실물이 생긴 만큼만 규범 문서를 동기한다.

Steps

1. Phase 3·4에서 만든 게이트·훅 전수를 최종 경로·이름 상태에서 한 번 더 발화 프로브로 확인한다 — 훅은 rename에 조용히 죽는 실측 이력이 있다.
2. 03장 「현재」 열을 갱신한다 — 실물이 생긴 행만 (검증 4 → G+S, 순서 4·5, 분업 1·2, 산출물 3 등). 관측 원칙을 지키고, 반영 후 fetch 대조로 확인한다.
3. M01 마감 커밋을 만든다. 원격 push는 하지 않는다 — push는 사용자 승인 게이트다.

DoD

- [ ] 게이트·훅 전수의 회귀 발화 프로브 기록이 로그에 있다.
- [ ] 03장 갱신이 fetch 대조로 확인됐고, 실물이 없는 행은 건드리지 않았다.
- [ ] `git log`에 Phase별 커밋이 있고 원격 push 이력이 없다.

검증 기록

- (기록 없음)

## 결정 대장

- [USER] 진행 방식: 부트스트랩 자체를 v2 루프로 도그푸딩 — Phase-Steps 문서·결정 대장·FAIL 줄 수기 운용, 게이트 역할은 사람 (2026-08-01)
- [USER] 저장소: `C:\Dev\Moodie_Agent_Workflow` 신규 생성 + 01장 표준 레이아웃 루트 이름도 동기 (2026-08-01, 노션 반영 완료)
- [USER] 스코프: 골격(레이아웃·템플릿·게이트 3종·재개 프로토콜·마감 요약·F3) + AgentDeck 실재료 예시본 — AgentDeck의 실제 구조를 예시 재료로 참조하되 AgentDeck 저장소는 무변경 (개편은 파일럿 09의 몫). 실측 항목(K·드라이버·Orca 통신·교차 검수)은 파일럿 동반 (2026-08-01)
- [USER] git: 로컬 `git init`과 커밋까지만 포함하고 원격 push는 제외 — push는 사용자 승인 게이트 (위임 브리프 명시, 2026-08-01)
- [USER] Phase 분해: 스케치 5단계의 골간을 유지하되, 계획 문서 이관을 Phase 1에 편입하고, 훅 발화 프로브를 각 훅 Phase의 DoD로 내재화하며, Phase 5를 전수 회귀 + 규범 동기로 재정의했다. (AI 제안 → 문답 승인 2026-08-01)
- [USER] 도메인 태그 어휘: `layout`·`template`·`hooks`·`harness-agents`·`docs` 5종 — harness-agents는 M01 미사용으로 파일럿에 예약. (AI 제안 → 문답 승인 2026-08-01)
- [USER] 소스 코드 태그(02_Project 커버)는 파일럿(쟁점 09)에서 추가 예약 — M01은 소스 작업이 없어 미정의. (AI 제안 → 문답 승인 2026-08-01)
- [USER] 게이트 구현 순서: 템플릿(Phase 2)을 게이트(Phase 3)보다 앞에 둔다 — 스폰 게이트의 판정 입력(결정 대장·검증 기록·태그)이 템플릿 규격이기 때문. (AI 제안 → 문답 승인 2026-08-01)
- [USER] 실물 거처: 계획 템플릿은 `00_Documents/02_Rules/`(규칙·규격 원본의 구역), 훅은 `.claude/hooks/`(Codex 측은 `.codex/hooks/`). (AI 제안 `98_Management/_HarnessGate/`는 01장 구역 역할(OPEN/CLOSE 툴 자리)과 충돌해 문답에서 재결정 2026-08-01)
- [USER] Phase 3과 4는 의존상 독립으로 표기하되, 도그푸딩 결정에 따라 실행은 수기 직렬로 한다 — 병렬화는 파일럿부터. (AI 제안 → 문답 승인 2026-08-01)

발동 판정 실측: [AI·비가역] 0 · [AI] 6 · [USER] 4 → [AI] 수 > [USER] 수로 주도권 게이트 **발동**.
재판정 (2026-08-01, 선택지형 문답 후): 여섯 줄 전부 [USER] 전환(거처는 재결정 병기) → [AI·비가역] 0 · [AI] 0 · [USER] 10, **발동 해제 — 실행 자격 획득** (기록은 지우지 않고 태그만 전환).

## 검증 기록 운용

검증 결과는 각 Phase 절 말미 「검증 기록」의 줄로만 유효하다 (fail-closed).
줄 스킴은 `FAIL`·`PASS`·`USER-INPUT` 세 마커이며, 타임스탬프와 한 문장(무엇이 왜 — FAIL이면 시도까지)을 적는다.
스폰 게이트는 마지막 USER-INPUT 줄 이후의 FAIL만 센다 — 3 이상이면 전진이 막히고 에스컬레이션으로 분기한다.
기록은 append-only다 — 리셋은 삭제가 아니라 기준점 이동이다.

<!-- 형식 예: - FAIL 2026-08-02T10:00 — DoD 3 불통과: 리셋 게이트 무발화. 시도: 매처 글롭 수정. -->

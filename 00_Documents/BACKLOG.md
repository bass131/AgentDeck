# BACKLOG — 미해소 안건 대장

> **여기가 정본이다.** 새 안건은 이 문서에 추가하고, 마일스톤 `-DONE.md` 안에 백로그 목록을 새로 만들지 않는다.
>
> **번호는 연번이며 재배열하지 않는다** — 저장소 곳곳(`CORE.md` · ADR · Phase 문서 · e2e 주석)이 *"백로그 15"* 처럼 **번호로만** 참조하기 때문이다. 번호를 다시 매기면 그 참조가 전부 조용히 어긋난다.

## ⚠️ "백로그 N"만으로는 가리키는 대상이 정해지지 않는다

이 문서를 만들면서 실측으로 드러났다. **번호 공간이 하나가 아니다** — HR2 계보(1~19, 여기)와 별개로 **HR2 이전 마일스톤들이 각자 지역 번호를 썼다**. 실제 충돌:

| 표기 | 어느 것인가 |
|---|---|
| `CORE.md:43` 의 **"A 스프린트 백로그 6"** | HR2 계보 6번 (CORE-06 게이트 구멍) |
| `01_Phases/07_BF3-backlog-sweep/05-*.md` 의 **"백로그 6"** | BF3 지역 6번 (멀티패널 스냅샷 상속) |

`99_Others/tests/e2e/**` 주석의 *"백로그 2"* 14건도 HR2의 2번이 아니라 그 시절 지역 번호다.

⭐ **그래서 앞으로는 문서 이름을 붙여 부른다** — `BACKLOG.md 15`. 번호만 쓰면 미래 세션이 엉뚱한 항목을 읽고도 **아무 신호를 받지 못한다.** 과거 문서의 지역 번호 표기는 **그 시점의 기록이라 고치지 않는다** — 고치면 무엇이 언제 어떻게 불렸는지가 사라진다.

## 왜 여기로 옮겼나 (2026-07-26 NC 창 2 신설)

백로그는 원래 `01_Phases/21_HR2-opus5-renewal/HR2-DONE.md` 「남긴 백로그」 절에 살았다. 그런데 **`-DONE.md` 는 끝난 마일스톤의 종결 기록 = 닫혀야 하는 문서**인 반면 **백로그는 계속 자라는 목록 = 열린 문서**다. 둘을 한 파일에 두면 다음 마일스톤이 안건을 추가할 때마다 **이미 끝난 마일스톤의 종결 문서를 다시 연다.**

증상이 실제로 나타났다:

- **유령 포인터** — `ADR-039` 가 *"→ 백로그 17"* 로 가리켰는데 목록에는 16번까지밖에 없었다. 가리키는 쪽만 쓰고 가리켜지는 쪽을 안 만든 것이다(2026-07-26 NC 창 2에서 발견·해소).
- **발견 가능성** — *"백로그가 어디 있나"* 의 답이 「끝난 마일스톤 폴더 안의 종결 문서」라서, 그 경로를 이미 아는 사람만 찾을 수 있었다.
- **좌표 참조의 노후** — 두 곳이 `HR2-DONE.md:157`(백로그 15)처럼 **줄 번호까지** 박아뒀는데, 그 문서를 편집할 때마다 낡는다. 이관하면서 **문서 + 항목번호**로만 가리키도록 바꿨다.

## 어느 문을 요구하는가

| 갈래 | 항목 | 이유 |
|---|---|---|
| **유지보수 창**(영호가 OpenGate 개방) | 14 · 15 · 16 | 하네스·ADR·`settings.json` 을 건드린다 |
| **Codex 세션**(영호가 기동) | (현재 없음 — 17·19·22 는 2026-07-27 세션 B 해소) | `.codex/**` 또는 Codex 런타임 소유 — **CORE-12 로 Claude 는 읽지도 고치지도 못한다.** 창을 열어도 해소되지 않는 부류다 |
| **둘 다 아님** — 일반 작업 | **21** · **23** · **24** | 테스트 인프라(`vitest.config.ts`·`99_Others/tests/**`)와 앱 코드 주석. 봉인 밖이라 평시에 진행 가능 |

> 📌 **18 번은 목록에서 빠졌다** — 2026-07-27 오탐으로 확정돼 19 번에 흡수됐다. 항목 자체는 *"판정기 결함이 유령 안건을 만든다"* 는 기록으로 남긴다.
>
> 📌 **17·19·22 는 2026-07-27 Codex 세션 B 로 해소됐다**(커밋 `224ecb0`, receipt 원문 = `.codex/NC-codex-handoff-2-B-DONE.md` — **CORE-12 로 Claude 는 원문을 읽지 못하며**, 여기 해소 서술은 영호가 전달한 receipt 기준이다). **20 은 같은 날 NC 창 3(예비 창)으로 해소됐다.** 항목은 기록으로 존치한다.

---

## 미해소 (6건 — 14·15·16·21·23·24)

14. **`supervisor-guard`가 읽기 전용 명령을 봉인으로 차단한 사례** (2026-07-26 추가 — 창 종결 직후 실측) — 재봉인 확인 중 `cmp` · `grep -c` · `wc -l <` · `git config` · `git check-attr` 로만 이뤄진 **쓰기 0인 명령**이 *"하네스에 대한 shell 우회 쓰기 — 봉인 중"* 으로 exit 2를 받았다(훅 메시지 자신이 *"읽기·git add/commit은 허용"* 이라 밝히는데도). ⭐ **개별 요소를 하나씩 프로브하면 5종 전부 통과**했고, 마지막 두 줄(`git config core.autocrlf` · `git check-attr … <하네스경로 2개>`)을 뺀 나머지 조합도 통과했다 — 즉 **단일 토큰이 아니라 조합에서만 발화**하며 재현 조건이 아직 좁혀지지 않았다. 유력 후보는 `git config`(키만 주면 읽기지만 값을 주면 쓰기라 서브커맨드 단위로는 구분이 어렵다)와 미등록 git 서브커맨드의 fail-closed 처리다. 📌 **우선순위는 낮다** — 방향이 *안전한 쪽*(읽기를 막음)이고 우회 없이 명령을 쪼개면 진행되므로 게이트 무력화가 아니라 **작업 마찰**이다. 다만 훅이 *스스로 허용한다고 말한 것*을 막고 있으므로, 메시지가 거짓이 되지 않으려면 언젠가 좁혀야 한다. ▪ ⭐ **2026-07-26 NC P07 — 재현 조건이 한 칸 좁혀졌다.** `git mv` 10건과 `grep --exclude-dir=.codex …` 를 **한 Bash 호출에 섞자** *"하네스 또는 다른 엔진 runtime에 대한 shell 우회 쓰기 — 봉인 중"* 으로 차단됐다. 격리 프로브 3회로 범인을 배제했다: ⓐ `.codex` 문자열 단독 → **통과** ⓑ `--exclude-dir=.codex` 단독 → **통과** ⓒ `git mv` 10건에서 grep 만 제거 → **통과**. ⇒ 단일 토큰이 아니라 **쓰기 명령과 조회 명령이 한 호출에 공존할 때** 발화한다(경로 후보 추출이 세그먼트를 넘어 섞이는 것으로 추정). 실용 규칙: **쓰기와 조회를 같은 Bash 호출에 섞지 않는다.** ⚠️ 이 오탐은 *"`.codex` 를 grep 제외 인자에 넣어라"*(CORE-12 준수 지침)와 **정면으로 부딪힌다** — 방어적으로 제외 인자를 붙이면 오히려 차단당하는 구간이 생긴다.

15. ⚠️ **conformance 게이트에 「어댑터별 준수」 축이 없다** (2026-07-26 추가 — 백로그 10 해소 직후 발견) — `conformance-check.mjs`가 대조하는 것은 조항 `v`와 `CORE.md` 헤더, 그리고 `impl` 경로의 **파일 실재**뿐이고, **각 어댑터가 그 조항을 실제로 지키는지는 보지 않는다.** 백로그 10이 바로 이 맹점 위에서 자랐다 — Codex가 execpolicy `prompt`(= CORE-06 **v1** semantics)에 머물러 있는 동안에도 게이트는 **13/13 green**이었고, 조항이 `v2`라는 사실만 보고 "두 어댑터 다 v2"로 읽혔다. ⭐ **고약한 점은 10번을 고치면서 이 맹점의 *기록*까지 함께 지워졌다는 것이다.** 매니페스트 note가 *"본 게이트는 조항 v와 CORE.md 헤더만 대조하므로 이 gap을 스스로 잡지 못한다"* 라는 **자기 결함 고백**을 달고 있었는데, gap이 해소되자 그 문장이 note 교체와 함께 사라졌다. **gap은 사라졌지만 맹점은 그대로다** — 다음에 어느 어댑터가 뒤처져도 게이트는 다시 조용히 green이고, 이번엔 경고해 줄 문장조차 없다. 📌 **설계 분기가 있어 창 + 판단이 필요하다**: 어댑터 준수를 기계로 보려면 각 어댑터의 `verify` ref를 *실행*해야 하는데, Claude는 `.codex/**`를 실행할 수 없다(CORE-12) — 즉 **게이트 자체가 크로스엔진 실행을 요구**한다. 대안은 ① 어댑터별 `conformedAt`·`conformedVersion` 선언을 매니페스트에 두고 조항 `v`와 대조(선언 기반 — 값이 낡으면 red) ② 각 엔진이 자기 doctor에서 자기 축만 검증하고 결과를 파일로 남기면 conformance가 그 파일의 신선도를 본다(교차 실행 없음). ⭐ **해결 전까지는 이 항목 자체가 유일한 기록**이므로 지우지 말 것. ▪ **2026-07-26 NC 창 2 — Codex 교차 감사가 같은 맹점을 독립 재발견하고 2단계 설계를 제안했다**(Finding 2, 🔴). 위 대안 ①②와 같은 방향이되 더 구체적이다. **1단계(저비용)**: 조항별 `claude.conformedVersion`·`codex.conformedVersion`을 CORE 버전과 대조 — 갱신 누락이 red가 되지만 **자기선언이라는 한계**는 남는다. **2단계(정본)**: 각 엔진 doctor가 *자기* `impl`과 실행 가능한 `verify`만 검사하고 **엔진 중립 receipt**(`clauseVersions` · CORE/manifest hash · 어댑터 입력 digest · doctor digest · PASS · `verifiedAt`)를 남긴다. 중앙 conformance는 **실행 없이** 그 digest·결과·신선도 TTL만 본다. ⚠️ **중립 receipt 공유를 CORE-12가 허용하려면 「runtime이 아닌 검증 증거」 예외를 ADR로 먼저 명문화**해야 한다 — 그 ADR이 선행 조건이다. ⭐ 이 재발견 자체가 개발-성향 19의 사례다(새 축을 얹으면 기존 토대의 결함이 드러난다 — NC 개명 대칭 작업이 conformance 맹점을 다시 비췄다).

16. ⚠️ **`.claude/CHANGELOG.md`의 「어댑터 소유권 ↔ 저장소 전체 이력」 충돌** (2026-07-26 추가 — Codex의 커밋 직전 질문에서 발견) — Codex가 자기 변경을 커밋하기 전에 *"이 변경 이력을 `.claude/CHANGELOG.md`에 기록해도 되는가"* 로 멈춰 서면서 드러났다. ⭐ **조문상으로는 아무것도 막고 있지 않다** — CORE-12의 격리 범위는 `hooks/**`·`state/**` 뿐이라 CHANGELOG는 격리 대상이 아니다. 그런데 **위치는 `.claude/` = Claude 어댑터 층**(ADR-034 3층)이고, 파일 스스로 선언한 역할은 *"헌법·ADR·하네스·공유계약 변경 이력"* = **저장소 전체 이력**이다. 한 파일이 두 정체성을 동시에 갖고 있어서, "Codex도 저장소를 바꾸는데 그 이력은 어디에 남는가"에 답이 없다. ▪ **이번 건의 처리 = 영호 결정 (B) 「메인이 쓴다」** — 잡무 기준 v1이 이미 *"CHANGELOG 문구 = 판단이 살아 있는 산출물 = 메인 직접"* 으로 정했고 그 기준은 엔진을 넘어 적용되므로, Codex는 자기 12파일만 커밋(`eb31aa5`)하고 이력 행은 Claude 세션이 썼다. ⚠️ **다만 이건 운영으로 비껴간 것이지 구조를 푼 게 아니다** — 다음에 Codex가 단독으로 무언가를 바꾸면 같은 질문이 그대로 다시 온다. ▪ **근본안 = CHANGELOG를 `.claude/` 밖(`00_Documents/`)으로 옮기고 `.claude/`에는 포인터만 남긴다.** trade-off: 옮기면 **엔진 중립 위치**를 얻는 대신, `CLAUDE.md`·`.claude/policies/**`·`session:start` 커맨드 등 **경로를 참조하는 곳 전부에 스윕**이 따라온다(백로그 5와 같은 종류의 작업이고, 그때 실측이 "3곳" 추정을 크게 벗어났다는 전례가 있으므로 착수 전 전수 grep 필수). ADR + 유지보수 창을 요구한다. ⭐ **본 `BACKLOG.md` 자체가 같은 종류 이동의 선례다**(2026-07-26) — 이관 자체보다 **역참조 스윕이 본체**라는 점이 실측으로 확인됐다.

17. ⚠️ **Codex 문서층 방어가 full-access 유지보수 세션에서는 0층** (2026-07-26 NC 창 2에서 **정식 등재** — 실은 `ADR-039` 가 이미 *"→ 백로그 17"* 로 가리키고 있었는데 **목록에 항목이 없어 유령 포인터였다.** 가리키는 쪽만 쓰고 가리켜지는 쪽을 안 만든 경우다) — 평시에는 `.codex/config.toml`의 기본 프로필 권한이 문서 쓰기를 막으므로 Codex 훅에 문서층 봉인을 **추가하지 않기로** 결정했다(영호, 2026-07-26 — *"같은 문을 두 번 잠그지 않는다"*). ⚠️ **단 full-access 유지보수 세션에서는 그 프로필 방어가 없어 문서층이 0층**이 된다. NC 개명이 만든 구멍이 아니라 **선재 구조**다. ⭐ **백로그 18과 함께 봐야 한다** — 18(프로필 초기화 실패)이 사실이면 그 프로필 방어는 **평시에도** 없는 셈이고, 그때 이 항목의 *"평시에는 막힌다"* 는 전제가 무너진다. ▪ ✅ **2026-07-27 해소 — 백로그 22 의 A안 실행으로 구멍이 닫혔다**(Codex 세션 B, 커밋 `224ecb0`). 전제 쪽은 18 의 오탐 확정으로 *"평시에는 프로필이 막는다"* 가 다시 참이 됐고, 남아 있던 유지보수 세션 구간은 **full-access 세션 안에서의 양성 프로브가 `00_Documents/00_Harness/CORE.md` 쓰기를 훅 EXIT=2 로 차단**하는 것이 실측돼 — sandbox 가 없는 구간에도 이제 훅 1겹이 확인됐다.

18. 🔴 **codex 프로필 3종이 초기화에 실패한다 — 방어 1층 손실** (2026-07-26 NC 창 2 직전, `agentdeck-assistant` 세션 실측) — `agentdeck-assistant`·`agentdeck-rescue`·`agentdeck-readonly` 전부 `default_permissions requires a [permissions] table` 로 sandbox 초기화가 실패한다(`--include-managed-config`를 붙여도 재현). codex-cli **0.145.0**의 config 스키마 변경으로 보인다. ⭐ **훅(2층)은 살아 있다** — 같은 세션에서 시크릿 경로 참조가 실제 PreToolUse로 차단됐고 봉인 밖 경로는 통과했다(양성·음성 확인). 즉 다층 방어에서 **권한 계층만 빠진 상태**다. ⚠️ **직전 Codex 세션 receipt와 판정이 모순된다** — 그때는 `profiles 3/3`·`WRITE-BOUNDARY 5/5`였는데 `agentdeck-assistant` 세션에서는 `0/3`·`3/5`다. 관측된 유일한 변수는 **세션 프로필**이며, 어느 쪽이 참인지 Claude는 CORE-12로 판정할 수 없다 — **모순 상태 그대로 기록한다.** 수정 주체 = **Codex 세션**. ▪ 🔴→✅ **2026-07-27 Codex 세션 A/B 대조로 오탐 확정 — 이 안건은 애초에 존재하지 않았다.** 두 실제 세션이 기동 입력에 맞게 **서로 다른 경계**를 적용했다: 옵션 없이 연 세션 = `permission_profile type="managed"` + workspace read-only + tmpdir write(**`agentdeck-assistant` 정의와 정확히 일치**하고 초기화 오류 0) / `-c default_permissions=":danger-full-access"` 로 연 세션 = `type="disabled"` + `file_system unrestricted`. **같은 메커니즘이 두 입력에 각각 요청대로 반응했다** ⇒ root 권한 계층은 살아 있다. ⭐ **그럼 원 관측 `0/3` 은 무엇이었나** — `harness-doctor` 가 판정에 쓰는 **`codex sandbox -P <프로필>` 자식 실행 표면**이 현재 config 해석과 비호환이라 그 프록시만 EXIT=1 을 낸다(`-P ':danger-full-access'` 만 EXIT=0). 즉 **실제 세션 권한이 아니라 판정 도구가 고장난 것**이고, 그 결과를 root 실패로 일반화한 것이 오탐이었다. ⇒ **19번(판정기 결함)에 흡수한다.** ⭐⭐ **방법론으로 남는 것 둘**: ① **판정기 결함은 유령 안건을 만들어낸다** — 18은 19가 낳은 자식이었고, 그 사이 *"평시에도 방어 1층이 없다"* 는 잘못된 위협 모델이 백로그 17까지 오염시켰다. ② **단일 관측의 판정 불가는 관측을 정교하게 만들어서가 아니라 「입력을 바꿔 한 번 더 관측」해서 푼다** — B 세션 단독 관측(`disabled`)은 「요청대로」와 「고장」이 같은 신호라 판정 불가였는데, A 세션이 대조군이 되자 즉시 갈렸다. 📌 **지우지 않고 남기는 이유**: 이 항목을 지우면 19를 고칠 동기의 절반이 사라진다.

19. 🔴 **`harness-doctor --live`가 판정기로서 신뢰할 수 없다 — 두 겹** (2026-07-26 NC 창 2 직전 발견) — ⓐ **`LIVE: PENDING`은 판정이 아니라 무조건 출력되는 상수 문자열이다**(`.codex/harness-doctor.mjs`, Codex 자기 보고). 재신뢰 여부를 읽지 않으므로 **정보량이 0**인데, 같은 출력의 다른 줄(`STATIC: PASS`·`HOOK-GUARD: PASS`)과 동일한 `축: 값` 형식이라 판정처럼 보인다 — 실제로 Claude 세션이 이것을 *"재신뢰 대기 상태"* 라는 상태 정보로 **오독했다.** ⓑ **`OS-READ-BOUNDARY`가 실패 원인을 구분하지 못한다** — sandbox가 설정 오류로 `exit 1` 한 것을 *"읽기가 차단됨"* 으로 분류해 `REVALIDATION_REQUIRED`를 냈다. **「돌지 못함」과 「차단됨」이 같은 신호**로 들어오므로 red가 무엇을 뜻하는지 알 수 없다. 📌 **개발-성향 17·18 축의 실사례다** — 17이 *"결정론적 검사의 red는 입력에 대한 정보"* 라고 말하는데 ⓑ는 그 전제가 깨진 경우이고, ⓐ는 18이 경고한 *"판정할 수 없는 것에 판정기 모양을 붙인 것"* 그 자체다. 수정 주체 = **Codex 세션**(`.codex/**`). ▪ ⭐ **2026-07-27 Codex 세션 A 가 두 항목을 확정하고 수리 설계까지 회수했다.** **ⓐ 확정** — `.codex/harness-doctor.mjs:427` 이 `LIVE: PENDING` 을 **조건 없이** 출력한다. **ⓑ 확정 + 더 나쁜 형태** — `writeBoundaries()` 가 **같은 초기화 오류를** 허용 2건에서는 FAIL 로, 거부 3건에서는 *"nonzero + 파일 없음"* 에 해당한다며 PASS 로 세어 `3/5` 를 만든다. **한 원인이 성공과 실패 양쪽으로 해석되는 자기모순**이다 — ⓑ의 원 서술(「돌지 못함과 차단됨이 같은 신호」)보다 한 단계 나쁘다. **ⓒ 신규(18에서 흡수)** — 판정 프록시 자체가 고장: `codex sandbox -P <이름>` 이 config 해석과 비호환이라 프로필 3종 전부 EXIT=1. ▪ **수리 입력(Codex 회수 — B 세션이 그대로 쓰면 된다)**: 자식 결과를 `PROFILE_INIT_ERROR`·`POLICY_DENIED`·`COMMAND_ERROR`·`COMMAND_SUCCEEDED` 로 **먼저 분류** / deny 성공은 *"프로필 초기화 완료 **그리고** 식별 가능한 정책 차단"* 일 때만 인정 / 초기화 오류는 모든 축에서 `INDETERMINATE` 로 통일 / `LIVE: PENDING` 은 실제 관측을 구현하거나 `LIVE: N/A(사유)` 로 변경 / **초기화 실패와 정책 차단을 서로 다른 픽스처로 고정**. ▪ **같은 세션이 발견한 곁가지 2건**(B 세션 범위): `.codex` 계약 테스트 2건이 **이미 제거된 구 경로** `00_Documents/harness/CORE.md` 를 여전히 단언해 실패한다(세션 ①의 병행 수용이 테스트까지 닿지 않았다) / `.codex/README.md` 가 구 경로를 *"현재"* 로 서술한다. ▪ 🔴→✅ **2026-07-27 해소**(Codex 세션 B, 커밋 `224ecb0`) — receipt 기준: sandbox 자식 결과를 `PROFILE_INIT_ERROR`·`POLICY_DENIED`·`COMMAND_ERROR`·`COMMAND_SUCCEEDED` 4-상태로 선분류 / 초기화·명령 실패는 `INDETERMINATE` 통일 / **정책 차단만** deny 성공으로 인정 / `LIVE: PENDING` 상수 제거(미실행 시 `LIVE: N/A(사유)`) / **음성 대조 실측** — 초기화 오류 fixture 가 `WRITE-BOUNDARY: INDETERMINATE (0/5)` 를 내, 초기화 실패가 더는 차단 성공으로 계산되지 않음을 확인 / 계약·훅 테스트 41/41 · digest 8/8 일치. 곁가지 2건(구 CORE 경로 계약 단언 · README stale)도 같은 커밋에서 수정. ⚠️ **잔여 사람 단계** = 영호의 digest 재신뢰(trusted 새 세션) + 그 세션에서 양성 카나리아 1회.

20. ⚠️ **`.claude/` 봉인층 문서 8줄이 개명 전 stem 을 가리킨다 — ADR-039 ③축이 P03 에서 누락됐다** (2026-07-26 NC P07 에서 발견) — `agents/shared-ipc.md:16·17·33` · `agents/agent-backend.md:21` · `agents/main-process.md:31` · `policies/grade-and-risk.md:60·61` · `commands/refactor-sweep.md:97` 이 `ipc-contract.ts`·`agent-events.ts` 를 **구 이름으로** 참조한다. ADR-039 §5-3 ③축의 판정은 **「신·구 병기」**였는데 P03(창 1)이 훅 3종과 명명 가드만 처리하고 이 8줄을 빠뜨렸고, 창 2에서도 잡히지 않았다. ⭐ **기계 강제는 살아 있다** — `risk-detector.sh:30·34` 가 `agentEvents*`·`ipcContract*` 를 병행 수용하고 있고 **라이브 발화도 확인됐다**(신 이름에 `shared-contract` 깃발이 guard 로그에 기록). 즉 위험 깃발은 죽지 않았다. ⚠️ **그런데 이 8줄은 성격이 다르다** — 이건 *에이전트가 읽는 지시문*이다(예: *"`ipc-contract.ts` 에 채널명 상수 + 요청/응답 타입"*). 낡으면 SubAgent 가 **존재하지 않는 파일을 찾는다.** CLAUDE.md 가 경고한 *"해야 할 것은 기계 강제가 없고 문장 자체가 유일한 방어선"* 에 해당하는 부류다. 📌 **`.claude/**` 는 CORE-11 봉인이라 유지보수 창을 요구한다.** P07 에는 창이 없다 — 그리고 이 상황은 **Phase 07 문서가 미리 예고했다**: *"여기서 `.claude/` 를 고쳐야 할 상황이 오면 그것은 P03·P06 이 빠뜨렸다는 신호이고, 진행이 아니라 보고 대상이다."* 예고대로 보고로 처리했다. ▪ **같은 창에서 함께 처리할 것** = ADR-039 에 **「파생 테스트 stem 은 소스를 따라간다」 각주** 추가. §5-2 매니페스트가 *"파일 13건"* 으로 못박혀 있어 P07 에서 파생 테스트 10건을 개명할 때 **범위인지 아닌지 판단이 한 번 갈렸다**(Phase 07 문서의 학습 포인트가 *"테스트는 개명 대상이 아니라 소스를 따라가는 것"* 이라고 이미 답을 갖고 있어 해소). ADR 에 없으면 다음에 또 갈린다. ▪ ✅ **2026-07-27 해소 (NC 창 3 — 예비 창)** — ① 8줄을 **신 이름 우선 + 구 이름 괄호 병기**로 갱신(ADR-039 §5-3 ③축 판정 그대로). 실좌표는 줄 번호가 아니라 `.claude/**` 전수 grep 으로 재확정 — 8줄 전부 일치, 추가 누락 0 ② ADR-039 §5-2 에 「파생 테스트 stem 은 소스를 따라간다」 각주 + ③축 표를 완료 기록으로 갱신 ③ `harness-review.md:69·93` 은 이미 신 경로(실측 — 창 2 처리분, 백로그 20 에 안 실린 이유) ④ 병기의 구 이름 표기 제거 타이머 = **훅 병행 수용과 동일**(NC 머지 + 1마일스톤). ⚠️ 범위 밖 관측 = `hooks/convention-size-guard.sh:8` 주석의 `ipc-contract`(예시 성격) — 불변 유지, 병기 일몰 때 함께 정리 후보.

21. 🔴 **테스트가 저장소 밖에 쓰는지 검사하는 게이트가 없다 — 실제 피해가 났다** (2026-07-27 NC P07 커밋 전, Codex `agentdeck-review` 가 발견) — `99_Others/tests/main/engineVersions.test.ts` 가 **영호의 실제 사용자 설정** `C:\Users\<user>\.agentdeck-dev\engine-config.json` 을 `{"activeVersion": null}` 로 덮었다. mtime 이 그 직전 `npx vitest run` 실행 시각과 일치해 **실측으로 확인**됐다. ⭐⭐ **왜 아무도 몰랐나 — 풀 액세스 환경에서는 「쓰면 안 될 곳에 쓰는 것」이 보이지 않는다.** 우리 세션은 권한이 있어 조용히 성공했고 **테스트는 green** 이었다. Codex 의 읽기 전용 세션에서만 EPERM 으로 red 가 났다. 📌 **같은 사각지대가 방향만 바꿔 재발한 것이다** — `HR1-DONE.md` 「발견2(P1)」: *"`doctor --live` 가 canary 를 `C:\Dev` 에 직접 생성 → `:tmpdir` 만 쓰기인 세션에서 EPERM 크래시. **내 CI/Bash 는 풀 액세스라 못 덮었던 사각지대**"*. 그때는 Codex 도구가 저장소에 썼고 이번엔 Claude 테스트가 사용자 홈에 썼다. ⇒ ⭐ **제한 권한 세션은 「테스트 격리 결함」을 찾는 도구다.** ▪ **근인 2겹**(qa 실측 — 내 최초 진단은 틀렸다): ⓐ `vi.mock('node:fs', … {...actual})` 이 펼치는 네임스페이스에 **`default` 키(진짜 fs)** 가 들어 있는데 앱은 `import fs from 'node:fs'`(default import)를 쓴다 — **모킹은 적용됐지만 앱이 지나는 문이 아닌 옆문에만 걸렸다.** ⓑ `vi.mock` 은 어디에 쓰든 최상단으로 호이스팅되고 같은 모듈을 여러 번 모킹하면 **마지막 하나가 파일 전체를 지배**한다 — 그 파일의 `vi.mock('node:fs')` **7개가 전부 마지막 1개로 붕괴**해 있었다. ⇒ **테스트별 fs 시나리오는 처음부터 존재하지 않았다.** 7개가 green 이었지만 아무것도 검증하지 않았다. ▪ **P07 에서 처리한 것**: 해당 파일을 fs 모킹 폐기 + per-test 임시 userData 주입으로 전환(앱 소스 변경 0 — `overrideUserData` 매개변수가 이미 있었다), 3중 방어(명시 주입 + `electron.app.getPath` 모킹 + 홈 config mtime/sha 카나리아), **뮤테이션 음성 대조로 가드가 실제로 무는지 확인**(일부러 망가뜨리면 red 가 난다). 22개 → 33개. ▪ **미해소로 남는 것**: ① **게이트가 없다** — 다음에 누가 같은 패턴을 쓰면 또 조용히 통과한다. 후보: vitest `globalSetup` 에서 홈·`%TEMP%` 스냅샷을 뜨고 종료 시 대조(이번 카나리아를 전역화), 또는 CI 를 제한 권한으로 돌리기 ② `02_Source/main/engineVersions.ts` 의 `getUserDataPath()` 가 electron 미초기화 시 **조용히 `os.homedir()/.agentdeck-dev` 로 폴백**하는 설계가 이 사고의 증폭기였다 — 비-electron 컨텍스트에서는 폴백 대신 throw 가 맞는지 논의 필요(main-process 도메인) ③ `99_Others/tests/renderer/w6-selection-ask.test.tsx:24-30` 이 `react-dom` 에 **같은 `{...actual}` 패턴**을 쓴다(소비처가 named import 라 지금은 동작하지만 default 로 바뀌면 같은 방식으로 조용히 빗나간다 — 파일시스템이 아니라 파괴력은 없다) ④ `%TEMP%/ev-test-*` 잔해 **54개**(옛 테스트가 `fsp.mkdir` 로 만들고 정리하지 않은 것 — 신 버전은 `afterAll` 에서 정리한다). 파괴 작업이라 **영호 승인 후 이름 확정 삭제**.

22. ⚠️ **두 엔진의 봉인 집합이 비대칭이다 — Codex 훅은 `00_Documents/00_Harness` 를 막지 않는다** (2026-07-27 NC P07 Codex 세션 A 의 live canary 에서 드러남) — Codex 훅의 양성 canary 는 `.codex/config.toml` 에서 정상 발화했다(*"하네스에 대한 shell 우회 쓰기는 봉인되어 있습니다"*). 그런데 **신 문서 경로 `00_Documents\00_Harness\…` 쓰기는 훅 차단 없이 통과**했고 sandbox 의 AskGate 까지 갔다. Codex 자기 설명: *"현재 `00_Documents/00_Harness` 를 봉인 집합에 포함하지 않고 평시 sandbox 에 맡기는 설계"*. ⚠️ **Claude 쪽은 같은 경로를 훅 + `settings.SEALED.json` deny 두 겹으로 막는다** — 즉 **같은 의미 정본(CORE.md)이 엔진에 따라 방어 두께가 다르다.** 📌 **백로그 17과 한 몸이다** — 17이 *"full-access 유지보수 세션에서는 프로필 방어가 없어 문서층이 0층"* 이라고 적은 그 구멍의 **구체적 좌표**가 이것이다. 17의 근거였던 *"평시에는 프로필이 막는다"* 는 이제 **18의 오탐 해소로 다시 참**이 됐으므로(권한 계층은 살아 있다), 남은 위험은 **유지보수 세션 구간으로 한정**된다. ⚠️ **판정에 필요한 것**: *"같은 문을 두 번 잠그지 않는다"*(영호 2026-07-26)가 이 경로에도 적용되는지 — 적용된다면 현행이 설계대로이고, 아니라면 Codex 훅에 문서층을 추가해야 한다. **CORE-12 로 Claude 는 `.codex/**` 를 못 보므로 이 판단은 영호 + Codex 세션이 한다.** ▪ ⚖️ **영호 결정 (2026-07-27): A안 — `00_Documents/00_Harness` 를 Codex 훅 봉인 집합에 추가한다.** 평시 sandbox 와의 이중 잠금은 감수한다 — 근거는 NC 마일스톤에서 *"문장만 방어선인 곳"* 이 실측으로 두 번 뚫린 것(봉인 deny 소실 · 테스트의 사용자 홈 덮어쓰기, 둘 다 어떤 게이트도 red 를 안 냈다). trade-off 로 지불하는 것 = 봉인 목록 유지비 — **다음 개명 때 이 경로도 병행 수용 대상**이 된다(훅 글롭이 rename 에 조용히 죽는 것을 실측한 그 규율을 `.codex` 쪽 README 에도 남기기로 함). 실행 주체 = Codex 세션 B — **receipt 회수 전까지 미해소 유지.** ▪ ✅ **2026-07-27 실행 완료**(Codex 세션 B, 커밋 `224ecb0`) — 봉인 패턴 = `00_Documents/(?:\d{2}_)?Harness/**`. **번호 프리픽스 유무를 모두 수용해 개명 내성이 패턴 자체에 들어갔다**(글롭이 rename 에 조용히 죽는 함정을 설계로 회피). 실측: 양성(`00_Harness/CORE.md` 쓰기) **EXIT=2 훅 차단** · 음성(`BACKLOG.md` 쓰기) **EXIT=0 통과** — 과봉인 없음. 구 경로 문자열은 개명 병행 수용을 보장하는 훅 테스트에만 의도적으로 존치.

23. ⚠️ **`src/` 옛 디렉토리 포인터가 코드 주석에 144건 남아 있다** (2026-07-27 NC P07 봉합 중 발견) — `02_Source/**`·`99_Others/**` 의 주석·JSDoc 에 `src/main/`·`src/shared/`·`src/renderer/`·`src/preload/` 형태가 **144건**. RF1 시절 `src/` → `02_Source/` 개명의 잔여이며 **NC 마일스톤 범위 밖**이다(NC 는 `00_Documents/` 폴더 개명 + `02_Source` 파일 stem 개명만 다뤘다). ⭐ **한 줄에 신·구가 공존하는 형태가 실재한다** — `02_Source/main/engineVersions.ts:22` 의 `구현 위치: src/main/engineVersions.ts` 는 **stem 은 신 이름(P07 이 고쳤다)인데 디렉토리 프리픽스는 구 이름**이다. 치환 축이 다르면 같은 줄에서 서로 다른 세대가 만난다. ⚠️ **성격은 백로그 20과 같다** — 런타임 영향 0(전부 주석)이지만 *에이전트가 읽는 지시문*이라 낡으면 없는 경로를 찾는다. 📌 **착수 전 판별 기준을 먼저 정할 것**: `diff.ts:164` 의 `@param relPath … (예: 'src/main/foo.ts')` 처럼 **예시 문자열**인 것과 실제 위치를 지시하는 것을 갈라야 한다. 144건을 일괄 치환하면 전자를 오폭한다 — NC P07 에서 *"규칙을 정교하게 만들기보다 범위를 좁히는 편이 싸다"* 로 배운 그대로다.

24. ⚠️ **개명 stem 의 「교차참조」 잔여 — 자기참조는 닫혔지만 남의 이름을 부르는 곳이 남았다** (2026-07-27 NC P07 2차 reviewer 가 발견) — P07 이 확정한 판별 원칙은 *"**개명된 파일 자신**이 옛 stem 을 쓰면 고친다"* 였고 그 스코프 안에서는 일관되게 닫혔다(전수 grep 확인 — 잔여는 선언된 예외 2곳뿐). ⚠️ **그런데 같은 「안 바뀌어서 틀려진」 부류가 비개명 파일의 교차참조로 남았다** — `02_Source` 약 27곳 + `99_Others` 테스트 주석 다수. 대표 좌표: `main/01_agents/queryFn.ts:54,61` · `ClaudeCodeBackend.ts:224,230` · `AgentBackend.ts:36,41,46` · `claudeAgentRun.ts:64,236,702,1740` · `main/02_fs/git.ts:368,411` · `shared/ipc/agent.ts:76,82,87,146` · `shared/ipc/engine.ts:125,230` · `renderer/src/lib/gaugeCalc.ts:5`. **전부 주석·런타임 영향 0** 이라 P07 커밋을 막지 않는다. 📌 **착수 전 판별 기준을 먼저 정할 것** — 이 목록에는 확장자·슬래시가 붙은 **명시 경로**와 `run-args 가 처리` 류의 **개념 호칭**이 섞여 있고, 후자를 치환하면 오폭이다(P07 에서 *"규칙을 정교하게 만들기보다 범위를 좁히는 편이 싸다"* 로 배운 그대로). ▪ **함께 처리할 qa 후속 3건**(같은 reviewer 회수, 전부 P07 커밋 비차단): ① `99_Others/tests/main/engineVersions.test.ts:370-379` 가 `process.env.ANTHROPIC_API_KEY` 를 더미로 덮은 뒤 **무조건 `delete`** 한다 — 실행 전 실제 값이 있었으면 **같은 워커를 공유하는 후속 테스트 파일에서 그 값이 사라진다**. 저장 후 복원 패턴으로(더미 값 자체는 가짜 리터럴이라 유출 위험 0) ② 같은 파일 `:107` 의 카나리아가 `engine-config.json` **단일 파일만** 본다 — electron 모킹이 죽고 *동시에* `installVersion` 주입이 빠지는 이중 실패 시 `~/.agentdeck-dev/engines/**` 쓰기는 미탐지. `~/.agentdeck-dev` 디렉토리 목록 스냅샷 한 줄로 값싸게 닫힌다 ③ 같은 파일 `:449,464,499` 의 `h.appDir.value` 변이가 미복원이라 테스트 간 순서 결합이 있다(현재는 무해) — `afterEach` 기본값 복원.

---

## 해소된 것 (1~13)

**HR2 마일스톤과 그 A 스프린트가 13건 전부 해소**했다(2026-07-25~26). 그중 4·7·10·13은 CORE-12 격리로 Claude가 손댈 수 없어 영호의 Codex 세션이 인수인계 브리프로 처리했다(커밋 `eb31aa5`).

📌 **해소 서술은 `01_Phases/21_HR2-opus5-renewal/HR2-DONE.md` 「남긴 백로그」 절이 소유한다** — 어떻게 해소했는가는 HR2의 성과 기록이므로 여기 복제하지 않는다. 이 문서는 **미해소 대장**이다.

---

## 새 안건을 추가할 때

1. **다음 연번**을 쓴다(현재 다음 번호 = **25**). 기존 번호는 재배열하지 않는다.
2. 「어느 문을 요구하는가」 표에도 갈래를 등록한다 — 유지보수 창 / Codex 세션 / 둘 다 아님.
3. 발견 경위와 **날짜**를 항목 안에 남긴다. 근거 없는 안건은 다음 세션이 판단할 수 없다.
4. ⚠️ **다른 문서에서 이 항목을 가리킬 때 줄 번호를 쓰지 말 것.** `BACKLOG.md` + 항목번호로만 가리킨다 — 줄 번호는 이 문서를 편집할 때마다 낡고, 낡아도 아무 신호가 나지 않는다.

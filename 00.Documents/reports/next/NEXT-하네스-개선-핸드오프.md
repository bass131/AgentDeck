# 하네스 개선 핸드오프 — 2026-07-17 점검 세션 산출

> 정본 상세 = [`00.Documents/reviews/2026-07-17-harness-review-all.md`](../../reviews/2026-07-17-harness-review-all.md) (`/harness-review scope=all`)
> 이전 점검 = 2026-07-11 ([`00.Documents/reviews/2026-07-11-harness-review-all.md`](../../reviews/2026-07-11-harness-review-all.md))
> 성격: 차기 하네스 개선 사이클 인수인계 — 발견 즉시 처리가 아니라 큐로 박제, 영호 결정 대기

## 현재 좌표 (착수 전제)

- TG1(사고 GUI 데스크톱 스타일)은 9/9 Phase 완주 상태다. 잔여는 두 가지 — 영호 육안 검수 22컷과 push·PR 게이트(CORE-06, 사람 GO 대기)뿐이다.
- 따라서 하네스 개선 착수는 **TG1 마감 이후**가 자연스러운 순서다. TG1 종결 전에 하네스를 건드리면 진행 중 마일스톤과 뒤엉킨다.
- 하네스 정정은 전건이 CORE-11 적용 대상이다 — **영호 단독 통제**. 어떤 항목도 에이전트에 위임하지 않고, **유지보수 창을 열어 → 수정 → 재봉인 → `.claude/CHANGELOG.md`에 기록**하는 절차를 반드시 거친다. 이 문서는 그 창에서 무엇을 처리할지의 후보 목록일 뿐이다.

## 점검 결과 한 줄 요약

- 판정 분포: 🔴 1건 / 🟡 17건 / 🟢 다수.
- 라이브 발화 배터리로 훅 9종 전원 생존을 확인했다. "무늬만 Hook(등록만 되어 있고 실제로는 발화하지 않음)" 의혹은 기각됐다 — 능동 프로브 7발이 전탄 차단됐고, 원장(CHANGELOG·상태 파일) 역사에서 훅 9종의 발화 흔적을 모두 확인했다.

## 우선순위 큐 (영호 결정 대기)

| 코드 | 제목 | 판정 · 규모 | 요지 |
|---|---|---|---|
| P1 | 깃발 세트 SSOT 동기화 | 🔴 · 유지보수 창 ~5분 | reviewer 무조건 발화 깃발이 4개 문서에서 불일치 |
| P2 | "조용히 죽는 검출 패턴" 구조 처치 | HR2 후보 Phase | circuit-breaker 등급 추출 실패를 계기로 훅 셀프테스트 상설화 |
| P3 | work-pin 다이어트 | 일부 즉시 가능 | 헤더 중복 제거·완료 Phase 이관·상한 지표 교체 |
| P4 | 봉인 비대칭 설계 결정 | 영호 설계 결정 1개 | 의미 정본 문서(harness/**·adr/)로 기술 봉인 확장 여부 |
| P5 | 문서 드리프트 일괄 정정 + 훅 견고성 꼬리 | 유지보수 창 묶음 | stale 인용·용어 표기 통일·훅 미세 봉합 |

### P1 · 깃발 세트 SSOT 동기화 (🔴 · 유지보수 창 ~5분)

reviewer가 무조건 발화해야 하는 깃발 세트가 4개 문서에서 서로 어긋난다. 정본(SSOT)은 `grade-and-risk.md:80-81`이다 — `backend-contract`·`shared-contract`는 무조건 발화, `ui-visual`은 버킷(b) 육안 대상(무조건 발화가 아님). 정본에 맞춰 동기화할 곳은 세 군데다.

- `review-tiering.md:32` — 계약 깃발 2종(backend-contract·shared-contract)이 누락됐고, ui-visual이 무조건 발화로 잘못 포함돼 있다.
- `reviewer.md:12` — 정본과 어긋난 깃발 세트.
- `work-run/SKILL.md:63` — 정본과 어긋난 깃발 세트.

### P2 · "조용히 죽는 검출 패턴" 구조 처치 (HR2 후보 Phase)

circuit-breaker가 pin에서 등급을 추출하지 못하는 버그가 발단이다 — `circuit-breaker.sh:29`의 `grep '^등급:'`이 등급을 줄 맨 앞에서 찾는데, 현재 pin은 등급이 PHASE 줄 중간에 위치한다. 그 결과 임계값 15가 설정돼 있어도 기본값 10으로 오동작한다(원장에 남은 "임계 10" 기록이 그 증거다).

이 사건을 계기로, 이번에 손으로 돌린 발화 배터리를 `_lib/*.test.mjs` 패턴의 경량 셀프테스트로 상설화하기를 권한다 — pin 포맷 계약 단언(등급 줄 위치 등)과 sealed 경로 차단 단언을 담는다. 검출 패턴이 조용히 죽은 선례가 2회(python 스텁 사건, 이번 등급 추출 실패) 쌓였다는 점이 비용 근거다.

### P3 · work-pin 다이어트 (일부 즉시 가능)

- ① `current-pin.txt`의 1행 헤더 제거 — `pin-injector.sh:24`가 헤더를 중복 주입하고 있다. state 구역이라 secretary가 즉시 처리 가능한 항목이다.
- ② 완료된 Phase의 상세 서술을 각 `-DONE.md` 포인터로 이관한다.
- ③ 목표 지표 "30~40줄"을 바이트/토큰 상한으로 교체한다. 실측: 현재 13줄이지만 14,189바이트로, 1.5일 새 +36% 불어났다 — 줄 수는 실제 부피를 못 잡는다.

### P4 · 봉인 비대칭 설계 결정 (영호 설계 결정 1개)

기술 봉인(실측 쓰기 차단)이 어댑터 층(`.claude/**`·`CLAUDE.md`·`.codex/**`)에만 걸려 있다. 의미 정본인 `00.Documents/harness/CORE.md`·`core-manifest.json`·ADR은 규범 게이트(문서상 금지)만 있고 기술 차단은 없다. 권고는 `00.Documents/harness/**`·`adr/`로 봉인을 확장하는 것이다 — 대안은 "의도적 제외"임을 명시적으로 박제해 두는 것이다. 인접 사안: CORE-12의 읽기 격리도 규범 선언뿐이다(단, 쓰기는 실측 차단이 확인됨).

### P5 · 문서 드리프트 일괄 정정 묶음 + 훅 견고성 꼬리

문서 드리프트(코드는 맞는데 문서 표기가 옛것으로 남은 것):

- `/work:plan` 콜론 표기 잔존 — `harness.md:2,19,28`·`doc-thresholds.md:20,95`·`pin-and-done.md:45`.
- `/harness` 커맨드를 CLAUDE.md 목록에 편입/정리할지 결정.
- "8→9" 훅 개수 표기 2곳 — `loop-driver.md:106`·`harness-review.md:55`.
- ADR-011 stale 인용 2곳 — `settings.json:2`·`dangerous-cmd-guard.sh:7` → CORE-06/07로 갱신.
- `INDEX.md:21` shared-contract 누락.
- 루트 config 소유 1줄 명시.
- plan-auditor 입력 계약 인라인화.
- reviewer flags 키 표기 통일.
- effort no-op 처리 명시.
- main-process ipc 라벨 정정.

훅 견고성 꼬리: bash -c/sh -c 중첩과 perl 등재는 싸게 봉합한다. 변수 간접참조 리다이렉트는 기지 백로그(fail-open 2건·C-full 꼬리)와 함께 훅 견고성 창으로 묶어 처리한다.

## 패키징 권고

- **유지보수 창 1회 (합계 1시간 안쪽)**: P1 + P3-① + P5의 문서 정정 묶음.
- **HR2 격 소형 Phase**: P2 + P3-②③ — HR1 후속 선례를 따른다.
- **영호 설계 결정 1개**: P4 — 봉인 확장 여부는 문서 정정이 아니라 설계 분기라 별도 판단이 필요하다.

## 점검 세션이 남긴 것

- 산출물: reviews 문서 1건(`2026-07-17-harness-review-all.md`) + 본 핸드오프 문서 + reports 카테고리 인덱스(`INDEX.md`).
- 프로브 임시 파일은 전부 제거 완료했다.
- 하네스는 무수정이다 — 점검은 읽기 전용 원칙을 준수했다. 실제 정정은 위 큐를 근거로 영호가 유지보수 창에서 수행한다.

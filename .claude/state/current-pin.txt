MODE: loop-driven **가동 중** — 영호 GO(2026-07-25) "마일스톤 끝까지 완주". attended·무인배치 X. 게이트는 work-judge (b)(c)에서만.
WORK-ID: HR2_opus5-renewal — 마일스톤 21_HR2-opus5-renewal / 브랜치 chore/harness-renewal-opus5
PHASE: **P01~P10 + P12 ✅ done (11/12)** / 남은 것 = **P11(마감 — 영호 게이트 대기)** / 등급: 대규모(하네스·영호 단독 통제 창) / 깃발: harness, trust-boundary
P08~P09 = **최상위 5폴더 언더스코어 개명**(구 `00.Documents` → `00_Documents` 외 4종, rename 1,333건) + 빌드 체인 33지점 + **대량 경로 치환 완료**(테스트 434파일 2,229건 · .claude 지시문 192건 · 정본 문서 11파일 80건 · 02_Source 주석 48건 · harness 정본 7건) + **e2e 유령 경로 5파일 11건 정정**. 게이트 6종 green.
현재 작업: 12 Phase 중 **9/12 완료**. P01 = ADR 4건 개정 + 헌법 「기억의 3층」. P02 = 별칭→full ID 9개 + 티어 4층 + 회귀 테스트 고정. P03 = **역할 9→10**(CTO 신설·coordinator 축소·secretary 격리 재정의·frontmatter 5키 10/10·잔재 스윕 13파일). P04 = **effort 도입**(창 안에서 판정 1회 반전 — 드롭 → 도입). 배분 `xhigh` 3(reviewer·plan-auditor·CTO) / `high` 7. P05 = **봉인 우회 1차 5건 + reviewer 2차 5건 봉합** + ReDoS + TTL 7h. P06 = **강제 출처 라벨링 3층**(policies 12/12 범례+라벨) + **①자기검증 독려 = 공집합 확정**(삭제 0건) + execution-owner 오분류 정정 + subagent-routing ③ 근거 재서술 + spawn cap 등재 + **CORE-13 v2 3점 동기**. P07 = **훅 경로 신·구 병행 수용**(`[._]` — 정규식 4·셸 glob 8·SEALED deny 16→20) + **GATE_FLAG OR 검사**(일원화 금지 지점) + tdd-guard 짝 갱신. 게이트 4종 green(typecheck 0 / test 395파일·5,330 / lint 0 / **훅 94/94**). P08 = **git mv 5폴더**(1,342 files) + 빌드 체인 8파일 33지점. P09 = **대량 치환**(ins 2,472/del 2,475, 차 −3은 전부 의도) + **CORE 조항 버전 방침 신설** + 저장소 밖 3곳. 게이트: 훅 94/94 · typecheck 0 · lint 0 · test 395파일 5,330 · build 성공 · **e2e core-loop 4 passed**.
P07 실측 고정 사실: ㉝⭐**개명이 봉인을 조용히 푼다가 실측 확정** — 옛 정규식으로 `00_Documents/harness/CORE.md`·`adr/**`·`ADR.md`·`98_Management/…/gate-open.flag` **4경로 전부 `false`(봉인 밖)**, `tee 00_Documents/harness/CORE.md`도 통과. P07 없이 P08을 했으면 의미 정본 층+OpenGate가 열린 채 개명이 끝났다 ㉞**`GATE_FLAG`는 일원화 금지** — 다른 경로는 틀려도 "차단이 늦게 걸릴 뿐"이지만 GATE_FLAG는 **회복 경로 자체를 끊는다**(창을 못 열어 봉인 파일을 못 고침) ㉟⚠️**역방향 주입 검증은 주입 성공을 먼저 확인할 것** — 되돌림 스크립트에서 `\.`가 `.`로 뭉개져(셸 이스케이프 한 겹) 와일드카드가 됐고 **되돌린 상태인데 테스트가 전부 통과**했다. 하마터면 "프로브가 무용"으로 오독 ㊱**OpenGate 배치파일·`openGateExecReason`은 개명에 이미 안전**(`%HERE%` 상대 / `harness_opengate` 마커 기반) ㊲**`.claude/settings.json`은 P07에서 무변경** — 창 개방 중 경로 deny를 써 넣으면 permission 계층은 flag로 우회 안 되므로 **자기 세션이 즉시 잠긴다**. 반영 확인은 P11 CLOSE 후
P12 완료(2026-07-25, 번호상 마지막이나 실행은 P11 앞): **상주 4,460 → 3,524 tok**(−936, 선불 −134 포함 −1,070). CLAUDE.md 2,881→**2,114**(133행→102행) · MEMORY 인덱스 717→548. 최대 절감 = 멀티에이전트 표(823→~320, **정보 손실 0** — `_routing.md`가 더 상세한 정본). ⚠️ **표에만 있던 CTO 승인 규칙**과 ⚠️ **"완료 폴더 보존"(CLAUDE.md가 유일 소유자, grep 0건)** 은 건져서 승격·이관. **description listing은 미수행 → P11 이관**(워처 사망으로 발화 프로브가 이 세션에선 거짓 결과). 검증: CRITICAL 무변경(diff 0줄) · 훅 94/94 · **CORE conformance 13/13**. 원장 = `12-context-diet-log.md`.
P10 완료(2026-07-25, **영호가 직접 기동한 Codex 세션**이 수행 — Claude는 `.codex/**` 한 줄도 안 씀): 8파일 **치환 63건 예측 일치**(토큰 65개). Claude 교차 검증 4종 통과 — 점 표기 0 · 이스케이프 정규식 0 · 의미 손상 0 · **fail-open 매처 본체**(`riskFlagsFor`·`isImplementationPath`·`htmlTarget`·테스트 루트·TDD deny) 전부 새 경로. ⭐ **Codex가 브리프에 없던 `hooks.json` SHA-256 8곳을 스스로 갱신**(훅 digest 불일치 = 훅 미신뢰 = 경로는 고쳤는데 훅이 안 도는 반쪽 수정). ⭐ 새 계약이 옛 것보다 강함 — 역할 5개 손 나열 → `readdirSync` 동적 열거 + `length === 10`. 게이트: `.codex` **30/31** · typecheck 0 · vitest **395파일 0 failed**. ⚠️ 잔여 = **`/hooks` 재검토·재신뢰**(digest 변경, 영호 몫).
P11 부분 진행(창 안에서 가능한 것 전부 완료): **프로브 ④⑤⑦ + 판정기 레이어 green** — ④(a)읽기전용 sed 통과·(b)`sed -i` 차단·(c)스크립트 `w` 차단 / ⑤ `tee .claude/settings.json # it's fine` **차단**(실측 고장 #4 봉합 직접 증거) / 새 경로 3종 `sealed` / ⑦ 옛 폴더 부활 0. 게이트: typecheck 0 · lint 0 · test 395파일 5,330 · build 성공 · 훅 94/94 · CORE conformance 13/13. ⚠️ **e2e `core-loop` 3 failed / 1 passed — 3연속 재현**. P12 무관 근거 확보(코드 변경 0·renderer 번들 해시 P09와 동일 `index-BZBY5D8e.js`·userData 오늘 변경 0·`.fe-blank-btn`에 위치 애니메이션 없음[`transition: background`뿐]). 실패 지점 = "폴더 선택" 클릭이 **stable 대기**에서 30s 타임아웃 → 2·3번은 폴더 미개방 연쇄. 가설 3개(부하·userData 오염·애니메이션) **전부 기각**, 원인 미규명 → 멈춤 규칙 발동. ⭐ **부수 발견: e2e가 Electron `userData`를 격리하지 않는다**(`--user-data-dir` 부재) — 사용자 실제 앱 상태에 의존하는 비결정론 + 오염 위험. HR2 범위 밖 → 백로그.
다음 액션: **P11 마감 — 영호 게이트 대기**. ① ⭐ **`CLOSE-GATE.bat` 실행(영호 단독·에이전트 deny)** — 창을 닫아야 프로브 ①(봉인 차단)·②(실행 경계)·③(open-gate 로그)이 실측 가능하다(창 안에서는 `supervisor-guard`가 `exit 0`으로 전체 통과시키는 게 **설계 의도**라 차단을 못 본다, ADR-038:3). CLOSE 직후 `.claude/settings.json` deny에 **새 경로 4줄**이 복원되는지 확인 = P07 이관 검증. ② **`/hooks` 재검토·재신뢰** — P10에서 `.codex/hooks.json` digest가 바뀌었다(신뢰된 새 세션 필요). ③ **새 세션에서** description listing 압축(P12 이관분) + 에이전트 정의 반영 확인 — 이 세션은 **정의 워처가 죽어 거짓 음성**을 낸다(실측 고정 ⑲). ④ **마일스톤 종결** — 등급 대규모이므로 `HR2-DONE.md` + 5단계 보고. ⑤ **push / PR**은 CORE-06 사람 게이트(현재 미실행, 커밋만 누적).
P05 실측 고정 사실(추가): ㉒**동일 fail-open이 2지점이었다** — `shell-policy.mjs`뿐 아니라 `shell-tokens.js`도(②절 실행 경계가 `git add . # it's fine`에 열려 있었음). *계획 열거 = 전수 아님* 2회차 ㉓**`open-gate` 감사는 2026-07-25 이전 구간이 영구 불가**(로테이션으로 복원 불능 — ADR-038 「보완」) ㉔**`node --test <dir>`는 디렉토리 인자를 받지 않는다** — `test:hooks`는 cwd 이동 방식 ㉕**circuit-breaker는 변이 도구(Edit|Write)만 감시** — Bash로 테스트하면 조기 exit 0이라 "발화 0"이 거짓 통과가 된다(양성 대조 필수) ㉖**훅 글루 레벨 차단은 창 안에서 실측 불가**(개방 시 전체 통과가 설계 의도) → P11 이관. ⚠️ **OpenGate TTL** — flag epoch `1784963850`, **TTL 7h**(2026-07-25 영호 지시로 4h→7h, ADR-038 개정 2). 만료 시 영호가 `OPEN-GATE.bat` 재실행(**에이전트 deny**).
P05 reviewer 2차 봉합 고정 사실: ㉗**reviewer가 🔴 4건을 잡았고 그중 1건은 P05가 만든 회귀** — 「오탐을 고치면 미탐이 생긴다」(sed 무조건 차단→조건부 전환이 부정 주소·임의 구분자·`e` 명령을 열었다). 작성자는 자기가 만든 구멍을 완료 조건의 "오탐 해소 ✅"로 읽는다 → **심판의 가치는 능력이 아니라 위치**(P06 §③ 근거 재서술의 실증 사례로 인용 가능) ㉘**개행은 세그먼트 구분자여야 한다** — 승격의 **짝이 heredoc 본문 제거**다(한쪽만 고치면 반대편이 깨진다: 개행이 안 나뉘던 성질이 heredoc 오탐을 막고 있었다) ㉙🔴**느린 정규식 = 보안 결함** — ReDoS 실측 n=40에서 **3,652ms**(수정 후 0.1ms). 판정기가 멈추면 훅 타임아웃 → **조용한 통과**. 정규식 대안이 한 글자를 소비하는 방법을 둘 이상 갖게 하지 말 것(`\\.|[^]`가 백슬래시에서 겹쳤다) ㉚**POSIX 이스케이프 처리의 대가 = Windows 백슬래시 경로 파손** — 토큰 단계에서 깨지므로 후보 소스에 **원본 명령**을 함께 넣고 OpenGate 판정은 정규화 사본도 본다(기존 테스트가 이 회귀를 잡았다) ㉛**계약 상수는 정본에서 읽는다** — 테스트가 `14401`을 복제해 TTL 확장 순간 거짓 통과가 됐다. 이제 `GATE_TTL_SEC`을 `supervisor-guard.sh`에서 파싱 ㉜**여전히 미검증**: `|| assignments=''`(node 부재) 분기 — 재현에 node 제거 필요. 정직하게 미결로 남김
📌 **HR2 회귀 기준선(P01 시점 2026-07-25)**: typecheck 0 · test **394파일 / 5,325 passed / 10 skipped** · lint 0 → **현재 395파일 / 5,330 passed**(P02 신설분 +1파일·+5, 개명 회귀 0). 훅 테스트 baseline 49/49 → **현재 94/94**(P05·P07 신설분).
영호 결정(2026-07-25 3건): ①effort xhigh = **메인 세션 + chief-tech-operator만**, 나머지 기본 high ②CLAUDE.md 다이어트 = P12로 편입 ③검증 구조 = **3분류 재편**(자기검증 독려=삭제 / 컨텍스트 밖 실측=유지 / 규율 축=유지+근거 재서술)
🗂 **HR2가 남긴 백로그 5건**(범위 밖으로 분리): ④ ⚠️ **Codex baseline 재실측 — `REVALIDATION_REQUIRED`(codex-cli 0.145.0 ≠ baseline 0.144.1)**. `.codex` 테스트 31건 중 유일한 red이고 **개명과 무관한 환경 드리프트**다. ADR-033:30이 설계한 *"CLI 불일치 시 결과가 같아도 exit 3"* 의 **2회차 발화**(1회차 = 2026-07-13). ⭐ baseline을 0.145.0으로 덮어쓰면 즉시 green이지만 그건 **재실측 없이 계약을 통과시키는 것** — 게이트를 green으로 만드는 가장 빠른 길이 게이트를 무력화하는 길이다. 절차(`codex-baseline.json:$comment`) = ⓐ 격리 canary로 읽기 deny 실태 재실측(**Codex 세션 재기동 필요**) → ⓑ `00_Documents/harness/codex-baseline.json` 갱신 → ⓒ ADR-033 재실측 이력 한 줄. ⓑⓒ는 **Claude 영역 + 봉인 해제 불필요**라 ⓐ만 받으면 즉시 처리 가능 ⑤ **ADR 산문 속 현재형 경로 포인터** — ADR 전체 옛 경로 45건 중 대부분은 *"근거 = `01.Phases/BF1-…`"* 같은 **과거 사건 기록**(P09 "산문 미수정" 원칙 대상)이지만, ADR-033:30·38과 ADR-034:5의 `00.Documents/harness/CORE.md`는 *"이 파일이 소유한다"* 는 **현재형 계약 포인터**다. 45건 개별 판정이 필요해 분리. ⚠️ 살아 있는 지시문(policies·agents·skills·commands·헌법·AGENTS·harness)은 **2건뿐이고 둘 다 의도된 대조 서술** — P09가 그쪽은 제대로 훑었다.
🗂 **선행 3건**(영호 결정 2026-07-25): ① **e2e `core-loop` 3 failed 원인 미규명** — "폴더 선택" 클릭이 stable 대기 30s 타임아웃, 3연속 재현. HR2 변경과 무관 확정(코드 0·번들 해시 동일·userData 오늘 변경 0·버튼에 위치 애니메이션 없음). 가설 3개 기각. ⭐ **범위 좁혀짐** — `engine-update.e2e.ts`가 같은 앱에서 **버튼 클릭 정상 동작**(2 passed, 실 npm registry 라이브)이므로 앱 전반·Playwright 클릭 자체가 아니라 **파일 탐색기 빈 상태의 "폴더 선택" 경로 국한**이다. 조사 출발점 ② ⭐ **e2e가 Electron `userData`를 격리하지 않는다** — `electron.launch`에 `--user-data-dir` 부재(`core-loop.e2e.ts:27-34`)라 **영호의 실제 앱 상태**(`%APPDATA%/AgentDeck`)를 공유한다. 비결정론 + 테스트가 사용자 데이터를 오염시킬 위험. ①의 유력 후보이기도 하다 ③ **테스트 주석 속 유령 마일스톤 경로 ~30곳** — 2026-07-11 리네임 잔재. 파일을 쓰지 않아 유령 폴더를 만들지는 않지만(P09에서 쓰기 지점 5파일 11건만 정정) 주석이 없는 경로를 가리킨다.
주의할 약속: 하네스 = 영호 단독 통제(ADR-038) · `.claude/settings.json`(M)·`gate-open.flag`는 **창 상태물이라 커밋 금지** · P10(.codex 대칭)은 CORE-12로 Claude 수행 불가 · P12는 번호상 마지막이나 **실행은 P11 마감 앞**
🔴 **P05로 넘긴 발견(P03 작업 중 실측)**: `guard-log.mjs:29`의 `action==='block'?'block':'notify'`가 훅이 넘긴 **`open-gate` 라벨을 삼킨다** → `grep open-gate guard-blocks.log` **0건**. ADR-038:9의 *"전량 open-gate로 남아 사후 감사 가능"* 계약 미이행 + **P11 발화 프로브 #3이 현 상태로 반드시 실패**. `05-hook-security.md` 우선순위 5로 등재됨.
실측 고정 사실(재논의 금지): ①`model: opus` 별칭은 claude-opus-4-8로 스폰 → Opus 5는 **full ID 필수** ②coordinator의 Agent는 v2.1.220+중첩OFF로 **이미 무력** → P03에서 반납 완료, 재귀차단 담보가 **런타임 중첩 OFF**로 이동 ③`tee .claude/settings.json # it's fine`이 봉인 통과 = **미봉합 fail-open**(shell-policy.mjs:50) ④개명은 추적파일 698개/3111줄·import 814건 ⑤**`.claude/settings.json`은 canonical이 아니라 OpenGate 파생물** — 편집은 settings.SEALED.json 단독 ⑥`gate-open.flag`가 .gitignore에 없음 = 개방 상태 커밋 위험(P05 등재) ⑦**공식**: "Code review 정확도는 낮은 effort에서도 유지" / "effort는 사고량만 바꾸지 응답 길이는 아님" / "'use a subagent to verify'와 legacy harness scaffolding 제거하라" — 단 "writer-verifier 패턴은 유효"도 같은 문서에 있음 ⑧@ctgptlb "effort 높이면 코딩 저하"는 **근거 미달로 반증** ⑨공식 권고 문구 상당수가 **Claude Code 시스템 프롬프트에 이미 반영됨** → CLAUDE.md에 중복 기재 금지 ⑩**메인 effort 설정 지점 = `~/.claude/settings.json`의 `effortLevel`, 현재값 `xhigh`** — "메인 xhigh"는 **이미 충족**, user scope라 전 프로젝트 전역. `CLAUDE_EFFORT=xhigh`는 레지스트리에 없어 **런타임 파생 신호**(현 턴 실효값 판독구). ⚠️ **정정** — `CLAUDE_CODE_EFFORT_LEVEL`은 *"존재하지 않는 변수"가 아니라* 이 환경에 **설정돼 있지 않을 뿐**이고, 실재하며 **frontmatter까지 무력화하는 최상위 손잡이**다. **우선순위 = env `CLAUDE_CODE_EFFORT_LEVEL` > frontmatter `effort` > 설정 `effortLevel` > 모델 기본(`high`)**. 서브 상속은 ⓪으로 **확정**(effort 키 없으면 세션값 `xhigh`) ⑪**봉인은 2층이다** — `settings.SEALED.json` deny **16줄**에 `Edit(CLAUDE.md)` **포함**(12번) + `supervisor-guard.sh` 별도 차단. ⚠️ 옛 표기 *"deny 4줄에 CLAUDE.md 없음"* 은 **오측**(4~5줄짜리는 OPEN 쪽 deny를 본 것) — 2026-07-25 전수 실측으로 정정 ⑫상주 예산 실측 **4,712 tok**(CLAUDE.md **2,880**[P03 분담표 +1행·재귀차단 재서술 반영, 132행] / description listing 1,015[⚠️ CTO 신설로 재측정 필요] / MEMORY 인덱스 717 / 글로벌 100) ⑬**별칭 규칙은 이미 한 번 무너진 전력** — CHANGELOG:80(2026-07-03) 기록 vs 디스크 5건 드리프트 → P02가 회귀 테스트로 고정 ⑭`harness-conformance.test.ts`는 **ADR을 검사하지 않는다**(318줄 전수, 매칭 0건) ⑮`99_Others/tests/agents/model-alias-sonnet5-live-probe.test.ts` 존재(skipped live probe) ⑯**훅은 `agent_type` 화이트리스트가 없다** — `[ -n "$AGENT_TYPE" ] && exit 0`(supervisor-guard:83)이라 **새 역할 신설이 훅에 영향 0**(CTO 등재 불요) ⑰**OpenGate 창이 열려 있으면 실행 경계 ②까지 전부 통과**한다(supervisor-guard:30-41, ADR-038:3 "전체 통과" = 설계 의도. fail-open 아님) — 창 안에서 메인이 게이트·커밋을 직접 돌려도 안 막히니 **판정표는 훅이 아니라 규율로 지켜야 함** ⑱**CLI frontmatter 스키마 실측**: 파싱 반환 객체 = `tools`·`disallowedTools`·`skills`·`initialPrompt`·`mcpServers`·`hooks` / 검증 에러 메시지 = `effort`·`permissionMode`·`maxTurns` / color 8색(`_FOR_SUBAGENTS_ONLY`) → **P04 종결: frontmatter `effort`는 작동한다**(공식 문서가 "세션 레벨 오버라이드" 명시). 과거 "no-op" 관찰 2건(2026-07-03·07-25 1차)은 **정의 워처 미반영 상태에서의 측정**일 가능성이 높음 ⑲⭐**정의 워처가 죽을 수 있다 — 이 세션에서 기계 확정**: 디스크 `model: claude-sonnet-5`인데 per-invocation 없이 스폰한 서브는 `claude-opus-5`로 뜸. ⇒ **이 창에서 고친 `.claude/agents/**` 전건이 현 세션 미반영**(P02 full ID·P03 disallowedTools/maxTurns/color·**coordinator Agent 반납**). **하네스 에이전트 정의를 건드린 창의 검증은 반드시 새 세션에서**(같은 세션 = 거짓 음성) → P11 편입 ⑳**판독구 3종** = `CLAUDE_EFFORT` env · 훅 payload `effort.level` · ⭐**서브 트랜스크립트 `agent-<id>.jsonl`의 `.effort` 필드**(사후·전수 소급 조회 가능, 짝 `.meta.json`에 `spawnDepth`=중첩 OFF 기계 증거) ㉑**별칭 비대칭**: `sonnet`→`claude-sonnet-5`(최신)인데 `opus`→`claude-opus-4-8`(최신 아님) ⇒ full ID 규범 유지
루프 상태: **가동** — P10 마감 커밋 완료. **11/12 done**, P11만 남았고 그건 영호 게이트(CLOSE-GATE·`/hooks` 재신뢰·새 세션)에 걸려 있다
마지막 갱신: 2026-07-25

# 헌법 (CODEX)

## 정체 선언

이 저장소는 AgentDeck입니다.
지금은 M01 Bootstrap 마일스톤을 수행 중이며, 그 목표는 Moodie_Agent_Workflow에서 검증된 훅 하네스를 이 저장소로 옮겨 AgentDeck이 스스로를 감독하게 만드는 것입니다.
이 파일은 Codex 측 에이전트가 읽는 헌법입니다. Claude 측 헌법은 루트의 `CLAUDE.md`이며 두 파일의 조항은 같습니다.

아래 조항 가운데 일부는 M01 Phase 4에서 훅이 켜지면 기계가 집행합니다.
그때까지는 사람과 에이전트가 규범으로 지킵니다. 집행 시점이 다를 뿐 효력은 지금부터입니다.

## 핵심 규칙 참조

- 현재 마일스톤의 계획, 결정 대장, 검증 기록의 진실의 원천은 `01_Milestones/M01_Bootstrap/_MilestonePreview.md`와 같은 폴더의 Phase 파일 여섯 개입니다.
- 세션 재개의 인수인계 표면은 `01_Milestones/M01_Bootstrap/01_Work_Pin.md`이고, 현재 좌표 포인터는 `98_Management/01_GateState/gate-config.json`입니다.
- 계획 템플릿과 규격 원본의 거처는 `00_Documents/02_Rules/`입니다.
- 되돌리기 어려운 결정의 거처는 `98_Management/00_ADR/`입니다.
- Codex 스킬의 거처는 `.agents/skills/`입니다. Claude 측 훅 정본은 `.claude/hooks/`이며, Codex 어댑터는 그것을 덮어쓰지 않고 대조만 합니다.

## 조항

### 1. 검증은 기록 줄로만 유효하다

검증 결과는 각 Phase 파일 말미 「검증 기록」 절의 줄로만 유효합니다.
줄 스킴은 `PASS`, `FAIL`, `USER-INPUT` 세 마커이며, 각 줄에 타임스탬프와 한 문장을 적습니다.
FAIL 줄에는 무엇이 왜 실패했는지에 더해 무엇을 시도했는지까지 적습니다.
기록은 append-only입니다 — 기존 줄을 지우지 않고, 리셋은 삭제가 아니라 기준점 이동으로 합니다.
줄로 남지 않은 검증 통과 주장은 무효입니다.

### 2. `--resume` 등 세션 이어붙이기를 금지한다

세션 재개는 생존 문서 네 종(계획 문서, work-pin, 결정 대장, 검증 기록)으로만 합니다.
`--resume`으로 이전 세션의 문맥을 이어받는 것은 전 구간 금지입니다.
문맥을 계승하면 앞 세션의 판단을 그대로 물려받아 확증편향이 시간축을 넘어 이어집니다. 새 세션에서 문서만 보고 다시 판단하는 것이 이 규율의 목적입니다.
Phase 경계는 예외 없이 새 세션입니다.

### 3. 메인 세션은 `01_Milestones`만 편집한다

메인 세션(코디네이터)이 직접 편집할 수 있는 곳은 `01_Milestones` 아래뿐입니다.
소스 코드를 포함한 구현 작업은 워커를 스폰해서 시킵니다.
메인이 계획과 구현을 겸하면 계획을 세운 주체가 자기 계획의 이행을 자평하게 되고, 판정 표면이 사라집니다.

### 4. 마감은 pin 요약과 스탬프로 한다

세션을 끝낼 때는 `01_Work_Pin.md`의 마감 요약을 갱신하고 스탬프 줄을 남깁니다.
마감 요약은 세 줄입니다 — 바뀐 것, 내린 결정, 봐야 할 것.
각 줄은 최대 두 문장입니다. 세부 근거는 커밋 메시지나 계획 문서에 두고 요약에서는 뺍니다.
스탬프의 시각은 세션이 추정하지 않고, SessionStart가 주입한 현재 시각을 기준으로 적습니다.

### 5. 마크다운 문서는 250줄을 넘기지 않는다

목표는 200줄이고 상한은 250줄입니다.
상한을 넘으면 문서를 하위 폴더로 분화합니다.
초과 편집은 되돌려지지 않고 사후 피드백으로 통보됩니다 — 통보를 받으면 다음 편집에서 분화하세요.

### 6. `/gate`의 하드 게이트 다섯 종은 커밋 전 전부 통과해야 한다

하드 게이트는 `npm run typecheck:node`, `npm run typecheck:web`, `npm run lint`, `npm run test:hooks`, `npm run build` 다섯입니다.
하나라도 실패하면 커밋하지 않습니다.
`npm test`는 다릅니다 — 통과 여부가 아니라 기준선과의 차분으로 읽습니다.
기준선은 실패 0이며, 실패가 하나라도 나오면 회귀 후보입니다. 판정 세칙은 `.claude/commands/gate.md`가 소유합니다.

### 7. FAIL이 세 번 쌓이면 멈추고 사용자를 기다린다

같은 Phase의 검증 기록에서, 마지막 `USER-INPUT` 줄 이후의 `FAIL`이 세 번에 이르면 전진이 막힙니다.
그 자리에서 `USER-INPUT` 줄을 남기고 사용자 판단을 기다립니다.
세 번째 실패까지 갔다는 것은 접근 자체가 틀렸을 가능성이 높다는 신호이므로, 같은 방법을 네 번째로 시도하지 않습니다.

### 8. Bash를 경유한 임의 파일 쓰기를 금지한다

`Bash(node ...)`처럼 셸에서 스크립트를 돌려 파일을 쓰는 것은 금지입니다.
이 경로는 편집 도구 훅을 지나지 않아 감독 밖에서 파일이 바뀝니다.
이 구멍은 기계로 막지 않습니다 — 막으면 정상 빌드·테스트 명령까지 걸리는 오탐이 무인 루프를 멈추기 때문입니다. 근거는 `98_Management/00_ADR/ADR-001-부트스트랩-이식-결정.md`의 결정 3입니다.
파일 편집은 편집 도구로만 합니다.

### 9. 역할 탈출구는 사용자 전용이다

`MOODIE_SESSION_ROLE=worker` 환경변수는 세션을 워커로 판정하게 만들어 편집 제한을 풉니다.
이 탈출구는 사용자만 설정합니다.
에이전트가 스스로 이 변수를 켜거나, 워커에게 켜라고 지시하거나, 스크립트에 심는 것은 금지입니다.
제한에 막혔을 때의 정상 경로는 탈출구가 아니라 워커 스폰입니다.

### 10. 원격 push는 사용자 승인 게이트다

`git push`와 패키지 publish는 에이전트가 단독으로 하지 않습니다.
위험 명령 가드가 승인 다이얼로그로 잡으며, 그 다이얼로그를 우회하는 방법을 찾는 것도 금지입니다.

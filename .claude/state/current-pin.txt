MODE: loop-driven **가동 중** — 영호 GO(2026-07-26) "대규모이면서 자율로 끝까지 완주하기 좋은 마일스톤". attended·무인배치 X. 게이트는 work-judge (b)(c)에서만.
WORK-ID: **nc-naming-placement** / 브랜치 `chore/nc-naming-docs`(P01~P06 → PR ①) · P07은 `chore/nc-naming-code`(→ PR ②)
PHASE: **01/07** — ADR 3건 정합(039 신설 · 027 개정2 · 013 개정) / 등급: 복잡 / 깃발: harness
📌 **상세는 pin이 아니라 문서가 소유한다** — 배경·규범·동결 경계·위험 전문 = `01_Phases/22_NC-naming-placement/_milestone-plan.md`. Phase별 작업·완료조건 = 같은 폴더 `01`~`07`. 여기 복제하면 드리프트만 생긴다.

현재 작업: 명명 규칙·배치 체계화. **진단이 통상과 반대다** — 규칙이 없는 게 아니라 규칙이 **미이행 상태로 done 처리**돼 있었다(`01_Phases/00_RF1-cleanup/08-docs-prefix-renumber.md` = frontmatter `done` ↔ 본문 `pending` ↔ 구현 0건). 그리고 **파일 층은 폴더마다 이미 일관**돼 있어 혼재는 **폴더 이름 층에만** 있다. 실제 문자 변경 ≈ 20건(884 소문자 시작 중 동결 256 · 파생 종속 451).
다음 액션: ⚠️ **유지보수 창 1 개방 대기** — P01~P03이 전부 CORE-11 봉인(`00_Documents/adr/**`·`CLAUDE.md`·`.claude/hooks/**`·`settings.SEALED.json`)이라 영호의 `OPEN-GATE.bat` 없이는 착수 불가. 개방되면 P01→P02→P03 자율 연속 진행.

✅ **사전 검증 2종 완료 — 계획이 크게 고쳐졌다**(2026-07-26). `plan-auditor`(Fable 5) 🔴5·🟡7 + **Codex 교차 리뷰** 14 findings(critical 1 · high 9). 전부 반영 완료. 특히:
- ⚠️ **게이트 명령 2종이 유령이었다** — `_selftest.sh`·`core-conformance.mjs` 는 **존재하지 않는 파일**이다. 실물 = `npm run test:hooks` + `node 00_Documents/harness/conformance-check.mjs`. 초안이 7곳에 복제해뒀고, 이 마일스톤이 잡으려는 병(유령 포인터)을 계획서가 스스로 앓고 있었다 → **`_milestone-plan.md` 「🧪 회귀 게이트 정본」에 한 번만 정의하고 Phase는 참조**한다. **복제 금지.** ⚠️ G2는 P05가 개명하는 폴더 안에 산다(P05 이후 신 경로)
- ⭐⭐ **Codex 전제가 반증됐다 — 위험 방향이 반대다.** Codex `isHarnessPath()` 는 `00_Documents/{harness,adr}` 를 **아예 보지 않는다**(ADR-037 명시). "Claude가 fail-open이니 Codex도 대칭"은 추정이었고 틀렸다. 실제로 깨지는 건 ① `reports` 리터럴 → **fail-closed 과차단** ② `harness-doctor` → **import 시 `process.exit(1)`** ③ 스템 리터럴 → **fail-open 침묵사**
- 🔴 **CRITICAL — Codex 훅 digest**: 본문 digest ≠ `hooks.json` 인자면 **모든 훅이 무출력 return**. 경로만 고치고 digest를 안 맞추면 시크릿·파괴·비가역·하네스·TDD 차단이 **전부 조용히 사라진다** → P04 브리프 필수 + `/hooks` 재신뢰는 **영호 실행**
- ⚠️ **위임 판정은 2축 AND** — ①기계/판단(잡무 기준 v1) **AND** ②경로 소유권(`_routing.md`). 초안은 ①만 보고 `qa`에 `.claude/hooks/**` 픽스처를, `secretary`에 ADR·`AGENTS.md` 치환을 맡겨 **R/W 경계를 위반**했다. **`.claude/**`·ADR·공유 문서는 전부 메인 직접**
- 🆕 **백로그 17 등재** — Codex 문서층은 full-access 세션에서 **0층**(평시엔 `config.toml` 프로필 권한이 막는다). 개명이 만든 구멍이 아니라 **선재 구조**라 이번엔 봉인 추가 안 함(영호 결정)

⭐ **이 마일스톤의 핵심 원리 — 순서가 안전이다**: 훅이 신·구 이름을 **모두 받아들이게 먼저 만들고(P03)**, 그 다음에 개명한다(P05·P07). `shell-policy.mjs`의 `classifyHarnessPath`는 정규식 미스 시 `'unrelated'`를 반환하는 **fail-open**이라 개명이 봉인을 **조용히** 푼다(HR2 실사고 — 안전장치 4곳 동시 무력화). 로그도 에러도 없다.
⭐ **창 2회가 최소인 이유 = 프로브**: `supervisor-guard.sh:57-59` — 창이 열려 있으면 훅 전체가 exit 0이라 **창 안에서는 봉인 발화를 실측할 방법이 원리적으로 없다.** P04는 창을 닫아야만 성립하는 **중간 게이트**이며, red면 P05로 진행하지 않고 예비 창을 연다.

사람 게이트 8지점(그 사이는 전부 자율): ①창1 개방 ②창1 폐쇄 ③P04 프로브 판정 ④**Codex 세션①**(훅 대칭 + `harness-review`) ⑤창2 개방·폐쇄 ⑥P06 후 **폴더 목록 before/after 육안 확인 1회** ⑦**Codex 세션②**(P07 커밋 전 `agentdeck-review`) ⑧push/PR ×2.

👥 **편성 — 이번 마일스톤 한정 격상**(영호 승인): `plan-auditor`·`reviewer` = **Fable 5** + **Codex 교차 리뷰 포함**. 격상은 파일 수정이 아니라 **`Agent` 도구의 `model` 파라미터 override** — `.claude/agents/**`(봉인) 수정 0이라 창 불필요하고, "에이전트 정의 편집은 같은 세션에 반영 안 됨" 함정도 피하며, "이번 한정"이 되돌릴 것 없이 성립한다.
⚠️ **Fable 5 고유 위험** — P03은 전부 방어적 보안 작업(봉인 정규식·우회 케이스 픽스처·deny·가드 훅)이라 안전장치 거부 선례가 있다. 브리프에 *"저장소 자신의 방어를 강화하는 작업"* 을 명시하고, 거부 시 `claude-opus-5` 재호출 + **품질 저하가 아니라 안전장치 발동임을 구분해 기록**한다. (설계 단계 CTO(Fable 5) 자문은 거부 없이 통과했다.)

⚠️ **CORE-06 v2 — AI는 비가역 명령을 실행할 수 없다.** `push`·`gh pr create/merge`·`gh release`·`npm publish`·`npm run package`는 `dangerous-cmd-guard` 축②가 GO 여부와 무관하게 exit 2로 자른다(승인으로 풀리는 게이트가 아니라 **실행 주체가 사람으로 고정**). AI는 명령을 조립해 제시하고 영호가 `! <명령>` 으로 실행한다. 차단은 설계대로 작동한 것이므로 **우회 시도 금지**.

주의할 약속:
- 하네스 = 영호 단독 통제(ADR-038·CORE-11) · `.claude/settings.json`과 `gate-open.flag`는 **창 상태물이라 커밋 금지**(단 창 폐쇄 후 재봉인분은 커밋 대상)
- CORE-12 격리 — `.codex/**`는 **읽기도** 금지. Codex 자기보고는 원리적으로 검증 불가 → **확인 불가 영역은 확인 불가라고 쓴다**
- ⚠️ **`98_Management/Harness_OpenGate`는 개명 범위 밖** — 이미 규칙 적합이고, 넣는 순간 **부트스트랩 자물쇠**(창을 여는 플래그 경로가 `supervisor-guard.sh:42-43`에 하드코딩)에 진입한다. HR2가 치른 비용이 이번엔 0인 이유다
- ⚠️ **대소문자 전용 개명은 범위에서 제외** — `core.ignorecase = true`라 `git mv` 1회는 **조용한 no-op**이고 typecheck는 green으로 남는다. `main.tsx`·`Screenshot` 철자가 여기 걸려 동결
- 📌 **Second Brain 축적 제안은 문턱을 높인다**(영호 2026-07-26: *"너무 과도한 축적은 Noise"*) — 마일스톤·창 종결급에서만

⏳ **이월 항목**:
- **타이머 만료 예정** — `settings.SEALED.json` 옛 폴더명 deny **4줄** + `$comment` 문단의 제거 조건이 *"master 머지(`63d91a7`) + 한 마일스톤 경과"* 이고 **이번 마일스톤이 그 한 마일스톤**이다 → **NC 종결 후 다음 창에서 삭제**. P03에서는 아직 지우지 않는다
- HR2 잔여 백로그 **14·15·16** (전부 창 요구)
- ⚠️ 에이전트 description 압축(`de61d05`)의 **자동 발화 미관측** — 통상 작업에서 1회 보면 pin에 기록, 3회 지나도록 못 보면 되돌린다(`subagent-routing.md` §4-5). **하네스 작업은 표본이 아니므로 이번 마일스톤도 표본이 안 된다**

루프 상태(기준선, HR2+A 스프린트 종결 시점): 훅 셀프테스트 **113/113** · CORE conformance **13/13** · harness-conformance vitest 12/12 · vitest **5330 passed | 10 skipped** · e2e `core-loop` 4/4 · typecheck 0 · lint 0(⚠️ eslint는 `.ts,.tsx`만 봐서 **훅 `.mjs`는 대상 밖** — 훅 변경은 골든 테스트가 유일한 기계 판정).
마지막 갱신: 2026-07-26

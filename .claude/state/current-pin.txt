MODE: loop-driven **가동 중** — 영호 GO(2026-07-26) "대규모이면서 자율로 끝까지 완주하기 좋은 마일스톤". attended·무인배치 X. 게이트는 work-judge (b)(c)에서만.
WORK-ID: **nc-naming-placement** / 브랜치 **`chore/nc-naming-code`**(P07 → PR ②) · P01~P06은 `chore/nc-naming-docs`(→ PR ①, 커밋 7개 완료)
PHASE: **07/07** — 코드 kebab → camelCase / 등급: 대규모 / 깃발: **trust-boundary** / `loop_track: human-gate` · **유지보수 창 닫힘**
📌 **상세는 pin이 아니라 문서가 소유한다** — 규범·동결 경계 = `01_Phases/22_NC-naming-placement/_milestone-plan.md`. 매니페스트 24건·역산 표 4축 = **ADR-039 §5**. 미해소 안건 = **`00_Documents/BACKLOG.md`**(**9건** — 14·15·16·17·19·20·21·22·23). P07 실측·Codex 감사 결과 = `07-code-kebab-to-camel.md` 「🧪 실행 결과」.

✅ **PR ① 브랜치 종결(커밋 7개)** — `dc49c3e` P01 · `6ef858f` P02 · `b58d593` P03 · `f2bdc27` P04 · `e6508e5`/`3dafd5a` CHANGELOG [H] ×2 · `d020c9e` Codex 트랙 · `6b31550` P05 개명 · `228b089` P06 · `5253d1c` 재봉인
✅ **창 2 종결 + 봉인 프로브 6/6** — `gate-open.flag` 부재 · deny 22줄 · `settings.json` ≡ SEALED 바이트 일치

🔄 **P07 = 미커밋이 정상 (커밋 게이트 통과 대기)**
- 개명 **23건** — `02_Source` 소스 13 + 파생 테스트 10. **전부 `R100`**(유사도 100%)
- ✅ 게이트: typecheck 0 · lint 0 · **vitest `395 passed | 6 skipped (401 files)` / `5336 passed | 10 skipped (5346)`**
- ⚠️ **기준선이 5330 → 5336 으로 바뀌었다**(+6, `engineVersions.test.ts` 27→33). 다음 게이트는 **5336** 과 대조할 것
- ✅ **파괴 카나리아 무변** — 홈 `~/.agentdeck-dev/engine-config.json` sha256·mtime 불변 · `%TEMP%/ev-test-*` 증가 0
- ✅ reviewer 1차(Fable 5) 통과 · ✅ Codex 세션 A 교차 감사 회수 · ✅ 🔴 1건 + 🟡 20줄 봉합 완료
- 🔬 **reviewer 2차(Fable 5) 백그라운드 — 세션 종료 시점 미회수**. 봉합분(특히 `engineVersions.test.ts` 전면 재작성 +396/−354)이 **아직 어떤 리뷰도 안 받았다** → 다음 세션에서 **재실행 필요**(브리프 요지는 아래 「다음 액션」 1)

🔴 **이번 세션 최대 발견 — 풀 액세스 환경에서는 「쓰면 안 될 곳에 쓰는 것」이 보이지 않는다**
- `99_Others/tests/main/engineVersions.test.ts` 가 **영호의 실제 사용자 설정** `~/.agentdeck-dev/engine-config.json` 을 `{"activeVersion": null}` 로 **실제로 덮었다**(mtime 이 게이트 실행 시각과 일치 — 실측). **테스트는 green 이었다.** Codex 의 읽기 전용 세션에서만 EPERM 으로 red
- ⚠️ **영호가 앱에서 SDK 활성 버전을 다시 지정해야 할 수 있다**(파일은 정상, 값만 null)
- 근인 2겹(qa 실측 — **내 최초 진단은 틀렸다**): ⓐ `{...actual}` 이 펼친 네임스페이스에 **`default` 키(진짜 fs)** 가 있는데 앱은 `import fs from 'node:fs'` — **모킹이 앱이 지나는 문이 아닌 옆문에만** 걸렸다 ⓑ `vi.mock` 호이스팅으로 같은 모듈 mock **7개가 마지막 1개로 붕괴** — 테스트별 fs 시나리오가 **처음부터 없었다**
- 📌 **같은 사각지대가 방향만 바꿔 재발** — `HR1-DONE.md` 「발견2(P1)」의 *"내 CI/Bash 는 풀 액세스라 못 덮었던 사각지대"*. ⇒ ⭐ **제한 권한 세션은 테스트 격리 결함을 찾는 도구다**
- 수리: fs 모킹 폐기 + per-test 임시 userData 주입(**앱 소스 변경 0**) + 3중 방어 + **뮤테이션 음성 대조**(일부러 망가뜨리면 red 확인)

🚦 **다음 액션 — 순서대로**
1. 🔬 **reviewer 2차 재실행**(Fable 5, `model: fable`) — 대상: `engineVersions.test.ts` 전면 재작성(🔴 최우선 — *"green 인데 아무것도 안 보던"* 병이었으니 **33개가 정말 검증하는지**) + 주석 14줄 + `ARCHITECTURE.md` 6줄. **1차 결론(개명 자체 위반 0)은 재검증 불요.** ⚖️ 판정 요청 1건: `lm1-effort-support-contract.test.ts:63·119` 를 갱신할 것인가(Codex 는 갱신 권고, 메인은 미변경 판단 — 근거는 Phase 문서)
2. 🙋 **P07 커밋**(secretary 위임) — reviewer 2차 green 후. ⚠️ **개명 + 테스트 격리 수리가 한 파일에서 겹쳐 분리 불가**(`engineVersions.test.ts` 는 rename + 전면 재작성). 커밋 메시지에 두 변경을 **다 쓴다**
3. 🙋 **Codex 세션 B**(유지보수: `AGENTDECK_HARNESS_MAINTENANCE=1` + `-c default_permissions=":danger-full-access"`) — 브리프 `NC-codex-handoff-2.md` §5 의 **B 열**: ⑦ 백로그 19 수리(수리 설계는 Codex 가 이미 회수해 백로그 19에 박아뒀다) · ④ **음성 canary**(A 에서는 원리적으로 불가) · 계약 테스트 2건의 구 CORE 경로 + `.codex/README.md` stale
   - ⚠️ PowerShell 은 `VAR=1 cmd` 문법이 없다 → `$env:AGENTDECK_HARNESS_MAINTENANCE="1"` (**창에 눌러앉으니** 끝나면 `Remove-Item Env:\…` 또는 창을 닫을 것 — TTL 자동 만료가 없다)
4. 🙋 **예비 창** — `BACKLOG.md` **20번**(`.claude/` 봉인층 8줄) + ADR-039 각주. 계획서에 예비 창이 **미리 명시**돼 있다
5. 🙋 **push / PR ×2** — PR ①(`chore/nc-naming-docs`) 먼저, PR ②가 그 위에 스택. master 가 아니라 docs 브랜치에서 분기한 이유: P03 훅 병행 수용이 그 브랜치에만 있어 master 분기 시 훅이 **fail-open 으로 조용히 놓친다**
6. NC-DONE.md + 5단계 보고 · CHANGELOG [H] · Second Brain 축적 제안(마일스톤 종결급 — 후보 3개가 아래에 쌓여 있다)

🧾 **이번 마일스톤이 알아낸 것 (Second Brain 축적 후보)**
- 🔴 ⭐⭐ **사본을 지키면 정본이 뚫린다** — 치환 제외 목록에 `.claude/settings.json`(사본)을 넣었는데 정본은 `settings.SEALED.json`. 봉인 deny 병행 수용이 지워졌고 **어떤 게이트도 red 를 안 냈다**(권한 deny 는 테스트 대상이 아니다). 복구 완료
- ⭐⭐ **풀 액세스는 「쓰면 안 될 곳에 쓰는 것」을 가린다** — 위 🔴 참조. 제한 권한 세션이 곧 검사 도구
- ⭐⭐ **판정기 결함은 유령 안건을 만든다** — 백로그 18(*"프로필 3종 초기화 실패"*)은 **오탐이었다.** doctor 가 쓰는 `codex sandbox -P` 프록시만 고장났고 실제 세션 권한은 정상. 그 사이 잘못된 위협 모델이 백로그 17까지 오염시켰다
- ⭐⭐ **단일 관측의 판정 불가는 「입력을 바꿔 한 번 더 관측」해서 푼다** — B 세션 단독(`disabled`)은 「요청대로」와 「고장」이 같은 신호였는데, A 세션(`managed`)이 대조군이 되자 즉시 갈렸다. 관측을 정교하게 만드는 것보다 **비교 대상을 만드는 것**이 쌌다
- ⭐ **diff 는 「안 바뀌어서 틀려진 것」을 못 본다** — `R100` rename 파일은 내용 변경 0이라 diff 에 안 뜨는데, 그 파일이 **자기 옛 이름을 참조**하면 안 바뀌었기 때문에 stale 이 된다. 전수 grep 으로만 잡힌다(reviewer 9곳 + Codex 2곳으로도 안 끝나 8파일이 더 나왔다)
- ⭐ **축으로 나누면 축의 교차점이 빈다** — P05(문서 영역) / P07(코드 영역) 분할에서 **「문서 안의 코드 참조」**(`ARCHITECTURE.md`)가 어느 축에도 안 들어갔다
- ⭐ **규칙을 정교하게 만들기보다 범위를 좁히는 편이 싸다** — 치환 규칙을 3종→4종으로 넓히면 개념형 ~35곳을 오폭한다. **열거 목록 표적 정리**를 택했다(`Harness_OpenGate` 를 범위에서 빼 부트스트랩 자물쇠를 회피한 것과 같은 수법)
- ⚠️ **before/after 표는 치환에 구조적으로 취약** — 치환기는 *"이 칸은 과거를 뜻한다"* 를 모른다. 화살표 양쪽이 같아진 문장을 정규식 스윕으로 복원
- ⚠️ **조각난 경로는 문자열 검색에 안 잡힌다** — `path.join(…, '00_Documents', 'harness', …)`
- ⚠️ **장시간 리뷰와 병행 편집은 판정 대상을 흔든다** — reviewer 가 워킹트리 이동을 관측(+753/−751 → +821/−796). ⇒ 착수 시점 해시를 브리프에 박거나, 미커밋이면 **"이후 안 건드린다"고 약속**
- ⚠️ **긴 출력의 복붙은 뭉개진다** — Codex receipt 에서 rename 두 줄이 한 줄로 합쳐져 짝이 안 맞아 보였다(실물은 정상). `->` 화살표가 든 대조 형태가 특히 취약
- ⚠️ **봉인 오탐 재현 조건**(백로그 14) — `git mv`(쓰기)와 `grep`(조회)을 **한 Bash 호출에 섞으면** 차단된다. ⇒ **쓰기와 조회를 같은 호출에 섞지 않는다**

⚠️ **창이 닫히면 실행 경계가 발화한다** — `git add`·`npm run typecheck` 가 *"secretary 에 위임하세요"* 로 차단된다. **영호가 secretary 위임을 승인(2026-07-26)** — 이번 마일스톤 남은 커밋·게이트에 계속 적용.
⚠️ **CORE-06 v2 — AI 는 비가역 명령을 실행할 수 없다.** `push`·`gh pr create/merge` 등 6종은 `dangerous-cmd-guard` 축②가 GO 와 무관하게 exit 2로 자른다. AI 는 명령을 조립해 제시하고 영호가 `! <명령>` 으로 실행한다. **우회 시도 금지.**

주의할 약속:
- 하네스 = 영호 단독 통제(CORE-11) · `.claude/settings.json` 은 창 상태물이라 창 안에서는 커밋 금지
- CORE-12 격리 — `.codex/**` 는 **읽기도** 금지. **확인 불가 영역은 확인 불가라고 쓴다**
- ⚠️ 대소문자 전용 개명은 범위 밖 — `core.ignorecase = true` 라 `git mv` 1회는 **조용한 no-op**
- ⚠️ `sed -i` 대량 치환은 줄끝을 LF 로 정규화한다 — 스테이징하면 사라지지만 그 전까지 `git status` 가 실변경을 가린다
- 📌 Second Brain 축적 제안은 **마일스톤·창 종결급에서만**(영호 2026-07-26 "과도한 축적은 Noise")
- ⚠️ **`%TEMP%/ev-test-*` 잔해 54개** — 옛 테스트가 정리하지 않은 것(신 버전은 `afterAll` 정리). **파괴 작업이라 영호 승인 + 이름 확정 목록으로만** 삭제

⏳ **이월**: ① `settings.SEALED.json` **옛 4줄**(`00.Documents`·`98.Management`) = HR2 머지 + 1마일스톤 ⚠️ **구 2줄**(`00_Documents/harness`·`adr`)은 타이머가 다르다(NC 머지 + 1마일스톤) — 함께 지우지 말 것 ② 백로그 14·15·16·20(창) ③ 17·19·22(Codex 세션 — 19는 **유지보수** 세션이어야 `.codex/**` 쓰기 가능) ④ 21·23(둘 다 아님 — 평시 진행 가능) ⑤ 에이전트 description 압축 자동 발화 미관측
루프 기준선(P07 봉합 후): vitest **401 files / 5336 passed | 10 skipped (5346)** · 훅 셀프테스트 **120/120** · CORE conformance **13/13** · build 751 modules · e2e core-loop 4/4 · 마크다운 링크 309/0 · typecheck 0 · lint 0(⚠️ eslint 는 `.ts,.tsx` 만 봐서 **훅 `.mjs`·`.sh` 는 대상 밖**).
마지막 갱신: 2026-07-27

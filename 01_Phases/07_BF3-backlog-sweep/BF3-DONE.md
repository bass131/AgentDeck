---
owner: 영호
milestone: BF3
title: BF3-backlog-sweep 마일스톤 종합 보고
date: 2026-07-03
status: done
---

# BF3-backlog-sweep — 마일스톤 종합 -DONE (5단계 보고)

> 브랜치 `feature/bf3-backlog-sweep` (master 9c07bfd 기점). Phase 7/7 완료.
> 최종 게이트: **3980 tests PASS · typecheck 0 · lint 0 · 비라이브 e2e 17/17 · 라이브 캡처 1 PASS · reviewer 전 Phase 통과 · 영호 육안 승인**.

## 🎯 무엇을 (목표)

LR2~BF2에 걸쳐 누적된 비차단 🟡 백로그를 "최대한 백로그 없이"(영호 지시) 일괄 정리:
소형 수리 6건 + 권한 UX 인라인 전환 1건(ADR-030, Track-1 충실도 의도적 이탈 명문화).

## 🤔 왜 (배경·결정)

- 백로그는 개별로는 비차단이지만 누적되면 다음 마일스톤(M5 배포)의 기반 신뢰를 깎음.
- 권한 모달은 원본 미러였으나 ① 권한 대기 중 ■(중단) 봉쇄 ② 대화 맥락 차단 ③ 멀티패널 응답 수단 부재 — 3중 사용성 결함. plan-auditor가 Track-1 순서 침범(결함-1)을 지적 → **ADR-030으로 waiver 명문화 후 진행**(영호 확정)이라는 거버넌스 경로를 밟음.
- 도중 영호 제기("배너 세션 독립")는 실측으로 "이미 성립" 확정 — 잔여 연속성 갭만 Phase 07로 흡수. P04-②(WAKEUP_LOOP_ID 싱글턴)는 유일한 의도적 제외(설계 제약, 결함 아님).

## 🛠️ 어떻게 (Phase별 커밋)

| # | 내용 | 커밋 | 핵심 |
|---|---|---|---|
| 01 | LT6 드레인 실효화 + bf1 단언 진단력 | `fdf1f06` | 제너레이터 break 시맨틱 — 실코드 관용구와 테스트 정합 |
| 02 | interrupt 에러문구 순화 | `498ead5` | tool_use 중 중단 throw suppress(BF1 패턴 일관) + 원문 로그 보존 |
| 03 | push μs창 봉합 | `6681790` | 판정-행동 재확인 이중 방어, abort 분리 불변조건 코드-트레이스 검증 |
| 04 | 인터리빙 배너 오판 | `7a8e382` | 턴 origin을 트래커 판정에 배선(레이어 분리가 근본 원인) |
| 05 | useMultiPersist 복원 레이스 | `256ed30` | 소유권 없는 폴백 제거 — 백로그 중 유일한 실질 데이터 오염 버그 |
| 07 | 배너 연속성 | `be92d68` | 앱수명 레지스트리 + 내구 runId 라우팅 — reviewer 🔴 2사이클 완전 밀봉 |
| 06 | 권한 인라인 카드 + 멀티패널 배선 | `08641df` | 모달 삭제·데이터 계약 0줄·e2e 7파일 이관·N등분(영호 피드백)·육안 승인 |

부수 수리(계획 밖 발견): ⓐ 활성 대화 재클릭 시 라이브 상태가 디스크로 덮이는 데이터 손실 경로 차단(same-id 가드, P07 중) ⓑ 권한 대기 중 Esc 이중동작(deny+abort 동시 발화) 선제 차단(P06 중) ⓒ Shell의 죽은 PermissionModal 데모 마운트 제거.

## 🧪 검증 (게이트 원칙)

- **매 Phase**: TDD RED→GREEN + **메인 git stash 실측**(구코드 실패를 메인 트랜스크립트에 박음 — Worker 자기보고 불신 원칙) + reviewer(backend-contract/ui-visual 무조건, renderer 조건부) + Phase별 atomic 커밋.
- **reviewer 성과**: P07에서 🔴 2건(축출-후-stale 부활, background-started 변종)을 연속 검출 — 봉합 2사이클로 수렴, 최종 재검 🟢. P06 🟡 4건(informed-consent 캡션 등) 즉시 봉합.
- **육안(버킷 b)**: 라이브 캡처 스펙(bf3-p06-permission-card-shots, opt-in)으로 4샷 산출 → 영호 N등분 피드백 반영 재캡처 → 승인(2026-07-03).
- **잔여 검증 부채**: 라이브 e2e 6파일(셀렉터 이관분)의 실 SDK 재실측 미실행 — 다음 라이브 기회에 일괄(코드·타입·skip 실행은 검증 완료).

## ➡️ 다음 (인계·잔여 결정)

1. **PR 생성** — 영호 GO(2026-07-03): push·PR·merge 진행.
2. **RMW lost-update** (P05 reviewer 🟡 발견) — JSON 파일 동시 저장 lost-update 선재 레이스. main-process 동시성 설계(CAS/세대 토큰). **영호 결정(2026-07-03): 별도 건** — 이 PR은 응집도 있는 스윕 완결체, RMW는 도메인·설계 무게가 달라 분리.
3. **`.claude/CHANGELOG.md`** — ADR-030 신설 [M] 엔트리 (하네스 영역, 영호와 세션 마감 시).
4. P04-② 싱글턴 슬롯 = 의도적 제외 유지(다중 동시 self-paced 루프 지원 시 재설계).
5. P06 reviewer 관찰 기록: 단발 펌프 emit 경로의 interrupt suppress 미커버(선재 갭, throw 경로만 봉합) / 사용자 인터리빙 취소 시 다음 wakeup까지 배너 지연창(정확한 표시).

## 🎓 이번 마일스톤에서 배운 것

1. **경합 창 봉합의 정석** — 판정과 행동 사이 재확인(double-check), 늦은 쪽의 취소, run-to-completion 활용. P03·P05·P07이 모두 이 패턴의 변주.
2. **폴백에는 소유권 검증** — "없으면 이거라도"가 남의 데이터를 줍는 순간 오염 벡터가 된다(P05).
3. **상태의 수명 스코프** — 컴포넌트 < 세션 스냅샷 < 앱수명 < 디스크. "루프 표시"는 루프 프로세스와 같은 수명이 정답이라 디스크 영속이 오히려 버그(stale)였다(P07).
4. **reviewer 적대 검증의 가치** — Worker가 공개한 트레이드오프("best-effort")의 이면(stale 방향)을 reviewer가 잡았고, 봉합의 봉합까지 2사이클 수렴. "통과했으니 끝"이 아니라 "남은 창이 어느 방향인가(소실 vs 오염)"를 묻는 게 핵심.
5. **거버넌스는 비용이 아니라 근거** — 충실도 이탈을 ADR로 명문화하니(plan-auditor 결함-1 → ADR-030) 결정의 이유가 미래에 남는다.

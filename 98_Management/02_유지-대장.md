# 유지 대장 — 마일스톤 사이의 부채 상환 기록

마일스톤 운용 중 실측으로 쌓인 운용 부채를 등재하고, 마일스톤 사이 유지 창에서 상환 여부를 판정합니다.
등재 없는 부채는 상환 대상이 아닙니다 — 실측 출처(마일스톤·검증 기록·결정 문서)가 있는 항목만 등재합니다.
서식의 원형은 Moodie_Agent_Workflow의 같은 이름 대장입니다.

- 항목 상태: **존치**(재발 관측 대기) · **진행**(유지 창에서 상환 중) · **상환**(실측 검증 완료) · **기각**(대응 비용 역전)
- 상환 판정은 실측으로만 합니다 — 스크립트·설정을 고친 사실이 아니라, 고친 것이 실제로 작동한 관측이 상환입니다.
- 이 대장은 상태 갱신형입니다(결정 대장과 달리 append-only가 아닙니다). 이력은 git이 보유합니다.

## 의제

### AD-01 Bash 경유 임의 파일 쓰기의 감독 밖 위험 (존치)

- 등재: 2026-08-10 — M01 마감, ADR-001 결정 3의 등재 약속 이행.
- 내용: `Bash(node ...)` 같은 셸 실행 경로는 편집 도구 훅을 지나지 않아 감독 밖에서 파일을 바꿀 수 있습니다.
  기계 차단은 정상 빌드·테스트 명령까지 잡는 오탐 비용 역전으로 채택하지 않았고, 헌법 8조가 규범으로 막습니다.
- 상태 근거: [USER] 위험 수락 (M01 결정 대장, 2026-08-09). 규범 위반이 실측으로 관측되면 재론합니다.

### AD-02 Orca dispatch 경로의 계획 게이트 미심사 갭 (존치)

- 등재: 2026-08-10 — M01 마감, ADR-001 잔여 위험 절의 등재 약속 이행.
- 내용: plan-gate는 Task·Agent 도구의 스폰만 심사합니다. Orca CLI의 dispatch·worker-start 경유 스폰은
  Bash 명령이라 계획 게이트를 지나지 않습니다 — 계획 없는 위임이 이 경로로는 막히지 않습니다.
- 상태 근거: ADR-001이 심각도 높음(H) 승격 후보로 표시한 갭입니다. 다음 하네스 수리 마일스톤의 의제 후보입니다.

### AD-03 위험 명령 가드의 공통 한계 3종 (존치)

- 등재: 2026-08-10 — M02 Phase 1, 동등성 기준표의 「공통 한계」 구획 등재와 함께.
- 내용: 변수 확장(`c=push; git $c`), `$()` 명령 치환(`git $(echo push)`), `cmd /c` 접두(`cmd /c git push`)는
  현행 가드와 Moodie 원본이 함께 못 잡습니다. 가드는 문자열 트립와이어라 셸 확장 이후의 실제 명령을 보지 못합니다.
  이 표면의 방어 담당은 가드가 아니라 Claude Code 내장 권한 분류기(모델 판정)입니다.
- 상태 근거: [USER] 기각 판정 — M02에서 수리하지 않습니다 (M02 결정 대장, 2026-08-10 계획 승인 문답 안건 2).
  동등성 판정과 기대-실패 집계에서도 제외합니다. 기준표는 `01_Milestones/M02_Hardening/02_동등성-기준표.md`입니다.
  우회가 실측으로 관측되면 재론합니다.
